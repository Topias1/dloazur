---
phase: quick-260529-o92
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/layouts/app.blade.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "Cliquer « Espace client » (header desktop, menu mobile, footer) ouvre le formulaire magic-link du portail, pas le formulaire de contact"
    - "Les liens « Contact » légitimes (nav, mailto, tel) continuent de pointer vers leurs cibles d'origine"
  artifacts:
    - path: "resources/views/layouts/app.blade.php"
      provides: "Liens « Espace client » pointant vers portail.magic-link.request"
      contains: "route('portail.magic-link.request')"
  key_links:
    - from: "resources/views/layouts/app.blade.php"
      to: "portail.magic-link.request (GET /auth/magic)"
      via: "route() helper sur les 3 liens Espace client"
      pattern: "Espace client.*portail.magic-link.request"
---

<objective>
Corriger les trois liens « Espace client » de la vitrine qui pointent par erreur vers le formulaire de contact (`route('contact')`) au lieu de l'entrée du portail client (`route('portail.magic-link.request')`, GET /auth/magic).

Purpose: Un visiteur déconnecté qui clique « Espace client » doit atterrir sur le formulaire de demande de lien de connexion magic-link, pas sur le formulaire de contact.
Output: `resources/views/layouts/app.blade.php` avec les 3 liens corrigés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Route cible (définie dans routes/portail.php, middleware guest:clients) :
- name: portail.magic-link.request
- method: GET
- uri: /auth/magic
- controller: App\Http\Controllers\Portail\MagicLinkController@requestForm

Les 3 liens « Espace client » à corriger dans resources/views/layouts/app.blade.php :
- ligne 54  — lien header desktop  (actuellement route('contact'))
- ligne 99  — lien menu mobile     (actuellement route('contact'))
- ligne 151 — lien footer          (actuellement route('contact'))

NE PAS TOUCHER : les liens Contact légitimes. Dans ce fichier, le footer Contact
n'utilise que mailto: et tel: (pas route('contact')). Si d'autres route('contact')
existent dans le fichier et NE sont PAS des liens « Espace client », les laisser intacts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rediriger les 3 liens « Espace client » vers le portail magic-link</name>
  <files>resources/views/layouts/app.blade.php</files>
  <action>
    Remplacer `route('contact')` par `route('portail.magic-link.request')` sur les
    UNIQUEMENT trois liens dont le libellé visible est « Espace client » :
    - ligne 54  (header desktop, classe `text-navy-700 hover:bg-navy-900/5`)
    - ligne 99  (menu mobile, classe `bg-azure-500 text-white`)
    - ligne 151 (footer, liste Contact, classe `hover:text-white`)
    Ne modifier aucun autre attribut (classes, @click, aria) — seul le href change.
    Ne PAS toucher aux liens Contact, mailto:, tel:, ni à tout autre route('contact')
    qui ne serait pas un lien « Espace client ».
  </action>
  <verify>
    <automated>! grep -n "route('contact')" resources/views/layouts/app.blade.php | grep -q "Espace client" && grep -c "route('portail.magic-link.request')" resources/views/layouts/app.blade.php | grep -qx 3</automated>
  </verify>
  <done>
    Les 3 liens « Espace client » utilisent route('portail.magic-link.request') (résout vers /auth/magic) ; aucun lien « Espace client » ne pointe plus vers route('contact') ; les liens mailto:/tel: et tout autre route('contact') légitime restent inchangés.
  </done>
</task>

</tasks>

<verification>
- `grep "Espace client" resources/views/layouts/app.blade.php` montre 3 occurrences, toutes avec href `route('portail.magic-link.request')`.
- `route('portail.magic-link.request')` existe et résout vers GET /auth/magic (routes/portail.php).
- Les liens Contact résiduels (mailto:, tel:) sont intacts.
</verification>

<success_criteria>
Un visiteur cliquant « Espace client » (header, mobile, footer) arrive sur le formulaire magic-link du portail (/auth/magic) et non sur le formulaire de contact.
</success_criteria>

<output>
Create `.planning/quick/260529-o92-fix-liens-espace-client-vers-portail-mag/260529-o92-SUMMARY.md` when done
</output>
