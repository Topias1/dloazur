---
slug: static-first-photo
date: 2026-05-31
---

Remove the dynamic multi-photo lightbox gallery in the client portail passage timeline; render only the first photo as a single static image. Reason: no proper before/after media available.

## Changes

- `resources/views/livewire/portail/passage-timeline.blade.php`:
  - Removed the Alpine `x-data` multi-photo lightbox gallery (thumbnail `x-for` grid, full-screen `x-teleport` modal, prev/next/keyboard/swipe handlers).
  - Replaced with a single static `<img>` of `$lastPassage->photos->first()`, using the same `Storage::disk(...)->temporaryUrl(...)` build wrapped in try/catch fallback.
  - Kept `@if ($lastPassage->photos->isNotEmpty())` guard and the "Photo de l'intervention" heading.
  - Left water-chemistry `ph_avant`/`ph_apres` display fully intact.
