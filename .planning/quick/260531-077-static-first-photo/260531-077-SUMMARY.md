---
status: complete
---

- Removed the dynamic Alpine.js multi-photo lightbox gallery (thumbnail grid + full-screen modal with prev/next/keyboard/swipe) from `resources/views/livewire/portail/passage-timeline.blade.php`.
- Now renders only the first photo (`$lastPassage->photos->first()`) as a single static, lazy-loaded, rounded responsive `<img>` (max-h-96), with the same temporary-URL build + try/catch fallback as before.
- Kept the `isNotEmpty()` guard and section heading; water-chemistry `ph_avant`/`ph_apres` display untouched.
- No leftover references to `lightbox`, `index:`, `touchStart`, `x-teleport`, or `x-for` in the file.
- Tests: `./vendor/bin/pest` => 461 passed, 4 skipped (1290 assertions). Green, matches HEAD baseline.
