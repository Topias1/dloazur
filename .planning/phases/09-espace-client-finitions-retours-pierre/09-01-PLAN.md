---
phase: 09-espace-client-finitions-retours-pierre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/livewire/portail/passage-timeline.blade.php
  - tests/Feature/PortailTimelineTest.php
  - .planning/ROADMAP.md
autonomous: true
requirements: [client-2, client-3, client-4]

must_haves:
  truths:
    - "Les deux sous-titres « Mes documents » affichent « Disponible avec la mise en place de la facturation. »"
    - "Le badge « Bientôt » reste présent sur chaque ligne « Mes documents »"
    - "Chaque bouton d'accordéon historique porte aria-controls pointant vers l'id de son panneau"
    - "Chaque panneau d'accordéon historique porte un id passage-panel-{id} unique"
    - "Le hero bandeau photo n'a plus loading=\"lazy\" (LCP au-dessus de la ligne de flottaison)"
    - "Un test Pest de régression couvre la structure a11y et le contenu déplié de la timeline"
  artifacts:
    - path: "resources/views/livewire/portail/passage-timeline.blade.php"
      provides: "Vue portail avec copy teaser corrigée, attributs a11y accordéon, hero sans lazy"
      contains: "Disponible avec la mise en place de la facturation."
    - path: "tests/Feature/PortailTimelineTest.php"
      provides: "Test de régression timeline historique (a11y + contenu)"
      contains: "PortailTimeline"
    - path: ".planning/ROADMAP.md"
      provides: "Note de dépendance Phase 3 pour « Mes documents »"
      contains: "Phase 3"
  key_links:
    - from: "button (accordéon)"
      to: "div panneau (accordéon)"
      via: "aria-controls / id"
      pattern: "aria-controls=\"passage-panel-"
    - from: "tests/Feature/PortailTimelineTest.php"
      to: "/portail/passages"
      via: "actingAs(client, 'clients')->get()"
      pattern: "actingAs.*'clients'"
---

<objective>
Trois finitions du portail client issues des retours Pierre, toutes sur un seul fichier de vue plus un nouveau test :

1. **client-3 (copy)** — Réécrire les deux sous-titres « Mes documents » en « Disponible avec la mise en place de la facturation. » (badge « Bientôt » inchangé) ; tracer la dépendance Phase 3 dans ROADMAP.md uniquement.
2. **client-2 (a11y + test)** — Relier bouton et panneau de l'accordéon historique via `aria-controls`/`id`, puis écrire un test Pest de régression complet.
3. **client-4 (perf)** — Retirer `loading="lazy"` du hero bandeau photo (au-dessus de la ligne de flottaison, nuit au LCP mobile Martinique).

Purpose: Fiabiliser et clarifier le portail client suite aux retours Pierre, avec couverture de régression sur l'historique dépliable jusqu'ici non testé.
Output: vue corrigée, nouveau test vert, note de dépendance ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-espace-client-finitions-retours-pierre/09-CONTEXT.md
@.planning/feedback/pierre-2026-06-03-reponses.md
@CLAUDE.md
@.claude/skills/dloazur-frontend-stack/SKILL.md

<interfaces>
<!-- Patterns extraits du code — l'exécuteur les utilise directement, pas d'exploration. -->

Test portail (tests/Feature/PortailAccessTest.php) :
- uses(\Illuminate\Foundation\Testing\RefreshDatabase::class);
- $client = Client::factory()->create(['name' => '...']);
- $piscine = Piscine::factory()->create(['client_id' => $client->id]);
- Passage::factory()->create(['client_id'=>..., 'piscine_id'=>..., 'visited_at'=>now()->subDays(N), 'ph_avant'=>..., 'chlore_libre'=>..., 'tac'=>..., 'actions'=>[...], 'notes'=>'...']);
- $this->actingAs($client, 'clients')->get('/portail/passages')->assertStatus(200);
- $response->assertSee('texte', false);  // false = ne pas échapper le HTML

PassageFactory defaults (database/factories/PassageFactory.php) :
- ph_avant => 7.2, chlore_libre => 1.5, tac => null, sel_g_l => null, actions => null, notes => null, status => 'draft'
- IMPORTANT : tac est null par défaut — le surcharger explicitement pour tester l'affichage TAC.

Structure accordéon historique (passage-timeline.blade.php ~204-300) :
- Ne s'affiche QUE si $passages->count() > 1 (le plus récent va dans « Dernier passage »).
- La boucle itère sur $passages->skip(1) — le passage testé pour le contenu déplié NE DOIT PAS être le plus récent.
- Le bouton (ligne ~215) a déjà :aria-expanded="open.toString()". Il manque aria-controls.
- Le panneau déplié (div x-show="open", ligne ~253) n'a pas d'id.
- Les tuiles pH / Cl libre / TAC sont toujours rendues dans le panneau (affichent « · » si null) ; Sel seulement si $showSel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corriger copy « Mes documents », a11y accordéon, lazy hero + note ROADMAP</name>
  <files>resources/views/livewire/portail/passage-timeline.blade.php, .planning/ROADMAP.md</files>
  <read_first>
    - resources/views/livewire/portail/passage-timeline.blade.php (hero ~34-38, accordéon bouton ~215-219 et panneau ~253-257, « Mes documents » ~323 et ~335)
    - .planning/ROADMAP.md (section « ### Phase 9: »)
    - .claude/skills/dloazur-frontend-stack/SKILL.md (conventions Blade, ne pas créer tailwind.config.js)
  </read_first>
  <action>
    Dans `resources/views/livewire/portail/passage-timeline.blade.php` :

    (a) client-3 — Remplacer le sous-titre du Contrat (ligne ~323, actuellement « Votre contrat et ses conditions. ») par exactement « Disponible avec la mise en place de la facturation. ». Remplacer le sous-titre des Factures (ligne ~335, actuellement « Vos factures, à télécharger en PDF. ») par exactement la même phrase « Disponible avec la mise en place de la facturation. ». Ne PAS toucher aux badges « Bientôt » ni aux titres « Contrat d'entretien » / « Factures ».

    (b) client-2 a11y — Sur le bouton de l'accordéon (ligne ~215, celui qui porte déjà `:aria-expanded="open.toString()"`), ajouter l'attribut statique `aria-controls="passage-panel-{{ $p->id }}"`. Sur le `<div x-show="open" x-cloak ...>` qui contient le détail déplié (ligne ~253), ajouter l'attribut `id="passage-panel-{{ $p->id }}"`. L'id doit être unique par passage via `$p->id`. Ne pas modifier la logique Alpine `x-data="{ open: false }"` / `@click="open = !open"`.

    (c) client-4 perf — Sur le `<img>` du hero bandeau (ligne ~37, src `$heroPhotoUrl ?? asset('assets/brand/photos/piscine-propre.jpg')`), supprimer l'attribut `loading="lazy"`. NE PAS ajouter `<x-picture>`/srcset (hors scope, D-08 : URLs R2 signées incompatibles). NE PAS retirer le `loading="lazy"` de la photo du passage en ligne ~194 (celle-ci est sous la ligne de flottaison, on la garde).

    (d) client-3 ROADMAP — Dans `.planning/ROADMAP.md`, section `### Phase 9:`, ajouter une note traçant la dépendance : « Mes documents » (contrat + factures) sera réellement branché en Phase 3 (facturation) ; le portail n'affiche pour l'instant qu'un teaser « Bientôt ». Ajouter cette note dans le corps de la section Phase 9 (par ex. sous **Depends on:** ou en ligne dédiée). NE PAS modifier REQUIREMENTS.md (D-02).
  </action>
  <verify>
    <automated>grep -c 'Disponible avec la mise en place de la facturation.' resources/views/livewire/portail/passage-timeline.blade.php | grep -qx 2 && grep -q 'aria-controls="passage-panel-{{ $p->id }}"' resources/views/livewire/portail/passage-timeline.blade.php && grep -q 'id="passage-panel-{{ $p->id }}"' resources/views/livewire/portail/passage-timeline.blade.php && [ "$(grep -c 'loading=\"lazy\"' resources/views/livewire/portail/passage-timeline.blade.php)" = "1" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - passage-timeline.blade.php contient exactement 2 occurrences de la chaîne « Disponible avec la mise en place de la facturation. »
    - Les anciennes chaînes « Votre contrat et ses conditions. » et « Vos factures, à télécharger en PDF. » sont absentes (`grep -c` = 0 pour chacune).
    - Les deux badges « Bientôt » sont toujours présents (`grep -c 'Bientôt'` = 2).
    - Le bouton accordéon contient `aria-controls="passage-panel-{{ $p->id }}"`.
    - Le div panneau déplié contient `id="passage-panel-{{ $p->id }}"`.
    - Le bloc hero `<img>` (src piscine-propre.jpg / $heroPhotoUrl) ne contient plus `loading="lazy"` ; l'`<img>` photo-du-passage (ligne ~194) le conserve.
    - .planning/ROADMAP.md section Phase 9 contient une note mentionnant « Phase 3 » / facturation pour « Mes documents ».
    - REQUIREMENTS.md inchangé (aucune modification git).
  </acceptance_criteria>
  <done>Copy teaser réécrite (2x même phrase), attributs aria-controls/id ajoutés sur l'accordéon, loading=lazy retiré du hero uniquement, note de dépendance Phase 3 dans ROADMAP.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test de régression Pest sur la timeline historique</name>
  <files>tests/Feature/PortailTimelineTest.php</files>
  <read_first>
    - tests/Feature/PortailAccessTest.php (helper auth `actingAs(..., 'clients')`, pattern Client/Piscine/Passage factory, `assertSee(..., false)`)
    - tests/Feature/DemoLoginTest.php (usage RefreshDatabase + factories)
    - database/factories/PassageFactory.php (defaults : tac=null, actions=null, notes=null — à surcharger)
    - resources/views/livewire/portail/passage-timeline.blade.php (structure accordéon ~204-300, conditions $passages->count() > 1 et skip(1))
  </read_first>
  <behavior>
    - Structure accordéon : la réponse contient `aria-expanded`, `aria-controls="passage-panel-{id}"` sur le bouton, et `id="passage-panel-{id}"` sur le panneau, avec l'id correspondant au passage historique seedé.
    - Contenu déplié : les valeurs pH, chlore libre et TAC du passage historique sont rendues dans le panneau (valeurs distinctives non par défaut).
    - Actions réalisées : si le passage seedé a des `actions`, chaque libellé est présent.
    - Notes : si le passage seedé a une `notes`, le texte est présent.
    - Compteur photos : structure de compteur présente quand des photos existent (ou cas couvert si non testable sans média réel — voir action).
  </behavior>
  <action>
    Créer `tests/Feature/PortailTimelineTest.php`, fichier dédié (D-05), modelé sur `PortailAccessTest.php`.

    En-tête : `uses(\Illuminate\Foundation\Testing\RefreshDatabase::class);` et `use` des modèles Client, Passage, Piscine.

    Seeding commun : créer un Client + une Piscine (`client_id`). Créer DEUX passages pour que l'historique s'affiche (la vue exige `$passages->count() > 1` et met le plus récent dans « Dernier passage » via `skip(1)`) :
    - Passage A « récent » : `visited_at => now()->subDays(1)` (ira dans « Dernier passage », hors historique).
    - Passage B « historique » : `visited_at => now()->subDays(10)` avec des valeurs DISTINCTIVES non-défaut pour pouvoir les asserter sans ambiguïté — par ex. `ph_avant => 7.4`, `chlore_libre => 2.3`, `tac => 95` (tac est null par défaut, le forcer), `actions => ['Nettoyage filtre', 'Brossage parois']`, `notes => 'Note historique XQ7Z'`. C'est le passage B (historique) dont on assert le contenu déplié et l'id.

    Tests à écrire (chacun `actingAs($client, 'clients')->get('/portail/passages')` puis assertions avec `assertSee(..., false)` pour ne pas échapper le HTML) :
    - test « structure accordéon a11y » : assertSee `aria-expanded`, assertSee `aria-controls="passage-panel-' . $passageB->id . '"`, assertSee `id="passage-panel-' . $passageB->id . '"`.
    - test « contenu déplié : mesures » : assertSee des valeurs formatées du passage B telles que rendues par la vue. ATTENTION au format Blade : pH/Cl via `number_format(.., 1, ',', '')` → « 7,4 » et « 2,3 » (virgule décimale) ; TAC via `number_format(.., 0, ',', '')` → « 95 ». Asserter ces chaînes exactes.
    - test « actions réalisées » : assertSee « Nettoyage filtre » et « Brossage parois ».
    - test « notes » : assertSee « Note historique XQ7Z ».
    - Compteur photos : si seeder une vraie photo n'est pas trivial (relation `photos` + média R2), couvrir le compteur par un test marqué clairement OU asserter l'absence de compteur quand aucune photo (cas par défaut). Préférer un test affirmatif léger : seeder un passage B SANS photo et asserter que le bloc compteur photo (icône caméra) n'apparaît pas pour ce passage, OU, si la relation photos accepte un create simple, créer une photo et asserter le « 1 » du compteur. Choisir l'option qui passe en SQLite mémoire sans dépendance R2.

    Réutiliser strictement les guards et routes existants (`'clients'`, `/portail/passages`). Pas de browser/Playwright (D-03).
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/PortailTimelineTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier tests/Feature/PortailTimelineTest.php existe et utilise RefreshDatabase + actingAs(..., 'clients').
    - `vendor/bin/pest tests/Feature/PortailTimelineTest.php` exit code 0, au moins 4 tests passants couvrant : structure a11y (aria-expanded + aria-controls + id correspondants), mesures déplié (pH 7,4 / Cl 2,3 / TAC 95), actions, notes.
    - Le test seede au moins 2 passages (sinon l'historique ne s'affiche pas) et assert le contenu du passage NON-récent.
    - Le compteur photos est couvert par un test (présence ou absence selon photos seedées).
    - Aucune dépendance browser/Playwright ni accès réseau R2 ; tourne en SQLite mémoire.
  </acceptance_criteria>
  <done>Test Pest dédié vert couvrant structure a11y, mesures dépliées (format virgule décimale exact), actions, notes, et compteur photos de l'accordéon historique.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pest tests/Feature/PortailTimelineTest.php` passe (nouveau test de régression vert).
- `vendor/bin/pest tests/Feature/PortailAccessTest.php` passe toujours (non-régression sur l'auth portail).
- grep confirme : 2x « Disponible avec la mise en place de la facturation. », 2x « Bientôt », `aria-controls="passage-panel-`, `id="passage-panel-`, et plus de `loading="lazy"` sur le hero.
- `git diff --name-only` n'inclut PAS REQUIREMENTS.md.
</verification>

<success_criteria>
- Les deux sous-titres « Mes documents » disent « Disponible avec la mise en place de la facturation. », badges « Bientôt » conservés (client-3).
- Dépendance Phase 3 tracée dans ROADMAP.md (client-3 / D-02).
- Accordéon historique : bouton↔panneau reliés par aria-controls/id uniques par passage (client-2 a11y).
- Test de régression Pest dédié couvre structure a11y + contenu déplié (pH/Cl/TAC), actions, notes, compteur photos (client-2).
- Hero bandeau sans `loading="lazy"`, sans srcset ajouté (client-4 / D-07, D-08).
</success_criteria>

<output>
Créer `.planning/phases/09-espace-client-finitions-retours-pierre/09-01-SUMMARY.md` quand terminé.
</output>
