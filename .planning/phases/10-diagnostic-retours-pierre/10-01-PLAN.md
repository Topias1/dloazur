---
phase: 10-diagnostic-retours-pierre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Livewire/DiagnosticWizard.php
  - tests/Feature/DiagnosticWizardTest.php
  - tests/Browser/CarnetLocalTest.php
autonomous: true
requirements: [diag-1, diag-2]

must_haves:
  truths:
    - "Le serveur reçoit mode='chemistry' quand l'utilisateur prend le parcours chimie (via setMode appelable)"
    - "Le serveur reçoit mode='symptom' quand l'utilisateur entre dans l'arbre symptôme"
    - "created_via reflète 'depannage' pour un diag symptôme et 'wizard' pour un diag chimie"
    - "La suite Pest reste verte après l'ajout de setMode et le découplage des tests vis-à-vis de l'écran S0"
  artifacts:
    - path: "app/Livewire/DiagnosticWizard.php"
      provides: "Méthode publique setMode(string $mode) qui assigne $this->mode après validation in:symptom,chemistry"
      contains: "function setMode"
    - path: "tests/Feature/DiagnosticWizardTest.php"
      provides: "Tests d'attribution created_via pour les deux parcours + assertSee découplé de S0"
      contains: "depannage"
  key_links:
    - from: "tests/Feature/DiagnosticWizardTest.php"
      to: "DiagnosticWizard::setMode"
      via: "->call('setMode', 'chemistry')"
      pattern: "call\\('setMode'"
    - from: "DiagnosticWizard::keepDiagnostic"
      to: "created_via column"
      via: "$this->mode === 'symptom' ? 'depannage' : 'wizard'"
      pattern: "created_via"
---

<objective>
Rendre l'attribution serveur `$mode` (`type_probleme` / `created_via` / payload WhatsApp) fiable AVANT la refonte Blade, et adapter les tests pour qu'ils ne dépendent plus de l'écran S0 (supprimé en Plan 02).

Constat vérifié sur le code : **`setMode()` n'existe PAS** dans `DiagnosticWizard.php`. Le commentaire Alpine « Synchroniser le mode côté serveur » (vue, ligne ~54) ne fait rien — `advance()` ne pose `this.mode` que côté client, sans round-trip serveur. Conséquence : `$mode` serveur reste TOUJOURS `null`, donc `created_via` est toujours `'wizard'` et `type_probleme` toujours `null`. C'est un bug latent préexistant que cette phase corrige (ROADMAP Phase 10, critère 2 : « la logique serveur $mode est conservée »).

Ce plan crée le point d'entrée serveur `setMode()` (consommé par le lien chimie du Plan 02) et fige le comportement par des tests, AVANT toute suppression DOM. Ordre test-first : la suite reste verte à chaque commit.

Purpose: garantir que la suppression de S0 (Plan 02) ne fait perdre aucune attribution serveur, et que le parcours chimie secondaire reste correctement tracé.
Output: méthode `setMode()` + tests d'attribution + tests existants découplés de S0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md
@.claude/skills/dloazur-frontend-stack/SKILL.md

<interfaces>
<!-- État serveur pertinent de DiagnosticWizard (vérifié dans le code). -->
<!-- $mode est null par défaut ; il alimente created_via et type_probleme dans keepDiagnostic(). -->

app/Livewire/DiagnosticWizard.php :
  public ?string $mode = null;   // 'symptom' | 'chemistry' | null — utilisé pour created_via
  public function acceptDisclaimer(): void   // pose disclaimerAccepted = true
  public function keepDiagnostic(): void      // écrit Diagnostic : created_via = $mode==='symptom' ? 'depannage' : 'wizard'
  public function computeAndPersist(): void   // computeDoses() puis keepDiagnostic()
  // PAS de setMode() — à créer dans ce plan.

Comportement actuel de keepDiagnostic() (ligne ~621) :
  'type_probleme' => $this->mode,
  'created_via'   => $this->mode === 'symptom' ? 'depannage' : 'wizard',
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajouter setMode() server-side + tests d'attribution created_via</name>
  <files>app/Livewire/DiagnosticWizard.php, tests/Feature/DiagnosticWizardTest.php</files>
  <read_first>
    - app/Livewire/DiagnosticWizard.php (propriété $mode ligne ~56, acceptDisclaimer() ligne ~500, keepDiagnostic() ligne ~595-622)
    - tests/Feature/DiagnosticWizardTest.php (forme des tests Livewire::test()->set()->call() ; bloc Req5 lignes ~89-134)
    - .planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md (D-04 : le lien chimie appellera setMode('chemistry'))
  </read_first>
  <behavior>
    - Test A : Livewire::test(DiagnosticWizard)->set('disclaimerAccepted',true)->call('setMode','chemistry')->set('volume','50')->set('ph','7.0')->call('keepDiagnostic') → Diagnostic::first()->created_via === 'wizard' ET type_probleme === 'chemistry'.
    - Test B : Livewire::test(DiagnosticWizard)->set('disclaimerAccepted',true)->call('setMode','symptom')->set('volume','50')->set('ph','7.0')->call('keepDiagnostic') → Diagnostic::first()->created_via === 'depannage' ET type_probleme === 'symptom'.
    - Test C : setMode('foo') (valeur hors liste) ne lève pas d'exception et n'altère pas $mode (reste null) — un keepDiagnostic ensuite donne created_via === 'wizard' (défaut).
  </behavior>
  <action>
    Dans app/Livewire/DiagnosticWizard.php, ajouter une méthode publique `setMode(string $mode): void` placée juste à côté de `acceptDisclaimer()`. Comportement : si `$mode` est dans `['symptom','chemistry']`, assigner `$this->mode = $mode` ; sinon, ne rien changer (ignore silencieux, aucune exception). Ne PAS toucher `keepDiagnostic()` — la ligne `created_via` ('depannage' pour 'symptom', sinon 'wizard') et `type_probleme = $this->mode` sont déjà correctes ; elles deviennent simplement atteignables. Préserver toutes les autres méthodes intactes.

    Dans tests/Feature/DiagnosticWizardTest.php, ajouter une section commentée « Attribution mode serveur (Phase 10 — diag-1/diag-2) » avec les tests A, B, C de <behavior>. Utiliser `->call('setMode', 'chemistry'|'symptom')` comme surrogate du clic Alpine (les tests Pest n'exécutent pas Alpine). Suivre exactement le pattern Livewire::test() des tests Req5 existants (set disclaimerAccepted/volume/ph, puis call keepDiagnostic, puis Diagnostic::first()).
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/DiagnosticWizardTest.php --filter="mode serveur" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function setMode" app/Livewire/DiagnosticWizard.php` >= 1 (une définition publique).
    - Le corps de setMode contient les littéraux `symptom` ET `chemistry` (validation in-list).
    - tests/Feature/DiagnosticWizardTest.php contient une assertion `created_via` égal `'depannage'` (parcours symptom) ET une égal `'wizard'` (parcours chemistry), plus `type_probleme` égal `'symptom'`/`'chemistry'` respectivement.
    - `vendor/bin/pest tests/Feature/DiagnosticWizardTest.php` sort en code 0.
    - Le diff sur keepDiagnostic() est vide (lignes created_via / type_probleme inchangées).
  </acceptance_criteria>
  <done>setMode() existe, valide l'entrée, et les tests prouvent created_via='depannage' (symptom) / 'wizard' (chemistry) + type_probleme correct.</done>
</task>

<task type="auto">
  <name>Task 2: Découpler les tests de l'écran S0 (assertSee + grep data-mode-*)</name>
  <files>tests/Feature/DiagnosticWizardTest.php, tests/Browser/CarnetLocalTest.php</files>
  <read_first>
    - tests/Feature/DiagnosticWizardTest.php (ligne 21 : assertSee('Trouver mon problème'))
    - tests/Browser/CarnetLocalTest.php (ligne 101 : assertSee('Trouver mon problème'))
    - .planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    D-08 — Dans tests/Feature/DiagnosticWizardTest.php ligne 21, remplacer `->assertSee('Trouver mon problème')` par `->assertSee('Avant de commencer')`. « Avant de commencer » est le titre H2 du disclaimer (vue ligne ~304), STABLE entre l'état actuel (Plan 01 committé, S0 encore présent mais le disclaimer est rendu dans le HTML serveur) et l'état final (Plan 02). Cela évite toute fenêtre rouge entre les deux commits. NE PAS asserter « J'accepte » dans CE plan : le libellé du bouton est encore « J'ai compris, voir les recommandations » jusqu'au Plan 02 ; le Plan 02 fera basculer l'assertion vers « J'accepte » une fois le libellé renommé (D-03). Ajouter un commentaire `// D-08 : assertion finale "J'accepte" posée en Plan 02 après renommage du CTA`.

    D-09 — Dans tests/Browser/CarnetLocalTest.php ligne 101, remplacer `->assertSee('Trouver mon problème')` par `->assertSee('Avant de commencer')` (même justification, stable Plan 01 et Plan 02).

    Vérifier qu'aucun test ne sélectionne `data-mode-symptom`/`data-mode-chemistry`/`data-mode-carnet` : relancer le grep ci-dessous. Le grep préalable n'a trouvé aucun match dans tests/ ; si un match apparaît à l'exécution, l'adapter/supprimer (ces attributs disparaissent avec S0 en Plan 02).

    NE PAS modifier tests/Feature/DiagnosticPdfTest.php (crée un Diagnostic en direct avec created_via='wizard', non impacté par setMode) ni tests/Feature/DiagnosticRouteTest.php (assertions de routing/middleware, pas de contenu S0).
  </action>
  <verify>
    <automated>grep -rn "data-mode-symptom\|data-mode-chemistry\|data-mode-carnet\|Trouver mon problème" tests/ ; vendor/bin/pest tests/Feature/DiagnosticWizardTest.php tests/Feature/DiagnosticRouteTest.php 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Trouver mon problème" tests/` ne retourne AUCUNE ligne (les 2 occurrences Feature + Browser sont remplacées).
    - `grep -rn "data-mode-symptom\|data-mode-chemistry\|data-mode-carnet" tests/` ne retourne AUCUNE ligne.
    - tests/Feature/DiagnosticWizardTest.php et tests/Browser/CarnetLocalTest.php contiennent `assertSee('Avant de commencer')` à la place.
    - `vendor/bin/pest tests/Feature/DiagnosticWizardTest.php tests/Feature/DiagnosticRouteTest.php` sort en code 0.
    - tests/Feature/DiagnosticPdfTest.php est inchangé (diff vide).
  </acceptance_criteria>
  <done>Plus aucun test ne dépend de 'Trouver mon problème' ni d'un attribut data-mode-* ; la suite Feature reste verte sur l'état committé du Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Alpine) → Livewire | Le clic appelle `$wire.call('setMode', value)` ; `value` est contrôlé côté vue (Plan 02) mais reçu comme input non-fiable côté serveur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | DiagnosticWizard::setMode | mitigate | Valider `$mode` contre la liste blanche `['symptom','chemistry']` ; toute autre valeur est ignorée (pas d'assignation, pas d'exception) — empêche d'injecter un created_via arbitraire |
| T-10-02 | Information disclosure | created_via / type_probleme | accept | Champs internes de catégorisation, pas de PII ; valeurs bornées par la liste blanche |
</threat_model>

<verification>
- `vendor/bin/pest tests/Feature/DiagnosticWizardTest.php tests/Feature/DiagnosticRouteTest.php` sort en code 0 (suite Feature verte sur l'état committé).
- `grep -rn "Trouver mon problème\|data-mode-" tests/` ne retourne rien.
- `grep -c "function setMode" app/Livewire/DiagnosticWizard.php` >= 1.
- Aucune install de paquet (npm/composer) — pas de gate de légitimité requis.
</verification>

<success_criteria>
- setMode() existe, validée contre ['symptom','chemistry'], et atteint $mode → created_via/type_probleme.
- Tests d'attribution prouvent : symptom → created_via='depannage'/type_probleme='symptom' ; chemistry → created_via='wizard'/type_probleme='chemistry'.
- Tous les tests Feature passent ; aucun ne dépend plus de l'écran S0.
- keepDiagnostic() inchangé.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostic-retours-pierre/10-01-SUMMARY.md` when done
</output>
