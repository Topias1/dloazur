---
phase: 10-diagnostic-retours-pierre
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - resources/views/livewire/diagnostic-wizard.blade.php
  - tests/Feature/DiagnosticWizardTest.php
autonomous: true
requirements: [diag-1, diag-2]

must_haves:
  truths:
    - "L'utilisateur arrive direct sur l'arbre symptôme (disclaimer S4) — plus aucun écran de fourche mode initial"
    - "Analyser mon eau est un lien texte secondaire sous le CTA du disclaimer, qui mene au wizard chimie ET pose mode=chemistry cote serveur"
    - "Le Carnet est un bouton secondaire sur le disclaimer, visible seulement si carnetEntries.length > 0"
    - "Tous les boutons Recommencer / Retour Carnet repartent sur step:tree/nodeId:start (jamais sur l'ecran mode supprime)"
    - "La logique serveur created_via/type_probleme reste correcte (symptom par defaut, chemistry via le lien)"
  artifacts:
    - path: "resources/views/livewire/diagnostic-wizard.blade.php"
      provides: "Wizard entrant direct dans l'arbre symptome, alternatives chimie+carnet sur le disclaimer"
      contains: "step: 'tree'"
  key_links:
    - from: "lien Analyser mon eau (disclaimer)"
      to: "DiagnosticWizard::setMode + wizard chimie"
      via: "$wire.call('setMode','chemistry') puis advance({value:'chemistry', next:{kind:'wizard', id:'chemistry'}})"
      pattern: "setMode'?,? *'chemistry'"
    - from: "bouton Carnet (disclaimer)"
      to: "vue S9 carnet"
      via: "loadCarnetEntries(); showCarnet = true"
      pattern: "showCarnet *= *true"
---

<objective>
Appliquer la refonte DOM : supprimer l'ecran mode (S0), faire entrer l'utilisateur direct dans l'arbre symptome via le disclaimer (S4), et releguer Analyser mon eau + le Carnet en actions secondaires sur le disclaimer. Conserver l'attribution serveur via setMode() (creee en Plan 01).

Depend de 10-01 : setMode() doit exister cote serveur AVANT que le lien chimie l'appelle, et les tests doivent deja etre decouples de S0 (assertSee Avant de commencer) pour rester verts pendant la bascule.

Purpose: fidelite au proto de Pierre (diag-1/diag-2) — pas de choix au depart.
Output: vue diagnostic-wizard.blade.php refondue + bascule de l'assertion test vers le CTA renomme J'accepte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md
@.claude/skills/dloazur-frontend-stack/SKILL.md
@.planning/phases/10-diagnostic-retours-pierre/10-01-SUMMARY.md

<interfaces>
<!-- Methodes/etat Alpine et Livewire consommes par cette refonte (verifies). -->

Alpine x-data (vue ligne ~23) :
  step: 'mode'    -> DOIT devenir step: 'tree'   (D-02 ; nodeId reste 'start', mode reste null)
  advance(option) : pousse l'history, navigue ; la branche if (this.step === 'mode') (ligne ~52) ne se declenchera plus jamais au demarrage
  loadCarnetEntries() : charge carnetEntries depuis localStorage
  carnetEntries : array (vide => bouton carnet masque)
  resumeFromCarnet(entry) : ligne ~178 contient this.step = 'mode' -> DOIT devenir 'tree'

Livewire ($wire) :
  $wire.call('setMode', 'chemistry')  // cree en Plan 01 — pose $mode='chemistry' cote serveur
  $wire.disclaimerAccepted, acceptDisclaimer()

Blocs DOM a editer (numeros de ligne indicatifs, RE-VERIFIER avant edition) :
  - S0 : <div x-show="step === 'mode' && !showCarnet"> lignes ~221-285  -> SUPPRIMER integralement
  - Disclaimer S4 : <div x-show="step === 'tree' && nodeId === 'start' && !$wire.disclaimerAccepted"> lignes ~292-337  -> greffer chimie+carnet + renommer CTA
  - resumeFromCarnet : ligne ~178  -> step:'tree'
  - Boutons Recommencer : lignes ~1292, ~1468  -> step:'tree'; nodeId:'start'
  - Retours Carnet : lignes ~1496, ~1526  -> step:'tree' (au lieu de 'mode')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer S0, basculer l'etat Alpine initial, corriger toutes les cibles step:'mode'</name>
  <files>resources/views/livewire/diagnostic-wizard.blade.php</files>
  <read_first>
    - resources/views/livewire/diagnostic-wizard.blade.php (x-data ligne ~23 ; advance() lignes ~48-68 ; resumeFromCarnet() lignes ~174-182 ; bloc S0 lignes ~221-285 ; boutons Recommencer lignes ~1288-1297 et ~1465-1473 ; retours Carnet lignes ~1494-1502 et ~1524-1531)
    - .planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md (D-01, D-02, D-07 + specifics ligne 99)
  </read_first>
  <action>
    D-01 — Supprimer INTEGRALEMENT le bloc S0 : la div x-show="step === 'mode' && !showCarnet" x-transition.opacity.duration.200ms (du commentaire S0 — Choix du mode jusqu'a la div fermante avant le commentaire S4 — Disclaimer gate), y compris les 3 boutons data-mode-symptom, data-mode-chemistry, data-mode-carnet. Le bouton carnet S0 portait x-init="loadCarnetEntries()" — sa migration vers le disclaimer est traitee en Task 2 (D-06).

    D-02 — Dans le x-data (ligne ~24), changer step: 'mode', -> step: 'tree',. Laisser nodeId: 'start', et mode: null, inchanges.

    D-07 — Dans resumeFromCarnet(entry) (ligne ~178), remplacer le corps par exactement (specifics CONTEXT ligne 99) : this.showCarnet = false; this.step = 'tree'; this.nodeId = 'start'; this.history = []; this.resultId = null; — c-a-d remplacer this.step = 'mode' par this.step = 'tree' (nodeId 'start' deja present).

    Boutons Recommencer (lignes ~1292 et ~1468) : remplacer step = 'mode' par step = 'tree' dans les deux @click (garder le reste : nodeId = 'start'; history = []; resultId = null; triedActions = []; et, pour le 1292, wizardStep = 1;). Sans ce correctif, Recommencer atterrirait sur un ecran supprime (page blanche).

    Retours Carnet (lignes ~1496 et ~1526) : remplacer @click="showCarnet = false; step = 'mode';" par @click="showCarnet = false; step = 'tree'; nodeId = 'start';". Meme raison.

    NE PAS toucher la branche if (this.step === 'mode') dans advance() (lignes ~52-55) : laissee inerte (jamais atteinte), elle n'est pas nuisible et son retrait n'est pas demande. La synchro serveur du mode passe desormais par setMode() (Task 2). Respecter SKILL : wire:ignore.self conserve, tokens @theme/OKLCH, pas de hex sauf #25D366.
  </action>
  <verify>
    <automated>grep -n "step: 'tree'\|step === 'mode'\|step = 'mode'\|data-mode-" resources/views/livewire/diagnostic-wizard.blade.php; vendor/bin/pest tests/Feature/DiagnosticRouteTest.php tests/Feature/DiagnosticWizardTest.php 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - x-data contient step: 'tree' (grep retourne la ligne ~24) et ne contient plus step: 'mode'.
    - Le fichier ne contient plus AUCUN data-mode-symptom/data-mode-chemistry/data-mode-carnet (grep vide).
    - Le fichier ne contient plus le bloc x-show="step === 'mode' && !showCarnet" (grep de cette chaine vide).
    - grep -c "step = 'mode'" resources/views/livewire/diagnostic-wizard.blade.php == 0 (resumeFromCarnet + 2 Recommencer + 2 retours Carnet tous migres vers tree).
    - resumeFromCarnet contient this.step = 'tree' et this.nodeId = 'start'.
    - vendor/bin/pest tests/Feature/DiagnosticRouteTest.php tests/Feature/DiagnosticWizardTest.php sort en code 0.
  </acceptance_criteria>
  <done>S0 supprime, etat initial = tree/start, plus aucune cible mode residuelle ; la suite Feature reste verte.</done>
</task>

<task type="auto">
  <name>Task 2: Greffer Analyser mon eau + Carnet sur le disclaimer et cabler setMode('chemistry')</name>
  <files>resources/views/livewire/diagnostic-wizard.blade.php</files>
  <read_first>
    - resources/views/livewire/diagnostic-wizard.blade.php (bloc disclaimer S4 lignes ~292-337 : la div racine, le bouton J'ai compris lignes ~322-330, le bloc px-6 pb-6)
    - ancien bouton chimie S0 supprime (reference du signature advance : advance({ value: 'chemistry', next: { kind: 'wizard', id: 'chemistry' } }))
    - ancien bouton carnet S0 supprime (reference : x-init="loadCarnetEntries()", x-show="carnetEntries.length > 0", @click="loadCarnetEntries(); showCarnet = true;")
    - .planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md (D-03, D-04, D-05, D-06, D-08 + specifics)
  </read_first>
  <action>
    D-06 — Sur la div racine du disclaimer (la div x-show="step === 'tree' && nodeId === 'start' && !$wire.disclaimerAccepted", ligne ~292), ajouter x-init="loadCarnetEntries()" (migre depuis l'ancien bouton carnet S0). Cela charge carnetEntries au bon moment pour conditionner le bouton carnet ci-dessous.

    D-03 (renommage CTA + D-08) — Le bouton principal du disclaimer (J'ai compris, voir les recommandations, ligne ~329) : renommer le libelle visible en J'accepte et commence. Conserver intacts wire:click="acceptDisclaimer" et @click="$wire.disclaimerAccepted = true" et les classes. Ce renommage rend l'assertion assertSee(J'accepte) valide — reactivee en Task 3.

    D-04 (lien chimie secondaire) — Juste SOUS le bouton J'accepte et commence (dans le meme conteneur px-6 pb-6, avant ou apres le petit texte En continuant), inserer un <button type="button"> de STYLE LIEN DISCRET (pas un bouton outline : classes type text-sm text-ink-500 hover:text-ink-800 underline underline-offset-2, focus-visible ring azure ; PAS de bg-* / ring-1 de carte). Libelle EXACT (vouvoiement) : Vous avez vos mesures ? -> Analyser mon eau. Au @click, dans l'ordre : $wire.call('setMode', 'chemistry'); advance({ value: 'chemistry', next: { kind: 'wizard', id: 'chemistry' } }). Le $wire.call('setMode','chemistry') est OBLIGATOIRE ici : depuis step:'tree', la branche if (this.step === 'mode') d'advance() ne se declenche pas, donc sans cet appel explicite le serveur perdrait l'attribution chimie (created_via/type_probleme). Note : l'utilisateur peut atteindre le wizard chimie meme si disclaimerAccepted est false — le guard serveur DIAG-03 dans computeDoses() bloque deja le calcul tant que le disclaimer n'est pas accepte ; le wizard chimie reaffiche d'ailleurs son propre encart disclaimer (vue ligne ~454). Donc le lien chimie n'a PAS a exiger l'acceptation prealable.

    D-05 (bouton Carnet secondaire) — Sous le lien chimie, inserer un <button type="button"> conditionne x-show="carnetEntries.length > 0", @click="loadCarnetEntries(); showCarnet = true;", libelle Mes diagnostics passes (variante secondaire discrete coherente avec le lien chimie). aria-label Mes diagnostics passes, carnet local.

    Respecter SKILL : OKLCH/@theme uniquement, pas de hex sauf #25D366, icones via <x-icon.NAME>, x-cloak deja gere sur la racine.
  </action>
  <verify>
    <automated>grep -n "J'accepte et commence\|Analyser mon eau\|setMode\|carnetEntries.length > 0\|loadCarnetEntries" resources/views/livewire/diagnostic-wizard.blade.php; vendor/bin/pest tests/Feature/DiagnosticWizardTest.php 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Le disclaimer contient le libelle J'accepte et commence (ancien J'ai compris, voir les recommandations remplace dans CE bloc).
    - Le disclaimer contient le lien Vous avez vos mesures ? -> Analyser mon eau en style lien discret (pas de bg-white/ring-1 de carte sur cet element).
    - Le @click du lien chimie contient $wire.call('setMode', 'chemistry') ET advance({ value: 'chemistry', next: { kind: 'wizard', id: 'chemistry' } }), dans cet ordre.
    - La div racine du disclaimer porte x-init="loadCarnetEntries()".
    - Le bouton Carnet est conditionne x-show="carnetEntries.length > 0" et fait @click="loadCarnetEntries(); showCarnet = true;".
    - vendor/bin/pest tests/Feature/DiagnosticWizardTest.php sort en code 0.
  </acceptance_criteria>
  <done>Le disclaimer offre J'accepte et commence + lien chimie (cable setMode) + bouton carnet conditionnel ; l'attribution serveur chimie est preservee.</done>
</task>

<task type="auto">
  <name>Task 3: Reactiver l'assertion J'accepte et verifier le parcours complet</name>
  <files>tests/Feature/DiagnosticWizardTest.php, tests/Browser/CarnetLocalTest.php</files>
  <read_first>
    - tests/Feature/DiagnosticWizardTest.php (assertion adaptee en Plan 01 : assertSee('Avant de commencer') ligne ~21)
    - tests/Browser/CarnetLocalTest.php (assertion adaptee en Plan 01 ligne ~101)
    - .planning/phases/10-diagnostic-retours-pierre/10-CONTEXT.md (D-08)
  </read_first>
  <action>
    D-08 — Maintenant que le CTA du disclaimer s'appelle J'accepte et commence (Task 2), basculer l'assertion du test /diagnostic de assertSee('Avant de commencer') vers assertSee("J'accepte") dans tests/Feature/DiagnosticWizardTest.php (ligne ~21). C'est le contenu premier visible avec l'etat initial step:'tree'/nodeId:'start'/disclaimerAccepted:false, robuste et lie au flux reel. Laisser tests/Browser/CarnetLocalTest.php sur Avant de commencer (titre stable) OU basculer aussi vers J'accepte au choix — l'un ou l'autre est acceptable tant que la chaine assertee est presente dans le HTML rendu.

    Apres bascule, executer la suite complete diagnostic pour prouver la non-regression de bout en bout.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/DiagnosticWizardTest.php tests/Feature/DiagnosticRouteTest.php tests/Feature/DiagnosticPdfTest.php tests/Browser/CarnetLocalTest.php 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/Feature/DiagnosticWizardTest.php ligne ~21 contient assertSee("J'accepte") et plus assertSee('Trouver mon probleme').
    - grep -rn "Trouver mon" tests/ ne retourne rien.
    - vendor/bin/pest tests/Feature/DiagnosticWizardTest.php tests/Feature/DiagnosticRouteTest.php tests/Feature/DiagnosticPdfTest.php tests/Browser/CarnetLocalTest.php sort en code 0.
    - Le test wizard x-data is complete and not truncated reste vert (le x-data refondu reste brace-balanced et contient toujours showRetestPrompt/onRetestNon/confidenceLabel).
  </acceptance_criteria>
  <done>L'assertion principale cible le CTA reel J'accepte ; toute la suite diagnostic (Feature + Browser) passe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (Alpine) -> Livewire | Le lien chimie appelle $wire.call('setMode','chemistry') ; la valeur est figee dans la vue mais recue comme input cote serveur |
| client localStorage -> vue carnet | carnetEntries est lu depuis localStorage (DIAG-07) et rendu via x-text uniquement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | setMode (appele par le lien chimie) | mitigate | Validation liste-blanche cote serveur (faite en Plan 01) ; la vue n'envoie qu'une constante 'chemistry' |
| T-10-03 | Tampering (XSS) | rendu carnet (S9) | mitigate | Conserver le rendu via x-text (jamais innerHTML) — invariant existant T-05-20, ne pas le rompre lors des edits |
| T-10-04 | Spoofing | acces wizard chimie sans disclaimer | accept | Le guard serveur DIAG-03 (computeDoses) bloque tout calcul tant que disclaimerAccepted est false ; l'affichage du formulaire sans calcul est inoffensif |
</threat_model>

<verification>
- vendor/bin/pest tests/Feature/DiagnosticWizardTest.php tests/Feature/DiagnosticRouteTest.php tests/Feature/DiagnosticPdfTest.php tests/Browser/CarnetLocalTest.php sort en code 0.
- grep "step: 'tree'" resources/views/livewire/diagnostic-wizard.blade.php retourne l'etat initial ; grep "step = 'mode'\|step === 'mode' && !showCarnet\|data-mode-" retourne vide.
- Le disclaimer contient J'accepte et commence + Analyser mon eau (lien) + bouton carnet conditionnel.
- Aucune install de paquet — pas de gate de legitimite requis.
</verification>

<success_criteria>
- Entree directe dans l'arbre symptome via le disclaimer ; ecran mode supprime (diag-1/diag-2).
- Analyser mon eau et Carnet relegues en actions secondaires sur le disclaimer.
- Attribution serveur preservee : symptom -> created_via='depannage' (defaut) ; chemistry via le lien -> created_via='wizard'/type_probleme='chemistry'.
- Tous les boutons de relance pointent sur tree/start.
- Suite diagnostic complete verte.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostic-retours-pierre/10-02-SUMMARY.md` when done
</output>
