---
phase: 02-mvp-suivi-offline-first
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - resources/views/admin/dashboard.blade.php
  - resources/views/components/admin/stat-card.blade.php
  - app/Http/Controllers/Admin/DashboardController.php
  - routes/admin.php
files_created:
  - app/Livewire/PassageIndex.php
  - resources/views/livewire/passage-index.blade.php
  - resources/views/admin/passages/index.blade.php
  - tests/Feature/PassageIndexTest.php
  - tests/Feature/DashboardStatsTest.php
autonomous: true
requirements:
  - PASS-05
user_setup: []
tags: [livewire, passages, dashboard, stat-cards, filters]
must_haves:
  truths:
    - "Pierre voit l'historique des passages sur /admin/passages avec filtres client_id (select) et plage de dates (deux inputs date)"
    - "La liste passages est triée visited_at DESC par défaut, paginée 25/page (D-62)"
    - "Pierre peut filtrer par client_id et par date_from/date_to ; changer un filtre reset à la page 1"
    - "Le dashboard /admin affiche 4 stat cards avec valeurs réelles : passages cette semaine (count), clients actifs (count), eau à surveiller (count passages avec au moins une mesure hors plage), à synchroniser (placeholder dynamique côté Alpine — Plan 02-05 alimentera, ici affichage '0' ou state offline neutre)"
    - "Le stat-card composant supporte 3 états : default (text-ink-950), offline (text-[oklch(0.5_0.11_72)] ambre), warn (text-danger rouge)"
    - "L'item Passages de la sidebar et de la bottom-nav est basculé en actif (route admin.passages.*) — non plus grisé bientôt"
    - "L'empty state liste passages affiche 'Aucun passage enregistré.' + sous-titre + CTA placeholder (bouton 'Nouveau passage' visible mais désactivé tant que Plan 02-05 n'active pas la route)"
  artifacts:
    - path: "app/Livewire/PassageIndex.php"
      provides: "Livewire 3 historique passages + filtres client_id + date range + pagination 25/page"
      contains: "WithPagination"
    - path: "resources/views/livewire/passage-index.blade.php"
      provides: "Vue listing cartes UI-SPEC §Historique passages (date, client, mesures résumé, statut chip, photos count)"
      contains: "visited_at"
    - path: "resources/views/components/admin/stat-card.blade.php"
      provides: "Composant avec 3 états (default/offline/warn) selon UI-SPEC §Dashboard admin Stat cards"
      contains: "offline"
  key_links:
    - from: "resources/views/admin/dashboard.blade.php"
      to: "App\\Models\\Passage::whereBetween('visited_at', [now()->startOfWeek(), now()->endOfWeek()])->count()"
      via: "DashboardController calcule \$passagesThisWeek"
      pattern: "passagesThisWeek"
    - from: "resources/views/admin/passages/index.blade.php"
      to: "App\\Livewire\\PassageIndex"
      via: "<livewire:passage-index />"
      pattern: "livewire:passage-index"
---

<objective>
Activer la liste historique des passages côté pro (PASS-05) + transformer le dashboard /admin du stub Phase 1 (4 valeurs `—`) en dashboard fonctionnel avec les 4 stat-cards réelles (D-62). Bascule de l'item Passages des nav components vers actif. Le bouton "Nouveau passage" du topbar reste désactivé : il sera activé par Plan 02-05 (qui ajoute la route `/admin/passages/create`). Le stat-card "À synchroniser" affiche `'0'` en attendant Plan 02-05 qui le branche sur le store Alpine `offlineQueue.pendingCount`.

Purpose : sans ce plan, Pierre n'a aucune vue d'ensemble de son activité. PASS-05 (filtres client + date) est un requirement métier livrable de Phase 2.

Output : 1 composant Livewire `PassageIndex` + 1 vue livewire/passage-index + 1 vue admin/passages/index + dashboard étoffé (DashboardController calcule les 4 compteurs, dashboard.blade.php passe les valeurs aux stat-cards) + stat-card étendu (3 états). 2 fichiers tests Pest (PassageIndexTest, DashboardStatsTest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-suivi-offline-first/02-CONTEXT.md
@.planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md
@.planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md
@.planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md
@.planning/phases/02-mvp-suivi-offline-first/02-02-SUMMARY.md
@.planning/phases/01-vitrine-fondations/01-02-SUMMARY.md
@.planning/phases/01-vitrine-fondations/01-05-SUMMARY.md
@CLAUDE.md
@DESIGN.md

<interfaces>

From app/Models/Passage.php :
- $fillable inclut `client_uuid, piscine_id, client_id, visited_at, status, ph_avant, ph_apres, chlore_libre, chlore_total, tac, th, sel_g_l, actions, notes`
- $casts : `visited_at => datetime, synced_at => datetime, actions => array, ph_avant => decimal:2, …`
- Relations : client() BelongsTo, piscine() BelongsTo, photos() HasMany, signature() HasOne

From app/Http/Controllers/Admin/DashboardController.php (état Phase 1 — simple GET → view) :
- `public function index(): \Illuminate\View\View { return view('admin.dashboard', ['user' => auth()->user()]); }`
- Phase 2 étend cette méthode pour calculer $passagesThisWeek, $clientsCount, $eauASurveiller, $aSynchroniser.

From resources/views/components/admin/stat-card.blade.php (Phase 1) :
- `@props(['label', 'value' => '—', 'state' => 'default'])`
- Actuellement deux états : default (text-ink-950), warn (text-danger).
- Phase 2 ajoute `'offline'` → couleur ambre `text-[oklch(0.5_0.11_72)]` (UI-SPEC §Dashboard admin Stat cards "Règle ambre" : `state='offline'` = ambre, `state='warn'` = rouge).

From resources/views/admin/dashboard.blade.php (Phase 1) :
- 4 stat cards avec value `—` : "Clients actifs", "Passages cette semaine", "À synchroniser", "Factures en attente"
- Phase 2 remplace "Factures en attente" par "Eau à surveiller" (UI-SPEC §Dashboard admin Stat cards) — c'est un changement de label, factures arrive en Phase 3.

From .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md §"Historique passages (PASS-05, Livewire)" :
- Filtres : `client_id` select `h-10 rounded-xl ring-1 ring-sand-200` + 2 inputs `date` (date_from, date_to). Desktop inline, mobile collapsible.
- Vue liste : cartes `rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-4`
  - Date + heure : Fredoka 600 + sub `text-sm text-ink-500`
  - Client + volume : `text-sm text-ink-500`
  - Résumé mesures : `text-sm text-ink-400` (pH x,x · Cl x,x)
  - Photos count : icône caméra + chiffre `text-xs text-ink-400`
  - Statut chip : `synced` → `success/10 text-success` · `draft` → `warn/15 text-[oklch(0.5_0.11_72)]` · `error` → `danger/10 text-danger`
- Pagination 25/page

From routes/admin.php (état après 02-02) :
- Routes clients.* nommées + GET / → dashboard
- Phase 2-03 ajoute : `Route::get('passages', \App\Livewire\PassageIndex::class)->name('passages.index');` OU controller fin avec `<livewire:passage-index />` (préférer route Livewire full-page pour simplicité ; mais besoin du shell admin → utiliser une vue qui embed le composant comme dans 02-02).

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : PassageIndex Livewire + tests filtres + route + stat-card extension</name>
  <files>app/Livewire/PassageIndex.php, resources/views/components/admin/stat-card.blade.php, routes/admin.php, tests/Feature/PassageIndexTest.php, tests/Feature/DashboardStatsTest.php, app/Http/Controllers/Admin/DashboardController.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "app/Livewire/PassageIndex.php", "resources/views/components/admin/stat-card.blade.php" — diff complet)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 8 (WithPagination) + D-62
    - .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md §"Dashboard admin — Stat cards" + §"Historique passages"
    - app/Livewire/ClientIndex.php (analog WithPagination + reset on update — copier le pattern)
    - app/Http/Controllers/Admin/DashboardController.php (état Phase 1)
    - resources/views/components/admin/stat-card.blade.php (état Phase 1 — 2 états)
    - database/factories/PassageFactory.php (pour les tests)
    - tests/Feature/AdminShellTest.php (analog pour DashboardStatsTest)
  </read_first>
  <behavior>
    **tests/Feature/PassageIndexTest.php** (couvre PASS-05) :
    - Test 1 : "Pierre voit la liste passages vide affiche l'empty state" — actingAs($pierre)->get(route('admin.passages.index'))->assertSee('Aucun passage enregistré.')
    - Test 2 : "Liste affiche les passages triés visited_at DESC" — créer 3 passages aux dates 2026-05-20, 2026-05-25, 2026-05-15, assertSeeInOrder pour les 3 ids
    - Test 3 : "Filtre client_id ne retourne que les passages de ce client" — créer client A avec 2 passages, client B avec 1 passage, `Livewire::test(PassageIndex::class)->set('clientId', $clientA->id)->assertSet('passages.total', 2)` (vérifier via le composant Livewire)
    - Test 4 : "Filtre date_from exclut les passages antérieurs" — créer 3 passages aux dates 2026-04-01, 2026-04-15, 2026-04-30, `Livewire::test(PassageIndex::class)->set('dateFrom', '2026-04-15')->assertSet('passages.total', 2)`
    - Test 5 : "Filtre date_to exclut les passages postérieurs" — symétrique
    - Test 6 : "Changement filtre client_id reset à la page 1" — set('clientId', $c->id)->assertSet('paginators.page', 1)
    - Test 7 : "Anonyme redirigé vers /login" — `$this->get(route('admin.passages.index'))->assertRedirect('/login');`

    **tests/Feature/DashboardStatsTest.php** :
    - Test 1 : "Dashboard affiche un compteur 'Passages cette semaine' qui matche les passages de la semaine courante" — créer 2 passages cette semaine + 1 passage semaine dernière, get(/admin), assertSee('2')
    - Test 2 : "Dashboard affiche 'Clients actifs' = nombre total de clients" — créer 5 clients, get(/admin), assertSee('5')
    - Test 3 : "Dashboard affiche 'Eau à surveiller' = nombre de passages dont au moins une mesure hors plage soft (D-63)" — créer 1 passage avec ph_avant=10.0 (> 9.0 hors plage), 1 passage ph_avant=7.2 (dans la plage), get(/admin), assertSee('1')
    - Test 4 : "Stat-card state='offline' applique la classe ambre OKLCH" — render le composant `<x-admin.stat-card label="À synchroniser" :value="0" state="offline" />` et `assertSee('text-[oklch(0.5_0.11_72)]', false)` OU vérification via blade-render et stringContains
    - Test 5 : "Stat-card state='warn' applique text-danger" — render avec state='warn' et assertSee('text-danger', false)
    - Test 6 : "Stat-card state='default' applique text-ink-950" — render et assertSee('text-ink-950', false)
  </behavior>
  <action>
    1. Créer `app/Livewire/PassageIndex.php` selon PATTERN map :
    ```
    namespace App\Livewire;
    use App\Models\Client;
    use App\Models\Passage;
    use Livewire\Component;
    use Livewire\WithPagination;

    class PassageIndex extends Component
    {
        use WithPagination;

        public string $clientId = '';
        public string $dateFrom = '';
        public string $dateTo   = '';

        public function updatedClientId(): void { $this->resetPage(); }
        public function updatedDateFrom(): void { $this->resetPage(); }
        public function updatedDateTo(): void { $this->resetPage(); }

        public function render(): \Illuminate\View\View
        {
            $passages = Passage::query()
                ->with(['client:id,name', 'piscine:id,nom,volume_m3'])
                ->withCount('photos')
                ->when($this->clientId !== '', fn ($q) => $q->where('client_id', (int) $this->clientId))
                ->when($this->dateFrom !== '', fn ($q) => $q->whereDate('visited_at', '>=', $this->dateFrom))
                ->when($this->dateTo !== '', fn ($q) => $q->whereDate('visited_at', '<=', $this->dateTo))
                ->orderBy('visited_at', 'desc')
                ->paginate(25);

            $clients = Client::orderBy('name')->get(['id', 'name']);

            return view('livewire.passage-index', compact('passages', 'clients'));
        }
    }
    ```

    2. Étendre `app/Http/Controllers/Admin/DashboardController.php` pour calculer les 4 compteurs :
    ```
    use App\Models\Client;
    use App\Models\Passage;
    use Illuminate\Support\Carbon;

    public function index(): \Illuminate\View\View
    {
        $passagesThisWeek = Passage::whereBetween('visited_at', [
            Carbon::now()->startOfWeek(),
            Carbon::now()->endOfWeek(),
        ])->count();

        $clientsCount = Client::count();

        // Eau à surveiller : count des passages dont au moins une mesure est hors plage soft (D-63)
        // Plages soft : pH [5.0, 9.0], chlore_libre [0, 10], chlore_total [0, 15], tac [50, 300], sel_g_l [0, 8], th [0, 50]
        $eauASurveiller = Passage::query()
            ->where(function ($q) {
                $q->whereNotBetween('ph_avant', [5.0, 9.0])
                  ->orWhereNotBetween('chlore_libre', [0, 10])
                  ->orWhereNotBetween('chlore_total', [0, 15])
                  ->orWhereNotBetween('tac', [50, 300])
                  ->orWhereNotBetween('sel_g_l', [0, 8])
                  ->orWhereNotBetween('th', [0, 50]);
            })
            ->whereDate('visited_at', '>=', Carbon::now()->subDays(30))
            ->count();

        // À synchroniser : placeholder serveur — Plan 02-05 brancher sur Alpine $store.offlineQueue
        // En attendant : 0 (le badge sera dynamique côté client)
        $aSynchroniser = 0;

        return view('admin.dashboard', [
            'user' => auth()->user(),
            'passagesThisWeek' => $passagesThisWeek,
            'clientsCount' => $clientsCount,
            'eauASurveiller' => $eauASurveiller,
            'aSynchroniser' => $aSynchroniser,
        ]);
    }
    ```

    3. Modifier `resources/views/components/admin/stat-card.blade.php` pour supporter 3 états :
    ```
    @props(['label', 'value' => '—', 'state' => 'default'])

    <div class="rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-5">
        <p class="text-sm text-ink-500">{{ $label }}</p>
        <p @class([
            'mt-1 font-display font-semibold text-3xl tabular-nums',
            'text-ink-950'              => $state === 'default',
            'text-[oklch(0.5_0.11_72)]' => $state === 'offline',
            'text-danger'               => $state === 'warn',
        ])>
            {{ $value }}
        </p>
    </div>
    ```
    Règle UI-SPEC §"Règle ambre" : `state='offline'` = ambre (synchro), `state='warn'` = rouge (eau hors plage). Ne JAMAIS inverser. (Critical Flag #5 PATTERNS.md)

    4. Ajouter dans `routes/admin.php` :
    ```
    Route::get('passages', App\Livewire\PassageIndex::class)->name('passages.index');
    ```
    Note : Livewire 3 supporte les "full-page components" via `Route::get(..., ComponentClass::class)`. La vue rendue par render() doit étendre le layout via `#[Layout('layouts.admin')]` attribut PHP, OU on garde le pattern existant avec une vue admin/passages/index.blade.php intermédiaire. Choix : utiliser une vue intermédiaire (cohérent avec 02-02 clients).
    Donc en fait : `Route::get('passages', fn () => view('admin.passages.index'))->name('passages.index');` OU via controller. Préférer controller pour la cohérence :
    Ajouter à `App\Http\Controllers\Admin\ClientController` ? Non, créer une route inline qui rend la vue. Solution simple :
    ```
    Route::view('passages', 'admin.passages.index')->name('passages.index');
    ```
    La vue `admin/passages/index.blade.php` (créée en Task 2) embed `<livewire:passage-index />`.

    5. Créer les 2 fichiers tests (PassageIndexTest, DashboardStatsTest) selon les contrats <behavior> ci-dessus. Pattern : `uses(RefreshDatabase::class);` en tête. Pour DashboardStatsTest Test 4/5/6 (render stat-card) : utiliser Blade::render() ou simplement HttpTest sur /admin après seed pour assertSee la classe CSS dans le HTML rendu.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PassageIndexTest.php tests/Feature/DashboardStatsTest.php --ci</automated>
  </verify>
  <acceptance_criteria>
    - `./vendor/bin/pest tests/Feature/PassageIndexTest.php --ci` : 7/7 verts
    - `./vendor/bin/pest tests/Feature/DashboardStatsTest.php --ci` : 6/6 verts
    - `grep -c "WithPagination" app/Livewire/PassageIndex.php` ≥ 1
    - `grep -c "client_id" app/Livewire/PassageIndex.php` ≥ 1
    - `grep -c "visited_at" app/Livewire/PassageIndex.php` ≥ 1
    - `grep -c "passages\|admin.passages.index" routes/admin.php` ≥ 1
    - `grep -c "'offline'" resources/views/components/admin/stat-card.blade.php` ≥ 1
    - `grep -c "oklch(0.5_0.11_72)" resources/views/components/admin/stat-card.blade.php` ≥ 1
    - `grep -c "passagesThisWeek\|eauASurveiller\|aSynchroniser\|clientsCount" app/Http/Controllers/Admin/DashboardController.php` = 4
    - `php artisan route:list --path=admin/passages 2>&1 | grep -c "admin.passages.index"` ≥ 1
    - `./vendor/bin/pest --ci` : suite totale verte
  </acceptance_criteria>
  <done>Composant PassageIndex avec filtres (client_id + date range) + pagination 25/page + tri visited_at DESC opérationnel ; stat-card étendu à 3 états ; DashboardController calcule 4 compteurs réels (sauf À synchroniser qui reste 0 jusqu'à Plan 02-05) ; 13 tests passants.</done>
</task>

<task type="auto">
  <name>Task 2 : Vues dashboard + admin/passages/index + livewire/passage-index + activation Passages dans sidebar/bottom-nav</name>
  <files>resources/views/admin/dashboard.blade.php, resources/views/admin/passages/index.blade.php, resources/views/livewire/passage-index.blade.php, resources/views/components/admin/sidebar.blade.php, resources/views/components/admin/mobile-bottom-nav.blade.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md (§"Historique passages", §"Dashboard admin — Stat cards" — labels exacts, §"Empty states", §"Admin shell — Sidebar/Mobile bottom-nav — ACTIVATION Phase 2")
    - resources/views/admin/dashboard.blade.php (état Phase 1 — 4 cards avec —)
    - resources/views/components/admin/sidebar.blade.php (état après 02-02 — Clients déjà actif, Passages encore grisé)
    - resources/views/components/admin/mobile-bottom-nav.blade.php (état après 02-02)
    - mockups/v1/dashboard.html (référence visuelle pour la vue dashboard et la liste passages)
    - DESIGN.md (tokens OKLCH ; Fredoka 600 titre, tabular-nums sur les chiffres)
  </read_first>
  <action>
    1. Modifier `resources/views/admin/dashboard.blade.php` pour passer les valeurs réelles aux 4 stat-cards. Remplacer la grille existante par :
    ```
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
        <x-admin.stat-card label="Passages cette semaine" :value="$passagesThisWeek" />
        <x-admin.stat-card label="À synchroniser" :value="$aSynchroniser" state="offline" />
        <x-admin.stat-card label="Clients actifs" :value="$clientsCount" />
        <x-admin.stat-card label="Eau à surveiller" :value="$eauASurveiller" :state="$eauASurveiller > 0 ? 'warn' : 'default'" />
    </div>
    ```
    Retirer le placeholder "Les données opérationnelles arrivent en Phase 2…" (Phase 2 EST là). Garder le greeting "Bonjour Pierre,".
    Optionnellement, ajouter une carte "Prochains passages" ou "Tournée du jour" (UI-SPEC §dashboard p.470 mockup), mais ce n'est pas un must-have de PASS-05 → laisser pour Plan 02-05 (qui ajoute la création de passages).

    2. Créer `resources/views/admin/passages/index.blade.php` (analog admin/clients/index.blade.php) :
    ```
    @extends('layouts.admin')
    @section('title', 'Passages · Dlo Azur')
    @section('sidebar') <x-admin.sidebar :user="auth()->user()" /> @endsection
    @section('topbar') <x-admin.topbar /> @endsection
    @section('main') <livewire:passage-index /> <x-admin.mobile-bottom-nav /> @endsection
    ```

    3. Créer `resources/views/livewire/passage-index.blade.php` selon UI-SPEC §"Historique passages" :
    - Wrapper `<div class="px-5 sm:px-8 py-7 space-y-7">`
    - Header bloc : titre Fredoka 600 `text-2xl sm:text-3xl text-ink-950` "Passages" + boutons à droite. Bouton "Nouveau passage" : `<a href="#" aria-disabled="true" class="h-11 px-5 rounded-xl bg-azure-500/40 text-white font-semibold cursor-not-allowed opacity-50">Nouveau passage</a>` tant que Plan 02-05 n'ajoute pas la route (TODO commentaire HTML).
    - Filtres bar : `<div class="flex flex-wrap gap-3 items-end">`
      - Select client : `<select wire:model.live="clientId" class="h-10 px-3 rounded-xl ring-1 ring-sand-200 bg-white text-sm"><option value="">Tous les clients</option>@foreach($clients as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select>`
      - Input date from : `<input wire:model.live.debounce.300ms="dateFrom" type="date" aria-label="Depuis" class="h-10 px-3 rounded-xl ring-1 ring-sand-200 bg-white text-sm">`
      - Input date to : `<input wire:model.live.debounce.300ms="dateTo" type="date" aria-label="Jusqu'à" class="h-10 px-3 rounded-xl ring-1 ring-sand-200 bg-white text-sm">`
      - Bouton reset si filtres actifs : `@if($clientId || $dateFrom || $dateTo)<button wire:click="$set('clientId','');$set('dateFrom','');$set('dateTo','')" type="button" class="h-10 px-3 rounded-xl text-sm text-ink-600 hover:bg-sand-100">Réinitialiser</button>@endif`
    - Liste : `<div class="space-y-3"> @forelse ($passages as $p) … @empty <empty-state /> @endforelse </div>`
    - Carte passage UI-SPEC : `<a href="#" class="block rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-4 hover:bg-sand-50 transition-colors">`
      - Date Fredoka 600 `font-semibold text-ink-900` : `{{ $p->visited_at?->isoFormat('dddd D MMMM') ?? '—' }}` + heure `text-sm text-ink-500` : `{{ $p->visited_at?->format('H\\hi') }}`
      - Client + piscine : `<p class="text-sm text-ink-500">{{ $p->client->name }} · {{ optional($p->piscine)->volume_m3 }} m³</p>`
      - Résumé mesures (Inter 400 text-sm text-ink-400 tabular-nums) : `pH {{ number_format($p->ph_avant ?? 0, 1) }} · Cl {{ number_format($p->chlore_libre ?? 0, 1) }} mg/L`
      - Compteur photos : `<span class="text-xs text-ink-400 inline-flex items-center gap-1">📷 {{ $p->photos_count }}</span>` (icône caméra SVG inline)
      - Statut chip selon UI-SPEC :
        ```
        @if($p->status === 'synced')<span class="rounded-full bg-success/10 text-success text-xs font-semibold px-2.5 py-0.5">Synchronisé</span>
        @elseif($p->status === 'draft')<span class="rounded-full bg-warn/15 text-[oklch(0.5_0.11_72)] text-xs font-semibold px-2.5 py-0.5">Brouillon</span>
        @elseif($p->status === 'error')<span class="rounded-full bg-danger/10 text-danger text-xs font-semibold px-2.5 py-0.5">Erreur</span>
        @endif
        ```
    - Empty state (UI-SPEC §Empty states "Liste passages vide") :
      ```
      <div class="rounded-2xl bg-sand-50 ring-1 ring-sand-200 p-8 text-center">
        <h2 class="font-display font-semibold text-xl text-ink-950">Aucun passage enregistré.</h2>
        <p class="text-ink-500 mt-2">Commencez par saisir un passage sur le terrain.</p>
        {{-- CTA "Nouveau passage" activé par Plan 02-05 --}}
      </div>
      ```
    - Pagination : `{{ $passages->links() }}`
    - Locale : pour `isoFormat('dddd D MMMM')` en français, le projet doit avoir `App::setLocale('fr')` ou `Carbon::setLocale('fr')` global. Vérifier qu'`config/app.php` `'locale' => 'fr'` (Phase 1 doit l'avoir mis ; sinon utiliser `format('d/m/Y')` qui est locale-indépendant).

    4. Modifier `resources/views/components/admin/sidebar.blade.php` :
    - Basculer l'item "Passages" du `<span aria-disabled>` vers `<a href="{{ route('admin.passages.index') }}" @class([... 'bg-white/10 text-white' => request()->routeIs('admin.passages.*') ...]) @if(request()->routeIs('admin.passages.*')) aria-current="page" @endif>` (même pattern que Clients en 02-02).
    - Retirer le badge "bientôt" pour l'item Passages.
    - Garder Factures + Catalogue grisés.

    5. Modifier `resources/views/components/admin/mobile-bottom-nav.blade.php` :
    - Basculer l'item "Passages" en lien actif (même pattern que Clients en 02-02).

    NOTE : Le bouton "Nouveau passage" du topbar reste désactivé jusqu'à Plan 02-05. Ne pas l'activer ici pour éviter une route 404.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/AdminShellTest.php tests/Feature/PassageIndexTest.php tests/Feature/DashboardStatsTest.php tests/Feature/AccessibilityTest.php --ci && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 5 fichiers de vues créés/modifiés
    - `grep -c "route('admin.passages" resources/views/components/admin/sidebar.blade.php` ≥ 1
    - `grep -c "route('admin.passages" resources/views/components/admin/mobile-bottom-nav.blade.php` ≥ 1
    - `grep -c ":value=\"\$passagesThisWeek\"" resources/views/admin/dashboard.blade.php` ≥ 1
    - `grep -c "state=\"offline\"" resources/views/admin/dashboard.blade.php` ≥ 1
    - `grep -c "Aucun passage enregistré" resources/views/livewire/passage-index.blade.php` ≥ 1
    - `grep -c "wire:model.live" resources/views/livewire/passage-index.blade.php` ≥ 3 (clientId + dateFrom + dateTo)
    - `npm run build` exit 0
    - Smoke test interactif : login → /admin → 4 stat-cards avec vraies valeurs (0 ou seedées) → cliquer sidebar Passages → liste vide affiche "Aucun passage enregistré" → filtre client_id select fonctionnel.
    - AccessibilityTest reste vert (aria-current sur l'item actif courant, aria-disabled toujours sur Factures/Catalogue).
    - `./vendor/bin/pest --ci` : suite totale verte.
  </acceptance_criteria>
  <done>Dashboard /admin affiche 4 stat-cards avec valeurs réelles selon UI-SPEC, liste /admin/passages opérationnelle avec filtres client_id + date range + pagination, sidebar/bottom-nav active "Passages", empty state conforme, suite Pest verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filtres URL → DB query | Inputs date_from/date_to/client_id arrivent via wire:model et sont passés à whereDate/where Eloquent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Input Validation | Filtres dates utilisateur | mitigate | `whereDate('visited_at', '>=', $this->dateFrom)` utilise binding Eloquent ; si l'utilisateur saisit du texte non-date, Postgres renvoie une erreur (catchée par Livewire validation soft). On peut ajouter `#[Validate('nullable|date')] public string $dateFrom = '';` pour bloquer en amont. Recommandé. |
| T-3-02 | Cross-tenant leak | client_id select | accept | Solo opérateur — pas de risque tenant. Tous les clients sont les clients de Pierre. |
| T-3-03 | Resource exhaustion | Pagination 25/page | mitigate | `paginate(25)` ne charge jamais plus que 25 lignes en mémoire. Pas de risque DoS via filtres. |
</threat_model>

<verification>
1. `./vendor/bin/pest --ci` : suite totale verte (125 fin 02-02 + 13 Plan 02-03 = 138+)
2. Smoke test interactif : login → /admin → 4 cards avec vraies valeurs → sidebar Passages → liste filtrable → URL pagination ?page=2 fonctionnelle
3. `php artisan route:list` : routes admin.clients.* (4) + admin.passages.index (1) + admin.dashboard (1) toutes présentes
4. Mockup correspondance : visiter `mockups/v1/dashboard.html` et comparer la liste passages — structure carte cohérente
</verification>

<success_criteria>
- [ ] `app/Livewire/PassageIndex.php` créé avec WithPagination + filtres clientId/dateFrom/dateTo + reset on update
- [ ] `routes/admin.php` contient route `admin.passages.index` (Route::view)
- [ ] Dashboard /admin affiche 4 stat-cards avec valeurs réelles : Passages semaine, À synchroniser (state offline), Clients, Eau à surveiller (state warn si > 0)
- [ ] DashboardController calcule les 4 compteurs (Carbon::startOfWeek pour cette semaine, whereNotBetween pour eau à surveiller)
- [ ] stat-card.blade.php supporte 3 états : default/offline/warn (règle ambre = synchro, rouge = eau)
- [ ] sidebar.blade.php : item Passages basculé en actif
- [ ] mobile-bottom-nav.blade.php : item Passages basculé en actif
- [ ] livewire/passage-index.blade.php : cartes UI-SPEC (date, client, mesures, photos count, statut chip), empty state, filtres bar
- [ ] PASS-05 : recherche + filtres + pagination + tri visited_at DESC opérationnels
- [ ] 13 nouveaux tests Pest passants (7 PassageIndex + 6 DashboardStats)
- [ ] `npm run build` exit 0
- [ ] AccessibilityTest et AdminShellTest restent verts
</success_criteria>

<output>
Créer `.planning/phases/02-mvp-suivi-offline-first/02-03-SUMMARY.md` à la fin du plan.
</output>
