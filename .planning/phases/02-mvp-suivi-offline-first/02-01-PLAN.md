---
phase: 02-mvp-suivi-offline-first
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/Models/Client.php
  - config/auth.php
  - bootstrap/app.php
  - composer.json
  - composer.lock
  - .planning/ROADMAP.md
files_created:
  - database/migrations/2026_05_28_000011_add_client_uuid_to_photos_meta.php
  - app/Http/Middleware/ServiceWorkerHeaders.php
  - routes/api.php
  - routes/portail.php
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - PASS-03
  - PASS-04
user_setup: []
tags: [foundation, auth, schema, middleware, packages]
must_haves:
  truths:
    - "Le modèle Client implémente Authenticatable et peut être logué via Auth::guard('clients')"
    - "Le guard 'clients' (driver session, provider clients) est défini dans config/auth.php"
    - "La table photos_meta possède une colonne client_uuid UUID UNIQUE NULLABLE (idempotence photo D-42)"
    - "Les packages cesargb/laravel-magiclink ^2.27 et spatie/laravel-medialibrary ^11.22 sont installés"
    - "Le middleware ServiceWorkerHeaders ajoute Service-Worker-Allowed: / sur /build/sw.js (D-60, Pitfall 2)"
    - "bootstrap/app.php enregistre routes/api.php sous middleware ['web','auth'] préfixe /api, et routes/portail.php sous middleware 'web'"
    - "Le validateur CSRF exempte /api/* (Pitfall 5)"
    - "ROADMAP §Phase 2 Success Criterion #2 contient 'sur smartphone' (et non 'sur iPhone') — D-37"
  artifacts:
    - path: "database/migrations/2026_05_28_000011_add_client_uuid_to_photos_meta.php"
      provides: "migration ajoutant photos_meta.client_uuid UUID UNIQUE"
      contains: "client_uuid"
    - path: "app/Models/Client.php"
      provides: "Client model extends Authenticatable + Notifiable + getEmailForPasswordReset() implicite"
      contains: "extends Authenticatable"
    - path: "config/auth.php"
      provides: "guard 'clients' + provider 'clients' Eloquent → App\\Models\\Client"
      contains: "'clients'"
    - path: "app/Http/Middleware/ServiceWorkerHeaders.php"
      provides: "Header Service-Worker-Allowed: / sur /build/sw.js"
      contains: "Service-Worker-Allowed"
    - path: "bootstrap/app.php"
      provides: "Enregistrement routes/api.php (web,auth,/api), routes/portail.php (web), middleware SW headers, CSRF exempt /api/*"
      contains: "ServiceWorkerHeaders"
    - path: "routes/api.php"
      provides: "Fichier de routes API (stub vide pour Plan 02-06)"
    - path: "routes/portail.php"
      provides: "Fichier de routes portail (stub vide pour Plan 02-07)"
  key_links:
    - from: "app/Models/Client.php"
      to: "Illuminate\\Foundation\\Auth\\User"
      via: "extends Authenticatable"
      pattern: "extends Authenticatable"
    - from: "config/auth.php"
      to: "App\\Models\\Client"
      via: "provider clients → eloquent"
      pattern: "'model' => \\\\?App\\\\Models\\\\Client::class"
    - from: "bootstrap/app.php"
      to: "routes/api.php"
      via: "Route::middleware(['web','auth'])->prefix('api')->name('api.')->group(base_path('routes/api.php'))"
      pattern: "routes/api.php"
    - from: "bootstrap/app.php"
      to: "App\\Http\\Middleware\\ServiceWorkerHeaders"
      via: "append in withMiddleware"
      pattern: "ServiceWorkerHeaders"
---

<objective>
Poser les fondations bloquantes de Phase 2 : (1) migration schéma `photos_meta.client_uuid` pour l'idempotence photo D-42, (2) bascule du modèle `Client` en `Authenticatable` avec guard `clients` isolé, (3) installation des deux packages composer absents (`cesargb/laravel-magiclink`, `spatie/laravel-medialibrary` qui tire `league/flysystem-aws-s3-v3`), (4) middleware `ServiceWorkerHeaders` + `buildBase: '/build/'` co-dépendant (D-60, Pitfall 2 — sans ce header le SW est scopé à `/build/` et n'intercepte rien sur `/admin/*`), (5) câblage `bootstrap/app.php` : enregistrement des routes `api`/`portail` (encore vides — remplies par 02-06 et 02-07) et exemption CSRF `/api/*` (Pitfall 5).

Purpose : sans cette wave 0 le guard `clients` lève `TypeError` au login, le UPSERT photo n'a pas de contrainte UNIQUE pour faire son `ON CONFLICT (client_uuid)`, et le Service Worker généré par `vite-plugin-pwa` (Plan 02-04) ne pourra pas contrôler `/admin/passages/create`.

Output : 4 fichiers créés + 5 fichiers modifiés. Aucun comportement utilisateur visible (foundation pure) — verification = `php artisan migrate --env=testing` passe, `composer show` retourne les nouveaux paquets, suite Pest existante (105 tests Phase 1) reste verte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mvp-suivi-offline-first/02-CONTEXT.md
@.planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md
@.planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md
@.planning/phases/01-vitrine-fondations/01-02-SUMMARY.md
@.planning/phases/01-vitrine-fondations/01-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrats existants Phase 1 que l'executor doit lire pour aligner les modifications -->
<!-- Extraits du codebase au moment du planning — pas besoin d'exploration -->

From app/Models/User.php (lignes 1-32, pattern Authenticatable à copier sur Client) :
- `use Illuminate\Foundation\Auth\User as Authenticatable;`
- `use Illuminate\Notifications\Notifiable;`
- `class User extends Authenticatable { use HasFactory, Notifiable; }`
- Attributs PHP 8.4 : `#[Fillable([...])]`, `#[Hidden([...])]` — utiliser cette même forme PHP sur Client
- Méthode `casts(): array` retournant les casts (pattern Laravel 13)

From app/Models/Client.php (état actuel — extends Model) :
- $fillable = ['uuid','name','email','phone','address','notes','magic_link_token','magic_link_expires_at']
- $casts = ['uuid' => 'string', 'magic_link_expires_at' => 'datetime']
- relations : piscines(), passages(), contrats(), factures()
- Les colonnes `magic_link_token` / `magic_link_expires_at` existent (migration Phase 1 ligne 99 du SUMMARY 01-02) mais sont obsolètes pour cesargb/laravel-magiclink v2 — laisser en place (compat schéma).

From config/auth.php (lignes 41-46, état actuel) :
- Un seul guard 'web' → provider 'users' → model App\Models\User
- Phase 2 ajoute : guard 'clients' (driver: session, provider: clients), provider 'clients' (driver: eloquent, model: App\Models\Client)

From bootstrap/app.php (état actuel) :
- ->withRouting(web: routes/web.php, commands: routes/console.php, then: …)
- Le bloc `then:` enregistre déjà routes/vitrine.php, routes/blog.php, routes/admin.php
- ->withMiddleware : alias 'cache.headers' + append CacheHeaders
- ->withExceptions : shouldRenderJsonWhen(is('api/*'))
- Phase 2 ajoute dans `then:` :
  - `Route::middleware(['web','auth'])->prefix('api')->name('api.')->group(base_path('routes/api.php'));`
  - `Route::middleware('web')->group(base_path('routes/portail.php'));`
- Phase 2 ajoute dans `withMiddleware` :
  - `$middleware->append(\App\Http\Middleware\ServiceWorkerHeaders::class);`
  - `$middleware->validateCsrfTokens(except: ['/api/*']);`

From app/Http/Middleware/CacheHeaders.php (structure analog à copier pour ServiceWorkerHeaders) :
- `namespace App\Http\Middleware;`
- `use Closure; use Illuminate\Http\Request; use Symfony\Component\HttpFoundation\Response;`
- Méthode `public function handle(Request $request, Closure $next): Response`

From database/migrations/2026_05_28_000006_create_photos_meta_table.php (analog migration créée Phase 1) :
- Table créée avec : `id, passage_id (FK cascadeOnDelete), disk default 'r2', path, mime_type, size_bytes, width, height, captured_at, timestamps`
- La nouvelle migration ajoute `client_uuid UUID NULLABLE UNIQUE` APRÈS `passage_id`

From ROADMAP.md §Phase 2 SC #2 (état actuel) :
- "Le pro saisit mesures + actions + notes + photos sur iPhone sans réseau et voit le badge « N passages en attente »"
- D-37 impose la substitution : `sur iPhone` → `sur smartphone`

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Migration photos_meta.client_uuid + extension MigrationsTest</name>
  <files>database/migrations/2026_05_28_000011_add_client_uuid_to_photos_meta.php, tests/Feature/MigrationsTest.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (section "database/migrations/…_add_client_uuid_to_photos_meta.php" — pattern complet)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §"Migration photos_meta.client_uuid (D-42)" (lignes 904-919, code exact à copier)
    - database/migrations/2026_05_28_000006_create_photos_meta_table.php (analog : Schema::create + structure)
    - tests/Feature/MigrationsTest.php (pattern existant SAVEPOINT pour tester contraintes UNIQUE Postgres — voir 01-02-SUMMARY.md "Auto-fixed Issues #2")
    - app/Models/PhotoMeta.php (à ne PAS modifier dans ce task — fillable/cast viendront en 02-06)
  </read_first>
  <behavior>
    - Test 14 (RED) : "photos_meta a une colonne client_uuid UUID nullable UNIQUE" — interroge `Schema::hasColumn('photos_meta', 'client_uuid')` puis `DB::select` sur `information_schema.columns` pour vérifier `data_type = 'uuid'` ET `is_nullable = 'YES'`
    - Test 15 (RED) : "photos_meta.client_uuid est unique (deuxième INSERT avec même UUID lève UniqueConstraintViolationException)" — utilise pattern SAVEPOINT (voir Auto-fixed Issue #2 de 01-02-SUMMARY.md) :
      ```
      DB::statement('SAVEPOINT before_dup');
      try { PhotoMeta::create([... 'client_uuid' => $uuid]); /* 2e insert */ }
      catch (Throwable $e) { DB::statement('ROLLBACK TO SAVEPOINT before_dup'); $caught = true; }
      DB::statement('RELEASE SAVEPOINT before_dup');
      expect($caught)->toBeTrue();
      ```
    - Les deux tests utilisent `uses(RefreshDatabase::class)` déjà présent en tête de fichier
  </behavior>
  <action>
    Créer `database/migrations/2026_05_28_000011_add_client_uuid_to_photos_meta.php` qui contient EXACTEMENT (D-42) :
    - `up()` : `Schema::table('photos_meta', function (Blueprint $table) { $table->uuid('client_uuid')->nullable()->unique()->after('passage_id'); });`
    - `down()` : `Schema::table('photos_meta', function (Blueprint $table) { $table->dropColumn('client_uuid'); });`
    - Suivre EXACTEMENT le pattern anonymous-class migration de Laravel 13 (return new class extends Migration { … };) déjà utilisé dans `2026_05_28_000006_create_photos_meta_table.php`.

    Étendre `tests/Feature/MigrationsTest.php` avec les Tests 14 et 15 décrits ci-dessus. Insérer après le Test 13 existant (google_reviews). Le test d'unicité DOIT utiliser le pattern SAVEPOINT (1463455 commit Phase 1 a établi ce pattern). NE PAS utiliser `expect()->toThrow()` qui casse la transaction Postgres.

    NE PAS modifier `app/Models/PhotoMeta.php` ici — l'ajout du `client_uuid` dans `$fillable`/`$casts` est explicitement délégué au Plan 02-06 (le model n'est pas utilisé en code applicatif tant que l'endpoint API n'existe pas).
  </action>
  <verify>
    <automated>php artisan migrate --env=testing && ./vendor/bin/pest tests/Feature/MigrationsTest.php --ci</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate --env=testing` exit 0 avec "Migrating: 2026_05_28_000011_add_client_uuid_to_photos_meta … DONE"
    - `./vendor/bin/pest tests/Feature/MigrationsTest.php --ci` retourne 15 tests passés (13 existants + 2 nouveaux)
    - Inspection psql sur DB de test : `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='photos_meta' AND column_name='client_uuid';` retourne `client_uuid | uuid | YES`
    - `SELECT indexname FROM pg_indexes WHERE tablename='photos_meta' AND indexname LIKE '%client_uuid%';` retourne au moins une ligne (index unique implicite)
    - `php artisan migrate:rollback --env=testing --step=1` rejoue le `down()` sans erreur
    - Suite Pest existante (105 tests Phase 1) reste verte : `./vendor/bin/pest --ci` exit 0
  </acceptance_criteria>
  <done>Migration 000011 appliquée en testing avec contrainte UNIQUE active sur photos_meta.client_uuid, 2 nouveaux tests passants (15 au total dans MigrationsTest), aucune régression sur les 105 tests Phase 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 : Client extends Authenticatable + guard 'clients' dans config/auth.php</name>
  <files>app/Models/Client.php, config/auth.php, tests/Feature/ClientsGuardTest.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (section "app/Models/Client.php — MODIFICATION CRITIQUE" — diff imports complet)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 6 (lignes 542-572 — config/auth.php cible) + §Open Questions §3
    - app/Models/User.php (analog Authenticatable + Notifiable + #[Fillable] + casts() méthode — état actuel à transposer sur Client)
    - app/Models/Client.php (état actuel — fillable array, relations à préserver)
    - config/auth.php (état actuel — un seul guard 'web')
    - tests/Feature/AuthLoginTest.php (pattern Pest 4 actingAs + RefreshDatabase — analog à copier)
    - database/factories/ClientFactory.php (factory existante — sera utilisée par le test)
  </read_first>
  <behavior>
    - Test 1 (RED) : "Le model Client est Authenticatable" — `expect(new App\Models\Client())->toBeInstanceOf(Illuminate\Contracts\Auth\Authenticatable::class)`
    - Test 2 (RED) : "Auth::guard('clients') peut logger un Client puis le rappeler" :
      ```
      $client = App\Models\Client::factory()->create();
      Auth::guard('clients')->login($client);
      expect(Auth::guard('clients')->check())->toBeTrue();
      expect(Auth::guard('clients')->user()->id)->toBe($client->id);
      ```
    - Test 3 (RED) : "Les guards 'web' et 'clients' sont isolés — login client ne loggue PAS l'utilisateur sur web" :
      ```
      Auth::guard('clients')->login($client);
      expect(Auth::guard('web')->check())->toBeFalse();
      expect($this->isAuthenticated('clients'))->toBeTrue();
      ```
  </behavior>
  <action>
    Réécrire `app/Models/Client.php` en gardant TOUTES les relations existantes (piscines, passages, contrats, factures) et TOUS les attributs `$fillable`/`$casts`, mais en changeant :
    - imports : ajouter `use Illuminate\Foundation\Auth\User as Authenticatable;` et `use Illuminate\Notifications\Notifiable;`
    - retirer l'import `use Illuminate\Database\Eloquent\Model;` (plus utilisé)
    - `class Client extends Authenticatable` (et non plus `extends Model`)
    - `use HasFactory, Notifiable;` (et non plus seulement `HasFactory`)
    - Conserver `$fillable` et `$casts` en propriétés classiques (NE PAS les migrer vers les attributs PHP `#[Fillable]` pour limiter le diff — c'est un choix de l'executor mais le pattern existant Phase 1 sur Client utilise `$fillable` array).
    - Aucune autre modification — relations, namespace, ordre des méthodes : inchangés.

    Éditer `config/auth.php` en :
    - Ajoutant à `'guards' =>` une entrée `'clients' => ['driver' => 'session', 'provider' => 'clients']`
    - Ajoutant à `'providers' =>` une entrée `'clients' => ['driver' => 'eloquent', 'model' => env('AUTH_CLIENT_MODEL', App\Models\Client::class)]`
    - Aucune modification du guard `'web'` ni du provider `'users'`. Aucune modification de `'defaults'`.

    Créer `tests/Feature/ClientsGuardTest.php` avec Pest 4 + `uses(RefreshDatabase::class)` et les 3 tests décrits ci-dessus. Pattern imports : copier en-tête de `tests/Feature/AuthLoginTest.php`. Le 3e test couvre l'isolation D-RESEARCH §Pattern 6.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/ClientsGuardTest.php --ci && ./vendor/bin/pest --ci</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "extends Authenticatable" app/Models/Client.php` retourne au moins une ligne
    - `grep -n "use HasFactory, Notifiable" app/Models/Client.php` retourne au moins une ligne
    - `grep -v '^[[:space:]]*//' config/auth.php | grep -c "'clients'"` retourne au moins 2 (un dans guards, un dans providers)
    - `php artisan tinker --execute="dump(config('auth.guards.clients'));"` retourne array avec `driver=session, provider=clients`
    - `./vendor/bin/pest tests/Feature/ClientsGuardTest.php --ci` retourne 3/3 passants
    - `./vendor/bin/pest --ci` retourne 105+3+2=110/110 passants (Phase 1 105 + ClientsGuardTest 3 + MigrationsTest +2 du Task 1)
    - Aucune régression sur AuthLoginTest, PierreSeederTest, AdminShellTest (Phase 1 auth)
  </acceptance_criteria>
  <done>Client est Authenticatable + Notifiable, guard `clients` opérationnel et isolé du guard `web`, 3 tests dédiés passants, aucune régression sur les flux auth pro Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3 : Packages composer + ServiceWorkerHeaders middleware + bootstrap/app.php + stubs routes/api.php & routes/portail.php + ROADMAP D-37</name>
  <files>composer.json, composer.lock, app/Http/Middleware/ServiceWorkerHeaders.php, bootstrap/app.php, routes/api.php, routes/portail.php, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "app/Http/Middleware/ServiceWorkerHeaders.php", "routes/api.php", "routes/portail.php" — patterns à copier ; section "Critical Flags" §3 et §4)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pitfall 2 (Service-Worker-Allowed header), §Pitfall 5 (CSRF exempt /api/*), §"Packages à installer en Phase 2"
    - app/Http/Middleware/CacheHeaders.php (analog : namespace, imports, signature `handle(Request, Closure): Response`)
    - bootstrap/app.php (état actuel — bloc `then:` à étendre + `withMiddleware` à étendre)
    - routes/admin.php (analog structure de fichier de routes nommées)
    - .planning/ROADMAP.md §Phase 2 SC #2 (à amender pour D-37)
    - composer.json (state actuel — packages à ajouter)
  </read_first>
  <action>
    1. Installation des deux packages composer manquants :
       - `composer require cesargb/laravel-magiclink:^2.27`
       - `composer require spatie/laravel-medialibrary:^11.22`
       Vérifier après que `composer show league/flysystem-aws-s3-v3` retourne ^3.x (dépendance transitive tirée par medialibrary). Si médialibrary publie des migrations (`php artisan vendor:publish --tag=medialibrary-migrations`), les inclure dans le commit — la table `media` est requise même si ce projet utilise `Storage::disk('r2')` direct (RESEARCH §Open Questions §2) pour ne pas casser le service provider auto-discover. NE PAS publier les migrations magiclink — `cesargb/laravel-magiclink` stocke ses tokens dans une table `magic_link` créée automatiquement (vérifier `php artisan migrate:status`).

    2. Créer `app/Http/Middleware/ServiceWorkerHeaders.php` en copiant la structure de namespace + imports de `app/Http/Middleware/CacheHeaders.php`. Pattern EXACT (D-60, Pitfall 2) :
       ```
       namespace App\Http\Middleware;
       use Closure;
       use Illuminate\Http\Request;
       use Symfony\Component\HttpFoundation\Response;

       class ServiceWorkerHeaders
       {
           public function handle(Request $request, Closure $next): Response
           {
               $response = $next($request);
               if ($request->is('build/sw.js')) {
                   $response->headers->set('Service-Worker-Allowed', '/');
                   $response->headers->set('Cache-Control', 'no-cache');
               }
               return $response;
           }
       }
       ```

    3. Créer `routes/api.php` (stub vide pour Plan 02-06) avec le header standard :
       ```
       <?php
       use Illuminate\Support\Facades\Route;
       /*
        | Routes API offline-sync. Enregistrées dans bootstrap/app.php sous
        | middleware(['web','auth'])->prefix('api')->name('api.')
        | Plan 02-06 ajoute POST /passages et POST /passages/{uuid}/photos.
        */
       ```
       (aucune route définie ici — c'est le Plan 02-06 qui les ajoute)

    4. Créer `routes/portail.php` (stub vide pour Plan 02-07) avec header similaire :
       ```
       <?php
       use Illuminate\Support\Facades\Route;
       /*
        | Routes portail client + magic link. Enregistrées dans bootstrap/app.php
        | sous middleware('web'). Plan 02-07 ajoute /auth/magic, /auth/confirm,
        | /portail/passages avec guards guest:clients / auth:clients.
        */
       ```

    5. Modifier `bootstrap/app.php` :
       a) Dans le bloc `then:` du `withRouting`, AJOUTER après les groupes existants (vitrine/blog/admin) :
          ```
          Route::middleware(['web', 'auth'])
              ->prefix('api')
              ->name('api.')
              ->group(base_path('routes/api.php'));

          Route::middleware('web')
              ->group(base_path('routes/portail.php'));
          ```
       b) Dans `withMiddleware(function (Middleware $middleware) { … })`, ajouter avant le `$middleware->append(CacheHeaders::class)` existant :
          ```
          $middleware->validateCsrfTokens(except: ['api/*']);
          $middleware->append(\App\Http\Middleware\ServiceWorkerHeaders::class);
          ```
          (CacheHeaders reste appended APRÈS pour préserver son ordonnancement actuel ; ServiceWorkerHeaders est ajouté avant car son scope est indépendant et n'interfère pas)
       c) NE PAS toucher `withExceptions` (déjà OK pour les JSON responses sur api/*).

    6. Modifier `.planning/ROADMAP.md` §"Phase 2: MVP Suivi Offline-First" → "Success Criteria" → item 2 : remplacer `sur iPhone` par `sur smartphone` (D-37). Vérifier que le texte exact devient : "Le pro saisit mesures + actions + notes + photos sur smartphone sans réseau et voit le badge « N passages en attente »". Aucun autre changement à ROADMAP.

    Note importante : les packages composer publient potentiellement des migrations. Si `php artisan migrate:status` montre des migrations `medialibrary` ou `magiclink` en attente, elles DOIVENT être appliquées dans cette même tâche (`php artisan migrate --env=testing`) pour que les tests downstream (02-06, 02-07) trouvent les tables présentes.
  </action>
  <verify>
    <automated>composer show cesargb/laravel-magiclink spatie/laravel-medialibrary league/flysystem-aws-s3-v3 && php artisan migrate --env=testing --pretend | head -30 && ./vendor/bin/pest --ci</automated>
  </verify>
  <acceptance_criteria>
    - `composer show cesargb/laravel-magiclink` retourne version ^2.27.x
    - `composer show spatie/laravel-medialibrary` retourne version ^11.22.x ou supérieure
    - `composer show league/flysystem-aws-s3-v3` retourne version ^3.x
    - `test -f app/Http/Middleware/ServiceWorkerHeaders.php` exit 0
    - `grep -c "Service-Worker-Allowed" app/Http/Middleware/ServiceWorkerHeaders.php` retourne au moins 1
    - `test -f routes/api.php && test -f routes/portail.php` exit 0
    - `grep -c "routes/api.php" bootstrap/app.php` retourne au moins 1
    - `grep -c "routes/portail.php" bootstrap/app.php` retourne au moins 1
    - `grep -c "ServiceWorkerHeaders" bootstrap/app.php` retourne au moins 1
    - `grep -c "validateCsrfTokens" bootstrap/app.php | grep -v '^#'` retourne au moins 1
    - `grep -c "api/\*\|'api/\*'" bootstrap/app.php` retourne au moins 1
    - `grep -v '^[[:space:]]*-' .planning/ROADMAP.md | grep -c "sur smartphone sans réseau"` retourne au moins 1
    - `grep -v '^[[:space:]]*-' .planning/ROADMAP.md | grep -c "sur iPhone sans réseau"` retourne 0
    - `php artisan route:list --path=api 2>&1 | grep -c "api/"` retourne 0 (stub vide — aucune route encore)
    - `./vendor/bin/pest --ci` retourne 110+/110 passants (aucune régression Phase 1)
    - Lancer manuellement `curl -sI http://127.0.0.1:PORT/build/sw.js` (si server actif) doit retourner `Service-Worker-Allowed: /` une fois le SW généré en Plan 02-04 — non vérifiable ici car SW pas encore généré ; le middleware sera testé fonctionnellement quand Plan 02-04 produit le fichier sw.js.
  </acceptance_criteria>
  <done>Les deux packages composer sont installés, ServiceWorkerHeaders middleware créé + enregistré, routes/api.php et routes/portail.php stubés et câblés dans bootstrap/app.php, CSRF exempt /api/* configuré, ROADMAP D-37 amendé, suite Pest entièrement verte sans régression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Auth::guard('clients')` vs `Auth::guard('web')` | Les deux sessions cohabitent — un Client logué ne doit JAMAIS hériter des permissions Operator (Pierre) et inversement. Vérifié par Test 3 du Task 2. |
| Schema migration | Une migration produit un changement structurel persistant — la down() doit fonctionner pour pouvoir rollback. |
| Package install (composer) | Dépendances tierces tirées de Packagist — voir audit légitimité RESEARCH §"Package Legitimacy Audit" (les 2 packages sont approuvés). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Elevation of Privilege | guard `clients` vs guard `web` cohabitation | mitigate | Provider 'clients' utilise model App\Models\Client uniquement ; provider 'users' inchangé sur App\Models\User. Test ClientsGuardTest::test3 valide l'isolation. |
| T-2-02 | Tampering | photos_meta.client_uuid contrainte UNIQUE | mitigate | Contrainte UNIQUE Postgres au niveau DB (pas applicative). Test 15 de MigrationsTest valide l'enforcement. |
| T-2-03 | Tampering | CSRF exempt sur /api/* | mitigate | L'exemption CSRF est compensée par middleware `auth` (session cookie). Pas de session = pas d'accès. Le PWA est same-domain donc le cookie est envoyé automatiquement. Documenté RESEARCH §Pitfall 5. |
| T-2-04 | Tampering | Service-Worker-Allowed: / header | accept | Étendre le scope SW à `/` est intentionnel (Pitfall 2). Le SW lui-même ne fait que precache + offline fallback (Plan 02-04) — pas d'élévation de privilèges. |
| T-2-05 | Information Disclosure | Client model exposé via guard auth | mitigate | Client model n'expose pas `password` (n'a pas de colonne password — auth via magic link sans mot de passe). Le `magic_link_token` legacy reste mais n'est plus utilisé par cesargb/laravel-magiclink (qui a sa propre table). |
| T-2-SC | Tampering | composer require de 2 packages tiers | mitigate | Packages audités RESEARCH §"Package Legitimacy Audit" — cesargb/laravel-magiclink (~70K/mois Packagist, ~5 ans, source vérifiée github.com/cesargb/laravel-magiclink), spatie/laravel-medialibrary (~2M/mois, ~9 ans, mainteneur établi). Pas de [SLOP], pas de [SUS]. |
</threat_model>

<verification>
À la fin du plan :
1. `php artisan migrate:fresh --env=testing && php artisan db:seed --env=testing` exit 0 (migrations propres + DevDataSeeder)
2. `./vendor/bin/pest --ci` retourne tous tests verts (105 Phase 1 + 2 nouveaux MigrationsTest + 3 nouveaux ClientsGuardTest = 110+)
3. `composer outdated cesargb/laravel-magiclink spatie/laravel-medialibrary` ne signale pas d'incompatibilité
4. `php artisan route:list --path=api` exit 0 sans erreur (stub vide attendu)
5. `php artisan route:list --path=portail` exit 0 sans erreur (stub vide attendu)
6. `php -r "require 'vendor/autoload.php'; var_dump(class_exists('MagicLink\\MagicLink'));"` retourne `bool(true)` — le namespace MagicLink est chargeable.
7. `grep -c "sur smartphone sans réseau" .planning/ROADMAP.md` ≥ 1 ET `grep -c "sur iPhone sans réseau" .planning/ROADMAP.md` = 0 (D-37 honoré)
</verification>

<success_criteria>
- [ ] Migration `2026_05_28_000011_add_client_uuid_to_photos_meta.php` créée + appliquée + 2 tests dédiés verts
- [ ] `Client` model `extends Authenticatable` + `use Notifiable` + relations préservées
- [ ] `config/auth.php` contient guard `clients` (driver session, provider clients → Client model)
- [ ] 3 tests `ClientsGuardTest` (instance Authenticatable, login via guard, isolation web/clients) passants
- [ ] `cesargb/laravel-magiclink` ^2.27 + `spatie/laravel-medialibrary` ^11.22 installés via composer
- [ ] `app/Http/Middleware/ServiceWorkerHeaders.php` créé, append dans `bootstrap/app.php`
- [ ] `routes/api.php` (stub) + `routes/portail.php` (stub) créés et enregistrés dans `bootstrap/app.php` avec les middlewares corrects
- [ ] `validateCsrfTokens(except: ['api/*'])` actif dans `bootstrap/app.php`
- [ ] ROADMAP.md Phase 2 SC#2 amendée : `sur iPhone` → `sur smartphone` (D-37)
- [ ] Suite Pest existante (105 tests Phase 1) reste verte, total 110+ passants
- [ ] Aucun fichier UI / aucun composant Livewire / aucune route métier introduit — c'est pur foundation
</success_criteria>

<output>
Créer `.planning/phases/02-mvp-suivi-offline-first/02-01-SUMMARY.md` à la fin du plan suivant le template summary.md.
</output>
