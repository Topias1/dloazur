---
phase: 02-mvp-suivi-offline-first
plan: 06
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - routes/api.php
  - app/Models/Passage.php
  - app/Models/PhotoMeta.php
files_created:
  - app/Http/Controllers/Api/PassageController.php
  - app/Http/Controllers/Api/PassagePhotoController.php
  - tests/Feature/PassageApiTest.php
  - tests/Feature/PhotoUploadTest.php
autonomous: true
requirements:
  - PASS-03
  - PASS-04
user_setup: []
tags: [api, upsert, idempotence, photos, r2, postgresql]
must_haves:
  truths:
    - "POST /api/passages avec client_uuid valide retourne 200 et crée un passage en DB"
    - "POST /api/passages avec le même client_uuid (passage déjà draft) retourne 200 (UPSERT D-38) — pas de doublon créé"
    - "POST /api/passages avec un client_uuid d'un passage status != 'draft' retourne 409 avec payload { error: 'already_closed', server_state: {...} } (D-40)"
    - "POST /api/passages sans client_uuid (ou non-UUID) retourne 422 validation error"
    - "POST /api/passages sans auth retourne 401/redirect (auth middleware web)"
    - "POST /api/passages/{passage_client_uuid}/photos uploade le blob sur disk r2 (path: passages/{uuid}/photos/{rand}) et insère une ligne photos_meta avec client_uuid UUID, passage_id, disk='r2', path, mime_type='image/jpeg', size_bytes, captured_at"
    - "POST /api/passages/{uuid}/photos avec un client_uuid photo déjà présent retourne 200 (UPSERT idempotent par client_uuid D-42)"
    - "POST /api/passages/{uuid}/photos rejette tout fichier non JPEG (mime check) avec 422"
    - "POST /api/passages/{uuid}/photos rejette les fichiers > 10 MB avec 422 (D-48)"
    - "L'endpoint /api/* est exempté du CSRF token (Pitfall 5 — exempt configuré en Plan 02-01) mais reste protégé par session cookie auth web"
    - "Le model PhotoMeta a 'client_uuid' dans $fillable et $casts (consommation de la migration Plan 02-01)"
  artifacts:
    - path: "app/Http/Controllers/Api/PassageController.php"
      provides: "POST /api/passages — UPSERT Postgres conditionnel ON CONFLICT (client_uuid) DO UPDATE WHERE status='draft', 409 sinon"
      contains: "DB::affectingStatement\\|ON CONFLICT"
    - path: "app/Http/Controllers/Api/PassagePhotoController.php"
      provides: "POST /api/passages/{uuid}/photos — putFile sur r2 + upsert photos_meta sur client_uuid UNIQUE"
      contains: "Storage::disk\\('r2'\\)"
    - path: "routes/api.php"
      provides: "2 routes nommées : passages.store, passages.photos.store"
      contains: "PassageController\\|PassagePhotoController"
  key_links:
    - from: "app/Http/Controllers/Api/PassageController.php"
      to: "Postgres ON CONFLICT (client_uuid) DO UPDATE WHERE status='draft'"
      via: "DB::affectingStatement(\"INSERT INTO passages ... ON CONFLICT (client_uuid) DO UPDATE SET ... WHERE passages.status = 'draft'\")"
      pattern: "ON CONFLICT \\(client_uuid\\) DO UPDATE.*WHERE.*status.*=.*'draft'"
    - from: "app/Http/Controllers/Api/PassagePhotoController.php"
      to: "Cloudflare R2 disk"
      via: "Storage::disk('r2')->putFile('passages/{uuid}/photos', \\$file)"
      pattern: "Storage::disk\\('r2'\\)->putFile"
    - from: "app/Http/Controllers/Api/PassagePhotoController.php"
      to: "PhotoMeta::updateOrCreate(['client_uuid' => ...], [...])"
      via: "PhotoMeta upsert sur client_uuid UNIQUE (D-42)"
      pattern: "updateOrCreate"
---

<objective>
Livrer les 2 endpoints API back-end qui rendent la saisie offline-first idempotente : (1) `POST /api/passages` qui fait un UPSERT Postgres conditionnel via `INSERT ... ON CONFLICT (client_uuid) DO UPDATE SET ... WHERE passages.status = 'draft'` (D-38) — si le passage existe déjà avec un status autre que 'draft' (Phase 3 le passera à 'closed'/'sent'/'billed'), la condition `WHERE status = 'draft'` ne match pas, `DB::affectingStatement` retourne 0, le controller renvoie 409 avec `{ error: 'already_closed', server_state: {...} }` (D-40). (2) `POST /api/passages/{passage_client_uuid}/photos` qui valide mime JPEG + max 10MB (D-48), put le fichier sur le disk `r2` (Cloudflare R2), et fait un `PhotoMeta::updateOrCreate(['client_uuid' => $uuid], [...])` — l'UNIQUE constraint sur `photos_meta.client_uuid` (Plan 02-01) garantit l'idempotence par photo (D-42).

Purpose : sans ce plan, Plan 02-05 ne peut pas synchroniser. Sans idempotence (D-38, D-42), un réseau Martinique instable produit des doublons.

Output : 2 controllers Api + extension PhotoMeta + Passage models + 2 routes nommées + 2 fichiers tests Pest (PassageApiTest avec scénarios INSERT/UPSERT/409/422, PhotoUploadTest avec scenarios upload/idempotent/mime reject).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-suivi-offline-first/02-CONTEXT.md
@.planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md
@.planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md
@.planning/phases/02-mvp-suivi-offline-first/02-01-SUMMARY.md
@.planning/phases/01-vitrine-fondations/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>

From routes/api.php (état après Plan 02-01) — stub vide avec header de commentaire.

From bootstrap/app.php (état après Plan 02-01) :
- `Route::middleware(['web', 'auth'])->prefix('api')->name('api.')->group(base_path('routes/api.php'));`
- `validateCsrfTokens(except: ['api/*'])` actif

From app/Models/Passage.php (état Phase 1) :
- $fillable inclut : client_uuid, piscine_id, client_id, visited_at, status, ph_avant, ph_apres, chlore_libre, chlore_total, tac, th, sel_g_l, actions, notes, pdf_path, signature_path, synced_at
- $casts : visited_at => datetime, synced_at => datetime, actions => array, mesures => decimal:2

From app/Models/PhotoMeta.php (état après Plan 02-01) :
- $table = 'photos_meta' (override)
- $fillable Phase 1 : passage_id, disk, path, mime_type, size_bytes, width, height, captured_at
- Phase 02-06 ajoute : 'client_uuid' à $fillable + cast 'client_uuid' => 'string'

From database/migrations/2026_05_28_000005_create_passages_table.php :
- `client_uuid` UUID UNIQUE (idempotence)
- `status` string DEFAULT 'draft'
- `synced_at` datetime nullable

From database/migrations/2026_05_28_000011_add_client_uuid_to_photos_meta.php (Plan 02-01) :
- `client_uuid` UUID NULLABLE UNIQUE (after passage_id)

From config/filesystems.php :
- disk 'r2' configuré avec endpoint Cloudflare R2 (S3-compatible), credentials via env R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY / R2_BUCKET / R2_ENDPOINT
- Tests utilisent `Storage::fake('r2')` pour ne pas frapper le vrai R2

From RESEARCH §Pattern 5 (lignes 492-538) : UPSERT SQL natif complet
From RESEARCH §Code Examples §Medialibrary (lignes 926-952) : storePhoto avec putFile R2 + updateOrCreate PhotoMeta
From PATTERNS §"app/Http/Controllers/Api/PassageController.php" et §"app/Http/Controllers/Api/PassagePhotoController.php" : patterns complets

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Tests PassageApiTest + PhotoUploadTest (RED) + extension PhotoMeta + Passage helper</name>
  <files>tests/Feature/PassageApiTest.php, tests/Feature/PhotoUploadTest.php, app/Models/PhotoMeta.php, app/Models/Passage.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "app/Models/PhotoMeta.php", "app/Models/Passage.php", "Tests Feature")
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 5 (UPSERT) + §Pitfall 7 (DB::affectingStatement) + §Pitfall 6 (HasMedia non requis — Storage::disk direct)
    - tests/Feature/MigrationsTest.php (analog pattern SAVEPOINT pour Postgres unique constraint tests)
    - tests/Feature/AuthLoginTest.php (analog actingAs $user → guard web)
    - app/Models/PhotoMeta.php (état actuel — pas de client_uuid)
    - app/Models/Passage.php (état actuel — pas de latestPhoto helper)
    - database/factories/PassageFactory.php, database/factories/ClientFactory.php, database/factories/PiscineFactory.php
  </read_first>
  <behavior>
    **tests/Feature/PassageApiTest.php** (uses(RefreshDatabase::class)) :
    - Test 1 : "POST /api/passages avec un client_uuid frais crée le passage et retourne 200 ok=true" — actingAs($pierre); postJson('/api/passages', ['client_uuid' => $uuid, 'client_id' => $clientId, 'piscine_id' => $piscineId, 'visited_at' => now()->toIso8601String(), 'ph_avant' => 7.2, 'actions' => ['Brossage'], 'notes' => 'Test']); assertStatus(200)->assertJson(['ok' => true]); expect(Passage::where('client_uuid', $uuid)->count())->toBe(1)
    - Test 2 : "POST /api/passages 2x avec le même client_uuid retourne 200 (UPSERT) et ne crée pas de doublon" — postJson 1 fois OK, postJson une 2e fois avec ph_avant=7.5 (modifié), assertStatus(200) ; expect Passage::where('client_uuid', $uuid)->count() → 1 ; expect Passage::where('client_uuid', $uuid)->first()->ph_avant → 7.5 (la mise à jour a bien appliqué)
    - Test 3 : "POST /api/passages sur un passage avec status != 'draft' retourne 409 avec error=already_closed" — créer un passage en DB directement avec `status='closed'`, postJson avec le même client_uuid, assertStatus(409)->assertJson(['error' => 'already_closed'])->assertJsonStructure(['server_state' => ['id', 'client_uuid', 'status']])
    - Test 4 : "POST /api/passages sans client_uuid retourne 422" — postJson([]); assertStatus(422)->assertJsonValidationErrors(['client_uuid'])
    - Test 5 : "POST /api/passages avec client_uuid non-UUID retourne 422" — postJson(['client_uuid' => 'not-a-uuid']); assertStatus(422)->assertJsonValidationErrors(['client_uuid'])
    - Test 6 : "POST /api/passages sans auth retourne redirect /login (web guard)" — pas d'actingAs ; postJson(...); assertRedirect('/login')
    - Test 7 : "POST /api/passages valide actions comme array si présent" — postJson([..., 'actions' => 'not-array']); assertStatus(422)->assertJsonValidationErrors(['actions'])
    - Test 8 : "Le passage UPSERT met à jour synced_at à NOW()" — assertNotNull Passage::where('client_uuid', $uuid)->first()->synced_at après le POST
    - Test 9 : "CSRF token absent ne bloque pas (api/* exempt)" — postJson sans csrf header ; assertStatus(200). Note : actingAs gère déjà l'auth ; pour vérifier l'exemption CSRF en isolation, faire un POST classique sans csrf et vérifier qu'on n'a pas 419.

    **tests/Feature/PhotoUploadTest.php** (uses(RefreshDatabase::class)) :
    - beforeEach : `Storage::fake('r2'); $passage = Passage::factory()->create(['client_uuid' => $uuid, 'status' => 'draft']);`
    - Test 1 : "POST /api/passages/{uuid}/photos avec un JPEG et un photo client_uuid frais retourne 200, crée un fichier sur r2 et une ligne photos_meta" — fakeFile = UploadedFile::fake()->image('photo.jpg', 1024, 768)->size(500); actingAs($pierre)->postJson("/api/passages/{$uuid}/photos", ['photo' => $fakeFile, 'client_uuid' => $photoUuid, 'captured_at' => now()->toIso8601String()]); assertStatus(200) ; Storage::disk('r2')->assertExists(...) ; expect PhotoMeta::where('client_uuid', $photoUuid)->exists()
    - Test 2 : "Photo upload idempotent : le même client_uuid photo POSTé 2x reste 1 ligne photos_meta" — POST 1ère fois, POST 2e fois (même photo_uuid mais différent contenu), assertStatus(200), expect PhotoMeta::where('client_uuid', $photoUuid)->count() → 1
    - Test 3 : "Photo sans 'photo' ou avec mime non-JPEG retourne 422" — assertions multiples : photo manquante, PDF uploadé, etc.
    - Test 4 : "Photo > 10 MB retourne 422" — UploadedFile::fake()->create('big.jpg', 11000)
    - Test 5 : "Photo client_uuid manquant retourne 422"
    - Test 6 : "Photo passage_uuid inexistant retourne 404" — postJson("/api/passages/inexistant-uuid/photos", [...])->assertStatus(404)
    - Test 7 : "Photo sans auth retourne redirect /login"
    - Test 8 : "Le path stocké suit le pattern passages/{passage_uuid}/photos/{random}.jpg" — assert que PhotoMeta::first()->path commence par "passages/{$uuid}/photos/"
  </behavior>
  <action>
    1. Modifier `app/Models/PhotoMeta.php` :
    - Ajouter `'client_uuid'` à `$fillable` (insérer en première ou seconde position pour cohérence)
    - Ajouter `'client_uuid' => 'string'` à `$casts`
    Pattern final :
    ```
    protected $fillable = [
        'client_uuid',
        'passage_id',
        'disk',
        'path',
        'mime_type',
        'size_bytes',
        'width',
        'height',
        'captured_at',
    ];
    protected $casts = [
        'client_uuid' => 'string',
        'captured_at' => 'datetime',
        'size_bytes'  => 'integer',
        'width'       => 'integer',
        'height'      => 'integer',
    ];
    ```

    2. Modifier `app/Models/Passage.php` :
    - Ajouter méthode helper `latestPhoto()` (utile pour le portail Plan 02-07 et la liste passages 02-03) :
    ```
    public function latestPhoto(): \Illuminate\Database\Eloquent\Relations\HasOne
    {
        return $this->hasOne(PhotoMeta::class)->latestOfMany('captured_at');
    }
    ```
    Pas d'autre modification — pas de HasMedia (RESEARCH §Open Questions §2 — Storage::disk direct préféré, évite double-table media/photos_meta).

    3. Créer `tests/Feature/PassageApiTest.php` et `tests/Feature/PhotoUploadTest.php` selon contrats <behavior> ci-dessus. Pattern Pest 4 + RefreshDatabase. Utiliser `Storage::fake('r2')` au début de chaque test photo. Pour Test 3 (409 sur passage clos), utiliser le pattern SAVEPOINT n'est pas nécessaire ici car le UPSERT ne génère pas d'exception — il retourne juste 0 lignes affectées. Test 9 (CSRF exempt) : utiliser `$this->post('/api/passages', [...], ['Accept' => 'application/json'])` sans préfixe `postJson` pour s'assurer que Laravel ne gère pas le CSRF différemment.

    Note : `actingAs($user)` sur le guard web suffit — `bootstrap/app.php` enregistre routes/api.php sous middleware `['web', 'auth']`, donc la session est active.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PassageApiTest.php tests/Feature/PhotoUploadTest.php --ci</automated>
  </verify>
  <acceptance_criteria>
    - 4 fichiers créés/modifiés (PhotoMeta.php, Passage.php, PassageApiTest.php, PhotoUploadTest.php)
    - `grep -c "client_uuid" app/Models/PhotoMeta.php` ≥ 2 (fillable + cast)
    - `grep -c "latestOfMany" app/Models/Passage.php` ≥ 1
    - `./vendor/bin/pest tests/Feature/PassageApiTest.php --ci` retourne **RED** (les 9 tests doivent ECHOUER car les controllers n'existent pas encore) — c'est le but du RED gate
    - `./vendor/bin/pest tests/Feature/PhotoUploadTest.php --ci` retourne **RED** (8 tests qui échouent)
    - Suite Pest existante reste verte sur les autres fichiers : `./vendor/bin/pest --exclude-testsuite=… --ci` exit 0
    - Commit ce niveau séparément : `test(02-06): RED — PassageApi + PhotoUpload tests`
  </acceptance_criteria>
  <done>17 tests RED écrits, PhotoMeta supporte client_uuid en fillable/cast, Passage a un helper latestPhoto. Le GREEN gate est Task 2.</done>
</task>

<task type="auto">
  <name>Task 2 : PassageController + PassagePhotoController + routes/api.php (GREEN gate)</name>
  <files>app/Http/Controllers/Api/PassageController.php, app/Http/Controllers/Api/PassagePhotoController.php, routes/api.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "app/Http/Controllers/Api/PassageController.php", "app/Http/Controllers/Api/PassagePhotoController.php", "routes/api.php")
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 5 (UPSERT complet à copier) + §Code Examples §Medialibrary (storePhoto) + §Pitfall 7 (affectingStatement vs statement)
    - tests/Feature/PassageApiTest.php, tests/Feature/PhotoUploadTest.php (les contrats à satisfaire — RED → GREEN)
    - app/Http/Controllers/Admin/DashboardController.php (analog namespace + Controller import)
    - routes/api.php (stub à compléter)
    - config/filesystems.php (vérifier disk r2)
    - app/Models/PhotoMeta.php (mis à jour Task 1)
  </read_first>
  <action>
    1. Créer `app/Http/Controllers/Api/PassageController.php` :
    ```
    <?php

    namespace App\Http\Controllers\Api;

    use App\Http\Controllers\Controller;
    use App\Models\Passage;
    use Illuminate\Http\JsonResponse;
    use Illuminate\Http\Request;
    use Illuminate\Support\Facades\DB;

    class PassageController extends Controller
    {
        /**
         * POST /api/passages
         * UPSERT idempotent conditionnel sur client_uuid (D-38, D-39).
         * 200 si insert ou update success (status === 'draft').
         * 409 si le passage existe avec status !== 'draft' (D-40).
         * 422 si validation fail.
         */
        public function store(Request $request): JsonResponse
        {
            $data = $request->validate([
                'client_uuid'   => ['required', 'uuid'],
                'piscine_id'    => ['nullable', 'integer', 'exists:piscines,id'],
                'client_id'     => ['nullable', 'integer', 'exists:clients,id'],
                'visited_at'    => ['nullable', 'date'],
                'ph_avant'      => ['nullable', 'numeric'],
                'ph_apres'      => ['nullable', 'numeric'],
                'chlore_libre'  => ['nullable', 'numeric'],
                'chlore_total'  => ['nullable', 'numeric'],
                'tac'           => ['nullable', 'numeric'],
                'sel_g_l'       => ['nullable', 'numeric'],
                'th'            => ['nullable', 'numeric'],
                'actions'       => ['nullable', 'array'],
                'actions.*'     => ['string', 'max:60'],
                'notes'         => ['nullable', 'string', 'max:2000'],
                'notes_privees' => ['nullable', 'string', 'max:2000'],
            ]);

            $visitedAt = $data['visited_at'] ?? now()->toIso8601String();
            $actionsJson = json_encode($data['actions'] ?? []);

            // UPSERT Postgres conditionnel sur status='draft' (D-38)
            $affected = DB::affectingStatement(
                <<<SQL
                INSERT INTO passages (
                    client_uuid, piscine_id, client_id, visited_at, status,
                    ph_avant, ph_apres, chlore_libre, chlore_total, tac, th, sel_g_l,
                    actions, notes,
                    synced_at, created_at, updated_at
                )
                VALUES (
                    :client_uuid, :piscine_id, :client_id, :visited_at, 'draft',
                    :ph_avant, :ph_apres, :chlore_libre, :chlore_total, :tac, :th, :sel_g_l,
                    :actions::jsonb, :notes,
                    NOW(), NOW(), NOW()
                )
                ON CONFLICT (client_uuid) DO UPDATE SET
                    piscine_id    = EXCLUDED.piscine_id,
                    client_id     = EXCLUDED.client_id,
                    visited_at    = EXCLUDED.visited_at,
                    ph_avant      = EXCLUDED.ph_avant,
                    ph_apres      = EXCLUDED.ph_apres,
                    chlore_libre  = EXCLUDED.chlore_libre,
                    chlore_total  = EXCLUDED.chlore_total,
                    tac           = EXCLUDED.tac,
                    th            = EXCLUDED.th,
                    sel_g_l       = EXCLUDED.sel_g_l,
                    actions       = EXCLUDED.actions,
                    notes         = EXCLUDED.notes,
                    synced_at     = NOW(),
                    updated_at    = NOW()
                WHERE passages.status = 'draft'
                SQL,
                [
                    'client_uuid'  => $data['client_uuid'],
                    'piscine_id'   => $data['piscine_id'] ?? null,
                    'client_id'    => $data['client_id'] ?? null,
                    'visited_at'   => $visitedAt,
                    'ph_avant'     => $data['ph_avant']     ?? null,
                    'ph_apres'     => $data['ph_apres']     ?? null,
                    'chlore_libre' => $data['chlore_libre'] ?? null,
                    'chlore_total' => $data['chlore_total'] ?? null,
                    'tac'          => $data['tac']          ?? null,
                    'th'           => $data['th']           ?? null,
                    'sel_g_l'      => $data['sel_g_l']      ?? null,
                    'actions'      => $actionsJson,
                    'notes'        => $data['notes']        ?? null,
                ]
            );

            if ($affected === 0) {
                // Passage déjà clos (D-40) — récupérer l'état serveur pour le client
                $serverState = Passage::where('client_uuid', $data['client_uuid'])->first();
                return response()->json([
                    'error'        => 'already_closed',
                    'server_state' => $serverState,
                ], 409);
            }

            return response()->json(['ok' => true], 200);
        }
    }
    ```

    Note Pitfall 7 : `DB::affectingStatement()` retourne le nombre de lignes affectées (1 pour INSERT, 1 pour UPDATE qui match, 0 si la `WHERE status='draft'` ne match pas). Ne PAS utiliser `DB::statement()` qui retourne juste un bool.

    2. Créer `app/Http/Controllers/Api/PassagePhotoController.php` :
    ```
    <?php

    namespace App\Http\Controllers\Api;

    use App\Http\Controllers\Controller;
    use App\Models\Passage;
    use App\Models\PhotoMeta;
    use Illuminate\Http\JsonResponse;
    use Illuminate\Http\Request;
    use Illuminate\Support\Facades\Storage;

    class PassagePhotoController extends Controller
    {
        /**
         * POST /api/passages/{passageUuid}/photos
         * Upload + UPSERT idempotent sur photos_meta.client_uuid (D-42).
         * Pas de medialibrary — Storage::disk('r2') direct (RESEARCH §Open Questions §2).
         */
        public function store(Request $request, string $passageUuid): JsonResponse
        {
            $request->validate([
                'photo'       => ['required', 'file', 'mimes:jpeg,jpg', 'max:10240'],
                'client_uuid' => ['required', 'uuid'],
                'captured_at' => ['nullable', 'date'],
            ]);

            // 404 explicite si le passage n'existe pas (sécurité — pas d'orphelin photo)
            $passage = Passage::where('client_uuid', $passageUuid)->firstOrFail();

            $file = $request->file('photo');
            $path = Storage::disk('r2')->putFile("passages/{$passageUuid}/photos", $file);

            // UPSERT idempotent sur client_uuid (D-42)
            PhotoMeta::updateOrCreate(
                ['client_uuid' => $request->input('client_uuid')],
                [
                    'passage_id'  => $passage->id,
                    'disk'        => 'r2',
                    'path'        => $path,
                    'mime_type'   => $file->getMimeType() ?: 'image/jpeg',
                    'size_bytes'  => $file->getSize(),
                    'captured_at' => $request->input('captured_at') ?? now(),
                ]
            );

            return response()->json(['ok' => true], 200);
        }
    }
    ```

    3. Remplir `routes/api.php` (stub vide créé Plan 02-01) :
    ```
    <?php

    use App\Http\Controllers\Api\PassageController;
    use App\Http\Controllers\Api\PassagePhotoController;
    use Illuminate\Support\Facades\Route;

    /*
     | Routes API offline-sync.
     | Enregistrées dans bootstrap/app.php sous middleware(['web','auth'])->prefix('api')->name('api.').
     | CSRF exempté pour /api/* dans bootstrap/app.php (Pitfall 5).
     */

    Route::post('passages', [PassageController::class, 'store'])->name('passages.store');
    Route::post('passages/{uuid}/photos', [PassagePhotoController::class, 'store'])->name('passages.photos.store');
    ```

    Note : Le `{uuid}` est passé tel quel au controller — Laravel ne fait pas de route model binding car on cherche par `client_uuid` (UNIQUE non-id). Le `firstOrFail()` dans le controller gère le 404. Optionnellement on peut ajouter `->whereUuid('uuid')` pour la validation regex au niveau route :
    ```
    Route::post('passages/{uuid}/photos', [PassagePhotoController::class, 'store'])
         ->whereUuid('uuid')
         ->name('passages.photos.store');
    ```

    4. Vérifier que `bootstrap/app.php` enregistre bien `routes/api.php` (posé Plan 02-01). Vérifier que `validateCsrfTokens(except: ['api/*'])` est actif.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PassageApiTest.php tests/Feature/PhotoUploadTest.php --ci && php artisan route:list --path=api</automated>
  </verify>
  <acceptance_criteria>
    - `./vendor/bin/pest tests/Feature/PassageApiTest.php --ci` : 9/9 GREEN
    - `./vendor/bin/pest tests/Feature/PhotoUploadTest.php --ci` : 8/8 GREEN
    - `php artisan route:list --path=api | grep -c "api.passages.store"` ≥ 1
    - `php artisan route:list --path=api | grep -c "api.passages.photos.store"` ≥ 1
    - `grep -c "DB::affectingStatement" app/Http/Controllers/Api/PassageController.php` ≥ 1
    - `grep -c "ON CONFLICT (client_uuid)" app/Http/Controllers/Api/PassageController.php` ≥ 1
    - `grep -c "WHERE passages.status = 'draft'" app/Http/Controllers/Api/PassageController.php` ≥ 1
    - `grep -c "Storage::disk('r2')" app/Http/Controllers/Api/PassagePhotoController.php` ≥ 1
    - `grep -c "updateOrCreate" app/Http/Controllers/Api/PassagePhotoController.php` ≥ 1
    - `./vendor/bin/pest --ci` : suite totale verte (140+ → 140+17 = 157+)
    - Commit séparément : `feat(02-06): GREEN — UPSERT idempotent passages + photos R2`
  </acceptance_criteria>
  <done>POST /api/passages UPSERT conditionnel idempotent (D-38) avec 200/409/422 ; POST /api/passages/{uuid}/photos avec stockage R2 + upsert photos_meta UNIQUE (D-42, D-48 max 10MB) ; 17 tests passants, suite totale verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Body JSON Alpine → `validate()` Laravel | Validation stricte avant tout traitement DB |
| File multipart Alpine → `mimes:jpeg` check | Validation MIME serveur en plus du pipeline client |
| Storage::disk('r2') → Cloudflare | Upload S3-compatible avec credentials env (pas user-supplied) |
| UPSERT WHERE status='draft' | Garantie applicative que les passages clos sont immuables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | Modifier un passage déjà clos via UPSERT | mitigate | `WHERE passages.status = 'draft'` empêche atomiquement la mise à jour. Test 3 du PassageApiTest le valide. |
| T-6-02 | Tampering | Upload de malware via input 'photo' | mitigate | `mimes:jpeg,jpg` + `max:10240` (10 MB). Le contenu est stocké sur R2 (CDN isolé), pas dans public_path. |
| T-6-03 | Information Disclosure | Path R2 prédictible | accept | Le path utilise `putFile` qui génère un nom aléatoire (Laravel default). Pas de stockage de nom user-supplied. Les URL temporaires (Plan 02-07) sont signées. |
| T-6-04 | DoS via UPSERT massif | Denial of Service | mitigate | Pas de rate limiting sur /api/passages — Pierre est seul opérateur. Risque acceptable. Pourrait être ajouté en throttle:60,1 si besoin futur. |
| T-6-05 | Injection SQL via UPSERT raw | Tampering | mitigate | Bindings nommés (:client_uuid, :ph_avant, etc.) — pas de string interpolation. `$actionsJson` via `json_encode` puis binding. |
| T-6-06 | Auth bypass via session cookie forgé | Spoofing | mitigate | Session cookies Laravel signés via APP_KEY. CSRF exempt pour /api/* mais auth web obligatoire (middleware). |
| T-6-07 | Validation actions non-string | Input Validation | mitigate | `'actions.*' => ['string', 'max:60']` empêche les payloads JSON anonymes ou objets. Test 7. |
</threat_model>

<verification>
1. `./vendor/bin/pest --ci` : 157+ tests verts (140 + 17 nouveaux)
2. Smoke test manuel : `curl -X POST http://127.0.0.1:8000/api/passages -H "Cookie: laravel_session=...; XSRF-TOKEN=..." -H "Content-Type: application/json" -d '{"client_uuid":"...","ph_avant":7.2}' --cookie-jar /tmp/cookies` (après login pro)
3. Smoke test 409 : insérer un passage avec status='closed' en DB (`Passage::factory()->create(['client_uuid' => $uuid, 'status' => 'closed'])`), POST le même UUID → 409
4. Smoke test photo upload : `curl -X POST http://127.0.0.1:8000/api/passages/{uuid}/photos -F "photo=@./test.jpg" -F "client_uuid=..." -F "captured_at=2026-05-28T12:00:00Z"`
5. Vérifier R2 staging bucket contient le fichier uploadé (via Cloudflare dashboard)
</verification>

<success_criteria>
- [ ] PASS-03 (sync sans doublon) : UPSERT `ON CONFLICT (client_uuid) DO UPDATE WHERE status='draft'` opérationnel
- [ ] PASS-04 (photos résilientes) : photo upload idempotent par client_uuid UNIQUE photo_meta + max 10 MB + mime JPEG check
- [ ] 17 tests Pest passants (9 PassageApi + 8 PhotoUpload)
- [ ] Routes nommées api.passages.store et api.passages.photos.store actives
- [ ] PhotoMeta : client_uuid en fillable + cast string
- [ ] Passage : helper latestPhoto() ajouté
- [ ] D-38 honoré (UPSERT conditionnel WHERE status='draft')
- [ ] D-40 honoré (409 avec error=already_closed + server_state)
- [ ] D-42 honoré (photos_meta.client_uuid UNIQUE + updateOrCreate)
- [ ] D-48 honoré (max 10240 KB = 10 MB)
- [ ] Pitfall 5 honoré (CSRF exempt /api/* depuis 02-01 + tests vérifient 200 sans CSRF token)
- [ ] Pitfall 7 honoré (DB::affectingStatement utilisé, pas DB::statement)
- [ ] 2 commits séparés : `test(02-06): RED — ...` puis `feat(02-06): GREEN — ...`
- [ ] Aucune régression sur les 140 tests cumulés Phase 1 + Plans 02-01..02-05
</success_criteria>

<output>
Créer `.planning/phases/02-mvp-suivi-offline-first/02-06-SUMMARY.md` à la fin du plan.
</output>
