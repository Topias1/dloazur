---
phase: 02-mvp-suivi-offline-first
plan: 04
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - vite.config.js
  - resources/js/app.js
  - resources/views/layouts/admin.blade.php
files_created:
  - resources/js/offline-queue.js
  - resources/js/photo-pipeline.js
  - resources/views/offline.blade.php
  - public/icons/pwa-192x192.png
  - public/icons/pwa-512x512.png
  - tests/Feature/PwaConfigTest.php
autonomous: false
requirements:
  - PASS-03
  - PASS-04
  - PASS-06
user_setup:
  - service: pwa-icons
    why: "Icônes PWA Home Screen (manifest 192x192 + 512x512 maskable). Génération possible via `npx @vite-pwa/assets-generator` ou outil graphique manuel depuis le logo Dlo Azur (drop azure-500)."
    files:
      - "public/icons/pwa-192x192.png"
      - "public/icons/pwa-512x512.png"
tags: [pwa, service-worker, indexeddb, photo-pipeline, vite-plugin-pwa]
must_haves:
  truths:
    - "Le build Vite (npm run build) génère public/build/sw.js et public/build/manifest.webmanifest"
    - "Le manifest contient name='Dlo Azur · Métier', short_name='Dlo Azur', theme_color='#0080ff', background_color='#fdfcf9', display='standalone', orientation='portrait', start_url='/admin/passages/create'"
    - "Le SW Workbox généré utilise navigateFallback='/offline' avec denylist [/^\\/admin\\//, /^\\/portail\\//]"
    - "Le SW Workbox contient un BackgroundSyncPlugin pour POST /api/passages avec queueName='passages-queue' et maxRetentionTime=1440 (24h en minutes)"
    - "Le module resources/js/offline-queue.js exporte openOfflineDB() qui ouvre IDB 'dloazur-offline-v1' v1 avec stores 'passages' et 'photos' (D-59)"
    - "Le module resources/js/photo-pipeline.js exporte processPhoto(file) qui détecte HEIC par magic bytes (offset 8-11 du ftyp box), convertit via heic2any si nécessaire, corrige EXIF via exifr, compresse Canvas max 2048px JPEG q=0.80 (D-43, D-44, Pitfall 8)"
    - "app.js enregistre le SW via registerSW({ onNeedRefresh, onOfflineReady }) du virtual:pwa-register (D-56 registerType:'prompt')"
    - "app.js déclare Alpine.store('offlineQueue', { pendingCount: 0, errorCount: 0 }) accessible globalement"
    - "layouts/admin.blade.php contient <meta name=\"csrf-token\" content=\"{{ csrf_token() }}\"> dans <head> (Pitfall 5)"
    - "resources/views/offline.blade.php affiche la page hors-ligne UI-SPEC §Page offline.html (logo + titre + corps + CTA Retour à la saisie)"
  artifacts:
    - path: "vite.config.js"
      provides: "Plugin VitePWA configuré (generateSW + buildBase /build/ + manifest + workbox runtimeCaching)"
      contains: "VitePWA"
    - path: "resources/js/offline-queue.js"
      provides: "Module IndexedDB via idb v8 — openOfflineDB + helpers savePassage/savePhoto/getPending/markStatus"
      contains: "openDB.*dloazur-offline-v1"
    - path: "resources/js/photo-pipeline.js"
      provides: "Module pipeline photo : détection HEIC par magic bytes + conversion heic2any lazy + EXIF + compression Canvas JPEG 0.80"
      contains: "heic2any\\|exifr"
    - path: "resources/views/offline.blade.php"
      provides: "Page hors-ligne précachée par Workbox"
      contains: "Vous êtes hors ligne"
    - path: "resources/js/app.js"
      provides: "Bootstrap Alpine + SW registration + Alpine.store('offlineQueue') + import passage-form (placeholder Plan 02-05)"
      contains: "registerSW\\|virtual:pwa-register"
    - path: "resources/views/layouts/admin.blade.php"
      provides: "Layout admin avec meta csrf-token (requis Pitfall 5)"
      contains: "csrf-token"
  key_links:
    - from: "vite.config.js"
      to: "vite-plugin-pwa"
      via: "import { VitePWA } from 'vite-plugin-pwa'; plugins: [VitePWA({...})]"
      pattern: "VitePWA"
    - from: "resources/js/app.js"
      to: "virtual:pwa-register"
      via: "import { registerSW } from 'virtual:pwa-register'"
      pattern: "virtual:pwa-register"
    - from: "resources/js/offline-queue.js"
      to: "idb"
      via: "import { openDB } from 'idb'"
      pattern: "import.*from 'idb'"
    - from: "resources/js/photo-pipeline.js"
      to: "heic2any (lazy)"
      via: "await import('heic2any')"
      pattern: "import\\('heic2any'\\)"
---

<objective>
Poser l'infrastructure PWA cross-platform : (1) config `vite-plugin-pwa` en mode `generateSW` (D-55) avec `buildBase: '/build/'` (D-60, co-dépendant du middleware ServiceWorkerHeaders posé en 02-01), (2) manifest Web App contractuel (UI-SPEC §PWA Manifest Contract), (3) cache strategies Workbox (NetworkFirst navigation + offline fallback / CacheFirst assets Vite / NetworkOnly+BackgroundSync /api/passages), (4) module IndexedDB `offline-queue.js` avec idb v8 et schéma typé (D-59 : DB `dloazur-offline-v1` v1, stores `passages` + `photos`), (5) module `photo-pipeline.js` (détection HEIC magic bytes + conversion `heic2any` lazy + EXIF rotation `exifr` + compression Canvas JPEG 0.80 max 2048px — D-43, D-44, Pitfall 8), (6) registration SW dans `app.js` (D-56 registerType 'prompt'), (7) page `offline.blade.php` précachée, (8) meta csrf-token dans le layout admin (Pitfall 5).

Purpose : ce plan ne livre AUCUN écran utilisateur observable directement (offline.html est précaché mais visible uniquement quand l'utilisateur est offline sur une route non cachée). Il prépare TOUT pour que Plan 02-05 (saisie passage Alpine) ait : (a) le SW qui intercepte les fetch, (b) le store IDB où persister, (c) le pipeline photo qui produit des JPEG compressés, (d) le Alpine.store('offlineQueue') qui alimente le badge "N en attente".

Output : 4 fichiers créés (offline-queue.js, photo-pipeline.js, offline.blade.php, PwaConfigTest.php) + 3 fichiers modifiés (vite.config.js, app.js, layouts/admin.blade.php) + 2 assets PWA (user-setup checkpoint).

Le plan a un checkpoint blocking pour la génération des icônes PWA (192x192 + 512x512) — Claude peut les générer automatiquement avec `npx @vite-pwa/assets-generator` à partir du logo, mais valide visuellement avec l'utilisateur avant de poursuivre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-suivi-offline-first/02-CONTEXT.md
@.planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md
@.planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md
@.planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md
@.planning/phases/02-mvp-suivi-offline-first/02-01-SUMMARY.md
@CLAUDE.md
@DESIGN.md

<interfaces>

From CLAUDE.md §"PWA / Offline" :
- `vite-plugin-pwa` ^1.3 (déjà installé package.json)
- `idb` ^8 (déjà installé)
- `heic2any` ^0.0.4 (déjà installé)
- `exifr` ^7.1.3 (déjà installé)
- Contraintes Laravel : `buildBase: '/build/'` + header `Service-Worker-Allowed: /` (posés respectivement ici en vite.config.js et en 02-01 via ServiceWorkerHeaders middleware)
- Background Sync iOS Safari = no-op silencieux → fallback Alpine 'online'/'visibilitychange' obligatoire (Plan 02-05 implementera dans passage-form.js)

From vite.config.js (état actuel) :
- import { defineConfig } from 'vite';
- import laravel from 'laravel-vite-plugin';
- import { bunny } from 'laravel-vite-plugin/fonts';
- import tailwindcss from '@tailwindcss/vite';
- plugins : [laravel(…), tailwindcss()]
- Aucun VitePWA pour l'instant.

From resources/js/app.js (état actuel — 3 lignes) :
- import Alpine from 'alpinejs';
- window.Alpine = Alpine;
- Alpine.start();

From resources/views/layouts/admin.blade.php (état actuel) :
- `<head>` contient `<meta charset>`, `<meta viewport>`, `<title>`, `<meta name="robots" content="noindex,nofollow">`, `@vite(['resources/css/app.css', 'resources/js/app.js'])`, `@stack('head')`
- AUCUN `<meta name="csrf-token">` actuellement → AJOUT obligatoire (Pitfall 5).

From .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 1 (IDB schema D-59), §Pattern 4 (photo pipeline), §Pattern 7 (vite.config.js cible) :
- DB name : 'dloazur-offline-v1', version 1
- Store passages keyPath 'id' autoIncrement : { id, client_uuid, payload_json, status, attempts, created_at, last_attempt_at }, indexes 'by-status' + 'by-created'
- Store photos keyPath 'id' autoIncrement : { id, client_uuid, passage_client_uuid, blob, status, attempts, captured_at }, indexes 'by-passage' + 'by-status'
- HEIC detection magic bytes : `bytes[4..7] = 'ftyp'`, `bytes[8..11]` brand in ['heic','heis','hevc','mif1','msf1']
- WebP interdit (régressions Safari iOS 17)

From UI-SPEC §"Page offline.html (précachée par Workbox)" :
- Layout `min-h-screen flex flex-col items-center justify-center bg-sand-50 px-6 text-center`
- Logo drop azure-500 h-16 w-16 + "Dlo Azur" Fredoka 700 text-2xl text-ink-950
- Titre Fredoka 600 text-xl text-ink-950 mt-8 : "Vous êtes hors ligne"
- Corps Inter 400 text-ink-500 mt-3 : "La saisie d'un passage reste disponible. Les autres pages se chargeront au retour du réseau."
- Bouton h-13 px-6 rounded-xl bg-azure-500 text-white font-bold mt-6 : "Retour à la saisie" → href="/admin/passages/create" (route créée Plan 02-05, mais OK car offline.blade.php pointe vers une route que le SW intercept-cache au premier load)
- Pas d'illustration ni d'icône wifi barrée

From UI-SPEC §"PWA Manifest Contract" :
```
{
  "name": "Dlo Azur · Métier",
  "short_name": "Dlo Azur",
  "description": "Saisie de passages offline-first — Dlo Azur Piscines",
  "theme_color": "#0080ff",
  "background_color": "#fdfcf9",
  "display": "standalone",
  "orientation": "portrait",
  "start_url": "/admin/passages/create",
  "icons": [
    { "src": "/icons/pwa-192x192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/pwa-512x512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Configurer vite-plugin-pwa dans vite.config.js + meta csrf-token dans layout admin</name>
  <files>vite.config.js, resources/views/layouts/admin.blade.php</files>
  <read_first>
    - vite.config.js (état actuel — 3 plugins)
    - resources/views/layouts/admin.blade.php (état actuel)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 7 (config complète à copier lignes 585-658)
    - .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md §"PWA Manifest Contract" (valeurs exactes)
    - CLAUDE.md §"PWA / Offline" (contraintes buildBase + Service-Worker-Allowed)
  </read_first>
  <action>
    1. Étendre `vite.config.js` (préserver intégralement le bloc `plugins: [laravel(…), tailwindcss()]` et l'ordre actuel) :
    - Ajouter `import { VitePWA } from 'vite-plugin-pwa';` en haut
    - Ajouter `VitePWA({...})` APRÈS `tailwindcss()` (toujours dernier plugin)
    - Config VitePWA exacte (D-55..D-60) :
    ```
    VitePWA({
        registerType: 'prompt',
        buildBase: '/build/',
        includeAssets: ['icons/*.png'],
        manifest: {
            name: 'Dlo Azur · Métier',
            short_name: 'Dlo Azur',
            description: 'Saisie de passages offline-first — Dlo Azur Piscines',
            theme_color: '#0080ff',
            background_color: '#fdfcf9',
            display: 'standalone',
            orientation: 'portrait',
            start_url: '/admin/passages/create',
            icons: [
                { src: '/icons/pwa-192x192.png', sizes: '192x192', type: 'image/png' },
                { src: '/icons/pwa-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' },
            ],
        },
        workbox: {
            navigateFallback: '/offline',
            navigateFallbackDenylist: [/^\/admin\//, /^\/portail\//, /^\/api\//],
            globPatterns: ['**/*.{js,css,woff2}'],
            runtimeCaching: [
                {
                    urlPattern: /\/build\/assets\//,
                    handler: 'CacheFirst',
                    options: {
                        cacheName: 'vite-assets',
                        expiration: { maxAgeSeconds: 365 * 24 * 60 * 60, maxEntries: 60 },
                    },
                },
                {
                    urlPattern: ({ url }) => url.pathname === '/offline',
                    handler: 'CacheFirst',
                    options: { cacheName: 'offline-fallback' },
                },
                {
                    urlPattern: /\/api\/passages/,
                    handler: 'NetworkOnly',
                    method: 'POST',
                    options: {
                        backgroundSync: {
                            name: 'passages-queue',
                            options: { maxRetentionTime: 24 * 60 },
                        },
                    },
                },
                {
                    urlPattern: /\/api\/passages\/.+\/photos/,
                    handler: 'NetworkOnly',
                    method: 'POST',
                    options: {
                        backgroundSync: {
                            name: 'photos-queue',
                            options: { maxRetentionTime: 24 * 60 },
                        },
                    },
                },
            ],
        },
        devOptions: {
            enabled: false,  // SW généré uniquement en build prod (Pitfall 9 — tests offline via build)
        },
    }),
    ```
    Note : `navigateFallbackDenylist` inclut `/admin/` car les routes admin doivent toujours faire un fetch réseau (NetworkFirst avec timeout fallback offline.html serait piégé — explication RESEARCH §Pattern 7 D-58). Plan 02-05 pourra basculer `/admin/passages/create` en CacheFirst pour qu'il fonctionne offline ; mais par défaut, navigateFallback ne sert que pour les routes inconnues / publiques.

    2. Modifier `resources/views/layouts/admin.blade.php` :
    - Ajouter dans le `<head>` AVANT `@vite(...)` :
      ```
      <meta name="csrf-token" content="{{ csrf_token() }}">
      ```
    - Aucune autre modification.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 && ls -la public/build/sw.js public/build/manifest.webmanifest 2>&1 && grep -c "csrf-token" resources/views/layouts/admin.blade.php</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 sans erreur
    - Le build produit `public/build/sw.js` (taille > 5 KB)
    - Le build produit `public/build/manifest.webmanifest`
    - `cat public/build/manifest.webmanifest | grep -c "Dlo Azur · Métier"` ≥ 1
    - `cat public/build/manifest.webmanifest | grep -c "/admin/passages/create"` ≥ 1
    - `grep -c "passages-queue" public/build/sw.js` ≥ 1 (BackgroundSync queue présente)
    - `grep -c "VitePWA" vite.config.js` ≥ 1
    - `grep -c "buildBase: '/build/'" vite.config.js` ≥ 1
    - `grep -c "registerType: 'prompt'" vite.config.js` ≥ 1
    - `grep -c "csrf-token" resources/views/layouts/admin.blade.php` ≥ 1
    - Pas de régression suite Pest : `./vendor/bin/pest --ci` reste vert
  </acceptance_criteria>
  <done>Vite-plugin-pwa configuré, manifest généré correctement, sw.js contient les caches Workbox + BackgroundSync queues, meta csrf-token présent dans le layout admin.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Génération des icônes PWA Home Screen (192x192 + 512x512) depuis le logo Dlo Azur (drop azure-500 sur fond sable). Claude tentera d'abord `npx @vite-pwa/assets-generator` (ou un outil ImageMagick si dispo) pour produire des PNG basiques.

    Options à présenter à l'utilisateur si la génération automatique n'est pas idéale :
    - **Option A** : Conserver les PNG générés automatiquement (drop azure-500 simple sur fond sand-50, suffisant pour MVP)
    - **Option B** : Demander un asset graphique custom (logo "Dlo Azur" Fredoka 700 + goutte d'eau sur fond marine ou azur, version maskable safe zone)
    - **Option C** : Reporter à un commit ultérieur (Plan 02-05 peut être déroulé sans les icônes, mais le manifest ne sera pas valide pour install-prompt)

    Fichiers attendus :
    - `public/icons/pwa-192x192.png` (192x192 PNG transparent OU avec fond)
    - `public/icons/pwa-512x512.png` (512x512 PNG avec safe zone maskable centrale 80% — `purpose: "any maskable"`)
  </what-built>
  <how-to-verify>
    1. Inspecter visuellement les 2 PNG (les ouvrir dans Finder/Preview).
    2. Vérifier que la version 512×512 a une safe zone : le contenu visible occupe seulement le carré central 80% (40px de marge) pour résister au cropping rond/squircle d'Android/iOS.
    3. Tester en local : `npm run build && php artisan serve` puis ouvrir Chrome DevTools → Application → Manifest, vérifier que les icônes apparaissent sans erreur.
    4. Optionnel : tester install prompt sur mobile réel (Chrome Android ou iOS Safari "Ajouter à l'écran d'accueil").
  </how-to-verify>
  <resume-signal>
    Répondre :
    - `approved` : valider les icônes en l'état
    - `regenerate avec [description]` : Claude reprendra la génération avec les contraintes spécifiées
    - `defer` : Plan 02-05 continuera sans icônes valides (manifest avertira mais le SW fonctionnera)
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3 : Modules JS offline-queue.js + photo-pipeline.js + app.js update + offline.blade.php + tests PwaConfigTest</name>
  <files>resources/js/offline-queue.js, resources/js/photo-pipeline.js, resources/js/app.js, resources/views/offline.blade.php, tests/Feature/PwaConfigTest.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "resources/js/app.js", "resources/js/offline-queue.js", "resources/js/photo-pipeline.js" — squelettes complets)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 1 (IDB schema D-59 complet, lignes 292-348), §Pattern 4 (photo pipeline complet, lignes 450-487), §Pattern 7 (registerSW)
    - .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md §"Page offline.html"
    - resources/js/app.js (état actuel — 3 lignes)
    - resources/views/auth/login.blade.php (analog page standalone hors layouts.admin — pour le pattern offline page)
  </read_first>
  <behavior>
    **tests/Feature/PwaConfigTest.php** (vérifications statiques du config + assets après build) :
    - Test 1 : "Le fichier public/build/manifest.webmanifest existe après build" — `expect(File::exists(public_path('build/manifest.webmanifest')))->toBeTrue();`
    - Test 2 : "Le manifest contient name='Dlo Azur · Métier'" — parse JSON et `expect($manifest['name'])->toBe('Dlo Azur · Métier')`
    - Test 3 : "Le manifest a start_url='/admin/passages/create'" — `expect($manifest['start_url'])->toBe('/admin/passages/create')`
    - Test 4 : "Le manifest a 2 icônes (192 + 512)" — `expect(count($manifest['icons']))->toBe(2)`
    - Test 5 : "Le SW généré contient 'passages-queue' (BackgroundSync queueName)" — `expect(File::get(public_path('build/sw.js')))->toContain('passages-queue')`
    - Test 6 : "La route GET /offline retourne 200 et affiche le titre 'Vous êtes hors ligne'" — `$this->get('/offline')->assertStatus(200)->assertSee('Vous êtes hors ligne');`
    - Test 7 : "La route /offline ne nécessite pas d'auth" — sans actingAs, accès libre (la page offline doit être lisible même pour un anonyme — c'est le navigateFallback)
    - Note : ces tests exigent que `npm run build` ait été exécuté AVANT — soit dans la CI (`npm run build` est déjà dans `.github/workflows/tests.yml` Phase 1), soit on saute Tests 1-5 en local et on les marque `->skip(! File::exists(public_path('build/sw.js')), 'Build prod requis');`
  </behavior>
  <action>
    1. Créer `resources/js/offline-queue.js` :
    ```
    import { openDB } from 'idb';

    /**
     * IndexedDB queue manager pour les passages + photos offline.
     * Schema D-59 — DB: 'dloazur-offline-v1' v1, stores: passages + photos.
     */
    const DB_NAME = 'dloazur-offline-v1';
    const DB_VERSION = 1;

    /** @returns {Promise<import('idb').IDBPDatabase>} */
    export async function openOfflineDB() {
        return openDB(DB_NAME, DB_VERSION, {
            upgrade(db) {
                if (!db.objectStoreNames.contains('passages')) {
                    const passStore = db.createObjectStore('passages', {
                        keyPath: 'id',
                        autoIncrement: true,
                    });
                    passStore.createIndex('by-status', 'status');
                    passStore.createIndex('by-created', 'created_at');
                }
                if (!db.objectStoreNames.contains('photos')) {
                    const photoStore = db.createObjectStore('photos', {
                        keyPath: 'id',
                        autoIncrement: true,
                    });
                    photoStore.createIndex('by-passage', 'passage_client_uuid');
                    photoStore.createIndex('by-status', 'status');
                }
            },
        });
    }

    /** Sauvegarde / met à jour un passage en IDB (idempotent sur client_uuid via index séparé). */
    export async function upsertPassage(record) {
        const db = await openOfflineDB();
        // Si record.id existe → update, sinon insert auto
        return db.put('passages', record);
    }

    /** Sauvegarde une photo (blob inclus). */
    export async function savePhoto(record) {
        const db = await openOfflineDB();
        return db.put('photos', record);
    }

    /** Récupère tous les passages avec status === filter (default 'pending'). */
    export async function getPassagesByStatus(status = 'pending') {
        const db = await openOfflineDB();
        return db.getAllFromIndex('passages', 'by-status', status);
    }

    /** Récupère toutes les photos d'un passage (par client_uuid du passage). */
    export async function getPhotosByPassage(passageClientUuid) {
        const db = await openOfflineDB();
        return db.getAllFromIndex('photos', 'by-passage', passageClientUuid);
    }

    /** Met à jour le status (pending/uploading/synced/error) d'un item. */
    export async function markStatus(storeName, id, status, attempts = null) {
        const db = await openOfflineDB();
        const item = await db.get(storeName, id);
        if (!item) return null;
        item.status = status;
        if (attempts !== null) item.attempts = attempts;
        item.last_attempt_at = new Date().toISOString();
        return db.put(storeName, item);
    }

    /** Compte les items non synchronisés (pending + uploading + error) pour le badge. */
    export async function countPendingAll() {
        const db = await openOfflineDB();
        const passages = await db.getAllFromIndex('passages', 'by-status', 'pending');
        const errors = await db.getAllFromIndex('passages', 'by-status', 'error');
        const uploading = await db.getAllFromIndex('passages', 'by-status', 'uploading');
        return {
            pending: passages.length + uploading.length,
            errors: errors.length,
            total: passages.length + uploading.length + errors.length,
        };
    }
    ```

    2. Créer `resources/js/photo-pipeline.js` :
    ```
    /**
     * Pipeline photo client-side : HEIC detect → conversion lazy → EXIF rotation → Canvas JPEG 0.80 max 2048px.
     * D-43, D-44, Pitfall 8 — file.type ment sur iOS Safari, magic bytes obligatoires.
     */

    const MAX_EDGE = 2048;
    const JPEG_QUALITY = 0.80;

    /**
     * Détecte HEIC/HEIF par magic bytes (ftyp box brand).
     * Bytes 4-7 = 'ftyp', bytes 8-11 = major brand.
     */
    export async function isHeicByMagicBytes(file) {
        const buf = await file.slice(0, 12).arrayBuffer();
        const bytes = new Uint8Array(buf);
        // Vérifier ftyp box
        const ftyp = String.fromCharCode(bytes[4], bytes[5], bytes[6], bytes[7]);
        if (ftyp !== 'ftyp') return false;
        const brand = String.fromCharCode(bytes[8], bytes[9], bytes[10], bytes[11]);
        return ['heic', 'heix', 'heis', 'hevc', 'mif1', 'msf1'].includes(brand);
    }

    /**
     * Pipeline complet : retourne un Blob JPEG compressé max 2048px.
     * @param {File} file
     * @returns {Promise<Blob>}
     */
    export async function processPhoto(file) {
        let blob = file;

        // 1. Détecter HEIC par magic bytes (Pitfall 8 — file.type ment)
        if (await isHeicByMagicBytes(file)) {
            const { default: heic2any } = await import('heic2any');
            const converted = await heic2any({
                blob: file,
                toType: 'image/jpeg',
                quality: 0.85,
            });
            blob = Array.isArray(converted) ? converted[0] : converted;
        }

        // 2. Lire l'orientation EXIF AVANT drawImage (D-44)
        const { default: exifr } = await import('exifr');
        let orientation = 1;
        try {
            orientation = (await exifr.orientation(blob)) || 1;
        } catch {
            orientation = 1;
        }

        // 3. createImageBitmap (orientation: 'from-image' applique l'EXIF nativement si supporté)
        const imageBitmap = await createImageBitmap(blob, {
            imageOrientation: 'from-image',
        }).catch(() => createImageBitmap(blob));

        // 4. Canvas resize max 2048px (D-44)
        const scale = Math.min(1, MAX_EDGE / Math.max(imageBitmap.width, imageBitmap.height));
        const w = Math.round(imageBitmap.width * scale);
        const h = Math.round(imageBitmap.height * scale);

        const canvas = document.createElement('canvas');
        canvas.width = w;
        canvas.height = h;
        const ctx = canvas.getContext('2d');

        // Si createImageBitmap n'a pas appliqué l'EXIF (vieux Safari), on le fait manuellement
        // Pour les orientations 5-8 (rotation 90°/270°), inverser w/h
        if (![1, 'from-image'].includes(orientation) && [5, 6, 7, 8].includes(orientation)) {
            canvas.width = h;
            canvas.height = w;
            ctx.save();
            applyExifTransform(ctx, orientation, w, h);
            ctx.drawImage(imageBitmap, 0, 0, w, h);
            ctx.restore();
        } else {
            ctx.drawImage(imageBitmap, 0, 0, w, h);
        }

        return new Promise((resolve) => {
            canvas.toBlob(resolve, 'image/jpeg', JPEG_QUALITY);
        });
    }

    /** Applique une transformation EXIF orientation 1-8 sur un contexte Canvas. */
    function applyExifTransform(ctx, orientation, w, h) {
        switch (orientation) {
            case 2: ctx.translate(w, 0); ctx.scale(-1, 1); break;
            case 3: ctx.translate(w, h); ctx.rotate(Math.PI); break;
            case 4: ctx.translate(0, h); ctx.scale(1, -1); break;
            case 5: ctx.rotate(0.5 * Math.PI); ctx.scale(1, -1); break;
            case 6: ctx.rotate(0.5 * Math.PI); ctx.translate(0, -h); break;
            case 7: ctx.rotate(0.5 * Math.PI); ctx.translate(w, -h); ctx.scale(-1, 1); break;
            case 8: ctx.rotate(-0.5 * Math.PI); ctx.translate(-w, 0); break;
        }
    }
    ```

    3. Modifier `resources/js/app.js` :
    ```
    import Alpine from 'alpinejs';

    // PWA SW registration — D-56 'prompt' (pas autoUpdate, protège la saisie en cours)
    import { registerSW } from 'virtual:pwa-register';

    const updateSW = registerSW({
        onNeedRefresh() {
            // Alpine store pour bannière update (Plan 02-05 affichera le toast)
            if (window.Alpine) {
                Alpine.store('pwaUpdate', { available: true, apply: () => updateSW(true) });
            }
        },
        onOfflineReady() {
            console.log('[SW] App ready for offline use');
        },
        onRegisteredSW(swUrl, registration) {
            console.log('[SW] registered at', swUrl, registration?.scope);
        },
    });

    // Alpine store global : compteur d'items en attente de synchro (badge topbar + bottom-nav)
    Alpine.store('offlineQueue', {
        pendingCount: 0,
        errorCount: 0,
        async refresh() {
            const { countPendingAll } = await import('./offline-queue.js');
            const counts = await countPendingAll();
            this.pendingCount = counts.pending;
            this.errorCount = counts.errors;
        },
    });

    // PWA update store (initialisé null tant que onNeedRefresh n'a pas été déclenché)
    Alpine.store('pwaUpdate', { available: false, apply: () => {} });

    window.Alpine = Alpine;
    Alpine.start();

    // Refresh initial du badge au boot (lit IDB si présent — sinon stay à 0)
    document.addEventListener('alpine:initialized', () => {
        Alpine.store('offlineQueue').refresh().catch(() => {});
    });

    // Persistance storage (D-57)
    if (navigator.storage?.persist) {
        navigator.storage.persisted().then((persisted) => {
            if (!persisted) navigator.storage.persist();
        });
    }
    ```
    Note : Plan 02-05 ajoutera `Alpine.data('passageForm', passageForm)` après import de `./passage-form.js`. Ne PAS l'ajouter ici — le module n'existe pas encore.

    4. Créer `resources/views/offline.blade.php` (page standalone hors layout admin pour rester ultra-légère et précachable) :
    ```
    <!doctype html>
    <html lang="fr">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Hors ligne · Dlo Azur Piscines</title>
        <meta name="robots" content="noindex,nofollow">
        @vite(['resources/css/app.css'])
    </head>
    <body class="bg-sand-50 text-ink-700 antialiased">
        <main class="min-h-screen flex flex-col items-center justify-center px-6 text-center">
            <div class="text-azure-500 mb-2">
                <x-icon.drop :size="64" />
            </div>
            <h1 class="font-display font-bold text-2xl text-ink-950">Dlo Azur</h1>
            <h2 class="font-display font-semibold text-xl text-ink-950 mt-8">Vous êtes hors ligne</h2>
            <p class="text-ink-500 mt-3 max-w-sm">La saisie d'un passage reste disponible. Les autres pages se chargeront au retour du réseau.</p>
            <a href="/admin/passages/create"
               class="h-13 px-6 rounded-xl bg-azure-500 text-white font-bold mt-6 inline-flex items-center justify-center">
                Retour à la saisie
            </a>
        </main>
    </body>
    </html>
    ```

    5. Ajouter la route /offline dans `routes/web.php` (pas /admin car la page doit être accessible sans auth pour servir de fallback) :
    ```
    Route::view('/offline', 'offline')->name('offline');
    ```
    (Si `routes/web.php` n'existe pas ou est différent, l'ajouter là où les autres routes publiques vivent — vérifier la structure actuelle.)

    6. Créer `tests/Feature/PwaConfigTest.php` selon <behavior>. Les tests qui dépendent de `public/build/sw.js` doivent utiliser `->skip(! File::exists(public_path('build/sw.js')), 'Build prod requis');` pour ne pas casser le local dev. Le CI builda toujours avant tests (workflow Phase 1).
  </action>
  <verify>
    <automated>npm run build && ./vendor/bin/pest tests/Feature/PwaConfigTest.php --ci && php -r "use Illuminate\Support\Facades\Route; require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$kernel = \$app->make(Illuminate\Contracts\Http\Kernel::class); \$response = \$kernel->handle(Illuminate\Http\Request::create('/offline', 'GET')); echo \$response->getStatusCode();"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 sans erreur
    - `test -f resources/js/offline-queue.js && test -f resources/js/photo-pipeline.js` exit 0
    - `grep -c "dloazur-offline-v1" resources/js/offline-queue.js` ≥ 1
    - `grep -c "by-status\\|by-passage\\|by-created" resources/js/offline-queue.js` ≥ 3
    - `grep -c "isHeicByMagicBytes\\|processPhoto" resources/js/photo-pipeline.js` ≥ 2
    - `grep -c "ftyp\\|heic\\|mif1" resources/js/photo-pipeline.js` ≥ 2
    - `grep -c "import('heic2any')" resources/js/photo-pipeline.js` ≥ 1 (lazy import D-43)
    - `grep -c "registerSW" resources/js/app.js` ≥ 1
    - `grep -c "virtual:pwa-register" resources/js/app.js` ≥ 1
    - `grep -c "Alpine.store('offlineQueue'" resources/js/app.js` ≥ 1
    - `grep -c "Vous êtes hors ligne" resources/views/offline.blade.php` ≥ 1
    - `grep -c "Retour à la saisie" resources/views/offline.blade.php` ≥ 1
    - `php artisan route:list --path=offline | grep -c offline` ≥ 1
    - `curl -s http://127.0.0.1:8000/offline 2>&1 | grep -c "Vous êtes hors ligne"` ≥ 1 (si serveur actif via php artisan serve)
    - `./vendor/bin/pest tests/Feature/PwaConfigTest.php --ci` retourne 5/7 verts en local (Tests 1-5 skip si build absent), 7/7 verts en CI (workflow build avant tests)
    - `./vendor/bin/pest --ci` : suite totale verte (aucune régression)
  </acceptance_criteria>
  <done>Modules offline-queue.js (IDB schema D-59) + photo-pipeline.js (HEIC magic bytes + Canvas JPEG 0.80) créés, app.js enregistre le SW et déclare le store Alpine offlineQueue, offline.blade.php accessible via GET /offline (sans auth), 7 tests PwaConfigTest passants en CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Worker scope | SW intercept toutes les routes (Service-Worker-Allowed: /) — peut servir des réponses cachées sans round-trip serveur |
| Photos uploaded via Canvas API | Blobs JPEG générés client-side, contenu non vérifié côté serveur (Plan 02-06 valide mimes:jpeg côté serveur) |
| IndexedDB persistence | Données métier (mesures pH/Cl/photos) stockées dans le navigateur — durabilité dépend de storage.persist() et ITP iOS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | Service Worker contrôle global du fetch | mitigate | `registerType: 'prompt'` (D-56) empêche skipWaiting auto qui couperait la saisie en cours. La logique SW est générée par Workbox (pas custom JS) — audit Workbox suffit. |
| T-4-02 | Information Disclosure | Données IDB visibles dans DevTools | accept | Le navigateur de Pierre est son propre device. Pas de scénario multi-utilisateur partagé. |
| T-4-03 | Tampering | XSS injecté dans une route /admin/* qui lit IDB | mitigate | CSP strict sur les pages admin (hors scope Phase 2 mais à documenter). Layout admin déjà sans innerHTML user-supplied. |
| T-4-04 | Denial of Service | IDB rempli sans limite (queue infinie) | mitigate | Plan 02-05 implémentera une purge auto des items `status='synced'` après upload réussi. Plan 02-04 expose `countPendingAll()` pour monitoring. |
| T-4-05 | Tampering | Photo HEIC malveillante crash le pipeline WASM | mitigate | `heic2any` lazy-import avec try/catch en Plan 02-05. Si conversion échoue → marker l'item `status='error'` et exposer dans le drawer sync. |
| T-4-SC | Tampering | npm install vite-plugin-pwa | accept | Package vérifié RESEARCH §Package Legitimacy Audit (>4M downloads/mois, 3 ans, source github.com/vite-pwa/vite-plugin-pwa). Déjà présent dans package.json Phase 1. |
</threat_model>

<verification>
1. `npm run build` exit 0 — public/build/sw.js + public/build/manifest.webmanifest générés
2. Smoke test SW : `php artisan serve` puis Chrome DevTools → Application → Service Workers — vérifier scope `/` et `passages-queue` dans Background Sync
3. Smoke test offline page : `curl -s http://127.0.0.1:8000/offline | grep -c "Vous êtes hors ligne"` ≥ 1
4. Smoke test IDB : ouvrir DevTools → Application → IndexedDB après visite de /admin (Plan 02-05 alimentera ; ici on vérifie juste que app.js charge sans erreur)
5. `./vendor/bin/pest --ci` : aucune régression
6. Lighthouse PWA audit : score ≥ 80 sur installability (icônes + manifest valides — checkpoint Task 2 valide)
</verification>

<success_criteria>
- [ ] `vite.config.js` configure VitePWA (generateSW + buildBase /build/ + manifest + workbox runtimeCaching)
- [ ] Manifest contient les valeurs UI-SPEC §"PWA Manifest Contract" (name, start_url, theme_color, etc.)
- [ ] Workbox génère navigateFallback /offline avec denylist /admin/, /portail/, /api/
- [ ] Workbox génère BackgroundSyncPlugin pour POST /api/passages et /api/passages/*/photos
- [ ] `resources/js/offline-queue.js` exporte openOfflineDB, upsertPassage, savePhoto, getPassagesByStatus, getPhotosByPassage, markStatus, countPendingAll
- [ ] `resources/js/photo-pipeline.js` exporte isHeicByMagicBytes, processPhoto avec lazy import heic2any + exifr + Canvas JPEG 0.80
- [ ] `resources/js/app.js` enregistre le SW (registerSW), déclare Alpine.store('offlineQueue') et Alpine.store('pwaUpdate'), appelle storage.persist() au boot
- [ ] `resources/views/offline.blade.php` accessible via GET /offline (sans auth), UI-SPEC conforme
- [ ] `resources/views/layouts/admin.blade.php` contient `<meta name="csrf-token">`
- [ ] Icônes PWA générées et validées par checkpoint user (192x192 + 512x512)
- [ ] 7 tests PwaConfigTest passants en CI (5/7 en local sans build)
- [ ] `npm run build` exit 0
- [ ] Aucune régression sur la suite Pest existante
</success_criteria>

<output>
Créer `.planning/phases/02-mvp-suivi-offline-first/02-04-SUMMARY.md` à la fin du plan.
</output>
