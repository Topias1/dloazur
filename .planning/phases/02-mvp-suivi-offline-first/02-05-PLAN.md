---
phase: 02-mvp-suivi-offline-first
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-04", "02-06"]
files_modified:
  - resources/js/app.js
  - resources/views/components/admin/topbar.blade.php
  - resources/views/components/admin/mobile-bottom-nav.blade.php
  - resources/views/components/admin/sidebar.blade.php
  - resources/views/livewire/passage-index.blade.php
  - routes/admin.php
files_created:
  - resources/js/passage-form.js
  - resources/js/sync-drawer.js
  - resources/views/admin/passages/create.blade.php
  - resources/views/components/admin/sync-drawer.blade.php
  - resources/views/components/admin/sync-badge.blade.php
  - resources/views/components/admin/pwa-update-toast.blade.php
  - app/Http/Controllers/Admin/PassageCreateController.php
  - tests/Browser/PassageOfflineFormTest.php
autonomous: false
requirements:
  - PASS-01
  - PASS-02
  - PASS-03
  - PASS-06
user_setup: []
tags: [alpine, indexeddb, offline-first, passage-form, photo-pipeline, sync-drawer]
must_haves:
  truths:
    - "Pierre ouvre /admin/passages/create depuis le topbar bouton 'Nouveau passage' (maintenant actif)"
    - "Au mount du formulaire, un client_uuid est généré via crypto.randomUUID() et persisté immédiatement en IDB store 'passages' avec status='draft' (D-39)"
    - "Pierre saisit les mesures (pH, chlore libre, chlore total, TAC, sel, TH) via des steppers (boutons -/+) — touch targets ≥ 44px (UI-SPEC §Spacing)"
    - "Une valeur hors plage soft (D-63 : pH [5.0,9.0], Cl libre [0,10], Cl total [0,15], TAC [50,300], sel [0,8] g/L, TH [0,50] °f) déclenche un toast warning Alpine non bloquant"
    - "Pierre coche/décoche des actions menées (liste de checkboxes UI-SPEC §Section Actions menées) — l'état est sauvegardé en IDB à chaque change"
    - "Pierre ajoute des photos via <input type='file' accept='image/*' capture='environment'> — caméra arrière par défaut (UI-SPEC §Interaction Contracts Photos)"
    - "Chaque photo passe par processPhoto() de Plan 02-04 : HEIC → JPEG si nécessaire, EXIF correction, Canvas max 2048px JPEG 0.80 — le blob compressé est persisté en IDB store 'photos' avec son propre client_uuid (D-42)"
    - "Le badge sync dans le topbar ('N en attente') et la bottom-nav (badge superposé) se met à jour en temps réel via Alpine.store('offlineQueue').refresh()"
    - "Si navigator.onLine === false, un bandeau hors-ligne ambre s'affiche en haut du formulaire (UI-SPEC §Bandeau hors-ligne)"
    - "Au submit, le passage passe en status='pending' en IDB ; si online, _flushQueue() POST vers /api/passages avec backoff 2s→8s→30s, puis upload des photos une par une vers /api/passages/{client_uuid}/photos (D-45, D-46)"
    - "Au retour réseau (window 'online' event) ou visibilitychange visible, _flushQueue() est appelé automatiquement (fallback iOS Safari pour Background Sync API — Pitfall 1)"
    - "Réponse 409 du serveur (passage déjà clos D-40) → toast 'Ce passage a déjà été clos' + marquer status='synced' en IDB pour purger la queue"
    - "Le sync drawer (panneau glissant depuis bas mobile / droite desktop) liste les passages en queue avec statut individuel (pending/uploading/error) + bouton 'Réessayer' par item + bouton 'Synchroniser maintenant' global (UI-SPEC §Sync drawer)"
    - "Le toast 'Mise à jour disponible' s'affiche quand Alpine.store('pwaUpdate').available === true, avec boutons 'Recharger' (applique updateSW) et 'Plus tard' (UI-SPEC §Mise à jour PWA)"
  artifacts:
    - path: "resources/js/passage-form.js"
      provides: "Alpine.data('passageForm') factory : clientUuid + mesures + actions + photos[] + _saveToIDB + _flushQueue + _uploadPhotos + warnings"
      contains: "crypto.randomUUID\\|_flushQueue"
    - path: "resources/js/sync-drawer.js"
      provides: "Alpine.data('syncDrawer') : liste IDB pending + retry + flush global"
      contains: "offline-queue"
    - path: "resources/views/admin/passages/create.blade.php"
      provides: "Vue saisie passage (Alpine x-data) — pas Livewire"
      contains: "x-data=\"passageForm\""
    - path: "routes/admin.php"
      provides: "Route admin.passages.create avec /admin/passages/create"
      contains: "passages/create"
  key_links:
    - from: "resources/views/admin/passages/create.blade.php"
      to: "resources/js/passage-form.js"
      via: "x-data=\"passageForm\" x-init=\"init()\""
      pattern: "passageForm"
    - from: "resources/js/passage-form.js"
      to: "resources/js/offline-queue.js"
      via: "import { upsertPassage, savePhoto, getPassagesByStatus, markStatus, countPendingAll } from './offline-queue.js'"
      pattern: "from './offline-queue"
    - from: "resources/js/passage-form.js"
      to: "resources/js/photo-pipeline.js"
      via: "import { processPhoto } from './photo-pipeline.js'"
      pattern: "from './photo-pipeline"
    - from: "resources/js/passage-form.js"
      to: "POST /api/passages"
      via: "fetch('/api/passages', { method: 'POST', headers: {X-CSRF-TOKEN, Content-Type, Accept}, body: payload_json })"
      pattern: "fetch.*\\/api\\/passages"
---

<objective>
Livrer le cœur de valeur Phase 2 : la saisie passage offline-first côté pro (PASS-01, PASS-02, PASS-03, PASS-06). Une page `/admin/passages/create` rendue avec `x-data="passageForm"` (Alpine 3 + IndexedDB — **PAS Livewire**, CF-02) qui : (1) génère un `client_uuid` côté client au mount et le persiste immédiatement en IDB (D-39), (2) sauvegarde l'état du formulaire à chaque interaction (draft auto-save implicite via watcher Alpine), (3) gère les photos via `<input capture='environment'>` + `processPhoto()` du Plan 02-04 (HEIC→JPEG, EXIF, Canvas 0.80), (4) au submit, marque `status='pending'` et appelle `_flushQueue()` qui POST vers `/api/passages` (endpoint créé par Plan 02-06) avec backoff 3 retries 2s→8s→30s, puis upload des photos une par une (D-45, D-46), (5) écoute `online`/`visibilitychange` pour relancer la queue à la reconnexion (fallback iOS Safari pour Background Sync — Pitfall 1), (6) affiche un badge "N en attente" temps réel dans le topbar + bottom-nav alimenté par `Alpine.store('offlineQueue')` (PASS-06), (7) propose un sync drawer (panneau glissant) listant les items en queue avec retry manuel.

Purpose : sans ce plan, Phase 2 ne livre pas son cœur. Le UPSERT idempotent de Plan 02-06 n'est pas consommé. Pierre n'a pas d'outil terrain.

Output : passage-form.js + sync-drawer.js + create.blade.php + 3 composants Blade (sync-drawer, sync-badge, pwa-update-toast) + modifications topbar (activation bouton "Nouveau passage" + badge sync) + activation sidebar bouton "Nouveau passage" partout + 1 test browser Pest 4 Playwright (offline form happy path). Le plan a un checkpoint visuel pour valider le mockup `mockups/v1/passage.html` est respecté 1:1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mvp-suivi-offline-first/02-CONTEXT.md
@.planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md
@.planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md
@.planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md
@.planning/phases/02-mvp-suivi-offline-first/02-02-SUMMARY.md
@.planning/phases/02-mvp-suivi-offline-first/02-04-SUMMARY.md
@.planning/phases/02-mvp-suivi-offline-first/02-06-SUMMARY.md
@CLAUDE.md
@DESIGN.md
@PRODUCT.md
@.claude/skills/impeccable/SKILL.md

<interfaces>

From resources/js/offline-queue.js (Plan 02-04) :
- `import { openOfflineDB, upsertPassage, savePhoto, getPassagesByStatus, getPhotosByPassage, markStatus, countPendingAll } from './offline-queue.js'`
- Schéma : passages = { id, client_uuid, payload_json (string), status, attempts, created_at, last_attempt_at }, photos = { id, client_uuid, passage_client_uuid, blob, status, attempts, captured_at }

From resources/js/photo-pipeline.js (Plan 02-04) :
- `import { processPhoto } from './photo-pipeline.js'` — retourne Promise<Blob> (image/jpeg, max 2048px, q=0.80)

From resources/js/app.js (Plan 02-04 état) :
- `Alpine.store('offlineQueue', { pendingCount, errorCount, refresh() })`
- `Alpine.store('pwaUpdate', { available, apply })`
- Phase 02-05 ajoute : `import { passageForm } from './passage-form.js'; import { syncDrawer } from './sync-drawer.js'; Alpine.data('passageForm', passageForm); Alpine.data('syncDrawer', syncDrawer);`

From POST /api/passages (Plan 02-06) :
- Body JSON : `{ client_uuid (uuid string), piscine_id?, client_id?, visited_at? (ISO date), status='draft' implicite, ph_avant?, ph_apres?, chlore_libre?, chlore_total?, tac?, th?, sel_g_l?, actions? (array), notes? }`
- Headers : `X-CSRF-TOKEN: <meta tag>`, `Content-Type: application/json`, `Accept: application/json`, `X-Requested-With: XMLHttpRequest`
- Réponses : 200 `{ ok: true }` (insert ou upsert success) | 409 `{ error: 'already_closed', server_state: {...} }` (passage déjà clos D-40) | 422 (validation error) | 500
- Auth : session cookie `web` (PWA same-domain, cookie envoyé automatiquement par fetch + `credentials: 'same-origin'` par défaut)

From POST /api/passages/{passage_client_uuid}/photos (Plan 02-06) :
- FormData : `photo` (Blob/File JPEG), `client_uuid` (UUID v4 photo, D-42), `captured_at` (ISO date)
- Headers : `X-CSRF-TOKEN`, `Accept: application/json`, X-Requested-With
- NE PAS définir Content-Type pour FormData (laisser le navigateur poser le boundary)
- Réponses : 200 `{ ok: true }` | 422 | 500

From UI-SPEC §"Saisie passage (Alpine + IndexedDB — pas Livewire)" :
- Layout : page entière bg-sand-50 min-h-screen
- Sticky header avec bouton retour + titre client + sous-titre
- Bandeau hors-ligne ambre (x-show="!online")
- Section "Mesures de l'eau" : grille 2 colonnes, cartes mesures (label + stepper -/+), valeur Fredoka 700 text-xl tabular-nums, statut "idéal" text-success ou unité text-ink-400
- Section "Actions menées" : checkboxes avec ring-1 (white/sand-200 par défaut, azure-50/azure-200 cochée)
- Section "Photos" : grille 3 colonnes, miniature aspect-square avec badge statut (Envoyée=success, En attente=warn, Erreur=danger), bouton "Ajouter" (border-dashed azure-300)
- Section "Notes" : textarea "Mot pour le client" + textarea "Note interne (privée)"
- Sticky save-bar : indicateur auto-save + bouton "Enregistrer le passage" (h-13 azure-500)
- Touch targets ≥ 44px partout (steppers h-12 w-11, miniatures min h-20, bouton submit h-13)

From UI-SPEC §"Sync drawer" :
- Mobile : translateY(100%) → translateY(0), animation 250ms ease-out-quint, respect prefers-reduced-motion
- Desktop : translateX(100%) → translateX(0)
- En-tête : "Synchro en attente" + bouton fermer
- Liste items : nom client + date capture + chip statut + bouton Réessayer si error
- Bouton "Synchroniser maintenant" (h-11 w-full azure-500)
- role="dialog" aria-modal="true" + focus trap

From UI-SPEC §"Admin topbar — Badge sync 'N passages en attente'" :
- Bouton h-9 px-3 rounded-xl bg-warn/15 ring-1 ring-warn/30 text-[oklch(0.5_0.11_72)] text-sm font-semibold
- Dot animate-pulse h-2 w-2 rounded-full bg-warn
- Visible uniquement si pendingCount > 0
- Tap → open syncDrawer

From mockups/v1/passage.html — RÉFÉRENCE VISUELLE 1:1 obligatoire pour la disposition des sections, espacements, ordre des champs

From .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 2 (passageForm), §Pattern 3 (flush queue backoff), §Pitfall 1 (iOS fallback), §Pitfall 5 (CSRF token meta)

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Alpine passage-form.js + sync-drawer.js + app.js wiring + route admin.passages.create + browser test offline happy path</name>
  <files>resources/js/passage-form.js, resources/js/sync-drawer.js, resources/js/app.js, app/Http/Controllers/Admin/PassageCreateController.php, routes/admin.php, tests/Browser/PassageOfflineFormTest.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (section "resources/js/passage-form.js", "resources/js/app.js")
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 2 (passageForm complet), §Pattern 3 (flush queue + backoff), §Pitfall 1 (iOS fallback online/visibilitychange), §Pitfall 5 (CSRF meta)
    - resources/js/offline-queue.js (Plan 02-04 — fonctions exportées)
    - resources/js/photo-pipeline.js (Plan 02-04 — processPhoto API)
    - resources/js/app.js (Plan 02-04 — stores Alpine déclarés)
    - tests/Feature/AdminShellTest.php (analog auth test pattern)
    - mockups/v1/passage.html (référence visuelle structure)
  </read_first>
  <behavior>
    **tests/Browser/PassageOfflineFormTest.php** (Pest 4 Browser via Playwright — RESEARCH §Test Framework) :
    - Test 1 : "L'utilisateur authentifié peut atteindre /admin/passages/create" — actingAs($pierre); visit /admin/passages/create; assertSeeText('Mesures de l'eau'); assertSeeText('Actions menées');
    - Test 2 : "Un client_uuid est généré au mount et stocké en IDB" — visit page, evaluate `JSON.stringify(await (await (await openDB('dloazur-offline-v1', 1)).getAll('passages')))` — au moins 1 entrée avec status='draft'
    - Test 3 : "Submit hors-ligne marque le passage 'pending' et déclenche un flush au retour online" — simuler `context.setOffline(true)`, fill mesures, click "Enregistrer", `expect(idb passages where status='pending').toHaveCount(1)` ; `context.setOffline(false)` + déclencher window.dispatchEvent(new Event('online')) ; await idle ; expect status to become 'synced' AND API endpoint POST /api/passages was called (Mock le endpoint pour ce test ou utiliser le vrai si Plan 02-06 dispo en CI).
    - Test 4 : "Le badge sync affiche le nombre de passages pending dans le topbar" — créer 2 entries pending en IDB via evaluate, refresh page, assertSeeText('2 en attente').

    Note : Pest 4 browser tests requiert Playwright installé (`npm i -D playwright` + `npx playwright install chromium`). Si pas disponible en CI, marquer le fichier `->skip(! class_exists('Pest\\Browser\\Browser'), 'Playwright requis');` et faire un test Feature équivalent (HTTP only) pour PASS-01 minimal (vue render).

    **Fallback Feature test (si Playwright indispo)** :
    - tests/Feature/PassageCreateViewTest.php :
      - Test A : "GET /admin/passages/create rendu pour Pierre" — actingAs($pierre)->get('/admin/passages/create')->assertStatus(200)->assertSee('Mesures de l\'eau')->assertSee('Actions menées')->assertSee('Enregistrer le passage')->assertSee('x-data="passageForm"', false);
      - Test B : "Anonyme redirigé vers /login" — get('/admin/passages/create')->assertRedirect('/login')
      - Test C : "La vue importe la meta csrf-token" — assertSee('csrf-token')
  </behavior>
  <action>
    1. Créer `resources/js/passage-form.js` (Alpine.data factory exportée) :
    ```
    import { upsertPassage, savePhoto, getPassagesByStatus, getPhotosByPassage, markStatus, countPendingAll } from './offline-queue.js';
    import { processPhoto } from './photo-pipeline.js';

    const SOFT_RANGES = {
        ph_avant:     [5.0, 9.0],
        chlore_libre: [0, 10],
        chlore_total: [0, 15],
        tac:          [50, 300],
        sel_g_l:      [0, 8],
        th:           [0, 50],
    };
    const RANGE_LABELS = {
        ph_avant: 'pH', chlore_libre: 'Chlore libre', chlore_total: 'Chlore total',
        tac: 'TAC', sel_g_l: 'Sel', th: 'TH',
    };

    export function passageForm(initialData = {}) {
        return {
            // identité
            clientUuid: '',
            idbId: null,
            clientId: initialData.clientId ?? null,
            piscineId: initialData.piscineId ?? null,
            // mesures (chaînes pour wire avec input number — converties au submit)
            ph_avant: '',
            ph_apres: '',
            chlore_libre: '',
            chlore_total: '',
            tac: '',
            sel_g_l: '',
            th: '',
            // actions menées (array de strings)
            actions: [],
            actionsAvailable: ['Mesure pH', 'Mesure chlore', 'Nettoyage skimmers', 'Brossage parois', 'Aspirateur fond', 'Lavage filtre', 'Ajout chlore', 'Ajout pH-', 'Vidange partielle', 'Vérification équipements'],
            // notes
            notes: '',
            notesPrivees: '',
            // photos (array de { clientUuid, blob, previewUrl, status, idbId })
            photos: [],
            // état UI
            online: navigator.onLine,
            warnings: [],
            saving: false,
            conflictMsg: '',
            visitedAt: new Date().toISOString(),

            async init() {
                // 1. Générer client_uuid et persister en IDB (D-39)
                this.clientUuid = crypto.randomUUID();
                this.idbId = await this._saveToIDB('draft');

                // 2. Listeners réseau (Pitfall 1 — fallback iOS Background Sync)
                window.addEventListener('online',  () => { this.online = true;  this._flushQueue(); });
                window.addEventListener('offline', () => { this.online = false; });
                document.addEventListener('visibilitychange', () => {
                    if (document.visibilityState === 'visible' && navigator.onLine) {
                        this._flushQueue();
                    }
                });

                // 3. storage.persist() (D-57) — déjà appelé dans app.js, no-op si déjà actif
                // 4. Watcher mesures : valider hors plage + auto-save draft
                this.$watch('ph_avant',     () => { this._checkRange('ph_avant',     this.ph_avant);     this._debouncedSave(); });
                this.$watch('chlore_libre', () => { this._checkRange('chlore_libre', this.chlore_libre); this._debouncedSave(); });
                this.$watch('chlore_total', () => { this._checkRange('chlore_total', this.chlore_total); this._debouncedSave(); });
                this.$watch('tac',          () => { this._checkRange('tac',          this.tac);          this._debouncedSave(); });
                this.$watch('sel_g_l',      () => { this._checkRange('sel_g_l',      this.sel_g_l);      this._debouncedSave(); });
                this.$watch('th',           () => { this._checkRange('th',           this.th);           this._debouncedSave(); });
                this.$watch('actions',      () => this._debouncedSave());
                this.$watch('notes',        () => this._debouncedSave());
                this.$watch('notesPrivees', () => this._debouncedSave());

                // 5. Refresh badge initial
                this.$store.offlineQueue.refresh().catch(() => {});
            },

            // ------- Mesures helpers (steppers) -------
            incr(field, step = 0.1, precision = 1) {
                const v = parseFloat(this[field]) || 0;
                this[field] = (v + step).toFixed(precision);
            },
            decr(field, step = 0.1, precision = 1) {
                const v = parseFloat(this[field]) || 0;
                const next = Math.max(0, v - step);
                this[field] = next.toFixed(precision);
            },

            _checkRange(field, val) {
                const num = parseFloat(val);
                if (isNaN(num)) return;
                const [min, max] = SOFT_RANGES[field] ?? [];
                if (min !== undefined && (num < min || num > max)) {
                    const label = RANGE_LABELS[field] ?? field;
                    this._pushWarning(`${label} ${num} est hors de la plage [${min}, ${max}]. La saisie est enregistrée — vérifiez votre lecture.`);
                }
            },

            _pushWarning(msg) {
                const id = Date.now() + Math.random();
                this.warnings.push({ id, msg });
                setTimeout(() => {
                    this.warnings = this.warnings.filter((w) => w.id !== id);
                }, 4000);
            },

            // ------- Actions checkbox helpers -------
            toggleAction(name) {
                if (this.actions.includes(name)) {
                    this.actions = this.actions.filter((a) => a !== name);
                } else {
                    this.actions = [...this.actions, name];
                }
            },
            isActionSelected(name) {
                return this.actions.includes(name);
            },

            // ------- Photos -------
            async onPhotoSelect(event) {
                const files = Array.from(event.target.files ?? []);
                event.target.value = ''; // reset input pour permettre re-upload du même fichier
                for (const file of files) {
                    const photoUuid = crypto.randomUUID();
                    const tempPreview = URL.createObjectURL(file);
                    const photoEntry = {
                        clientUuid: photoUuid,
                        passage_client_uuid: this.clientUuid,
                        previewUrl: tempPreview,
                        status: 'processing',
                        idbId: null,
                    };
                    this.photos.push(photoEntry);
                    try {
                        const blob = await processPhoto(file);
                        const idbId = await savePhoto({
                            client_uuid: photoUuid,
                            passage_client_uuid: this.clientUuid,
                            blob,
                            status: 'pending',
                            attempts: 0,
                            captured_at: new Date().toISOString(),
                        });
                        photoEntry.idbId = idbId;
                        photoEntry.status = 'pending';
                        // Remplacer la preview par le blob compressé pour économiser la RAM
                        URL.revokeObjectURL(tempPreview);
                        photoEntry.previewUrl = URL.createObjectURL(blob);
                    } catch (e) {
                        console.error('[passage-form] photo processing failed', e);
                        photoEntry.status = 'error';
                    }
                }
            },

            // ------- IDB save -------
            _debouncedSave: (() => {
                let timer = null;
                return function () {
                    clearTimeout(timer);
                    timer = setTimeout(() => this._saveToIDB('draft'), 500);
                };
            })(),

            async _saveToIDB(status = 'draft') {
                const payload = this._toPayload();
                const record = {
                    id: this.idbId ?? undefined,
                    client_uuid: this.clientUuid,
                    payload_json: JSON.stringify(payload),
                    status,
                    attempts: 0,
                    created_at: payload.visited_at,
                    last_attempt_at: null,
                };
                const id = await upsertPassage(record);
                if (!this.idbId) this.idbId = id;
                await this.$store.offlineQueue.refresh();
                return id;
            },

            _toPayload() {
                return {
                    client_uuid: this.clientUuid,
                    client_id: this.clientId,
                    piscine_id: this.piscineId,
                    visited_at: this.visitedAt,
                    ph_avant:     this.ph_avant === '' ? null : parseFloat(this.ph_avant),
                    ph_apres:     this.ph_apres === '' ? null : parseFloat(this.ph_apres),
                    chlore_libre: this.chlore_libre === '' ? null : parseFloat(this.chlore_libre),
                    chlore_total: this.chlore_total === '' ? null : parseFloat(this.chlore_total),
                    tac:          this.tac === '' ? null : parseFloat(this.tac),
                    sel_g_l:      this.sel_g_l === '' ? null : parseFloat(this.sel_g_l),
                    th:           this.th === '' ? null : parseFloat(this.th),
                    actions:      this.actions,
                    notes:        this.notes || null,
                    notes_privees: this.notesPrivees || null,
                };
            },

            // ------- Submit + flush queue -------
            async submit() {
                this.saving = true;
                await this._saveToIDB('pending');
                if (navigator.onLine) {
                    await this._flushQueue();
                }
                this.saving = false;
            },

            async _flushQueue() {
                const pending = await getPassagesByStatus('pending');
                for (const item of pending) {
                    await this._uploadPassage(item);
                }
                await this.$store.offlineQueue.refresh();
            },

            async _uploadPassage(item) {
                const delays = [2000, 8000, 30000];
                for (let attempt = 0; attempt < delays.length; attempt++) {
                    try {
                        await markStatus('passages', item.id, 'uploading');
                        const res = await fetch('/api/passages', {
                            method: 'POST',
                            headers: this._headers(true),
                            credentials: 'same-origin',
                            body: item.payload_json,
                        });

                        if (res.status === 409) {
                            // Passage déjà clos (D-40)
                            await markStatus('passages', item.id, 'synced');
                            this.conflictMsg = "Ce passage a déjà été clos. Tes modifications n'ont pas été enregistrées.";
                            return;
                        }
                        if (res.ok) {
                            await markStatus('passages', item.id, 'synced');
                            await this._uploadPhotosForPassage(item.client_uuid);
                            return;
                        }
                        throw new Error('Server error ' + res.status);
                    } catch (e) {
                        console.warn('[passage-form] upload attempt', attempt + 1, 'failed', e);
                        if (attempt < delays.length - 1) {
                            await new Promise((r) => setTimeout(r, delays[attempt]));
                        } else {
                            await markStatus('passages', item.id, 'error', (item.attempts || 0) + 1);
                        }
                    }
                }
            },

            async _uploadPhotosForPassage(passageClientUuid) {
                const photos = await getPhotosByPassage(passageClientUuid);
                for (const photo of photos.filter((p) => p.status !== 'synced')) {
                    await this._uploadPhoto(photo);
                }
            },

            async _uploadPhoto(photo) {
                const delays = [2000, 8000, 30000];
                for (let attempt = 0; attempt < delays.length; attempt++) {
                    try {
                        await markStatus('photos', photo.id, 'uploading');
                        const fd = new FormData();
                        fd.append('photo', photo.blob, `${photo.client_uuid}.jpg`);
                        fd.append('client_uuid', photo.client_uuid);
                        fd.append('captured_at', photo.captured_at);
                        const res = await fetch(`/api/passages/${photo.passage_client_uuid}/photos`, {
                            method: 'POST',
                            headers: this._headers(false),
                            credentials: 'same-origin',
                            body: fd,
                        });
                        if (res.ok) {
                            await markStatus('photos', photo.id, 'synced');
                            // Mettre à jour le statut dans le tableau Alpine.photos si présent
                            const local = this.photos.find((p) => p.clientUuid === photo.client_uuid);
                            if (local) local.status = 'synced';
                            return;
                        }
                        throw new Error('Photo upload failed ' + res.status);
                    } catch (e) {
                        console.warn('[passage-form] photo upload attempt', attempt + 1, 'failed', e);
                        if (attempt < delays.length - 1) {
                            await new Promise((r) => setTimeout(r, delays[attempt]));
                        } else {
                            await markStatus('photos', photo.id, 'error', (photo.attempts || 0) + 1);
                            const local = this.photos.find((p) => p.clientUuid === photo.client_uuid);
                            if (local) local.status = 'error';
                        }
                    }
                }
            },

            _headers(isJson) {
                const csrf = document.querySelector('meta[name="csrf-token"]')?.content ?? '';
                const h = {
                    'X-CSRF-TOKEN': csrf,
                    'Accept': 'application/json',
                    'X-Requested-With': 'XMLHttpRequest',
                };
                if (isJson) h['Content-Type'] = 'application/json';
                return h;
            },

            dismissConflict() {
                this.conflictMsg = '';
            },
        };
    }
    ```

    2. Créer `resources/js/sync-drawer.js` :
    ```
    import { openOfflineDB, getPassagesByStatus, markStatus, countPendingAll } from './offline-queue.js';

    export function syncDrawer() {
        return {
            open: false,
            items: [],  // { id, client_uuid, status, payload, attempts }
            loading: false,

            async toggle() {
                this.open = !this.open;
                if (this.open) await this.refresh();
            },

            async refresh() {
                this.loading = true;
                const db = await openOfflineDB();
                const all = await db.getAll('passages');
                this.items = all
                    .filter((it) => it.status !== 'synced')
                    .map((it) => {
                        let p = {};
                        try { p = JSON.parse(it.payload_json); } catch {}
                        return {
                            id: it.id,
                            client_uuid: it.client_uuid,
                            status: it.status,
                            attempts: it.attempts || 0,
                            visited_at: p.visited_at,
                            actionsCount: (p.actions || []).length,
                        };
                    });
                this.loading = false;
            },

            async retry(id) {
                await markStatus('passages', id, 'pending');
                await this.refresh();
                window.dispatchEvent(new CustomEvent('passage-form:flush'));
            },

            async flushAll() {
                // Force tous les pending/error → pending puis dispatch flush
                const db = await openOfflineDB();
                const all = await db.getAll('passages');
                for (const it of all) {
                    if (it.status === 'error') await markStatus('passages', it.id, 'pending');
                }
                await this.refresh();
                window.dispatchEvent(new CustomEvent('passage-form:flush'));
            },

            statusLabel(status) {
                return { pending: 'En attente', uploading: 'En cours', error: 'Erreur' }[status] ?? status;
            },
            statusClass(status) {
                return {
                    pending:   'bg-warn/15 text-[oklch(0.5_0.11_72)]',
                    uploading: 'bg-azure-50 text-azure-700',
                    error:     'bg-danger/10 text-danger',
                }[status] ?? '';
            },
        };
    }
    ```

    3. Modifier `resources/js/app.js` pour enregistrer les 2 Alpine.data + écouter l'event custom 'passage-form:flush' :
    Ajouter après les imports SW :
    ```
    import { passageForm } from './passage-form.js';
    import { syncDrawer } from './sync-drawer.js';
    Alpine.data('passageForm', passageForm);
    Alpine.data('syncDrawer', syncDrawer);

    // Custom event pour relancer le flush depuis le sync-drawer
    window.addEventListener('passage-form:flush', async () => {
        const { getPassagesByStatus } = await import('./offline-queue.js');
        const pending = await getPassagesByStatus('pending');
        if (pending.length === 0) return;
        // Le composant passageForm pourrait être démonté — on recrée juste un mini-flush ici
        // ou on dispatch un re-render. Pour MVP : laisser le passageForm s'en occuper
        // (s'il est monté sur la page courante), sinon refresh global.
        if (window.Alpine?.store('offlineQueue')) {
            window.Alpine.store('offlineQueue').refresh();
        }
    });
    ```

    4. Ajouter la route admin.passages.create dans `routes/admin.php` :
    ```
    Route::view('passages/create', 'admin.passages.create')->name('passages.create');
    ```
    Note : la vue est statique côté serveur (rendue par Blade au premier load, puis Alpine prend le relais). Aucune logique controller — c'est volontairement minimal car 100% du travail est côté client.

    OU créer un controller fin `App\Http\Controllers\Admin\PassageCreateController` qui calcule les valeurs initiales (client_id depuis query string `?client_id=X` si Pierre lance la saisie depuis la fiche client). Préférer le controller fin pour le query binding :
    ```
    public function create(Request $request): \Illuminate\View\View
    {
        $client = $request->filled('client_id') ? Client::find($request->integer('client_id')) : null;
        $piscine = $client?->piscines->first(); // D-64 auto-pick si 1 piscine
        return view('admin.passages.create', compact('client', 'piscine'));
    }
    ```
    Route :
    ```
    Route::get('passages/create', [PassageCreateController::class, 'create'])->name('passages.create');
    ```

    5. Créer `tests/Browser/PassageOfflineFormTest.php` (Pest 4 Browser) OU `tests/Feature/PassageCreateViewTest.php` (fallback) selon disponibilité de Playwright. Vérifier `vendor/bin/pest --help | grep -i browser` ou `composer show pestphp/pest-plugin-browser` pour décider.

    Recommandation : commencer par le Feature test (assertSeeText + assertSee 'x-data="passageForm"') pour ne pas bloquer ; ajouter le Browser test en TODO Plan ultérieur de QA mobile.
  </action>
  <verify>
    <automated>npm run build && ./vendor/bin/pest tests/Feature/PassageCreateViewTest.php --ci</automated>
  </verify>
  <acceptance_criteria>
    - 6 fichiers créés/modifiés sur le scope Task 1
    - `grep -c "crypto.randomUUID" resources/js/passage-form.js` ≥ 1 (D-39)
    - `grep -c "addEventListener('online'" resources/js/passage-form.js` ≥ 1 (Pitfall 1)
    - `grep -c "visibilitychange" resources/js/passage-form.js` ≥ 1
    - `grep -c "from './offline-queue" resources/js/passage-form.js` ≥ 1
    - `grep -c "from './photo-pipeline" resources/js/passage-form.js` ≥ 1
    - `grep -c "fetch.*'/api/passages'" resources/js/passage-form.js` ≥ 1
    - `grep -c "X-CSRF-TOKEN" resources/js/passage-form.js` ≥ 1
    - `grep -c "409" resources/js/passage-form.js` ≥ 1 (gestion conflit D-40)
    - `grep -c "Alpine.data('passageForm'" resources/js/app.js` ≥ 1
    - `grep -c "Alpine.data('syncDrawer'" resources/js/app.js` ≥ 1
    - `php artisan route:list --path=admin/passages/create | grep -c "passages.create"` ≥ 1
    - `./vendor/bin/pest tests/Feature/PassageCreateViewTest.php --ci` 3/3 verts (Feature fallback) OU `./vendor/bin/pest tests/Browser --ci` verts
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>Module passage-form.js + sync-drawer.js opérationnels, route admin.passages.create active, app.js enregistre les 2 Alpine.data, tests minimum passants.</done>
</task>

<task type="auto">
  <name>Task 2 : Vue admin/passages/create.blade.php (Alpine x-data, mockup 1:1) + composants sync-drawer/sync-badge/pwa-update-toast + activation topbar/sidebar/bottom-nav</name>
  <files>resources/views/admin/passages/create.blade.php, resources/views/components/admin/sync-drawer.blade.php, resources/views/components/admin/sync-badge.blade.php, resources/views/components/admin/pwa-update-toast.blade.php, resources/views/components/admin/topbar.blade.php, resources/views/components/admin/mobile-bottom-nav.blade.php, resources/views/components/admin/sidebar.blade.php, resources/views/livewire/passage-index.blade.php</files>
  <read_first>
    - mockups/v1/passage.html (RÉFÉRENCE 1:1 — structure, ordre des sections, classes)
    - .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md (§"Saisie passage", §"Sync drawer", §"Admin topbar — Badge sync", §"Sticky save-bar", §"Section Mesures de l'eau", §"Section Actions menées", §"Section Photos", §"Section Notes", §"Toast warnings mesures (soft validation)", §"Toast erreur synchro 409", §"Mise à jour PWA (Update prompt)", §"Page Structure" Vue saisie passage)
    - resources/views/admin/dashboard.blade.php (analog @extends layouts.admin + @section main)
    - resources/views/components/admin/topbar.blade.php (état après 02-02 — bouton Nouveau passage encore disabled)
    - resources/views/components/admin/sidebar.blade.php (état après 02-03 — Passages déjà actif comme item liste, mais pas de bouton "Nouveau passage" sticky bottom)
    - resources/views/livewire/passage-index.blade.php (état après 02-03 — bouton "Nouveau passage" disabled dans le header)
    - DESIGN.md (Fredoka 700 sur valeurs mesures, tabular-nums, OKLCH ambre pour offline)
    - .claude/skills/impeccable/SKILL.md (règles : touch targets 44px, active:scale-95 sur steppers exception)
  </read_first>
  <action>
    **A. Vue principale resources/views/admin/passages/create.blade.php** (transposition 1:1 de mockups/v1/passage.html avec Alpine bindings)

    Structure :
    ```
    @extends('layouts.admin')

    @section('title', 'Nouveau passage · Dlo Azur')

    @section('sidebar')
        <x-admin.sidebar :user="auth()->user()" />
    @endsection

    @section('topbar')
        <x-admin.topbar />
    @endsection

    @section('main')
        <div x-data="passageForm({ clientId: {{ $client?->id ?? 'null' }}, piscineId: {{ $piscine?->id ?? 'null' }} })" x-init="init()">

            {{-- Sticky header --}}
            <header class="sticky top-0 z-20 bg-sand-50/95 backdrop-blur border-b border-sand-200 px-4 pt-9 pb-3 flex items-center gap-3">
                <a href="{{ url()->previous() }}" class="h-10 w-10 rounded-xl bg-white ring-1 ring-sand-200 text-navy-700 grid place-items-center active:scale-95" aria-label="Retour">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m15 18-6-6 6-6"/></svg>
                </a>
                <div class="flex-1 min-w-0">
                    <p class="font-display font-semibold text-lg text-ink-950 truncate">{{ $client?->name ?? 'Nouveau passage' }}</p>
                    <p class="text-sm text-ink-500 truncate">{{ $piscine?->volume_m3 ? $piscine->volume_m3.' m³ · ' : '' }}{{ now()->isoFormat('dddd D MMMM') }}</p>
                </div>
            </header>

            <main class="px-4 pb-32 space-y-6 pt-4">

                {{-- Bandeau hors-ligne (Alpine) --}}
                <div x-show="!online" x-transition.opacity.duration.150ms role="status" aria-live="assertive"
                     class="flex items-start gap-3 rounded-2xl bg-warn-bg ring-1 ring-[oklch(0.85_0.09_82)] p-3.5">
                    <span class="h-9 w-9 shrink-0 rounded-full bg-warn/25 text-[oklch(0.5_0.11_72)] grid place-items-center">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M1 1l22 22M16.72 11.06A10.94 10.94 0 0 1 19 12.55M5 12.55a10.94 10.94 0 0 1 5.17-2.39M10.71 5.05A16 16 0 0 1 22.58 9M1.42 9a15.91 15.91 0 0 1 4.7-2.88M8.53 16.11a6 6 0 0 1 6.95 0M12 20h.01"/></svg>
                    </span>
                    <div>
                        <p class="font-semibold text-[oklch(0.42_0.10_70)] text-sm">Hors ligne · ta saisie est sauvegardée</p>
                        <p class="text-xs text-[oklch(0.5_0.08_72)] leading-snug">Elle partira automatiquement au retour du réseau.</p>
                    </div>
                </div>

                {{-- Section Mesures de l'eau --}}
                <section>
                    <h2 class="font-display font-semibold text-lg text-ink-950 mb-3">Mesures de l'eau</h2>
                    <div class="grid grid-cols-2 gap-3">
                        @foreach ([
                            ['field' => 'ph_avant',    'label' => 'pH',          'step' => 0.1, 'precision' => 1, 'unit' => ''],
                            ['field' => 'chlore_libre','label' => 'Chlore libre','step' => 0.1, 'precision' => 1, 'unit' => 'mg/L'],
                            ['field' => 'chlore_total','label' => 'Chlore total','step' => 0.1, 'precision' => 1, 'unit' => 'mg/L'],
                            ['field' => 'tac',         'label' => 'TAC',         'step' => 5,   'precision' => 0, 'unit' => 'mg/L'],
                            ['field' => 'sel_g_l',     'label' => 'Sel',         'step' => 0.1, 'precision' => 1, 'unit' => 'g/L'],
                            ['field' => 'th',          'label' => 'TH',          'step' => 1,   'precision' => 0, 'unit' => '°f'],
                        ] as $m)
                            @php($f = $m['field'])
                            <div class="rounded-2xl bg-white ring-1 ring-sand-200 p-3">
                                <div class="flex items-center justify-between">
                                    <span class="text-sm font-semibold text-ink-900">{{ $m['label'] }}</span>
                                    <span class="text-xs text-ink-400">{{ $m['unit'] }}</span>
                                </div>
                                <div class="flex items-stretch gap-1.5 mt-2">
                                    <button type="button" @click="decr('{{ $f }}', {{ $m['step'] }}, {{ $m['precision'] }})"
                                            class="w-11 h-12 rounded-xl bg-sand-100 ring-1 ring-sand-200 text-ink-700 active:scale-95 grid place-items-center"
                                            aria-label="Diminuer {{ $m['label'] }}">−</button>
                                    <div class="flex-1 h-12 rounded-xl bg-sand-50 grid place-items-center">
                                        <span x-text="{{ $f }} || '·'" class="font-display font-bold text-xl text-ink-950 tabular-nums"></span>
                                    </div>
                                    <button type="button" @click="incr('{{ $f }}', {{ $m['step'] }}, {{ $m['precision'] }})"
                                            class="w-11 h-12 rounded-xl bg-azure-500 text-white active:scale-95 grid place-items-center"
                                            aria-label="Augmenter {{ $m['label'] }}">+</button>
                                </div>
                            </div>
                        @endforeach
                    </div>
                </section>

                {{-- Section Actions menées --}}
                <section>
                    <h2 class="font-display font-semibold text-lg text-ink-950 mb-3">Actions menées</h2>
                    <div class="space-y-2">
                        <template x-for="action in actionsAvailable" :key="action">
                            <label class="flex items-center gap-3 h-12 px-3.5 rounded-xl cursor-pointer transition-colors"
                                   :class="isActionSelected(action) ? 'bg-azure-50 ring-1 ring-azure-200' : 'bg-white ring-1 ring-sand-200'">
                                <span class="h-6 w-6 rounded-md grid place-items-center"
                                      :class="isActionSelected(action) ? 'bg-azure-500 text-white' : 'bg-white ring-1 ring-sand-300'">
                                    <template x-if="isActionSelected(action)">
                                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
                                    </template>
                                </span>
                                <input type="checkbox" class="sr-only" :checked="isActionSelected(action)" @change="toggleAction(action)" :aria-label="action">
                                <span class="font-medium" :class="isActionSelected(action) ? 'text-ink-900' : 'text-ink-700'" x-text="action"></span>
                            </label>
                        </template>
                    </div>
                </section>

                {{-- Section Photos --}}
                <section>
                    <h2 class="font-display font-semibold text-lg text-ink-950 mb-3">Photos</h2>
                    <div class="grid grid-cols-3 gap-2.5">
                        <template x-for="photo in photos" :key="photo.clientUuid">
                            <div class="relative aspect-square rounded-xl overflow-hidden ring-1 ring-sand-200 bg-sand-100">
                                <img :src="photo.previewUrl" :alt="`Photo passage · ${visitedAt}`" class="w-full h-full object-cover" loading="lazy">
                                <span class="absolute bottom-1 left-1 rounded-md text-[10px] font-bold px-1.5 py-0.5"
                                      :class="{
                                          'bg-success/90 text-white':  photo.status === 'synced',
                                          'bg-warn text-[oklch(0.32_0.09_70)]': photo.status === 'pending' || photo.status === 'uploading',
                                          'bg-danger text-white':      photo.status === 'error',
                                      }"
                                      x-text="{ pending:'En attente', uploading:'En cours', synced:'Envoyée', error:'Erreur', processing:'Traitement…' }[photo.status]"></span>
                            </div>
                        </template>
                        <label class="aspect-square rounded-xl border-2 border-dashed border-azure-300 bg-azure-50/50 grid place-items-center text-azure-600 active:scale-95 cursor-pointer">
                            <input type="file" accept="image/*" capture="environment" multiple class="sr-only" @change="onPhotoSelect($event)">
                            <div class="flex flex-col items-center gap-1">
                                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
                                <span class="text-xs font-semibold">Ajouter</span>
                            </div>
                        </label>
                    </div>
                </section>

                {{-- Section Notes --}}
                <section class="space-y-4">
                    <div>
                        <label for="notes" class="block text-sm font-semibold text-ink-900 mb-1.5">Mot pour le client</label>
                        <textarea id="notes" x-model="notes" rows="3"
                                  placeholder="Eau parfaitement équilibrée, rien à signaler."
                                  class="w-full rounded-xl bg-white ring-1 ring-sand-200 p-3 text-ink-900 placeholder:text-ink-400 focus:ring-2 focus:ring-azure-500 outline-none resize-none"></textarea>
                    </div>
                    <div>
                        <label for="notes-privees" class="block text-sm font-semibold text-ink-900 mb-1.5">
                            Note interne <span class="text-ink-400 font-normal">(privée, non visible par le client)</span>
                        </label>
                        <textarea id="notes-privees" x-model="notesPrivees" rows="3"
                                  placeholder="Prévoir cartouche de filtration au prochain passage."
                                  aria-describedby="notes-privees-desc"
                                  class="w-full rounded-xl bg-white ring-1 ring-sand-200 p-3 text-ink-900 placeholder:text-ink-400 focus:ring-2 focus:ring-azure-500 outline-none resize-none"></textarea>
                    </div>
                </section>

            </main>

            {{-- Sticky save-bar --}}
            <div class="sticky bottom-0 bg-sand-50/95 backdrop-blur border-t border-sand-200 px-4 py-3 pb-[calc(0.75rem+env(safe-area-inset-bottom))] space-y-2">
                <div class="text-xs text-ink-500 flex items-center gap-1.5">
                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="text-lagon-600" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
                    Brouillon sauvegardé automatiquement
                </div>
                <button type="button" @click="submit" :disabled="saving"
                        class="w-full h-13 rounded-xl bg-azure-500 text-white font-bold text-base shadow-md hover:bg-azure-600 active:scale-[0.99] transition-colors disabled:opacity-60 disabled:cursor-not-allowed">
                    <span x-show="!saving">Enregistrer le passage</span>
                    <span x-show="saving">Enregistrement…</span>
                </button>
            </div>

            {{-- Toasts warnings mesures (soft validation) --}}
            <div class="fixed bottom-20 left-4 right-4 z-50 space-y-2" aria-live="polite">
                <template x-for="warning in warnings" :key="warning.id">
                    <div class="rounded-2xl bg-warn-bg ring-1 ring-warn/40 px-4 py-3 flex items-center gap-2.5 shadow-md">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="text-[oklch(0.5_0.11_72)] shrink-0" aria-hidden="true"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
                        <p class="text-sm font-semibold text-[oklch(0.42_0.10_70)]" x-text="warning.msg"></p>
                    </div>
                </template>
            </div>

            {{-- Toast erreur 409 (passage déjà clos) --}}
            <div x-show="conflictMsg" class="fixed bottom-20 left-4 right-4 z-50">
                <div class="rounded-2xl bg-danger/10 ring-1 ring-danger/30 px-4 py-3 flex items-start gap-2.5 shadow-md">
                    <p class="text-sm font-semibold text-danger flex-1" x-text="conflictMsg"></p>
                    <button type="button" @click="dismissConflict" class="text-danger" aria-label="Fermer">✕</button>
                </div>
            </div>

            <x-admin.mobile-bottom-nav />
            <x-admin.sync-drawer />
            <x-admin.pwa-update-toast />
        </div>
    @endsection
    ```

    Remarque locale française : `now()->isoFormat('dddd D MMMM')` requiert que `Carbon::setLocale('fr')` soit appliqué globalement (à vérifier ; sinon `now()->translatedFormat('l j F')` ou format simple `now()->format('d/m/Y')`).

    **B. Composant resources/views/components/admin/sync-drawer.blade.php** :
    ```
    <div x-data="syncDrawer()" x-cloak>
        {{-- Backdrop --}}
        <div x-show="open" x-transition.opacity @click="open = false" class="fixed inset-0 z-40 bg-navy-950/40"></div>

        {{-- Drawer --}}
        <div x-show="open" x-transition:enter="transition ease-out duration-250" x-transition:enter-start="translate-y-full sm:translate-x-full sm:translate-y-0" x-transition:enter-end="translate-y-0 sm:translate-x-0" x-transition:leave="transition ease-in duration-150" x-transition:leave-start="translate-y-0 sm:translate-x-0" x-transition:leave-end="translate-y-full sm:translate-x-full sm:translate-y-0"
             role="dialog" aria-modal="true" aria-label="Passages en attente de synchronisation"
             class="fixed bottom-0 inset-x-0 sm:inset-x-auto sm:right-0 sm:top-0 sm:bottom-0 sm:w-[420px] z-50 bg-white rounded-t-3xl sm:rounded-none sm:rounded-l-3xl shadow-2xl flex flex-col">
            <header class="flex items-center justify-between p-4 border-b border-sand-200">
                <h2 class="font-display font-semibold text-lg text-ink-950">Synchro en attente</h2>
                <button type="button" @click="open = false" class="h-10 w-10 rounded-xl hover:bg-sand-100 grid place-items-center" aria-label="Fermer">✕</button>
            </header>
            <ul class="flex-1 overflow-y-auto p-4 space-y-2">
                <template x-for="item in items" :key="item.id">
                    <li class="flex items-center gap-3 rounded-2xl bg-sand-50 ring-1 ring-sand-200 p-3">
                        <div class="flex-1 min-w-0">
                            <p class="text-sm font-semibold text-ink-900 truncate" x-text="item.visited_at"></p>
                            <p class="text-xs text-ink-500" x-text="`${item.actionsCount} action(s) · tentatives ${item.attempts}`"></p>
                        </div>
                        <span class="rounded-full text-xs font-semibold px-2.5 py-0.5" :class="statusClass(item.status)" x-text="statusLabel(item.status)"></span>
                        <template x-if="item.status === 'error'">
                            <button type="button" @click="retry(item.id)" class="h-8 px-3 rounded-xl bg-azure-500 text-white text-sm font-semibold">Réessayer</button>
                        </template>
                    </li>
                </template>
                <template x-if="items.length === 0 && !loading">
                    <li class="text-center text-ink-500 text-sm py-8">Tout est synchronisé.</li>
                </template>
            </ul>
            <div class="p-4 border-t border-sand-200">
                <button type="button" @click="flushAll" class="h-11 w-full rounded-xl bg-azure-500 text-white font-semibold hover:bg-azure-600 transition-colors">Synchroniser maintenant</button>
            </div>
        </div>
    </div>
    ```

    **C. Composant resources/views/components/admin/sync-badge.blade.php** :
    ```
    <div x-data="{ openDrawer() { Alpine.evaluate(document.body, '$dispatch(\\'sync-drawer:open\\')'); } }" aria-live="polite">
        <template x-if="$store.offlineQueue.pendingCount > 0">
            <button type="button" @click="$dispatch('sync-drawer:open')"
                    class="flex items-center gap-1.5 h-9 px-3 rounded-xl bg-warn/15 ring-1 ring-warn/30 text-[oklch(0.5_0.11_72)] text-sm font-semibold">
                <span class="h-2 w-2 rounded-full bg-warn animate-pulse"></span>
                <span x-text="`${$store.offlineQueue.pendingCount} en attente`"></span>
            </button>
        </template>
        <template x-if="$store.offlineQueue.errorCount > 0">
            <button type="button" @click="$dispatch('sync-drawer:open')"
                    class="flex items-center gap-1.5 h-9 px-3 rounded-xl bg-danger/10 ring-1 ring-danger/30 text-danger text-sm font-semibold ml-2">
                <span class="h-2 w-2 rounded-full bg-danger"></span>
                <span x-text="`${$store.offlineQueue.errorCount} erreur(s) · Réessayer`"></span>
            </button>
        </template>
    </div>
    ```
    Note : le sync-drawer écoute `window.addEventListener('sync-drawer:open', ...)` OU on s'appuie sur Alpine `$dispatch` qui bubble. Adapter sync-drawer.js : ajouter `init()` qui écoute `window.addEventListener('sync-drawer:open', () => this.toggle())`. Alternativement, externaliser `open` dans un `Alpine.store('syncDrawer', { open: false, openDrawer() {…} })`. Implémentation choisie : Alpine store (plus simple) :
    - Remplacer `Alpine.data('syncDrawer', syncDrawer)` par `Alpine.store('syncDrawer', syncDrawerStore())` dans `app.js`
    - Le composant sync-drawer.blade.php utilise `$store.syncDrawer.open` au lieu de `open` local
    - sync-badge.blade.php fait `@click="$store.syncDrawer.open = true; $store.syncDrawer.refresh && $store.syncDrawer.refresh()"`

    **D. Composant resources/views/components/admin/pwa-update-toast.blade.php** :
    ```
    <div x-data x-show="$store.pwaUpdate.available" x-cloak
         class="fixed top-4 left-4 right-4 z-50 max-w-sm mx-auto">
        <div class="rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-md px-4 py-3 flex items-center justify-between gap-3">
            <p class="text-sm text-ink-700">Mise à jour disponible</p>
            <div class="flex items-center gap-1.5">
                <button type="button" @click="$store.pwaUpdate.apply()" class="h-8 px-3 rounded-xl bg-azure-500 text-white text-sm font-semibold">Recharger</button>
                <button type="button" @click="$store.pwaUpdate.available = false" class="h-8 px-3 rounded-xl text-ink-500 text-sm">Plus tard</button>
            </div>
        </div>
    </div>
    ```

    **E. Modifications admin shell components**

    Modifier `resources/views/components/admin/topbar.blade.php` :
    - Activer le bouton "Nouveau passage" : remplacer `<button disabled aria-disabled cursor-not-allowed opacity-50>` par `<a href="{{ route('admin.passages.create') }}" class="inline-flex items-center gap-2 h-11 px-5 rounded-xl bg-azure-500 text-white font-semibold hover:bg-azure-600 transition-colors shadow-sm"><svg ...>+</svg> <span class="hidden sm:inline">Nouveau passage</span><span class="sm:hidden">Passage</span></a>`
    - Insérer juste avant ce bouton le composant `<x-admin.sync-badge />` qui affiche le badge "N en attente"
    - Le bouton "Nouveau client" (ajouté en 02-02) reste sur les pages /admin/clients*

    Modifier `resources/views/components/admin/mobile-bottom-nav.blade.php` :
    - Ajouter un badge superposé sur l'item Passages : juste avant `</a>` de l'item Passages, ajouter :
      ```
      <span x-show="$store.offlineQueue.pendingCount > 0" x-cloak
            class="absolute -top-1 -right-1 h-4 w-4 rounded-full bg-warn text-white text-[10px] font-bold grid place-items-center"
            x-text="$store.offlineQueue.pendingCount"></span>
      ```
    - Pour positionner le badge : le `<a>` doit être `class="relative ..."`.

    Modifier `resources/views/components/admin/sidebar.blade.php` :
    - Ajouter un badge dans l'item Passages :
      ```
      <span x-show="$store.offlineQueue.pendingCount > 0" x-cloak
            class="ml-auto h-5 min-w-5 rounded-full bg-warn text-[oklch(0.32_0.09_70)] text-xs font-bold grid place-items-center px-1"
            x-text="$store.offlineQueue.pendingCount" aria-live="polite"></span>
      ```

    Modifier `resources/views/livewire/passage-index.blade.php` :
    - Remplacer le bouton "Nouveau passage" disabled (créé en 02-03) par un lien actif :
      ```
      <a href="{{ route('admin.passages.create') }}" class="h-11 px-5 rounded-xl bg-azure-500 text-white font-semibold hover:bg-azure-600 inline-flex items-center gap-2">+ Nouveau passage</a>
      ```
    - Remplacer aussi le commentaire TODO dans l'empty state — ajouter le CTA "+ Nouveau passage" actif (UI-SPEC §Empty states).

    Ajouter la classe utilitaire `x-cloak { display: none !important; }` dans `resources/css/app.css` si absente (Tailwind Alpine pattern standard) — vérifier d'abord son existence.
  </action>
  <verify>
    <automated>npm run build && ./vendor/bin/pest tests/Feature/PassageCreateViewTest.php tests/Feature/PassageIndexTest.php tests/Feature/AccessibilityTest.php --ci</automated>
  </verify>
  <acceptance_criteria>
    - 8 fichiers créés/modifiés sur le scope Task 2
    - `npm run build` exit 0
    - `grep -c "x-data=\"passageForm" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "capture=\"environment\"" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "x-show=\"!online\"" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "Mesures de l'eau" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "Actions menées" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "Mot pour le client" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "Enregistrer le passage" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "Brouillon sauvegardé automatiquement" resources/views/admin/passages/create.blade.php` ≥ 1
    - `grep -c "Synchro en attente" resources/views/components/admin/sync-drawer.blade.php` ≥ 1
    - `grep -c "Synchroniser maintenant" resources/views/components/admin/sync-drawer.blade.php` ≥ 1
    - `grep -c "Mise à jour disponible" resources/views/components/admin/pwa-update-toast.blade.php` ≥ 1
    - `grep -c "route('admin.passages.create" resources/views/components/admin/topbar.blade.php` ≥ 1
    - `grep -c "sync-badge" resources/views/components/admin/topbar.blade.php` ≥ 1
    - `grep -c "offlineQueue.pendingCount" resources/views/components/admin/mobile-bottom-nav.blade.php` ≥ 1
    - Suite Pest verte (140+ tests cumulés)
    - AccessibilityTest reste vert (touch targets vérifiés)
  </acceptance_criteria>
  <done>Vue saisie passage Alpine 1:1 mockup avec mesures/actions/photos/notes/save-bar fonctionnel ; sync-drawer + sync-badge + pwa-update-toast opérationnels ; topbar/sidebar/bottom-nav badges actifs ; bouton "Nouveau passage" actif partout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Le formulaire de saisie passage hors-ligne complet :
    - URL : http://127.0.0.1:8000/admin/passages/create (après login Pierre)
    - Header sticky avec retour + nom client (ou "Nouveau passage" si aucun client_id en query string)
    - Bandeau hors-ligne ambre (visible si tu désactives le wifi DevTools)
    - 6 cartes de mesures (pH, Cl libre, Cl total, TAC, sel, TH) avec steppers ±
    - 10 actions cochables (UI-SPEC §Section Actions menées)
    - Section Photos avec bouton "Ajouter" + caméra arrière (test sur mobile réel)
    - Sticky save-bar avec indicateur "Brouillon sauvegardé automatiquement" + bouton "Enregistrer le passage"
    - Topbar : badge "N en attente" affiché si pending IDB > 0
    - Bottom-nav mobile : badge superposé sur Passages
  </what-built>
  <how-to-verify>
    1. `npm run build && php artisan serve --host 0.0.0.0 --port 8000`
    2. Ouvrir Chrome sur desktop OU iPhone Safari sur ton réseau local
    3. Login en tant que Pierre (`/login`)
    4. Cliquer "Nouveau passage" depuis le topbar ou la sidebar
    5. Vérifier visuellement la conformité avec mockups/v1/passage.html (ouvrir le mockup à côté pour comparaison)
    6. Tester un cas online :
       - Ajuster pH à 7.4, Cl libre à 1.5, cocher 3 actions
       - Ajouter une photo (peut être une image de test sur ton disque ou caméra mobile)
       - Cliquer "Enregistrer le passage"
       - Attendre le badge "N en attente" disparaître → vérifier dans /admin/passages que le passage apparaît
    7. Tester un cas offline :
       - DevTools → Network → Offline (ou désactiver wifi)
       - Recharger la page (avec SW elle doit s'afficher)
       - Saisir un passage complet
       - Cliquer "Enregistrer le passage"
       - Vérifier badge "1 en attente" actif
       - Réactiver le réseau → attendre quelques secondes → badge passe à 0
    8. Tester le sync drawer :
       - En offline, créer 2 passages successifs
       - Cliquer le badge "2 en attente" du topbar → drawer s'ouvre
       - Vérifier la liste des 2 items avec leur statut
       - Cliquer "Synchroniser maintenant" (en online) → items deviennent synced
    9. Tester sur iPhone réel (critique pour validation terrain Phase 2 — bloqueur Phase 3) :
       - Background Sync API absente sur iOS → vérifier que le fallback Alpine `visibilitychange` fonctionne
       - storage.persist() : ajouter la PWA en Home Screen, fermer Safari, rouvrir → données IDB persistantes
    10. Mesurer Lighthouse mobile :
        - Performance ≥ 80
        - Best Practices ≥ 90
        - PWA installability score
  </how-to-verify>
  <resume-signal>
    Répondre :
    - `approved` : Pierre peut commencer à saisir des passages en production
    - `issues: [description]` : Claude prend en compte les retours et corrige
    - `iOS Safari fail` : signal critique → Claude investigue Pitfall 1/3, retravaille le fallback
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alpine x-data → fetch('/api/passages') | Le payload JSON est construit côté client (mesures, notes) puis envoyé via session cookie web |
| File picker camera → IDB blob → R2 | Photo brute → processPhoto (Canvas) → IDB blob → multipart upload → R2 via Plan 02-06 |
| client_uuid generation | crypto.randomUUID() côté client — pas de signature, mais UNIQUE constraint serveur (D-38) empêche la fraude par collision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Tampering | Modifier le payload JSON dans IDB avant upload | mitigate | Pierre est l'opérateur autorisé. Pas de risque tenant. Le serveur valide chaque champ (numeric, in_array actions) en Plan 02-06. |
| T-5-02 | Tampering | Forger un client_uuid pour cibler un passage clos | mitigate | UPSERT conditionnel `WHERE status = 'draft'` (D-38) + retour 409 si déjà clos. Pas d'écrasement possible. |
| T-5-03 | XSS via notes textarea | Tampering | mitigate | Blade `{{ }}` escape par défaut, Eloquent stocke en string, pas d'innerHTML dans le portail (Plan 02-07). |
| T-5-04 | DoS via photos massives | Denial of Service | mitigate | processPhoto compresse à ~300-400 KB JPEG (D-44). Côté serveur validation `max:10240` (10 MB) — Plan 02-06. |
| T-5-05 | iOS storage eviction (ITP) | Information Disclosure | mitigate | storage.persist() au boot (D-57) + instruction utilisateur d'installer la PWA en Home Screen. Le drawer affiche les pending non synchros. |
| T-5-06 | CSP bypass via inline event handlers (@click) | Tampering | accept | Alpine compile les @click en attribut bindings — c'est attendu, pas un XSS. CSP `script-src 'self'` couvrirait ; mais hors scope Phase 2. |
</threat_model>

<verification>
1. `npm run build` exit 0
2. `./vendor/bin/pest --ci` : suite totale verte (140+ tests cumulés)
3. Smoke test online (Task 3 checkpoint) : saisie complète + submit → passage apparait dans /admin/passages
4. Smoke test offline (Task 3 checkpoint) : DevTools Offline + saisie + submit → status pending → réseau on → synced
5. Smoke test iOS Safari (Task 3 checkpoint critique) : Background Sync API no-op + fallback online event OK
6. Lighthouse PWA audit ≥ 80 sur installability
7. AccessibilityTest reste vert (touch targets 44px+, aria-current, aria-live)
8. `grep -rn "x-cloak" resources/css/app.css` : utility présente (sinon ajouter `[x-cloak] { display: none !important; }`)
</verification>

<success_criteria>
- [ ] PASS-01 : saisie pH/Cl libre/Cl total/TAC/sel/TH via steppers ±, validation soft hors plage (D-63)
- [ ] PASS-02 : photos via input file accept image/* capture environment + processPhoto pipeline HEIC→JPEG+EXIF+Canvas 0.80
- [ ] PASS-03 : saisie offline persiste en IDB, sync à la reconnexion (online event + visibilitychange) + Background Sync fallback Workbox (Plan 02-04 config)
- [ ] PASS-06 : badge "N en attente" actif dans topbar (sync-badge component) + bottom-nav (badge superposé) + sidebar (badge inline)
- [ ] passage-form.js implémente : init() avec crypto.randomUUID + IDB save, watcher mesures + actions + notes pour auto-save draft, submit avec _flushQueue backoff 2s/8s/30s, gestion 409 (D-40), upload photos séquentiel
- [ ] sync-drawer.js et composant Blade : panneau glissant avec liste pending + retry par item + flush all
- [ ] pwa-update-toast composant : affichage si $store.pwaUpdate.available, boutons Recharger/Plus tard
- [ ] Topbar bouton "Nouveau passage" actif (`route('admin.passages.create')`), badge sync visible si pending > 0
- [ ] Bottom-nav badge superposé sur Passages
- [ ] Sidebar badge inline sur Passages
- [ ] Vue passage create transposée 1:1 de mockups/v1/passage.html (sections + ordre + classes)
- [ ] Touch targets ≥ 44px (steppers h-12, bouton submit h-13, miniatures h-20+)
- [ ] Bandeau hors-ligne ambre `x-show="!online"` + role="status" aria-live="assertive"
- [ ] Toast warnings mesures soft validation (D-63)
- [ ] Toast erreur 409 (D-40) "Ce passage a déjà été clos"
- [ ] Checkpoint user PASS sur le smoke test online + offline + iOS Safari fallback
- [ ] Suite Pest verte (PassageCreateViewTest + suite Phase 1-2)
- [ ] AccessibilityTest reste vert
</success_criteria>

<output>
Créer `.planning/phases/02-mvp-suivi-offline-first/02-05-SUMMARY.md` à la fin du plan.
</output>
