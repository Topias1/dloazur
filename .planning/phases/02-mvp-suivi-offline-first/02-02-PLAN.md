---
phase: 02-mvp-suivi-offline-first
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - resources/views/components/admin/sidebar.blade.php
  - resources/views/components/admin/mobile-bottom-nav.blade.php
  - resources/views/components/admin/topbar.blade.php
  - routes/admin.php
files_created:
  - app/Livewire/ClientIndex.php
  - app/Livewire/ClientForm.php
  - app/Livewire/PiscineForm.php
  - app/Http/Controllers/Admin/ClientController.php
  - resources/views/livewire/client-index.blade.php
  - resources/views/livewire/client-form.blade.php
  - resources/views/livewire/piscine-form.blade.php
  - resources/views/admin/clients/index.blade.php
  - resources/views/admin/clients/show.blade.php
  - resources/views/admin/clients/create.blade.php
  - resources/views/admin/clients/edit.blade.php
  - tests/Feature/ClientCrudTest.php
  - tests/Feature/PiscineCrudTest.php
  - tests/Feature/ClientSearchTest.php
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
user_setup: []
tags: [livewire, crud, clients, piscines, admin-shell-activation]
must_haves:
  truths:
    - "Pierre crée un client avec nom + email + téléphone + adresse + notes depuis /admin/clients/create"
    - "Pierre modifie un client existant depuis /admin/clients/{id}/edit"
    - "Pierre enregistre/modifie la piscine (1-to-1 UI par D-64) d'un client : nom, volume_m3, type, filtration, traitement, équipements, notes"
    - "Pierre recherche un client par nom/email/téléphone/adresse (ILIKE Postgres) avec debounce 300ms et résultats paginés 25/page (D-61)"
    - "La liste clients est triée updated_at DESC par défaut (D-61)"
    - "Les items 'Clients' de la sidebar desktop et de la bottom-nav mobile sont des liens actifs (non plus grisés)"
    - "Le bouton 'Nouveau passage' du topbar est désactivé tant que Plan 02-05 n'active pas la route (cohérent avec l'état actuel)"
    - "L'écran liste affiche un empty state spécifique 'Aucun client pour l'instant' + CTA 'Ajouter un client' (UI-SPEC §Empty states)"
    - "La recherche sans résultat affiche 'Aucun résultat pour « [terme] »' avec le terme exact saisi (UI-SPEC §Empty states)"
  artifacts:
    - path: "app/Livewire/ClientIndex.php"
      provides: "Livewire 3 component liste clients + recherche ILIKE + pagination"
      contains: "WithPagination"
    - path: "app/Livewire/ClientForm.php"
      provides: "Livewire 3 component create/edit client avec validation #[Validate]"
      contains: "#[Validate"
    - path: "app/Livewire/PiscineForm.php"
      provides: "Livewire 3 component create/edit piscine liée à un client"
      contains: "client_id"
    - path: "routes/admin.php"
      provides: "Routes clients : index, create, store, show, edit, update + 1 route pour piscine create/edit nested"
      contains: "clients"
    - path: "resources/views/admin/clients/index.blade.php"
      provides: "Vue liste clients étendant layouts.admin"
      contains: "<livewire:client-index"
  key_links:
    - from: "resources/views/admin/clients/index.blade.php"
      to: "app/Livewire/ClientIndex.php"
      via: "<livewire:client-index />"
      pattern: "livewire:client-index"
    - from: "resources/views/components/admin/sidebar.blade.php"
      to: "route('admin.clients.index')"
      via: "<a href=route(...)>Clients</a>"
      pattern: "admin.clients.index"
    - from: "app/Livewire/ClientIndex.php"
      to: "App\\Models\\Client::query()"
      via: "->when(\$this->search, ...)->orderBy('updated_at','desc')->paginate(25)"
      pattern: "ILIKE\\|paginate\\(25\\)"
---

<objective>
Activer le CRUD clients + piscines en back-office Livewire 3 (CRUD CLI-01/02/03) et basculer les items "Clients" de la sidebar desktop + bottom-nav mobile de l'état "grisé bientôt" (Phase 1) vers des liens actifs. La recherche utilise `ILIKE` Postgres sur `name + email + phone + address` avec pagination 25/page, debounce 300ms côté wire:model.live, tri `updated_at DESC` (D-61). La fiche client embarque la fiche piscine (1-to-1 UI v1, D-64) — si Pierre a un client multi-piscines (modèle DB le permet), un sélecteur s'affiche.

Purpose : sans ce plan, Pierre ne peut ni créer ses 10 clients ni leur attacher une piscine — la saisie passage Plan 02-05 sera bloquée car aucune cible n'existe pour `passages.piscine_id` / `passages.client_id`.

Output : 3 composants Livewire + 1 controller fin + 4 vues admin/clients + 3 vues livewire + 3 fichiers tests Pest. Modifications de sidebar/bottom-nav/topbar pour activer les liens Clients (laisser Factures+Catalogue grisés — Phase 3) et activer le bouton "Nouveau client" du topbar (mais "Nouveau passage" reste désactivé jusqu'à Plan 02-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-mvp-suivi-offline-first/02-CONTEXT.md
@.planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md
@.planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md
@.planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md
@.planning/phases/02-mvp-suivi-offline-first/02-01-SUMMARY.md
@.planning/phases/01-vitrine-fondations/01-05-SUMMARY.md
@CLAUDE.md
@DESIGN.md
@.claude/skills/impeccable/SKILL.md

<interfaces>
<!-- Contrats existants Phase 1 et Plan 02-01 -->

From app/Livewire/ContactForm.php (pattern Livewire 3 #[Validate] + try/catch + render — copier pour ClientForm) :
- `use Livewire\Attributes\Validate;`
- `use Livewire\Component;`
- `#[Validate('required|string|max:80')] public string $name = '';`
- `public function submit(): void { $this->validate(); try { ... } catch (\Throwable $e) { Log::error(...); $this->addError('save', ...); return; } }`
- `public function render(): \Illuminate\View\View { return view('livewire.contact-form'); }`

From app/Models/Client.php (état après Plan 02-01) :
- `extends Authenticatable`
- `$fillable = ['uuid', 'name', 'email', 'phone', 'address', 'notes', 'magic_link_token', 'magic_link_expires_at']`
- Relations: piscines() HasMany, passages() HasMany, contrats() HasMany, factures() HasMany

From app/Models/Piscine.php :
- `$fillable = ['client_id','nom','volume_m3','type','filtration','traitement','equipements','notes']`
- `$casts = ['volume_m3' => 'decimal:2', 'equipements' => 'array']`
- Relations: client() BelongsTo, passages() HasMany

From routes/admin.php (état actuel — un seul GET / → DashboardController@index) :
- Routes ajoutées par Plan 02-02 : clients.index, clients.create, clients.store, clients.show, clients.edit, clients.update, clients.destroy + piscines nested (un seul `Route::resource('clients', ...)` suffit ; piscines.update embedded dans clients.update via le composant Livewire PiscineForm).

From resources/views/components/admin/sidebar.blade.php (état Phase 1) :
- L'item "Clients" est actuellement `<span aria-disabled="true" tabindex="-1" class="... opacity-60 cursor-default">` avec badge `bientôt`
- Pattern actif copiable depuis "Tableau de bord" (lignes 30-40) : `<a href="{{ route('admin.dashboard') }}" @class([... 'bg-white/10 text-white' => request()->routeIs('admin.dashboard') ...]) aria-current="page">`

From resources/views/components/admin/mobile-bottom-nav.blade.php (état Phase 1) :
- Item "Clients" est un `<span aria-disabled="true" class="... opacity-60 cursor-default">`. Pattern actif copiable depuis "Accueil" (lignes 14-19) : `<a href="{{ route('admin.dashboard') }}" class="... text-azure-600" aria-current="page">`

From resources/views/components/admin/topbar.blade.php (état Phase 1) :
- Le bouton "Nouveau passage" est `disabled aria-disabled="true" cursor-not-allowed opacity-50`
- Pour ce plan : laisser ce bouton désactivé (Plan 02-05 l'activera). En revanche AJOUTER un bouton "Nouveau client" actif quand on est sur `/admin/clients*`. UI-SPEC §Liste clients : `h-11 px-5 rounded-xl bg-azure-500 text-white font-bold shadow-sm hover:bg-azure-600`.
- Activer également le search input desktop : aujourd'hui `disabled` avec placeholder "Recherche disponible en Phase 2…". Phase 2 le rend fonctionnel (mais c'est le composant Livewire client-index qui porte la recherche — le topbar peut soit déléguer via `wire:model.live` sur l'input du topbar, soit garder son input visuel et router la recherche par la querystring). Choix : input topbar = délégation visuelle non interactive si page ≠ /admin/clients, recherche réelle uniquement dans la vue liste clients via le composant Livewire (sa propre `<input wire:model.live.debounce.300ms="search">`). Mettre à jour le placeholder du topbar disabled : "Rechercher un client, un passage…" et laisser disabled tant que pas sur la liste clients/passages.

From .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md §Liste clients (CLI-01 à CLI-03) :
- Topbar search input : `h-10 pl-10 pr-4 rounded-xl bg-white ring-1 ring-navy-900/10 focus:ring-2 focus:ring-azure-500` (desktop)
- Bouton "Nouveau client" : `h-11 px-5 rounded-xl bg-azure-500 text-white font-bold shadow-sm hover:bg-azure-600`
- Carte client : `rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-4 flex items-center gap-4`
- Avatar initiales : `h-11 w-11 rounded-full bg-azure-50 text-azure-700 font-display font-semibold grid place-items-center`
- Chip piscine : `rounded-full bg-sand-100 text-ink-500 text-xs px-2.5 py-0.5`
- Empty states : "Aucun client pour l'instant." + body + CTA "Ajouter un client"
- Recherche sans résultat : "Aucun résultat pour « [terme] »."

From .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md §"app/Livewire/ClientIndex.php" :
- Pattern complet ILIKE concaténé sans injection (binding Eloquent) :
  ```
  Client::query()
    ->when($this->search, fn ($q) =>
        $q->where(
            DB::raw("name || ' ' || COALESCE(email,'') || ' ' || COALESCE(phone,'') || ' ' || COALESCE(address,'')"),
            'ILIKE',
            '%' . $this->search . '%'
        )
    )
    ->orderBy('updated_at', 'desc')
    ->paginate(25);
  ```
- Anti-pattern explicite : NE PAS écrire `DB::raw("... LIKE '%$this->search%'")` — uniquement le binding `'%...%'` passé en 3e arg de `where()`.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 : Tests Pest + composants Livewire ClientIndex/ClientForm/PiscineForm + routes admin clients</name>
  <files>tests/Feature/ClientCrudTest.php, tests/Feature/PiscineCrudTest.php, tests/Feature/ClientSearchTest.php, app/Livewire/ClientIndex.php, app/Livewire/ClientForm.php, app/Livewire/PiscineForm.php, app/Http/Controllers/Admin/ClientController.php, routes/admin.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "app/Livewire/ClientIndex.php", "app/Livewire/ClientForm.php", "app/Livewire/PiscineForm.php", "routes/api.php" pour le pattern Route)
    - .planning/phases/02-mvp-suivi-offline-first/02-RESEARCH.md §Pattern 8 (Livewire 3 CRUD + WithPagination, lignes 663-696)
    - app/Livewire/ContactForm.php (pattern exact Livewire 3 #[Validate] + try/catch — à copier mot pour mot)
    - app/Models/Client.php (état après 02-01 : $fillable + relations)
    - app/Models/Piscine.php ($fillable + cast equipements => array)
    - database/factories/ClientFactory.php, database/factories/PiscineFactory.php (pour les tests)
    - tests/Feature/ContactFormTest.php (pattern Livewire::test + assertHasNoErrors)
    - tests/Feature/AuthLoginTest.php (pattern actingAs + RefreshDatabase)
    - routes/admin.php (état actuel — un seul GET / → DashboardController@index, à étendre)
  </read_first>
  <behavior>
    **tests/Feature/ClientCrudTest.php** (couvre CLI-01) — toujours `uses(RefreshDatabase::class)`:
    - Test 1 : "Pierre voit la liste clients vide affiche l'empty state" — `actingAs($pierre)->get(route('admin.clients.index'))->assertSee('Aucun client pour l\'instant.')`
    - Test 2 : "Pierre crée un client via Livewire" — `Livewire::test(ClientForm::class)->set('name','Marie Dubois')->set('email','marie@ex.com')->set('phone','0696112233')->set('address','12 rue de la Plage, Schoelcher')->call('submit')->assertHasNoErrors();` puis `expect(Client::where('email','marie@ex.com')->exists())->toBeTrue();`
    - Test 3 : "ClientForm refuse un nom vide" — `Livewire::test(ClientForm::class)->set('name','')->call('submit')->assertHasErrors(['name' => 'required']);`
    - Test 4 : "ClientForm refuse un email mal formé" — `Livewire::test(ClientForm::class)->set('name','x')->set('email','pas-un-email')->call('submit')->assertHasErrors(['email']);`
    - Test 5 : "Pierre modifie un client existant" — passer `clientId=$existing->id` au mount, set('name','Modifié'), call('submit'), expect(Client::find($id)->name)->toBe('Modifié')
    - Test 6 : "Un visiteur non logué est redirigé vers /login sur /admin/clients" — `$this->get(route('admin.clients.index'))->assertRedirect('/login');`

    **tests/Feature/PiscineCrudTest.php** (couvre CLI-02):
    - Test 1 : "Pierre crée une piscine liée à un client" — `Livewire::test(PiscineForm::class, ['clientId' => $client->id])->set('nom','Lagon')->set('volume_m3','25.5')->set('type','enterrée')->set('filtration','sable')->set('traitement','chlore')->call('submit')->assertHasNoErrors();` puis `expect($client->piscines()->count())->toBe(1);`
    - Test 2 : "PiscineForm refuse volume_m3 non numérique" — `Livewire::test(PiscineForm::class, ['clientId' => $client->id])->set('nom','x')->set('volume_m3','abc')->call('submit')->assertHasErrors(['volume_m3']);`
    - Test 3 : "Piscine modifiée préserve client_id" — set clientId+piscineId existant, modify, assert FK inchangée

    **tests/Feature/ClientSearchTest.php** (couvre CLI-03):
    - Test 1 : "Recherche par nom retourne le bon client" — créer 3 clients (Marie, Jean, Sophie), `Livewire::test(ClientIndex::class)->set('search','marie')->assertSee('Marie')->assertDontSee('Jean');`
    - Test 2 : "Recherche par téléphone fonctionne (ILIKE inclut phone)" — `set('search','0696112233')->assertSee('Marie')`
    - Test 3 : "Recherche sans résultat affiche le message empty avec terme exact" — `set('search','zzz-inconnu')->assertSee('Aucun résultat pour « zzz-inconnu »')`
    - Test 4 : "Tri par défaut updated_at DESC" — créer 3 clients, toucher (touch()) un client B, recherche vide, assert l'ordre dans get(route('admin.clients.index')) avec assertSeeInOrder([B->name, ...])
    - Test 5 : "Pagination 25/page : 30 clients génèrent 2 pages" — créer 30 clients via factory, `Livewire::test(ClientIndex::class)->assertSet('paginators.page', 1);` et expect((string) get(route('admin.clients.index'))->getContent())->toContain('?page=2')
    - Test 6 : "Changement de search réinitialise la page (updatedSearch resetPage)" — set search → assertSet('paginators.page', 1)
  </behavior>
  <action>
    Créer `app/Livewire/ClientIndex.php` strictement selon PATTERN map :
    - namespace `App\Livewire`, imports : `Client`, `DB`, `Livewire\Component`, `Livewire\WithPagination`
    - `use WithPagination;`
    - `public string $search = '';`
    - `public function updatedSearch(): void { $this->resetPage(); }`
    - `public function render(): \Illuminate\View\View { … return view('livewire.client-index', compact('clients')); }`
    - Implémentation EXACTE du `render()` selon RESEARCH §Pattern 8 :
      ```
      $clients = Client::query()
          ->when($this->search, fn ($q) =>
              $q->where(
                  DB::raw("name || ' ' || COALESCE(email,'') || ' ' || COALESCE(phone,'') || ' ' || COALESCE(address,'')"),
                  'ILIKE',
                  '%' . $this->search . '%'
              )
          )
          ->withCount('passages')
          ->with(['piscines:id,client_id,nom,volume_m3,type'])
          ->orderBy('updated_at', 'desc')
          ->paginate(25);
      ```
    - Le `withCount('passages')` permet d'afficher "Dernier passage : il y a X jours" plus tard (PASS-05) ; le `with('piscines:...')` charge les caractéristiques de la piscine 1-to-1 pour le chip de carte (D-64).

    Créer `app/Livewire/ClientForm.php` copiant la structure de `ContactForm.php` :
    - imports : `App\Models\Client`, `Illuminate\Support\Facades\Log`, `Livewire\Attributes\Validate`, `Livewire\Component`
    - Propriétés publiques avec `#[Validate]` :
      - `#[Validate('required|string|max:80')] public string $name = '';`
      - `#[Validate('nullable|email|max:160')] public string $email = '';`
      - `#[Validate('nullable|string|max:30')] public string $phone = '';`
      - `#[Validate('nullable|string|max:200')] public string $address = '';`
      - `#[Validate('nullable|string|max:2000')] public string $notes = '';`
      - `public ?int $clientId = null;`
    - `public function mount(?int $clientId = null): void { if ($clientId) { $client = Client::findOrFail($clientId); $this->clientId = $client->id; $this->name = $client->name; $this->email = (string) $client->email; $this->phone = (string) $client->phone; $this->address = (string) $client->address; $this->notes = (string) $client->notes; } }`
    - `public function submit(): void { $this->validate(); try { Client::updateOrCreate(['id' => $this->clientId], ['uuid' => \Illuminate\Support\Str::uuid()->toString(), 'name' => $this->name, 'email' => $this->email ?: null, 'phone' => $this->phone ?: null, 'address' => $this->address ?: null, 'notes' => $this->notes ?: null]); } catch (\Throwable $e) { Log::error('Client save failed', ['exception' => $e->getMessage()]); $this->addError('save', "L'enregistrement a échoué."); return; } $this->dispatch('client-saved'); $this->redirect(route('admin.clients.index'), navigate: true); }`
    - Note : `updateOrCreate(['id' => $this->clientId], …)` passe `null` en création ; Eloquent interprète correctement comme INSERT.
    - Pour les nouveaux clients : forcer un `uuid` (Str::uuid()) car la migration Phase 1 a la colonne `uuid` UNIQUE.
    - `public function render(): \Illuminate\View\View { return view('livewire.client-form'); }`

    Créer `app/Livewire/PiscineForm.php` (analog ClientForm) :
    - Propriétés `#[Validate]` :
      - `#[Validate('required|integer|exists:clients,id')] public int $clientId;`
      - `#[Validate('nullable|string|max:60')] public string $nom = '';`
      - `#[Validate('nullable|numeric|min:1|max:1000')] public string $volume_m3 = '';`
      - `#[Validate('nullable|string|max:30')] public string $type = '';`
      - `#[Validate('nullable|string|max:30')] public string $filtration = '';`
      - `#[Validate('nullable|string|max:30')] public string $traitement = '';`
      - `#[Validate('nullable|string|max:1000')] public string $notes = '';`
      - `public array $equipements = [];` (cast array, pas de validation #[Validate])
      - `public ?int $piscineId = null;`
    - `mount(int $clientId, ?int $piscineId = null)` : si `$piscineId` chargé via `Piscine::where('client_id', $clientId)->findOrFail($piscineId)`.
    - `submit()` : updateOrCreate similaire avec `dispatch('piscine-saved')` puis `redirect(route('admin.clients.show', $this->clientId))`.

    Créer `app/Http/Controllers/Admin/ClientController.php` (controller fin pour les vues — Livewire porte la logique) :
    - imports : `App\Http\Controllers\Controller`, `App\Models\Client`
    - `index()` : `return view('admin.clients.index');` (vue qui embed `<livewire:client-index />`)
    - `create()` : `return view('admin.clients.create');`
    - `show(Client $client)` : `return view('admin.clients.show', compact('client'));`
    - `edit(Client $client)` : `return view('admin.clients.edit', compact('client'));`

    Étendre `routes/admin.php` (ajouter sous la ligne `Route::get('/', [DashboardController::class, 'index'])->name('dashboard');`) :
    ```
    use App\Http\Controllers\Admin\ClientController;
    Route::get('clients', [ClientController::class, 'index'])->name('clients.index');
    Route::get('clients/create', [ClientController::class, 'create'])->name('clients.create');
    Route::get('clients/{client}', [ClientController::class, 'show'])->name('clients.show');
    Route::get('clients/{client}/edit', [ClientController::class, 'edit'])->name('clients.edit');
    ```
    NE PAS définir de routes POST/PATCH/DELETE — les actions write passent par les composants Livewire `ClientForm` et `PiscineForm`. Le route model binding `{client}` résout Client par id (default).

    Créer les 3 fichiers tests en suivant exactement le format Pest 4 du codebase (top du fichier `uses(RefreshDatabase::class);`, tests sous forme `it('...', function () { ... });`).

    NOTE Livewire 3 + redirect navigate : `$this->redirect(..., navigate: true)` requiert Livewire 3.5+ — vérifier dans `composer.lock` que c'est OK (CONTEXT.md verrouille Livewire 3.x). Si problème, fallback `$this->redirect(route('admin.clients.index'))` sans navigate.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/ClientCrudTest.php tests/Feature/PiscineCrudTest.php tests/Feature/ClientSearchTest.php --ci</automated>
  </verify>
  <acceptance_criteria>
    - Les 3 fichiers tests existent
    - `./vendor/bin/pest tests/Feature/ClientCrudTest.php --ci` : 6/6 verts
    - `./vendor/bin/pest tests/Feature/PiscineCrudTest.php --ci` : 3/3 verts
    - `./vendor/bin/pest tests/Feature/ClientSearchTest.php --ci` : 6/6 verts
    - `grep -c "WithPagination" app/Livewire/ClientIndex.php` ≥ 1
    - `grep -c "ILIKE" app/Livewire/ClientIndex.php` ≥ 1
    - `grep -c "#\[Validate" app/Livewire/ClientForm.php` ≥ 3
    - `grep -c "#\[Validate" app/Livewire/PiscineForm.php` ≥ 3
    - `php artisan route:list --path=admin/clients 2>&1 | grep -c "admin.clients."` ≥ 4 (index, create, show, edit)
    - `./vendor/bin/pest --ci` : suite totale verte (sans régression Phase 1 + Plan 02-01)
  </acceptance_criteria>
  <done>3 composants Livewire opérationnels avec validation/error handling, 4 routes nommées sous /admin/clients, 15 tests Pest passants, recherche ILIKE Postgres + pagination 25/page + tri updated_at DESC actifs.</done>
</task>

<task type="auto">
  <name>Task 2 : Vues Blade admin/clients/* + livewire/client-* + activation sidebar/bottom-nav/topbar</name>
  <files>resources/views/admin/clients/index.blade.php, resources/views/admin/clients/show.blade.php, resources/views/admin/clients/create.blade.php, resources/views/admin/clients/edit.blade.php, resources/views/livewire/client-index.blade.php, resources/views/livewire/client-form.blade.php, resources/views/livewire/piscine-form.blade.php, resources/views/components/admin/sidebar.blade.php, resources/views/components/admin/mobile-bottom-nav.blade.php, resources/views/components/admin/topbar.blade.php</files>
  <read_first>
    - .planning/phases/02-mvp-suivi-offline-first/02-UI-SPEC.md (§"Liste clients", §"Fiche client / piscine", §"Admin shell — Sidebar (desktop) — ACTIVATION Phase 2", §"Admin shell — Mobile bottom-nav — ACTIVATION Phase 2", §"Admin topbar", §"Empty states", §"Error states")
    - .planning/phases/02-mvp-suivi-offline-first/02-PATTERNS.md (sections "resources/views/admin/clients/index.blade.php", "resources/views/components/admin/sidebar.blade.php", "resources/views/components/admin/topbar.blade.php", "resources/views/components/admin/mobile-bottom-nav.blade.php")
    - resources/views/admin/dashboard.blade.php (analog `@extends('layouts.admin')` + `@section('sidebar')` + `@section('topbar')` + `@section('main')`)
    - resources/views/components/admin/sidebar.blade.php (état Phase 1 à modifier — items Clients/Passages encore grisés)
    - resources/views/components/admin/mobile-bottom-nav.blade.php (état Phase 1)
    - resources/views/components/admin/topbar.blade.php (état Phase 1 — bouton Nouveau passage disabled)
    - mockups/v1/dashboard.html, mockups/v1/index.html (références visuelles UI-SPEC)
    - DESIGN.md (tokens OKLCH + Fredoka/Inter)
    - .claude/skills/impeccable/SKILL.md (règles design system + photo grade + scale rule)
  </read_first>
  <action>
    **A. Vues admin/clients/*.blade.php** — chacune étend `layouts.admin` et embed le composant Livewire approprié.

    Créer `resources/views/admin/clients/index.blade.php` :
    - `@extends('layouts.admin')`, `@section('title', 'Clients · Dlo Azur')`, `@section('sidebar') <x-admin.sidebar :user="auth()->user()" /> @endsection`, `@section('topbar') <x-admin.topbar /> @endsection`, `@section('main') <livewire:client-index /> <x-admin.mobile-bottom-nav /> @endsection`.

    Créer `resources/views/admin/clients/show.blade.php` :
    - Wrapper `@extends('layouts.admin')` identique au dashboard.
    - `@section('main')` : header avec bouton retour (h-10 w-10 rounded-xl bg-white ring-1 ring-sand-200) + titre Fredoka 600 `text-xl text-ink-950` `{{ $client->name }}` + sous-titre Inter 400 `text-sm text-ink-500` avec `{{ $client->email }} · {{ $client->phone }}`.
    - 3 sections : "Informations client" (carte rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-6 avec liste dl/dt/dd des champs `address`, `notes`), "Piscine" (carte montrant la piscine 1-to-1 OU "Pas encore de piscine — Ajouter une piscine" CTA → `<livewire:piscine-form :clientId="$client->id" :piscineId="optional($client->piscines->first())->id" />` selon D-64 auto-pick), "Historique des passages" (liste paginée `$client->passages()->orderBy('visited_at','desc')->paginate(10)` — Plan 02-03 affinera, ici stub avec `@forelse @empty Aucun passage pour ce client. @endforelse`).
    - Boutons : "Modifier" → `route('admin.clients.edit', $client)` (`h-11 px-5 rounded-xl bg-azure-500 text-white font-semibold`), "Supprimer" → modale de confirmation (UI-SPEC §Actions destructives) — peut être un POST form `route('admin.clients.destroy', $client)` MAIS la route destroy n'est pas créée Plan 02-02 → laisser ce bouton actif uniquement si la route existe (Phase 2 différée si pas implémenté).
    - Décision : NE PAS implémenter destroy en Plan 02-02 (différé Phase 2 future ou hors scope MVP). Le bouton "Supprimer" peut être omis ou commenté avec TODO.

    Créer `resources/views/admin/clients/create.blade.php` :
    - `@extends('layouts.admin')`, header "Nouveau client" + bouton retour, embed `<livewire:client-form />`.

    Créer `resources/views/admin/clients/edit.blade.php` :
    - `@extends('layouts.admin')`, header "{{ $client->name }}" + bouton retour, embed `<livewire:client-form :clientId="$client->id" />` ET `<livewire:piscine-form :clientId="$client->id" :piscineId="optional($client->piscines->first())->id" />` (D-64 auto-pick).

    **B. Vues Livewire (resources/views/livewire/*.blade.php)**

    Créer `resources/views/livewire/client-index.blade.php` (UI-SPEC §Liste clients) :
    - Wrapper `<div class="px-5 sm:px-8 py-7 space-y-7">`
    - Header bloc : `<div class="flex items-center justify-between gap-4">` avec à gauche `<h1 class="font-display font-semibold text-2xl sm:text-3xl text-ink-950">Clients</h1>` + à droite : `<a href="{{ route('admin.clients.create') }}" class="h-11 px-5 rounded-xl bg-azure-500 text-white font-bold shadow-sm hover:bg-azure-600 inline-flex items-center gap-2">+ Nouveau client</a>`
    - Search input : `<input wire:model.live.debounce.300ms="search" type="search" placeholder="Rechercher un client…" class="w-full h-10 pl-10 pr-4 rounded-xl bg-white ring-1 ring-navy-900/10 focus:ring-2 focus:ring-azure-500 outline-none" aria-label="Rechercher un client">` avec icône loupe SVG à gauche absolute.
    - Liste : `@forelse ($clients as $client)` carte EXACTE selon UI-SPEC §Liste clients : `<a href="{{ route('admin.clients.show', $client) }}" class="rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-4 flex items-center gap-4 hover:bg-sand-50 transition-colors">`
      - Avatar initiales (calcul des initiales depuis `{{ $client->name }}` via PHP : `Str::of($client->name)->explode(' ')->map(fn($w) => Str::substr($w,0,1))->take(2)->implode('')`) dans `<span class="h-11 w-11 rounded-full bg-azure-50 text-azure-700 font-display font-semibold grid place-items-center shrink-0">`
      - Bloc nom : `<p class="font-semibold text-ink-900">{{ $client->name }}</p>` + ligne contact `<p class="text-sm text-ink-500">{{ $client->email ?? '—' }} · {{ $client->phone ?? '—' }}</p>`
      - Chip piscine si `$client->piscines->isNotEmpty()` : `<span class="rounded-full bg-sand-100 text-ink-500 text-xs px-2.5 py-0.5">{{ $client->piscines->first()->type ?: 'piscine' }} · {{ $client->piscines->first()->volume_m3 }} m³</span>`
      - Chevron SVG droite + text-ink-400
    - `@empty` : si `$search === ''` afficher empty state UI-SPEC : `<div class="rounded-2xl bg-sand-50 ring-1 ring-sand-200 p-8 text-center"><h2 class="font-display font-semibold text-xl text-ink-950">Aucun client pour l'instant.</h2><p class="text-ink-500 mt-2">Ajoutez votre premier client pour commencer à saisir des passages.</p><a href="{{ route('admin.clients.create') }}" class="mt-4 inline-flex h-11 px-5 rounded-xl bg-azure-500 text-white font-semibold items-center">Ajouter un client</a></div>`
      Si `$search !== ''` : `<div class="rounded-2xl bg-white ring-1 ring-sand-200 p-6 text-center"><p class="text-ink-700">Aucun résultat pour « {{ $search }} ».</p><p class="text-ink-500 text-sm mt-1">Vérifiez l'orthographe ou essayez un autre nom.</p></div>` (UI-SPEC §Empty states "Aucun résultat pour « [terme] »")
    - `@endforelse`
    - Pagination : `{{ $clients->links() }}` en bas (Livewire 3 par défaut utilise tailwind paginator — vérifier que `config/livewire.php` ou la vue blade pagination est OK pour Tailwind 4 ; sinon publish via `php artisan vendor:publish --tag=livewire:pagination` et adapter).
    - Touch targets ≥ 44px (UI-SPEC §Accessibility) : carte `min-h-[64px]`, avatar `h-11`, chevron link area large.

    Créer `resources/views/livewire/client-form.blade.php` :
    - Wrapper `<form wire:submit.prevent="submit" class="space-y-5">` avec sections :
      - Champ Nom : `<label class="block text-sm font-semibold text-ink-900 mb-1.5">Nom <span class="text-danger" aria-hidden="true">*</span></label><input wire:model="name" type="text" maxlength="80" class="w-full h-12 px-4 rounded-xl bg-sand-50 ring-1 ring-sand-200 focus:ring-2 focus:ring-azure-500 outline-none" autocomplete="name"> @error('name')<p class="mt-1 text-sm text-danger">{{ $message }}</p>@enderror`
      - Champ Email : `<input wire:model="email" type="email" inputmode="email" class="..." autocomplete="email">` + label "E-mail"
      - Champ Téléphone : `<input wire:model="phone" type="tel" inputmode="tel" class="..." autocomplete="tel">` + label "Téléphone"
      - Champ Adresse : `<input wire:model="address" type="text" class="..." autocomplete="street-address">` + label "Adresse"
      - Champ Notes : `<textarea wire:model="notes" rows="4" class="w-full rounded-xl bg-sand-50 ring-1 ring-sand-200 p-3 focus:ring-2 focus:ring-azure-500 outline-none resize-none"></textarea>` + label "Notes"
      - `@error('save') ... @enderror` (UI-SPEC §Error states "L'enregistrement a échoué.")
    - Bouton submit : `<button type="submit" class="h-12 px-6 rounded-xl bg-azure-500 text-white font-semibold hover:bg-azure-600 transition-colors" wire:loading.attr="disabled" wire:loading.class="opacity-60"><span wire:loading.remove>Enregistrer</span><span wire:loading>Enregistrement…</span></button>`
    - Règle UI-SPEC : focus-visible 2px ring azure-500 (hérité Phase 1 tokens).

    Créer `resources/views/livewire/piscine-form.blade.php` (analogue client-form) :
    - Champs : Nom (optionnel), Volume (m³), Type (select : enterrée/hors-sol/semi-enterrée — UI list), Filtration (select : sable/cartouche/diatomée), Traitement (select : chlore/sel/UV/oxygène-actif), Équipements (checkboxes group → `wire:model="equipements"` array : "Volet", "Robot", "Pompe à chaleur", "PAC", "Bâche", "Échelle"), Notes.
    - Pattern checkboxes group : `<div class="flex flex-wrap gap-2"> @foreach (['Volet','Robot','PAC','Bâche','Échelle'] as $opt) <label class="inline-flex items-center gap-2 h-11 px-3.5 rounded-xl bg-white ring-1 ring-sand-200 cursor-pointer has-checked:bg-azure-50 has-checked:ring-azure-200"><input type="checkbox" wire:model="equipements" value="{{ $opt }}" class="sr-only">{{ $opt }}</label> @endforeach </div>` (Tailwind 4 supporte `has-*:` selectors).

    **C. Modifications composants admin (sidebar / bottom-nav / topbar)**

    Modifier `resources/views/components/admin/sidebar.blade.php` :
    - Remplacer le `<span aria-disabled="true" …>` "Clients" par un `<a href="{{ route('admin.clients.index') }}" @class([... 'bg-white/10 text-white' => request()->routeIs('admin.clients.*'), 'hover:bg-white/8 hover:text-white' => !request()->routeIs('admin.clients.*'), ...]) @if(request()->routeIs('admin.clients.*')) aria-current="page" @endif>` (copier le pattern actif depuis l'item "Tableau de bord" lignes 30-40). Conserver l'icône SVG et le label "Clients", supprimer le badge "bientôt" pour cet item.
    - Garder le `<span>` grisé pour "Passages" (Plan 02-05/02-03 activera) → le badge "bientôt" reste.
    - Garder Factures + Catalogue grisés (Phase 3).

    Modifier `resources/views/components/admin/mobile-bottom-nav.blade.php` :
    - Remplacer le `<span aria-disabled="true" …>` "Clients" par un `<a href="{{ route('admin.clients.index') }}" class="flex flex-col items-center justify-center gap-1 {{ request()->routeIs('admin.clients.*') ? 'text-azure-600' : 'text-ink-400' }}" @if(request()->routeIs('admin.clients.*')) aria-current="page" @endif>` (pattern actif de l'item Accueil).
    - Garder Passages grisé (Plan 02-03 activera).
    - Garder Factures grisé (Phase 3).

    Modifier `resources/views/components/admin/topbar.blade.php` :
    - Mettre à jour le placeholder du search input disabled : "Rechercher un client, un passage…" (Phase 2 va finir l'activer plan-après-plan). Garder `disabled` car la recherche réelle vit dans `<livewire:client-index>` quand on est sur cette page — éviter de dupliquer le search en topbar.
    - Conserver le bouton "Nouveau passage" en `disabled aria-disabled="true" cursor-not-allowed opacity-50` (Plan 02-05 l'activera). Mettre à jour le `title` de l'attribut : "Disponible bientôt — saisie passage en cours d'activation".
    - AJOUTER un bouton "Nouveau client" actif visible UNIQUEMENT sur les pages clients : `@if(request()->routeIs('admin.clients.*')) <a href="{{ route('admin.clients.create') }}" class="inline-flex items-center gap-2 h-11 px-5 rounded-xl bg-azure-500 text-white font-semibold hover:bg-azure-600 transition-colors shadow-sm">+ Nouveau client</a> @endif` (à placer avant le bouton "Nouveau passage" disabled).
    - Garder `noindex,nofollow` via `<meta name="robots">` du layout.

    Garder l'AccessibilityTest existant vert : `aria-disabled="true" tabindex="-1"` sur les nav items Passages/Factures/Catalogue qui restent grisés ; `aria-current="page"` sur les liens actifs.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/AdminShellTest.php tests/Feature/ClientCrudTest.php tests/Feature/PiscineCrudTest.php tests/Feature/ClientSearchTest.php tests/Feature/AccessibilityTest.php --ci && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 (compile Tailwind 4 + Vite avec les nouvelles vues)
    - 10 fichiers de vues créés/modifiés : 4 admin/clients/*, 3 livewire/*, 3 composants admin (sidebar/bottom-nav/topbar)
    - `grep -c "route('admin.clients" resources/views/components/admin/sidebar.blade.php` ≥ 1
    - `grep -c "route('admin.clients" resources/views/components/admin/mobile-bottom-nav.blade.php` ≥ 1
    - `grep -c "Nouveau client" resources/views/components/admin/topbar.blade.php` ≥ 1
    - `grep -c "livewire:client-index" resources/views/admin/clients/index.blade.php` ≥ 1
    - `grep -c "livewire:client-form" resources/views/admin/clients/create.blade.php` ≥ 1
    - `grep -c "livewire:piscine-form" resources/views/admin/clients/edit.blade.php` ≥ 1
    - `grep -c "wire:model.live.debounce.300ms" resources/views/livewire/client-index.blade.php` ≥ 1
    - `grep -c "Aucun client pour l'instant" resources/views/livewire/client-index.blade.php` ≥ 1
    - `grep -c "Aucun résultat pour" resources/views/livewire/client-index.blade.php` ≥ 1
    - Smoke test manuel via `php artisan serve --env=testing` : visiter `/admin/clients` après login, voir liste vide + bouton "Ajouter un client" ; cliquer → formulaire ; submit avec données → retour liste avec le nouveau client.
    - `./vendor/bin/pest tests/Feature/AdminShellTest.php --ci` reste vert (les tests Phase 1 qui vérifient les items grisés Passages/Factures/Catalogue doivent passer — seuls Clients/sidebar items basculent)
    - `./vendor/bin/pest --ci` retourne la suite totale verte (110 Phase 1+02-01 + 15 Plan 02-02 = 125+)
  </acceptance_criteria>
  <done>Le CRUD clients/piscines est fonctionnel via Livewire 3, les composants admin sont activés pour Clients (sidebar + bottom-nav) avec UI-SPEC conforme (avatar/chip/pattern carte), bouton "Nouveau client" actif dans le topbar, empty states + recherche sans résultat affichés correctement, suite Pest entièrement verte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pierre (guard web) → formulaire client | Inputs utilisateur via wire:model, persistés en DB après validation Livewire |
| Recherche ILIKE | Input free-form bindé via `'%' . $this->search . '%'` côté query builder Eloquent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Tampering | Recherche ILIKE sur clients | mitigate | Le terme de recherche passe via le 3e arg de `where(DB::raw, 'ILIKE', $bindable)` — binding Eloquent. NE PAS faire `DB::raw("... ILIKE '%$this->search%'")` (anti-pattern RESEARCH §Anti-Patterns). Test ClientSearchTest avec terme `'%; DROP TABLE clients; --'` peut être ajouté (optionnel). |
| T-2-07 | Input Validation | ClientForm / PiscineForm | mitigate | `#[Validate]` attributes sur toutes les propriétés publiques. Validation Laravel native (email, string max, numeric). Pas de bypass possible — `$this->validate()` appelé avant chaque persistence. |
| T-2-08 | Information Disclosure | Liste clients exposée | accept | Toutes les routes /admin/* sont sous middleware `auth` (guard web). Seul Pierre voit la liste. Pas de filtre par tenant — il est seul opérateur. |
| T-2-09 | Cross-tenant leak | piscines.client_id forge | mitigate | PiscineForm utilise `#[Validate('required|integer|exists:clients,id')]` + mount() reçoit `clientId` en route binding (uniquement disponible depuis la fiche client de Pierre). Pas de cross-tenant car solo opérateur. |
</threat_model>

<verification>
1. `./vendor/bin/pest --ci` : suite totale verte (125+ tests)
2. `php artisan route:list --path=admin/clients` : 4 routes nommées
3. Smoke test interactif : login → /admin → cliquer "Clients" sidebar → liste vide + empty → "Ajouter un client" → formulaire → submit → liste avec le nouveau → recherche → ILIKE actif → cliquer le client → fiche show → "Modifier" → formulaire édit avec piscine.
4. Lighthouse mobile audit (optionnel) : touch targets ≥ 44px vérifiés sur l'écran liste.
5. AccessibilityTest reste vert : aria-current sur Clients, aria-disabled sur Passages/Factures/Catalogue.
</verification>

<success_criteria>
- [ ] 3 composants Livewire fonctionnels : ClientIndex (search ILIKE + pagination 25/page), ClientForm (create/edit), PiscineForm (create/edit)
- [ ] Controller fin ClientController avec 4 actions GET (index, create, show, edit)
- [ ] 4 routes `admin.clients.*` actives dans `routes/admin.php`
- [ ] 4 vues admin/clients/*.blade.php + 3 vues livewire/* respectant UI-SPEC (avatar initiales, chip piscine, empty state, recherche sans résultat)
- [ ] Sidebar desktop : item "Clients" basculé en actif (route admin.clients.*), badge "bientôt" retiré
- [ ] Mobile bottom-nav : item "Clients" basculé en actif (route admin.clients.*)
- [ ] Topbar : bouton "Nouveau client" actif sur pages clients, placeholder search mis à jour, bouton "Nouveau passage" reste disabled
- [ ] Empty state "Aucun client pour l'instant" + CTA "Ajouter un client" (UI-SPEC)
- [ ] Recherche sans résultat : "Aucun résultat pour « [terme] »." avec terme exact
- [ ] CLI-01 : create/edit fonctionnels, validation name required
- [ ] CLI-02 : create/edit piscine fonctionnels, equipements en checkboxes group
- [ ] CLI-03 : ILIKE sur name+email+phone+address actif, debounce 300ms, pagination 25/page, tri updated_at DESC
- [ ] 15 nouveaux tests Pest passants, aucune régression Phase 1 / Plan 02-01
- [ ] Touch targets ≥ 44px conservés (steppers, boutons, cartes)
- [ ] `npm run build` exit 0
</success_criteria>

<output>
Créer `.planning/phases/02-mvp-suivi-offline-first/02-02-SUMMARY.md` à la fin du plan.
</output>
