---
phase: 08-vitrine-corrections-retours-pierre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Vitrine/HeroV1Test.php
  - tests/Feature/Vitrine/DepannageRouteTest.php
  - tests/Feature/Vitrine/CallCenterVoixTest.php
  - resources/views/vitrine/partials/hero.blade.php
  - resources/views/vitrine/services/entretien-recurrent.blade.php
  - resources/views/vitrine/services/analyse-eau.blade.php
autonomous: true
requirements:
  - V1

must_haves:
  truths:
    - "Le hero ne contient plus 'ma tournée' ni aucune occurrence de je/ma/mon"
    - "Le hero contient 'notre zone d'intervention' et invite à appeler"
    - "'toute la Martinique' n'apparaît plus sur /services/entretien-recurrent ni /services/analyse-eau"
    - "Les tests HeroV1 passent au vert"
  artifacts:
    - path: "tests/Feature/Vitrine/HeroV1Test.php"
      provides: "Assertions hero 3e personne + absence toute la Martinique"
    - path: "tests/Feature/Vitrine/DepannageRouteTest.php"
      provides: "Stubs RED pour plan 02"
    - path: "tests/Feature/Vitrine/CallCenterVoixTest.php"
      provides: "Stubs RED pour plan 03"
    - path: "resources/views/vitrine/partials/hero.blade.php"
      provides: "Copy hero corrigée (3e personne, zone honnête)"
    - path: "resources/views/vitrine/services/entretien-recurrent.blade.php"
      provides: "Ligne 105 purgée de 'toute la Martinique'"
    - path: "resources/views/vitrine/services/analyse-eau.blade.php"
      provides: "Ligne 117 purgée de 'toute la Martinique'"
  key_links:
    - from: "tests/Feature/Vitrine/HeroV1Test.php"
      to: "resources/views/vitrine/partials/hero.blade.php"
      via: "get('/') response content assertions"
      pattern: "assertDontSee.*tournée|assertSee.*zone"
---

<objective>
Wave 0 test stubs + V1 geo honest corrections.

Purpose: Create the failing test stubs that gate plans 02/03/04, then immediately make HeroV1Test green by correcting the hero copy and purging "toute la Martinique" from two service pages.

Output: 3 test files (HeroV1 green, DepannageRoute and CallCenterVoix RED as stubs), updated hero.blade.php, entretien-recurrent.blade.php, analyse-eau.blade.php.
</objective>

<execution_context>
@/Users/amnesia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amnesia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-RESEARCH.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write failing test stubs for V1, V5, V12/V14</name>
  <files>
    tests/Feature/Vitrine/HeroV1Test.php,
    tests/Feature/Vitrine/DepannageRouteTest.php,
    tests/Feature/Vitrine/CallCenterVoixTest.php
  </files>
  <read_first>
    tests/Feature/HomePageTest.php (structure + assertion patterns to mirror)
    tests/Feature/StaticPagesTest.php (simple get/assertOk pattern)
    tests/Feature/Seo/VitrinePagesTest.php (service page assertion style)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-RESEARCH.md (§Phase Requirements → Test Map)
  </read_first>
  <behavior>
    HeroV1Test (should PASS after Task 2):
    - "hero is in 3rd person: no occurrence of 'ma tournée' in GET /" — assertDontSee('ma tournée', false)
    - "hero contains 'notre zone d'intervention' in GET /" — assertSee('notre zone', false)
    - "hero contains call-to-call invitation in GET /" — assertSee('Un appel suffit', false)
    - "GET /services/entretien-recurrent does not contain 'toute la Martinique'" — assertDontSee('toute la Martinique', false)
    - "GET /services/analyse-eau does not contain 'toute la Martinique'" — assertDontSee('toute la Martinique', false)

    DepannageRouteTest (stub — should FAIL, plan 02 makes green):
    - "GET /services/depannage returns 200" — get('/services/depannage')->assertStatus(200)
    - "depannage page contains h1 with Dépannage" — assertSee('Dépannage', false)
    - "depannage page contains WhatsApp CTA link wa.me/596696940054" — assertSee('wa.me/596696940054', false)
    - "depannage page contains BreadcrumbList JSON-LD" — assertSee('"@type":"BreadcrumbList"', false)
    - "depannage card in services-grid links to /services/depannage" — get('/services')->assertSee(route('services.depannage'), false)
    - "sitemap.xml includes /services/depannage" — get('/sitemap.xml')->assertSee('/services/depannage', false)

    CallCenterVoixTest (stub — should FAIL, plan 03 makes green):
    - "home call-center occurrences ≤ 2" — count occurrences of 'call-center|centre d\'appel|standard' in GET / response ≤ 2 (use substr_count)
    - "home contains Notre approche section heading" — assertSee('Notre approche', false)
    - "home does not contain Nos engagements heading" — assertDontSee('Nos engagements', false)
  </behavior>
  <action>
    Create the three test files under tests/Feature/Vitrine/ (create the directory if absent — no analog exists there yet).

    Use Pest PHP 4 syntax: `it('...', function () { ... });` with the `use function Pest\Laravel\get;` import at the top (or `$this->get()` if using class-based — follow the style of HomePageTest.php which uses `get()`).

    HeroV1Test.php: all 5 assertions listed in behavior. These must pass once Task 2 is done — write them to FAIL against the current (unedited) files.

    DepannageRouteTest.php: 6 assertions as stubs. The route does not exist yet → the 200 assertion will fail. The sitemap assertion uses `get('/sitemap.xml')`.

    CallCenterVoixTest.php: 3 assertions. "Notre approche" does not exist yet — this stub will fail. For the count assertion: `expect(substr_count($response->getContent(), 'call-center') + substr_count($response->getContent(), 'centre d\'appel') + substr_count($response->getContent(), 'standard'))->toBeLessThanOrEqual(2)`.

    Do NOT create a ServicesPageTest — StaticPagesTest.php already covers GET /services 200 at line 10-13.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Vitrine/HeroV1Test.php tests/Feature/Vitrine/DepannageRouteTest.php tests/Feature/Vitrine/CallCenterVoixTest.php --stop-on-failure 2>&1 | tail -20</automated>
    Expected: HeroV1Test fails (5 tests RED), DepannageRouteTest fails (6 tests RED), CallCenterVoixTest fails (3 tests RED). All 14 tests discovered and running.
  </verify>
  <done>3 test files exist under tests/Feature/Vitrine/; all tests run and fail for the right reasons (missing route 404, existing copy not matching assertions)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: V1 — Hero 3rd person + purge "toute la Martinique"</name>
  <files>
    resources/views/vitrine/partials/hero.blade.php,
    resources/views/vitrine/services/entretien-recurrent.blade.php,
    resources/views/vitrine/services/analyse-eau.blade.php
  </files>
  <read_first>
    resources/views/vitrine/partials/hero.blade.php (full file — current copy at line 22)
    resources/views/vitrine/services/entretien-recurrent.blade.php (line 105 context)
    resources/views/vitrine/services/analyse-eau.blade.php (line 117 context)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md (§hero.blade.php, §entretien-recurrent, §analyse-eau)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    hero.blade.php — Replace the paragraph at line 22 (inside `<p class="mt-5 text-lg sm:text-xl text-navy-100 max-w-xl leading-relaxed">`).

    Current: "Entretien régulier, dépannage et analyse de l'eau. Un service à taille humaine, du nord-atlantique au centre de la Martinique. Un appel suffit pour voir si votre piscine entre dans ma tournée."

    Replace with (per D-01/D-02/D-03): "Entretien régulier, dépannage et analyse de l'eau. Un service à taille humaine dans notre zone d'intervention. Un appel suffit pour voir si votre piscine entre dans notre zone."

    Rules:
    - No "je/ma/mon" anywhere in the paragraph (per D-01)
    - No named communes in hero (per D-02) — the zone pages handle that detail
    - Preserve "Un appel suffit" invitation (per D-03)
    - Do not touch any other line of hero.blade.php

    entretien-recurrent.blade.php — Replace text at line 105 (inside `<p class="text-ink-700 leading-relaxed max-w-[65ch]">`).

    Current: "Un service client réactif, professionnel et personnalisé. Interventions sur toute la Martinique : Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets et communes alentour."

    Replace with (per D-04): "Un service client réactif, professionnel et personnalisé. Interventions dans notre zone d'intervention — du corridor atlantique au corridor caraïbe."

    analyse-eau.blade.php — Replace text at line 117 (inside CTA band `<p class="mt-4 text-lg text-azure-50 max-w-xl mx-auto">`).

    Current: "Réservez une analyse professionnelle : intervention rapide sur toute la Martinique."

    Replace with (per D-04): "Réservez une analyse professionnelle : intervention rapide dans notre zone d'intervention."

    Do not touch any other lines in either file.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Vitrine/HeroV1Test.php -x 2>&1 | tail -15</automated>
    Expected: 5 tests GREEN. No "ma tournée" in hero response; "notre zone" present; "Un appel suffit" present; no "toute la Martinique" on entretien-recurrent or analyse-eau.
  </verify>
  <done>HeroV1Test: 5/5 green. The word "tournée" no longer appears in hero.blade.php line 22. The phrase "toute la Martinique" no longer appears in entretien-recurrent.blade.php line 105 nor analyse-eau.blade.php line 117.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public GET routes | Unauthenticated visitors; all routes in this plan are static read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | hero.blade.php | accept | Static copy edit; no PII, no credentials |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
Run full phase gate after plan 01:

```
./vendor/bin/pest tests/Feature/Vitrine/HeroV1Test.php -x
./vendor/bin/pest --filter='StaticPages|HomePage' --stop-on-failure
```

HeroV1Test must be 5/5 green. StaticPages and HomePage suite must remain green (no regressions from hero copy edit).
</verification>

<success_criteria>
- tests/Feature/Vitrine/HeroV1Test.php: 5 tests green
- tests/Feature/Vitrine/DepannageRouteTest.php: 6 tests exist and fail (route not yet created)
- tests/Feature/Vitrine/CallCenterVoixTest.php: 3 tests exist and fail ("Notre approche" not yet present)
- GET /: response does not contain "ma tournée", contains "notre zone"
- GET /services/entretien-recurrent: response does not contain "toute la Martinique"
- GET /services/analyse-eau: response does not contain "toute la Martinique"
- Existing test suites (StaticPages, HomePage) remain green
</success_criteria>

<output>
Create `.planning/phases/08-vitrine-corrections-retours-pierre/08-01-SUMMARY.md` when done
</output>
