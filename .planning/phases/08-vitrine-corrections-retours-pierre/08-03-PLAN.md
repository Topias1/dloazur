---
phase: 08-vitrine-corrections-retours-pierre
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - resources/views/vitrine/partials/philosophie.blade.php
  - resources/views/vitrine/partials/engagements.blade.php
  - resources/views/vitrine/partials/pierre.blade.php
  - resources/views/vitrine/partials/final-cta.blade.php
  - resources/views/vitrine/partials/services-detail.blade.php
  - resources/views/vitrine/home.blade.php
  - tests/Feature/HomePageTest.php
autonomous: true
requirements:
  - V12
  - V14

must_haves:
  truths:
    - "La home contient une section intitulée 'Notre approche' (fusion philosophie+engagements)"
    - "La home ne contient plus 'Nos engagements' comme titre de section"
    - "Le total des occurrences de 'call-center', 'centre d'appel', 'standard' dans le rendu de GET / est ≤ 2"
    - "Aucune occurrence mot-à-mot de 'interlocuteur unique' entre les sections fusionnées et la section pierre"
    - "CallCenterVoixTest: 3/3 tests green"
  artifacts:
    - path: "resources/views/vitrine/partials/philosophie.blade.php"
      provides: "Section fusionnée 'Notre approche' (anciens philosophie + engagements)"
      contains: "Notre approche"
    - path: "resources/views/vitrine/partials/engagements.blade.php"
      provides: "Fichier vidé — include retiré de home.blade.php"
    - path: "resources/views/vitrine/partials/pierre.blade.php"
      provides: "Mention call-center tournée positif (ligne 19 sans 'Pas de centre d\\'appel')"
    - path: "resources/views/vitrine/partials/final-cta.blade.php"
      provides: "Mention call-center tournée positif (ligne 11 sans 'jamais à un standard')"
    - path: "resources/views/vitrine/partials/services-detail.blade.php"
      provides: "Mention call-center supprimée (ligne 202 sans 'sans standard téléphonique')"
  key_links:
    - from: "resources/views/vitrine/home.blade.php"
      to: "resources/views/vitrine/partials/philosophie.blade.php"
      via: "@include('vitrine.partials.philosophie') — kept; @include('vitrine.partials.engagements') — removed"
      pattern: "@include.*philosophie"
---

<objective>
V12/V14 — Brand voice consolidation: fuse philosophie+engagements into "Notre approche", reduce call-center mentions to ≤2 positive occurrences, eliminate "interlocuteur unique" duplication.

Purpose: Pierre's feedback identified 5 occurrences of defensive "pas de call-center / standard" language and semantic repetition of "interlocuteur unique" across 3 sections. This plan collapses both issues: one merged section with a fresh angle, and the remaining two call-center mentions reframed as affirmative proximity statements.

Output: philosophie.blade.php rewritten as "Notre approche" (absorbs engagements), engagements.blade.php emptied, 3 other partials with targeted copy edits, home.blade.php with one @include removed, HomePageTest updated for the new section name.
</objective>

<execution_context>
@/Users/amnesia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amnesia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-RESEARCH.md

@.planning/phases/08-vitrine-corrections-retours-pierre/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns extracted for executor — do not re-read these sections, use excerpts below -->

From resources/views/vitrine/partials/philosophie.blade.php (current structure, 41 lines):
  - Outer section: `<section class="bg-lagon-50/40 border-y border-navy-900/8 ...">` — KEEP this wrapper
  - Line 24 contains: "Un seul interlocuteur qui connaît votre bassin, pas de sous-traitance" — REPLACE

From resources/views/vitrine/partials/engagements.blade.php (current structure, 49 lines):
  - 4-card grid: `grid sm:grid-cols-2 lg:grid-cols-4 gap-5`
  - Card structure with `rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-sm p-7`
  - Line 25: "Vous tombez sur Pierre, jamais sur un standard." — REFORMULATE
  - Line 44: card title "Toujours le même interlocuteur" — RENAME

From resources/views/vitrine/home.blade.php (current includes around lines 15 and 31):
  - Line ~15: @include('vitrine.partials.philosophie')   ← KEEP
  - Line ~31: @include('vitrine.partials.engagements')   ← REMOVE

From tests/Feature/HomePageTest.php (line 140–142):
  - it('home renders Nos engagements section before final CTA (D-35)', ...) { assertSeeText('Nos engagements') }
  ← This test must be updated to assert "Notre approche" instead

Call-center / interlocuteur changes table (from 08-RESEARCH.md §Code Examples):
  - pierre.blade.php line 19: remove "Pas de centre d'appel, pas de sous-traitance :" — keep the rest
  - final-cta.blade.php line 11: replace "jamais à un standard" with "sans intermédiaire"
  - engagements.blade.php line 25: replace "jamais sur un standard" with "Vous parlez directement à Pierre."
  - philosophie.blade.php line 24: reformulate without negation
  - services-detail.blade.php line 202: replace "sans standard téléphonique ni rotation d'interlocuteurs" with "avec un compte-rendu après chaque intervention"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fuse philosophie + engagements → "Notre approche"</name>
  <files>
    resources/views/vitrine/partials/philosophie.blade.php,
    resources/views/vitrine/partials/engagements.blade.php,
    resources/views/vitrine/home.blade.php
  </files>
  <read_first>
    resources/views/vitrine/partials/philosophie.blade.php (full file — current 41 lines)
    resources/views/vitrine/partials/engagements.blade.php (full file — current 49 lines with card structure)
    resources/views/vitrine/home.blade.php (full file — locate the two @include lines for philosophie and engagements)
    tests/Feature/HomePageTest.php (lines 140–145 — the "Nos engagements" assertion to update)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md (§philosophie.blade.php, §engagements.blade.php, §home.blade.php)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <action>
    philosophie.blade.php — Rewrite as the merged "Notre approche" section (Option A per RESEARCH.md §Pattern 3). Keep the outer section element with its existing background classes (`bg-lagon-50/40 border-y border-navy-900/8` or whatever classes the current file uses — do not change section wrapper classes).

    Structure of the rewritten file:
    1. Outer section wrapper (keep classes from current philosophie.blade.php)
    2. Inner container `mx-auto max-w-content px-5 sm:px-8 py-16 sm:py-20`
    3. Section header:
       `<h2 class="font-display font-bold text-3xl sm:text-4xl text-ink-950 text-center mb-3">Notre approche</h2>`
       Sub: `<p class="text-center mt-2 mb-12 text-lg text-ink-700 max-w-xl mx-auto">Un entretien suivi, un interlocuteur direct, un compte-rendu après chaque passage.</p>`
    4. 4-card grid (absorb from engagements.blade.php — keep the card HTML structure `rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-sm p-7 hover:shadow-md hover:-translate-y-0.5 transition duration-300 flex flex-col gap-4`):
       Card 1 — icon.shield: "Eau toujours conforme" / corps: same as current engagements card 1 (keep if no call-center language)
       Card 2 — icon.whatsapp or similar: "Joignable sur WhatsApp" / corps: reformulate from "Vous tombez sur Pierre, jamais sur un standard." → "Vous parlez directement à Pierre — un message suffit." (per D-10, positive only)
       Card 3 — icon of your choice: "Compte-rendu après chaque passage" / corps: "Mesures, actions, photos quand c'est utile. Votre historique est accessible en ligne à tout moment." (new angle per D-11 — not "interlocuteur unique")
       Card 4 — icon.check or similar: "Même prestataire à chaque visite" / corps: "Pierre connaît votre bassin, vos équipements, et les ajustements déjà faits." (angle: continuité + connaissance, pas "interlocuteur unique" verbatim)

    Rules for the new section:
    - The string "interlocuteur unique" must NOT appear anywhere in the section (per D-11)
    - The strings "jamais", "standard", "call-center", "centre d'appel", "sous-traitance" must NOT appear (per D-10)
    - Section heading must be exactly "Notre approche" (CallCenterVoixTest and HomePageTest will assert this)
    - Voix 3e personne, vouvoiement côté client

    engagements.blade.php — After the content has been merged into philosophie.blade.php, empty this file to just a Blade comment: `{{-- Contenu fusionné dans partials/philosophie.blade.php (Phase 8 — Notre approche) --}}`. Do NOT delete the file (home.blade.php @include will be removed below, but keeping the file avoids any stray include on other pages causing a 500).

    home.blade.php — Find the line `@include('vitrine.partials.engagements')` (currently around line 31) and remove it. The line `@include('vitrine.partials.philosophie')` (around line 15) stays. Preserve the positions of all other @includes — do not reorder sections (per Pitfall 7 in RESEARCH.md).

    tests/Feature/HomePageTest.php — Find the test "home renders Nos engagements section before final CTA (D-35)" (lines 140–142) and update:
    - Change the test description to: `'home renders Notre approche section (D-09 fusion — Phase 8)'`
    - Change `$response->assertSeeText('Nos engagements')` to `$response->assertSeeText('Notre approche')`
    - Keep the test function structure unchanged
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/HomePageTest.php --filter="Notre approche" -x 2>&1 | tail -10</automated>
    Expected: 1 test green — home contains "Notre approche" text.
    Also run: ./vendor/bin/pest tests/Feature/HomePageTest.php -x 2>&1 | tail -5
    Expected: full HomePageTest suite green (updating the assertion above means no regression from the section rename).
  </verify>
  <done>GET / response contains "Notre approche"; does not contain "Nos engagements" as section heading; "interlocuteur unique" not present in the merged section; "jamais"/"standard" not present in the merged section. HomePageTest fully green.</done>
</task>

<task type="auto">
  <name>Task 2: Purge call-center language from pierre, final-cta, services-detail</name>
  <files>
    resources/views/vitrine/partials/pierre.blade.php,
    resources/views/vitrine/partials/final-cta.blade.php,
    resources/views/vitrine/partials/services-detail.blade.php
  </files>
  <read_first>
    resources/views/vitrine/partials/pierre.blade.php (full file — line 19 and pull-quote at line 21)
    resources/views/vitrine/partials/final-cta.blade.php (full file — line 11)
    resources/views/vitrine/partials/services-detail.blade.php (full file — lines 199–208 context around line 202)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md (§pierre.blade.php, §final-cta.blade.php, §services-detail.blade.php)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md (D-10, D-11)
  </read_first>
  <action>
    pierre.blade.php — line 19. Current: "Pas de centre d'appel, pas de sous-traitance : vous échangez directement avec celui qui plonge l'épuisette. C'est ça, un service à taille humaine."
    Replace with (per D-10 CONSERVER — remove only the negative prefix): "Vous échangez directement avec celui qui plonge l'épuisette. C'est ça, un service à taille humaine."
    Note: This keeps the two allowed positive mentions within the ≤2 cap (pierre + final-cta).

    pierre.blade.php — pull-quote at line 21. Current: `<p ...>Un seul interlocuteur,<br>qui connaît votre bassin.</p>`
    Per D-11 and PATTERNS.md §pierre.blade.php: the "Notre approche" section now uses proximity angles. If pierre's pull-quote says "Un seul interlocuteur" while the merged section avoids that phrase, it is acceptable (different section, different angle). However to avoid exact duplication with any card title in "Notre approche" (which uses "Même prestataire" instead), you may keep "Un seul interlocuteur, qui connaît votre bassin." as is — it is biographical context, not a repeated feature claim. Claude's discretion: keep if it reads naturally in the pierre section context, rewrite to "Pierre connaît votre bassin, il passe quand il le faut." if the pull-quote feels redundant after reviewing the full page.

    final-cta.blade.php — line 11. Current: "Devis gratuit et sans engagement. Vous parlez directement à Pierre, jamais à un standard."
    Replace with (per D-10 CONSERVER — turn positive): "Devis gratuit et sans engagement. Vous parlez directement à Pierre, sans intermédiaire."

    services-detail.blade.php — lines ~201–203. Current: "À l'écoute de vos besoins, joignable directement par WhatsApp, sans standard téléphonique ni rotation d'interlocuteurs."
    Replace with (per D-10 SUPPRIMER — positive orthogonal argument): "À l'écoute de vos besoins, joignable directement par WhatsApp, avec un compte-rendu après chaque intervention."

    Do not touch any other lines in these three files.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Vitrine/CallCenterVoixTest.php -x 2>&1 | tail -15</automated>
    Expected: 3/3 tests green — call-center count ≤2, "Notre approche" present (from Task 1), "Nos engagements" absent.
  </verify>
  <done>CallCenterVoixTest 3/3 green. GET / response has ≤2 occurrences of "call-center|centre d'appel|standard". pierre.blade.php line 19 no longer contains "Pas de centre d'appel". final-cta.blade.php line 11 no longer contains "jamais à un standard". services-detail.blade.php line ~202 no longer contains "sans standard téléphonique".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public GET / and GET /services | Unauthenticated visitor; static Blade partials, no input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | philosophie/engagements partials | accept | Static marketing copy; no PII |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
```bash
./vendor/bin/pest tests/Feature/Vitrine/CallCenterVoixTest.php -x
./vendor/bin/pest tests/Feature/HomePageTest.php -x
./vendor/bin/pest --filter='StaticPages' --stop-on-failure
```

CallCenterVoixTest 3/3 green. HomePageTest fully green (updated assertion for "Notre approche"). StaticPages green.
</verification>

<success_criteria>
- GET / contains exactly "Notre approche" as section heading
- GET / does not contain "Nos engagements" as section heading
- Total occurrences of "call-center" + "centre d'appel" + "standard" in GET / response ≤ 2
- pierre.blade.php: line 19 starts with "Vous échangez directement" (no "Pas de centre d'appel" prefix)
- final-cta.blade.php: line 11 ends with "sans intermédiaire" (no "jamais à un standard")
- services-detail.blade.php: the card body contains "compte-rendu après chaque intervention" (no "sans standard téléphonique")
- "interlocuteur unique" does not appear verbatim in philosophie.blade.php
- CallCenterVoixTest: 3/3 green
- HomePageTest: all tests green
</success_criteria>

<output>
Create `.planning/phases/08-vitrine-corrections-retours-pierre/08-03-SUMMARY.md` when done
</output>
