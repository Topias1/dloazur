---
phase: 08-vitrine-corrections-retours-pierre
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - routes/vitrine.php
  - app/Http/Controllers/VitrineController.php
  - resources/views/vitrine/services/depannage.blade.php
  - resources/views/vitrine/partials/services-grid.blade.php
  - app/Http/Controllers/SitemapController.php
autonomous: true
requirements:
  - V5

must_haves:
  truths:
    - "GET /services/depannage retourne 200 avec h1 contenant Dépannage"
    - "La page contient un lien WhatsApp vers wa.me/596696940054"
    - "La page contient un JSON-LD BreadcrumbList avec 3 niveaux (Accueil > Services > Dépannage)"
    - "La carte Dépannage rapide dans services-grid pointe vers route('services.depannage')"
    - "/sitemap.xml liste /services/depannage"
    - "DepannageRouteTest: 6/6 tests green"
  artifacts:
    - path: "resources/views/vitrine/services/depannage.blade.php"
      provides: "Page service légère hero + 4 bullets + CTA WhatsApp"
    - path: "app/Http/Controllers/VitrineController.php"
      provides: "Méthode depannage() avec BreadcrumbSchema"
    - path: "routes/vitrine.php"
      provides: "Route GET /services/depannage nommée services.depannage dans le groupe cache.headers:vitrine"
  key_links:
    - from: "routes/vitrine.php"
      to: "app/Http/Controllers/VitrineController.php"
      via: "Route::get('/services/depannage', [VitrineController::class, 'depannage'])->name('services.depannage')"
      pattern: "services.depannage"
    - from: "resources/views/vitrine/partials/services-grid.blade.php"
      to: "routes/vitrine.php"
      via: "route('services.depannage') in href"
      pattern: "route\\('services\\.depannage'\\)"
---

<objective>
V5 — Create the /services/depannage page and wire it everywhere it needs to be linked.

Purpose: The "Dépannage rapide" card in services-grid currently points to route('services') — a generic fallback. Creating a dedicated page gives the card a proper destination, adds a coherent service detail page, and improves SEO discoverability for "dépannage piscine Martinique".

Output: One new Blade view, one new controller method, one new route, one sitemap entry, one href update in services-grid.
</objective>

<execution_context>
@/Users/amnesia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amnesia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md
@.planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md

@.planning/phases/08-vitrine-corrections-retours-pierre/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns extracted from codebase for executor reference -->

From routes/vitrine.php (line 29 — existing spa route inside cache.headers:vitrine group):
```php
Route::get('/services/spa', [VitrineController::class, 'spa'])->name('services.spa');
```

From app/Http/Controllers/VitrineController.php (lines 133–146 — spa() method to copy):
```php
public function spa(BreadcrumbSchema $breadcrumb): View
{
    return view('vitrine.services.spa', [
        'title'            => 'Entretien de spa en Martinique · Dlo Azur Piscines',
        'description'      => 'Entretien et dépannage de spa et jacuzzi en Martinique...',
        'canonical'        => url('/services/spa'),
        'ogImage'          => asset('assets/brand/og-default.jpg'),
        'breadcrumbJsonLd' => $breadcrumb->toScript([
            ['name' => 'Accueil', 'url' => url('/')],
            ['name' => 'Services','url' => url('/services')],
            ['name' => 'Spa',     'url' => url('/services/spa')],
        ]),
    ]);
}
```

From app/Http/Controllers/SitemapController.php (lines 22–26 — pattern to mirror):
```php
->add(Url::create(route('services.spa'))->setPriority(0.8)->setLastModificationDate(now()))
```

From resources/views/vitrine/partials/services-grid.blade.php (line 29 — href to update):
```blade
<a href="{{ route('services') }}" class="group rounded-3xl bg-white ...">
    ...
    <h3 class="font-display font-semibold text-xl text-ink-950">Dépannage rapide</h3>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Route + controller method for /services/depannage</name>
  <files>
    routes/vitrine.php,
    app/Http/Controllers/VitrineController.php
  </files>
  <read_first>
    routes/vitrine.php (full file — locate cache.headers:vitrine group, find line 29 spa route)
    app/Http/Controllers/VitrineController.php (locate spa() method at lines 133–146; check imports at top — BreadcrumbSchema already imported)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md (§VitrineController and §routes/vitrine.php)
  </read_first>
  <action>
    routes/vitrine.php — Inside the `Route::middleware('cache.headers:vitrine')->group(function () { ... })` block, insert after the spa route line (currently around line 29):
      Route::get('/services/depannage', [VitrineController::class, 'depannage'])->name('services.depannage');

    app/Http/Controllers/VitrineController.php — Add a new public method `depannage(BreadcrumbSchema $breadcrumb): View` after the `spa()` method. Copy the spa() structure exactly, changing:
    - Method name: `depannage`
    - View: `vitrine.services.depannage`
    - title: `'Dépannage piscine en Martinique · Dlo Azur Piscines'`
    - description: `'Panne de pompe, filtration HS, eau trouble : dépannage rapide en Martinique. Contactez Dlo Azur sur WhatsApp pour une intervention le jour même.'`
    - canonical: `url('/services/depannage')`
    - Breadcrumb 3rd item: `['name' => 'Dépannage', 'url' => url('/services/depannage')]`
    - ogImage stays `asset('assets/brand/og-default.jpg')` (same as all other service pages)

    The view does not exist yet (created in Task 2) — Laravel will 404 until Task 2 is done, which is acceptable mid-task.
  </action>
  <verify>
    <automated>php artisan route:list --name=services.depannage 2>&1</automated>
    Expected: one line showing `GET /services/depannage` bound to `VitrineController@depannage`, name `services.depannage`.
  </verify>
  <done>Route `services.depannage` listed; GET /services/depannage resolves to VitrineController::depannage (view 404 until Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Depannage Blade view + services-grid link + sitemap entry</name>
  <files>
    resources/views/vitrine/services/depannage.blade.php,
    resources/views/vitrine/partials/services-grid.blade.php,
    app/Http/Controllers/SitemapController.php
  </files>
  <read_first>
    resources/views/vitrine/services/spa.blade.php (gabarit exact — full file)
    resources/views/vitrine/partials/services-grid.blade.php (line 29 — the Dépannage card href to update)
    app/Http/Controllers/SitemapController.php (lines 20–26 — Url::create pattern)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-PATTERNS.md (§depannage.blade.php — full section with code excerpts)
    .planning/phases/08-vitrine-corrections-retours-pierre/08-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <action>
    depannage.blade.php — Create the file by following the spa.blade.php gabarit (light structure: NOT entretien-recurrent, per D-05). Structure:

    1. `@extends('layouts.app')` + `@section('content')` + outer div `pt-32 pb-0`
    2. Hero band `<section class="bg-navy-900 pt-16 pb-12 md:pt-24 md:pb-16">`:
       - Eyebrow: `<p class="text-xs font-bold uppercase tracking-[0.18em] text-lagon-400 mb-3">DÉPANNAGE PISCINE</p>`
       - H1: "Dépannage piscine<br class='hidden sm:block'> en Martinique" (font-display bold clamp)
       - Sub paragraph: "Panne de pompe, filtration hors service, eau trouble incontrôlable : Pierre intervient rapidement dans notre zone d'intervention. Un appel et il évalue la situation avec vous."
       - Primary CTA (per D-06 — WhatsApp is the main CTA): `<a href="https://wa.me/596696940054" ...>` using the WhatsApp CTA pattern from spa.blade.php lines 20–24 (green bg-[#25D366], icon.whatsapp :size="20", text "Appeler Pierre sur WhatsApp")
       - Breadcrumb nav under CTAs: 3-level Accueil › Services › Dépannage (pattern from 08-PATTERNS.md §breadcrumb)
       - JSON-LD: `{!! $breadcrumbJsonLd !!}` (passed from controller — output in hero band or just before @endsection, consistent with other service pages)
    3. Content body `<section class="mx-auto max-w-3xl px-5 sm:px-8 py-16">`:
       - H2 "Types de pannes fréquentes" (font-display semibold text-2xl text-ink-950)
       - Intro sentence (1 ligne)
       - 4 bullets `<ul class="space-y-2 text-ink-700 leading-relaxed max-w-[65ch]">` with ✓ azure-500 icons (per D-07 Claude's discretion — pannes courantes Martinique):
         a. "Pompe bloquée ou bruyante (surchauffe, roulement usé)"
         b. "Filtration hors service (pression anormale, sable ou cartouche colmaté)"
         c. "Eau verte incontrôlable malgré un traitement récent"
         d. "Fuite visible sur le circuit hydraulique ou la margelle"
    4. CTA band `<section class="relative bg-navy-800 text-white overflow-hidden">`:
       - H2 "Une panne ? Pierre est joignable directement."
       - Sub: "Pas d'intermédiaire, pas de ticket — un appel ou un message WhatsApp suffit."
       - Primary CTA: WhatsApp `href="https://wa.me/596696940054"` with phone number "0696 94 00 54" (larger CTA h-14 px-7 pattern from spa.blade.php lines 121–125)
       - Secondary CTA: `href="{{ route('contact') }}"` "Devis gratuit" (azure-500 style)
    5. `@endsection`

    Voix marque: 3e personne (Pierre/Dlo Azur), vouvoiement côté client. No "je/ma/mon". No "toute la Martinique".

    services-grid.blade.php — On the "Dépannage rapide" card (line 29), change:
      `href="{{ route('services') }}"` → `href="{{ route('services.depannage') }}"`
    Do not touch any other card or any other attribute on that element.

    SitemapController.php — After the line `->add(Url::create(route('services.spa'))->setPriority(0.8)->setLastModificationDate(now()))`, insert:
      `->add(Url::create(route('services.depannage'))->setPriority(0.8)->setLastModificationDate(now()))`
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Vitrine/DepannageRouteTest.php -x 2>&1 | tail -15</automated>
    Expected: 6/6 tests green. GET /services/depannage returns 200; h1 contains "Dépannage"; wa.me/596696940054 present; BreadcrumbList JSON-LD present; services-grid card links to /services/depannage; sitemap.xml contains /services/depannage.
  </verify>
  <done>DepannageRouteTest 6/6 green. GET /services/depannage returns 200. /sitemap.xml contains /services/depannage entry. GET /services shows Dépannage card linking to /services/depannage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public GET /services/depannage | Unauthenticated visitor; static page, no input processing |
| sitemap.xml | Public, read-only — route names only, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | depannage.blade.php | accept | Static marketing page; no PII, no auth tokens |
| T-08-03 | Spoofing | WhatsApp CTA | accept | External URL hardcoded (`wa.me/596696940054`); no SSRF vector (no server-side fetch) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
```bash
./vendor/bin/pest tests/Feature/Vitrine/DepannageRouteTest.php -x
./vendor/bin/pest --filter='StaticPages|VitrinePage' --stop-on-failure
php artisan route:list --name=services.depannage
```

All 6 DepannageRoute tests green. StaticPages and VitrinePage suites remain green (no regressions).
</verification>

<success_criteria>
- GET /services/depannage returns 200
- Page h1 contains "Dépannage" (case-sensitive Blade output)
- Page body contains `wa.me/596696940054` (primary CTA per D-06)
- Page JSON-LD contains `"@type":"BreadcrumbList"` with 3 items
- GET /services shows "Dépannage rapide" card with href pointing to route('services.depannage')
- GET /sitemap.xml body contains `/services/depannage`
- DepannageRouteTest: 6/6 green
- No regressions in StaticPagesTest or VitrinePagesTest
</success_criteria>

<output>
Create `.planning/phases/08-vitrine-corrections-retours-pierre/08-02-SUMMARY.md` when done
</output>
