---
phase: 11-audit-impeccable-ux-ui-wording
plan: 06
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - resources/views/admin/dashboard.blade.php
  - resources/views/layouts/admin.blade.php
  - resources/views/components/admin/sidebar.blade.php
  - resources/views/components/admin/mobile-bottom-nav.blade.php
  - resources/views/components/admin/topbar.blade.php
  - resources/views/admin/clients/show.blade.php
  - resources/views/admin/clients/edit.blade.php
  - resources/views/livewire/client-index.blade.php
  - resources/views/livewire/post-index.blade.php
  - resources/views/livewire/client-form.blade.php
  - resources/views/livewire/piscine-form.blade.php
  - resources/views/livewire/post-form.blade.php
autonomous: true
requirements: [D-10, P2-LANDMARKS, P2-RECAP-NAV, P2-CLIENT-HISTORY, P2-BOTTOMNAV-BLOG, P1-LOADING-STATES, P1-CLIENT-EDIT-TITLE, D-07-EMPTYSTATES, P3-TOPBAR-SEARCH, P3-EMDASH-TITLE, P3-INLINE-SUCCESS]

must_haves:
  truths:
    - "Dashboard leads with the agenda du jour; warn/offline cards are clickable; vanity counts demoted; the 4-identical-card grid is broken (D-10, P2)"
    - "Admin layout has no nested complementary/banner landmarks and no dead empty <nav> (P2)"
    - "Live-search lists show a loading state instead of freezing silently (SC-6, P1)"
    - "Operator empty-states use tu register (D-07); client-facing copy untouched (D-08)"
    - "Recap mensuel is reachable from the sidebar nav (P2)"
    - "client/piscine/post forms give an inline success acknowledgement, not only a silent redirect (P3)"
  artifacts:
    - path: "resources/views/admin/dashboard.blade.php"
      provides: "agenda-led dashboard with clickable actionable cards + demoted vanity strip"
      contains: "recap.index"
  key_links:
    - from: "resources/views/admin/dashboard.blade.php actionable cards"
      to: "passages.index filtered"
      via: "<a> / wire:navigate wrappers"
      pattern: "route\\('admin.passages"
    - from: "resources/views/components/admin/sidebar.blade.php"
      to: "route('admin.recap.index')"
      via: "new Recap nav item"
      pattern: "recap.index"
---

<objective>
Admin structure remediation (P2 layout/IA + the P1 loading-states and client-edit-title
findings + D-07 operator empty-states + P3 inline form success). Restructures the dashboard
per D-10 (agenda hoisted, actionable cards clickable, vanity demoted, grid uniformity broken),
fixes the nested landmarks + dead nav in the admin layout, adds Recap to nav, fixes the
bottom-nav Blog/Factures slot, adds live-search loading states, fixes the literal-interpolation
client-edit title, aligns operator empty-states to tu, and adds inline success feedback to the
admin forms. Wave 2 because it co-edits layouts/admin.blade.php with Plan 11-01 (sync-drawer
mount) - sequenced to avoid conflict.

Purpose: SC-6 (loading states) + D-10 dashboard usefulness + admin a11y.
Output: A dashboard Pierre can act from, with consistent IA and register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-FINDINGS.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-UI-SPEC.md
@.claude/skills/dloazur-frontend-stack/SKILL.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-01-SUMMARY.md

<interfaces>
- Recap route name: admin.recap.index (routes/admin.php:50). Confirmed live.
- Agenda du jour block to hoist lives in resources/views/admin/agenda/index.blade.php
  lines 24-50 ("Aujourd'hui") + 54-86 ("A revoir"). Reuse via @include partial or duplicate
  the markup; do NOT create a new route/model (Mode: quick).
- Operator empty-states with "votre": client-index.blade.php:72, post-index.blade.php:78.
- Sidebar has 6 items (incl. Blog active, Factures greyed). mobile-bottom-nav has grid-cols-5
  with Factures greyed and NO Blog. Desktop badge sidebar.blade.php:93 uses aria-live=polite;
  mobile badge mobile-bottom-nav.blade.php:55 is aria-hidden.
- layouts/admin.blade.php: outer <aside> (L18) wraps @yield('sidebar') whose component is
  itself an <aside>; outer <header> wraps topbar which is a banner; dead empty <nav> at L35.
  NOTE: Plan 11-01 mounts <x-admin.sync-drawer> in this file - preserve that mount.
- client-form/piscine-form/post-form currently rely on redirect with no inline success toast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard restructure (D-10) + admin layout landmarks + Recap nav + bottom-nav</name>
  <files>resources/views/admin/dashboard.blade.php, resources/views/layouts/admin.blade.php, resources/views/components/admin/sidebar.blade.php, resources/views/components/admin/mobile-bottom-nav.blade.php</files>
  <read_first>
    - resources/views/admin/dashboard.blade.php (full, esp. 30-49 the 4 stat cards)
    - resources/views/admin/agenda/index.blade.php (lines 24-86, the agenda du jour blocks to hoist)
    - resources/views/layouts/admin.blade.php (full, incl. the sync-drawer mount added by Plan 11-01; L18-20 aside, L25-27 header, L35 dead nav)
    - resources/views/components/admin/sidebar.blade.php (item list + badge aria at :93)
    - resources/views/components/admin/mobile-bottom-nav.blade.php (grid-cols-5, Factures greyed, badge aria at :55)
    - 11-UI-SPEC.md sections "Admin dashboard restructure", "A11y fixes" (landmarks), "Bottom-nav mobile", "Recap nav"
    - 11-FINDINGS.md P2 dashboard, P2 landmarks, P2 recap orphelin, P2 bottom-nav
  </read_first>
  <action>
    1. D-10 dashboard (UI-SPEC hierarchy):
       (a) Hoist the agenda du jour ("Aujourd'hui" + "A revoir") to the TOP of dashboard.blade.php
           - full/2-3 width, visually dominant, distinct surface (e.g. navy-900 or prominent border).
           Reuse the agenda markup via an @include partial or inline; no new route/model.
       (b) Actionable cards row: "A synchroniser" (offline pending count) clickable ->
           route('admin.passages.index') filtered status=pending; "Eau a surveiller" clickable ->
           passages.index filtered needs_attention (use existing filter param). Use <a>/wire:navigate
           wrappers (not decorative). cursor-pointer + hover:shadow-md + a chevron/"Voir" affordance.
           Zero count -> success/neutral; non-zero -> warn-bg/warn ring. Copy per UI-SPEC "Key CTA labels".
       (c) Demote vanity: "Clients actifs" + "Passages cette semaine" -> compact text-ink-500 text-sm
           strip (or remove). NOT the same weight as actionable cards.
       (d) Break the 4-identical-card grid (D-10d): agenda full width; 2 actionable cards in a 2-col
           grid; vanity as a text strip. No four identical cards.
    2. P2 landmarks (UI-SPEC A11y): layouts/admin.blade.php - change outer <aside> (L18-20) and
       outer <header> (L25-27) wrappers to <div> (the included components own the landmark). Remove
       the dead empty <nav> at L35. Preserve the Plan 11-01 sync-drawer mount.
    3. P2 Recap nav (UI-SPEC): add a "Recap" item to sidebar.blade.php -> route('admin.recap.index').
    4. P2 bottom-nav (UI-SPEC): mobile-bottom-nav.blade.php - remove greyed "Factures"; replace with
       "Blog" (-> route('admin.blog.index')) or drop to grid-cols-4; add aria-live="polite" + visible
       label to the mobile sync badge (:55) mirroring the desktop sidebar badge (:93).
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && grep -q "recap.index" resources/views/components/admin/sidebar.blade.php && grep -q "passages.index" resources/views/admin/dashboard.blade.php && ! grep -q "<aside" resources/views/layouts/admin.blade.php && grep -q "x-admin.sync-drawer" resources/views/layouts/admin.blade.php && grep -q 'aria-live="polite"' resources/views/components/admin/mobile-bottom-nav.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.blade.php leads with the agenda du jour block (Aujourd'hui + A revoir), visually distinct/full-width.
    - "A synchroniser" and "Eau a surveiller" cards are wrapped in <a>/wire:navigate to filtered passages.index, with cursor-pointer + hover affordance + chevron/"Voir".
    - Vanity counts (Clients actifs, Passages cette semaine) are a demoted text-sm strip or removed; not four identical cards.
    - layouts/admin.blade.php outer wrappers are <div> (no <aside>/<header> wrapping the components); no dead empty <nav>; sync-drawer mount from Plan 11-01 still present.
    - sidebar has a Recap item -> route('admin.recap.index').
    - mobile-bottom-nav has no greyed Factures; sync badge has aria-live="polite" + a label.
  </acceptance_criteria>
  <done>Dashboard is agenda-led and actionable; landmarks clean; Recap reachable; bottom-nav fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Live-search loading + client-edit title + client history + operator empty-states (tu) + topbar search + inline form success</name>
  <files>resources/views/livewire/client-index.blade.php, resources/views/livewire/post-index.blade.php, resources/views/admin/clients/edit.blade.php, resources/views/admin/clients/show.blade.php, resources/views/components/admin/topbar.blade.php, resources/views/livewire/client-form.blade.php, resources/views/livewire/piscine-form.blade.php, resources/views/livewire/post-form.blade.php</files>
  <read_first>
    - resources/views/livewire/client-index.blade.php (lines 22-28 list container, 72 empty-state "votre premier client")
    - resources/views/livewire/post-index.blade.php (lines 22-28 list container, 78 empty-state "votre premier article")
    - resources/views/admin/clients/edit.blade.php (line 3 literal {{ }} in @section title + em-dash)
    - resources/views/admin/clients/show.blade.php (lines 111-117 history rows, intra-card paginate(10))
    - resources/views/components/admin/topbar.blade.php (lines 28-31 disabled search input)
    - resources/views/livewire/client-form.blade.php, piscine-form.blade.php, post-form.blade.php (current redirect-only success path)
    - 11-UI-SPEC.md sections "Loading states", "Typography" (@section title fix), "Topbar search", "Operator-facing copy fixes (D-07)", "Em-dashes prose"
    - 11-FINDINGS.md P1 loading, P1 client edit title, P2 client history, P3 topbar search, P1/P3 tu-vous, P3 "Composant-state : pas de succes inline"
  </read_first>
  <action>
    1. P1 loading (UI-SPEC "Loading states"): on client-index.blade.php:22-28 and
       post-index.blade.php:22-28, add to the list container
       wire:loading.class="opacity-50 pointer-events-none" wire:target="search", plus a skeleton row
       (wire:loading wire:target="search", class "h-12 bg-sand-100 rounded-xl animate-pulse",
       aria-hidden="true"). bg-sand-100 not white; no spinner glyph.
    2. P1 client-edit title (UI-SPEC Typography): clients/edit.blade.php:3 currently
       @section('title', '{{ $client->name }} - Modifier ...') renders literally. Change to
       @section('title', $client->name . ' · Modifier · Dlo Azur') (concatenation, no {{ }}; also
       drops the prose em-dash for · per P3).
    3. P2 client history (UI-SPEC + FINDINGS): clients/show.blade.php:111-117 - make rows clickable
       to the passage detail, add a 1-line summary (chlore/pH/actions), and replace the intra-card
       ->paginate(10) controls with "N derniers + voir tout" (or move the full history out of the
       card) so pagination no longer reloads the whole client page mid-card.
    4. D-07 operator empty-states (UI-SPEC): client-index.blade.php:72 "votre premier client" ->
       "ton premier client"; post-index.blade.php:78 "votre premier article" -> "ton premier
       article". Operator-facing only. Do NOT touch any client-facing copy.
    5. P3 topbar search (UI-SPEC): topbar.blade.php:28-31 disabled search - hide it except on routes
       where wired (clients.index, passages.index, posts.index); elsewhere hide or render as a link
       to clients.index?search=.
    6. P3 inline form success (FINDINGS "Composant-state : pas de succes inline"): client-form,
       piscine-form, post-form rely on redirect only. Add a calm inline success acknowledgement
       (token success, e.g. a flash/session('status') banner or a Livewire-dispatched inline note)
       on save before/at redirect, so the operator gets positive feedback (Nielsen #1). Operator
       register = tu (D-07). Keep it calm (success token, not assertive). No new route/model.
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && grep -q 'wire:loading' resources/views/livewire/client-index.blade.php && grep -q 'wire:loading' resources/views/livewire/post-index.blade.php && grep -q "client->name . '" resources/views/admin/clients/edit.blade.php && grep -q "ton premier" resources/views/livewire/client-index.blade.php && grep -q "ton premier" resources/views/livewire/post-index.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - client-index + post-index list containers use wire:loading.class + wire:target="search" with a bg-sand-100 skeleton (aria-hidden).
    - clients/edit.blade.php title uses `$client->name . ' · Modifier · Dlo Azur'` concatenation; no literal {{ }} in the @section string; no em-dash.
    - clients/show.blade.php history rows are clickable with a 1-line summary; no intra-card pagination reload (replaced with "voir tout" or moved).
    - client-index:72 and post-index:78 empty-states use "ton premier ..." (tu).
    - topbar disabled search is hidden/relinked off the wired routes.
    - client-form/piscine-form/post-form show an inline calm success acknowledgement on save (success token), in tu register.
    - No client-facing copy switched to tu (D-08).
  </acceptance_criteria>
  <done>Live-search shows loading; client-edit title interpolates; client history actionable; operator empty-states in tu; topbar search no dead grey input; admin forms acknowledge success inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser to admin filtered routes | Dashboard cards link to passages.index with filter params (already-authenticated admin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | n/a | dashboard/layout/nav cosmetic + IA | accept | No new attack surface. Card links use existing authenticated admin routes + existing filter params; no new endpoint or query path. |
| T-11-06-02 | Tampering | client-edit title interpolation | mitigate | $client->name rendered via Blade @section is auto-escaped on output; concatenation does not introduce unescaped output. |
| T-11-06-03 | Information Disclosure | inline form success / flash | accept | Success acknowledgement contains no sensitive data beyond what the operator just entered; admin-only, authenticated. |
| T-11-SC | Tampering | npm installs | mitigate | No new packages; blade/Livewire only. |
</threat_model>

<verification>
- npx vite build passes; dashboard renders agenda-led with clickable cards.
- Manual: type in client search -> list dims with skeleton, no silent freeze.
- Manual: client edit browser tab shows the real client name, not literal {{ }}.
- Manual: Recap reachable from sidebar; Blog reachable on mobile bottom-nav.
- Manual: save a client/piscine/post -> a calm inline success acknowledgement appears.
</verification>

<success_criteria>
SC-6 satisfied (loading states on live-search). D-10 dashboard restructure landed; admin landmarks/IA fixed; operator empty-states in tu; admin form inline success added; client vouvoiement intact.
</success_criteria>

<output>
Create .planning/phases/11-audit-impeccable-ux-ui-wording/11-06-SUMMARY.md when done.
</output>
