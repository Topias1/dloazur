---
phase: 11-audit-impeccable-ux-ui-wording
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/portail/magic-link-request.blade.php
  - resources/views/portail/confirm.blade.php
  - resources/views/auth/login.blade.php
  - resources/views/auth/forgot-password.blade.php
  - resources/views/auth/reset-password.blade.php
  - resources/views/emails/magic-link.blade.php
  - resources/views/errors/404.blade.php
autonomous: true
requirements: [P0-MAGICLINK-ERROR, P1-SUBMITTING, P1-EMAIL-STRIPE, P2-LOGIN-TAB, P2-DEMO-BTN, P3-WHATSAPP-HEX, P3-CONFIRM-EXPIRY, P3-AUTH-BTN-HEIGHT]

must_haves:
  truths:
    - "A client with an expired/invalid magic-link sees the error message (@error('ml') wired) (SC-2)"
    - "Auth submit buttons show a submitting state and cannot double-submit (SC-6)"
    - "The magic-link email has no banned side-stripe border-left"
    - "Client-facing copy stays vous strict on every auth/portail surface (D-08 hard lock)"
  artifacts:
    - path: "resources/views/portail/magic-link-request.blade.php"
      provides: "@error('ml') block that renders the controller recovery messages"
      contains: "@error('ml')"
  key_links:
    - from: "app/Http/Controllers/Portail/MagicLinkController.php"
      to: "resources/views/portail/magic-link-request.blade.php @error('ml')"
      via: "redirect ->withErrors(['ml' => ...])"
      pattern: "@error\\('ml'\\)"
---

<objective>
Fix the P0 swallowed magic-link error (the most-important recovery messages are
written but never rendered) plus the P1 auth state findings and the smaller auth
polish (email side-stripe, login dead tab, demo-button demotion, WhatsApp hex,
confirm expiry copy, button height). All client-facing, so vouvoiement stays
strict (D-08, Forces #4) and the anti-enumeration controller logic is untouched
(Forces #3).

Purpose: SC-2 (expired-link message shows) and SC-6 (submitting states).
Output: A trustworthy passwordless flow with visible recovery + no double-submit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-FINDINGS.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-UI-SPEC.md
@app/Http/Controllers/Portail/MagicLinkController.php
@DESIGN.md
@.claude/skills/dloazur-frontend-stack/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire @error('ml') + remove dead confirm error blocks + email side-stripe fix</name>
  <files>resources/views/portail/magic-link-request.blade.php, resources/views/portail/confirm.blade.php, resources/views/emails/magic-link.blade.php</files>
  <read_first>
    - app/Http/Controllers/Portail/MagicLinkController.php (lines 99,107,121 redirects with the ml error keys; confirm exactly which key is used)
    - resources/views/portail/magic-link-request.blade.php (lines 48,67 existing @error blocks for email/throttle)
    - resources/views/portail/confirm.blade.php (lines 38-50 dead error blocks, 67 expiry copy)
    - resources/views/emails/magic-link.blade.php (lines 8,9,14,16,31,37 hex + border-left side-stripe)
    - 11-UI-SPEC.md sections "Error / edge-state copy" (@error('ml') styling) + "Email magic-link" (hex->token mapping) + P3 confirm expiry
    - 11-FINDINGS.md P0 "Erreur magic-link" + P1 "Email magic-link" + P3 "Portail expiry contradictoire"
    - DESIGN.md section 6 (no bordure laterale coloree)
  </read_first>
  <action>
    1. P0 (FINDINGS): In magic-link-request.blade.php, near the existing email/throttle error
       blocks, add an @error('ml') block rendering the controller message with UI-SPEC styling:
       text-danger bg-danger/10 ring-1 ring-danger/30 rounded-xl p-3 mt-4 text-sm. Confirm the
       error bag key matches the controller (read it; it redirects to portail.magic-link.request
       at :99,107,121). Do NOT change controller logic (Forces #3, anti-enumeration). Do NOT
       override the controller message text (it is deliberately non-disclosing).
    2. Remove the dead/never-shown error blocks in confirm.blade.php:38-50.
    3. P3 expiry copy: align confirm.blade.php:67 ("Usage unique par session") and the email's
       expiry statement to the real backend config (FINDINGS notes email says "jusqu'a 3 fois").
       Read the controller/config for the true single-use-vs-N-visits rule and make both surfaces
       state the same thing. vous register.
    4. P1 email side-stripe (UI-SPEC "Email magic-link", DESIGN.md section 6): remove
       border-left: 3px solid ... from magic-link.blade.php; replace with a full border / tinted
       background block. Keep hex (email clients strip OKLCH) but add comments mapping each hex to
       its token: #fefdf8->sand-50, #1a2c40->navy-900, #0080ff->azure-500, #154c79->navy-600.
       Verify #0080ff and #154c79 match azure-500/navy-600 rendered values.

    All copy stays vous (D-08, Forces #4).
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && grep -q "@error('ml')" resources/views/portail/magic-link-request.blade.php && ! grep -q "border-left" resources/views/emails/magic-link.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - magic-link-request.blade.php contains @error('ml') with the danger styling from UI-SPEC.
    - The ml error key matches the controller redirect bag (verified against MagicLinkController).
    - confirm.blade.php no longer contains the dead error blocks (lines 38-50 removed).
    - confirm.blade.php and the email state the SAME single-use/expiry rule (no contradiction).
    - magic-link.blade.php contains no `border-left` and has hex->token mapping comments.
    - No client-facing copy switched to tu; controller logic unchanged.
  </acceptance_criteria>
  <done>Expired-link message renders; dead blocks gone; expiry copy consistent; email has no side-stripe.</done>
</task>

<task type="auto">
  <name>Task 2: Submitting states on auth buttons + login dead-tab fix + demo button demote + WhatsApp hex + button height</name>
  <files>resources/views/auth/login.blade.php, resources/views/auth/forgot-password.blade.php, resources/views/auth/reset-password.blade.php, resources/views/portail/magic-link-request.blade.php, resources/views/portail/confirm.blade.php, resources/views/errors/404.blade.php</files>
  <read_first>
    - resources/views/auth/login.blade.php (line 90 submit, lines 97-121 disabled client tab)
    - resources/views/auth/forgot-password.blade.php (line 55 submit)
    - resources/views/auth/reset-password.blade.php (line 72 submit)
    - resources/views/portail/magic-link-request.blade.php (line 52 submit, 71-100 demo buttons, 109 whatsapp hex)
    - resources/views/portail/confirm.blade.php (line 57 submit)
    - resources/views/errors/404.blade.php (line 23 whatsapp hex)
    - 11-UI-SPEC.md sections "Submitting states", "Auth tab client / demo button", "WhatsApp hex inline", "Key CTA labels"
    - 11-FINDINGS.md P1 "Aucun etat submitting", P2 auth tab, P2 demo-login, P3 whatsapp hex, P3 auth button height
  </read_first>
  <action>
    1. P1 submitting (UI-SPEC "Submitting states"): on each of login:90, forgot-password:55,
       reset-password:72, magic-link-request:52, confirm:57, wrap the form with the Alpine
       x-data="{ sending: false }" @submit="sending = true" pattern; the submit button gets
       :disabled="sending", :class="sending ? 'opacity-60 cursor-not-allowed' : ''", and an
       x-text swapping to the sending label. Labels: magic-link/forgot/reset/confirm = "Envoi...";
       login = "Connexion...". No spinner SVG. Respect prefers-reduced-motion (opacity is an
       instantaneous state; do NOT add animate-spin without motion-safe:). The magic-link button
       has a deliberate 1-3s controller usleep, so double-tap protection here prevents a throttle.
    2. P2 login dead tab (UI-SPEC): login.blade.php:97-121 "Bientot disponible" disabled client
       tab -> make the CTA a link to route('portail.magic-link.request') (the live page), OR remove
       the tab. Do not leave a dead-end coexisting with the live magic-link page.
    3. P2 demo button (UI-SPEC): in magic-link-request.blade.php (demo block gated config
       app.demo_login), demote "Demo Client" from bg-azure-500 primary to secondary
       (bg-sand-50 ring-1 ring-azure-200 text-azure-700) matching "Demo Admin". Two stacked azure
       primaries -> one primary.
    4. P3 whatsapp hex (UI-SPEC): magic-link-request.blade.php:109 and 404.blade.php:23
       bg-[#25D366] -> bg-whatsapp (token --color-whatsapp already declared).
    5. P3 button height (FINDINGS): bump primary auth submit buttons from h-12 (48px) to h-13
       (52px) per brief (the 404 page already uses h-13). Apply to the primary submit buttons only.

    All copy stays vous (D-08).
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && grep -q "sending" resources/views/auth/login.blade.php && grep -q "sending" resources/views/portail/magic-link-request.blade.php && ! grep -q "bg-\[#25D366\]" resources/views/portail/magic-link-request.blade.php && ! grep -q "bg-\[#25D366\]" resources/views/errors/404.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All five auth/portail submit forms use the x-data sending pattern; submit buttons are disabled while sending with the correct sending label.
    - login.blade.php disabled client tab is now a link to route('portail.magic-link.request') (or the tab is removed); no "Bientot disponible" dead-end remains.
    - "Demo Client" uses secondary styling (not bg-azure-500 primary), matching "Demo Admin".
    - No `bg-[#25D366]` remains in magic-link-request.blade.php or 404.blade.php (replaced by bg-whatsapp).
    - Primary auth submit buttons are h-13 (52px).
    - No client-facing copy switched to tu (D-08).
  </acceptance_criteria>
  <done>Auth buttons show submitting state and block double-submit; login tab points to the live page; demo button demoted; whatsapp hex tokenized; button heights aligned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser to auth/magic-link endpoints | Untrusted email/token input crosses to the passwordless auth flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | @error('ml') message | mitigate | Render only the controller's existing non-disclosing message verbatim; do NOT add email/token detail to the error copy. No enumeration leak (Forces #3). |
| T-11-03-02 | Spoofing/DoS | magic-link request double-submit | mitigate | Submitting-state disables the button, reducing duplicate requests against the deliberate 1-3s usleep + existing throttle. Controller throttle logic unchanged. |
| T-11-03-03 | Tampering | client-facing register | mitigate | vouvoiement preserved (D-08); no copy or logic that weakens anti-enumeration. |
| T-11-SC | Tampering | npm installs | mitigate | No new packages; blade/Alpine only. |
</threat_model>

<verification>
- Manual: visit an expired magic-link -> redirected to request form WITH the visible error message.
- Manual: tap "Recevoir mon lien" -> button shows "Envoi..." and is disabled; no double request.
- Manual: email renders with no left side-stripe; login client tab links to the live magic-link page.
- npx vite build passes.
</verification>

<success_criteria>
SC-2 satisfied (expired-link error visible) and SC-6 satisfied (submitting states on auth buttons). Client vouvoiement intact (D-08).
</success_criteria>

<output>
Create .planning/phases/11-audit-impeccable-ux-ui-wording/11-03-SUMMARY.md when done.
</output>
