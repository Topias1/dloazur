---
phase: 11-audit-impeccable-ux-ui-wording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/app.js
  - resources/js/passage-form.js
  - resources/js/sync-drawer.js
  - resources/js/offline-queue.js
  - resources/views/layouts/admin.blade.php
  - resources/views/admin/passages/create.blade.php
  - resources/views/admin/passages/offline.blade.php
autonomous: true
requirements: [P0-OFFLINE-DRAWER, P0-OFFLINE-ZOMBIE, P2-SYNC-FEEDBACK, P2-ARIA-BANNER, P2-STEPPERS, D-07-OFFLINE]

must_haves:
  truths:
    - "A queued offline passage is flushable from any admin page, not only the create screen (SC-1)"
    - "An item interrupted in 'uploading' is automatically re-queued to 'pending' and retried, zero zombies (SC-1)"
    - "Pierre gets a calm success confirmation on the triggering page when the queue empties"
    - "Offline banner announces politely, not assertively, to screen readers"
    - "Offline/PWA copy uses the operator 'tu' register (D-07)"
  artifacts:
    - path: "resources/js/app.js"
      provides: "Global passage-form:flush handler that calls a shared flush, not just refresh()"
      contains: "Alpine.store('offlineQueue')"
    - path: "resources/views/layouts/admin.blade.php"
      provides: "sync-drawer mounted once at layout level (available on every admin page)"
      contains: "x-admin.sync-drawer"
  key_links:
    - from: "resources/js/app.js"
      to: "Alpine.store('offlineQueue').flush()"
      via: "passage-form:flush event handler"
      pattern: "passage-form:flush"
    - from: "resources/views/layouts/admin.blade.php"
      to: "<x-admin.sync-drawer>"
      via: "single layout-level mount"
      pattern: "sync-drawer"
---

<objective>
Close the two P0 data-loss findings at the core of the product value: the offline
queue is unflushable off the create screen, and uploading items can become
un-retried zombies. Move flush/upload into the shared Alpine.store('offlineQueue'),
mount the sync-drawer once at the admin layout, and recover orphaned uploading
items on init. Also lands the small offline-surface a11y + register + ergonomics
fixes that live in the same files (P2 sync feedback, P2 aria-live banner, P2
56px steppers, D-07 tu register on offline copy).

Purpose: "L'operateur enregistre chaque passage de facon fiable meme sans reseau."
This is the one thing that must work. SC-1.
Output: A shared offline store with working flush from any page + zombie recovery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-FINDINGS.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-UI-SPEC.md
@.claude/skills/dloazur-frontend-stack/SKILL.md

<interfaces>
resources/js/offline-queue.js exports:
- openOfflineDB()
- upsertPassage(record), savePhoto(record)
- getPassagesByStatus(status = 'pending')   // returns records with that status
- getPhotosByPassage(passageClientUuid)
- markStatus(storeName, id, status, attempts = null)
- countPendingAll()                          // currently counts 'uploading' as pending too
- clearSynced()

resources/js/app.js (current state, lines 53-96):
- Alpine.store('offlineQueue', { ... refresh() ... })   // badge counter store, has refresh() only
- Alpine.store('syncDrawer', syncDrawerStore())
- window.addEventListener('passage-form:flush', ...) currently only calls offlineQueue.refresh()

resources/js/passage-form.js (flush/upload logic lives ONLY in the passageForm Alpine component):
- _flushQueue()  (line 382) reads getPassagesByStatus('pending'), retries synced+produits_pending
- _uploadPassage(item) (line 455) markStatus('uploading') BEFORE fetch, backoff [2000,8000,30000]
- _syncProduits(p), _headers(authed) helpers used by the above

resources/js/sync-drawer.js (syncDrawerStore factory):
- flushAll() (line 77) promotes only 'error' to 'pending', then dispatches passage-form:flush
- retry(id) (line 66) single 'error' to 'pending' then dispatch

All admin pages use @extends('layouts.admin'). create.blade.php:421 mounts
x-admin.sync-drawer per-page; the badge in topbar.blade.php:38 is on every page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move flush/upload into shared offlineQueue store + recover uploading zombies</name>
  <files>resources/js/app.js, resources/js/passage-form.js, resources/js/sync-drawer.js, resources/js/offline-queue.js</files>
  <read_first>
    - resources/js/app.js (full, store definitions + the passage-form:flush relay at :88)
    - resources/js/passage-form.js (lines 1-130 init, 339-520 flush/upload logic)
    - resources/js/sync-drawer.js (full, flushAll/retry)
    - resources/js/offline-queue.js (full, getPassagesByStatus, markStatus, countPendingAll)
    - 11-FINDINGS.md P0 "File offline ingerable" + P0 "Items bloques en uploading"
  </read_first>
  <action>
    Implements FINDINGS P0 "File offline ingerable" + P0 "uploading zombies".

    1. Promote the flush/upload pipeline to Alpine.store('offlineQueue') in app.js so it works
       whether or not the passageForm component is mounted. Add a flush() method to the store
       that: (a) recovers orphans, promotes every IDB passage in status uploading older than a
       few seconds back to pending (re-queue per FINDINGS fix), then (b) uploads all pending
       items via the same backoff fetch logic currently in passage-form.js:_uploadPassage
       ([2000,8000,30000] ms, markStatus uploading then synced/error, transient failure to
       pending not dead error per Forces #1), then (c) retries synced && produits_pending===true
       items via the existing _syncProduits path. Factor the upload helper so both the store and
       passageForm share ONE implementation (move it to a module function imported by the store).
       Do NOT duplicate divergent copies.
    2. In app.js, change the passage-form:flush handler (line 88) to call
       Alpine.store('offlineQueue').flush() (which refreshes the badge after) instead of only
       refresh(). The badge must never exist without a working flush behind it (FINDINGS).
    3. Zombie recovery on init: in the offlineQueue store init (or alpine:initialized boot at
       app.js:95), call the orphan-recovery step so any uploading left from a killed tab/SW is
       re-queued at boot. Keep passageForm._flushQueue working (it may delegate to the store or
       keep its own path that also handles uploading).
    4. sync-drawer.js flushAll() (line 77): promote uploading to pending in addition to error to
       pending, then dispatch flush. retry(id) may stay error-only.
    5. offline-queue.js countPendingAll (line 96): keep counting uploading as pending (an
       in-flight item is still en attente for the badge), since recovery guarantees a retry.
       No silent decrement.

    Preserve Forces #1 (UUID-before-write, autosave debounce, transient to pending) and Forces #2
    (offline tone stays warn/ambre). Do NOT use Livewire for any offline path (SKILL.md).
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && grep -q "store('offlineQueue').flush" resources/js/app.js && grep -A12 "flushAll" resources/js/sync-drawer.js | grep -q "uploading" && npx vite build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <acceptance_criteria>
    - resources/js/app.js defines a flush() method on Alpine.store('offlineQueue') and the
      passage-form:flush handler invokes store('offlineQueue').flush().
    - resources/js/sync-drawer.js flushAll() promotes uploading to pending (grep hit in body).
    - At boot, any IDB passage in uploading is re-queued to pending (recovery code present).
    - The upload fetch logic exists in exactly ONE place (no duplicated [2000, 8000, 30000]
      backoff literal in two functions).
    - npx vite build succeeds (prints BUILD_OK).
    - No Livewire request introduced into any offline path.
  </acceptance_criteria>
  <done>Offline queue flushable from any admin page via shared store; uploading orphans recovered to pending at boot and in flushAll; transient-failure to pending discipline intact.</done>
</task>

<task type="auto">
  <name>Task 2: Mount sync-drawer at layout + sync-success feedback + offline a11y/ergonomics/register fixes</name>
  <files>resources/views/layouts/admin.blade.php, resources/views/admin/passages/create.blade.php, resources/views/admin/passages/offline.blade.php, resources/js/sync-drawer.js</files>
  <read_first>
    - resources/views/layouts/admin.blade.php (full)
    - resources/views/admin/passages/create.blade.php (lines 1-12 layout decl, 76 aria banner, 120-139 steppers, 418-424 mounts, tu/vous lines 55,88,344,404-408)
    - resources/views/admin/passages/offline.blade.php (full, lines 16-17 vous to tu)
    - 11-UI-SPEC.md sections Spacing (steppers 56px), A11y fixes (aria-live banner), Copywriting (D-07), Sync success confirmation
    - 11-FINDINGS.md P2 steppers, P2 aria-live banner, P2 sync success feedback
  </read_first>
  <action>
    1. MOUNT (FINDINGS P0): Add x-admin.sync-drawer to layouts/admin.blade.php so it is present
       on every admin page (the topbar badge at topbar.blade.php:38 exists on all pages). Place
       it inside body after the main grid so it overlays. Then REMOVE the per-page mount at
       create.blade.php:421 to avoid a double-mount. The badge must never exist without the drawer.
    2. SYNC SUCCESS (FINDINGS P2): After a successful flush empties the queue (count to 0),
       surface a brief calm confirmation on the triggering page even if the drawer is closed.
       UI-SPEC copy "Tout est synchronise" with a check, token success, NOT toast-assertive.
       Wire from the offlineQueue store flush completion (Task 1) to a small Alpine confirmation
       element placed in the layout near the drawer.
    3. ARIA BANNER (FINDINGS P2): create.blade.php:76 offline banner aria-live assertive to
       polite. Offline is reassuring, not urgent (Forces #2). Keep warn/ambre visuals.
    4. STEPPERS (UI-SPEC Spacing exception, FINDINGS P2): create.blade.php:120-124,135-139
       stepper buttons w-11 h-12 to w-14 h-14 (56px) for both +/- if grid permits; minimum w-12.
       Keep the azure(+)/sand(-) split and existing tokens.
    5. REGISTER D-07 (UI-SPEC Copywriting): convert operator-facing vous to tu in
       create.blade.php:55,88,344,404-408 and offline.blade.php:16-17. Anchor on existing
       "Ta semaine". Do NOT touch client-facing copy (none here). Keep meaning, French.
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && [ "$(grep -c 'x-admin.sync-drawer' resources/views/layouts/admin.blade.php)" = "1" ] && ! grep -q "x-admin.sync-drawer" resources/views/admin/passages/create.blade.php && grep -q 'aria-live="polite"' resources/views/admin/passages/create.blade.php && grep -Eq "w-14 h-14|w-12" resources/views/admin/passages/create.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - layouts/admin.blade.php mounts x-admin.sync-drawer exactly once; create.blade.php no longer mounts it.
    - create.blade.php:76 banner uses aria-live="polite" (no assertive remaining on that banner).
    - Steppers are w-14 h-14 (or at least w-12) on both +/- controls.
    - A success confirmation "Tout est synchronise" with check renders after queue empties (success token, calm).
    - create.blade.php and offline.blade.php operator copy uses tu (no "votre"/"vous" operator strings on the listed lines).
  </acceptance_criteria>
  <done>sync-drawer available on every admin page; offline banner polite; steppers 56px; sync-success confirmation calm; offline copy in tu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alpine/IndexedDB to /api/passages | Offline-queued passage payloads cross from client storage to the server on flush |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | offline-queue flush/recovery | mitigate | Recovery only changes IDB status uploading->pending; payload_json is untouched; UUID-before-write discipline preserved (Forces #1). No new write path to /api/passages introduced. |
| T-11-02 | Denial of Service | retry loop | accept | Backoff [2000,8000,30000] retained; flush is user/online-event triggered, not a tight loop. Re-queue of orphans is one-shot at boot. |
| T-11-SC | Tampering | npm installs | mitigate | No new packages installed in this plan (vite build uses existing lockfile). No package legitimacy gate needed. |
</threat_model>

<verification>
- npx vite build passes.
- Manual: queue a passage offline, navigate to another admin page, trigger flush from the topbar badge -> drawer opens and item uploads.
- Manual: simulate a killed upload (leave an IDB item in uploading), reload -> item returns to pending and retries.
</verification>

<success_criteria>
SC-1 satisfied: offline queue is flushable from any admin page and uploading orphans are auto-recovered (zero zombies). Offline a11y/ergonomics/register P2+D-07 fixes landed without regressing offline discipline.
</success_criteria>

<output>
Create .planning/phases/11-audit-impeccable-ux-ui-wording/11-01-SUMMARY.md when done.
</output>
