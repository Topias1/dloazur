---
phase: 11-audit-impeccable-ux-ui-wording
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/css/app.css
  - resources/views/admin/agenda/index.blade.php
  - .github/workflows/tests.yml
  - bin/check-undeclared-tokens.sh
autonomous: true
requirements: [P1-WHITE-OVERRIDE, P1-PHANTOM-TOKENS, D-02, D-03, D-04, D-05, D-06, P3-AGENDA-CHIP]

must_haves:
  truths:
    - "No pure bg-white/text-white renders; white resolves to warm sand-50 globally (SC-4, D-02)"
    - "No undeclared Tailwind v4 token classes (lagon-50, warn-700, ink-600) emit zero CSS (SC-4, D-05)"
    - "A CI guardrail fails the build if an undeclared token class reappears (D-06)"
    - "WhatsApp #25D366 and the intentional QR white card are NOT affected by the white override (D-03)"
  artifacts:
    - path: "resources/css/app.css"
      provides: "--color-white override + missing lagon-50/warn-700 nuances in @theme"
      contains: "--color-white"
    - path: "bin/check-undeclared-tokens.sh"
      provides: "grep guardrail for undeclared token classes"
  key_links:
    - from: ".github/workflows/tests.yml"
      to: "bin/check-undeclared-tokens.sh"
      via: "CI step"
      pattern: "check-undeclared-tokens"
---

<objective>
Fix the two systemic theming findings (the weakest audit dimension, 2/4). Add the
single white-token override per D-02 (no class sweep), add the two genuinely-missing
@theme nuances and remap ink-600, and add a CI grep guardrail so phantom token
classes cannot silently return. Isolated to resources/css/app.css plus the one blade
remap and the CI script, so it never collides with other plans editing views.

Purpose: SC-4 (no pure white, no phantom tokens) and SC-8 (theming dimension >= 3/4).
Output: A self-consistent @theme that obeys DESIGN.md section 6, guarded by CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-FINDINGS.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-UI-SPEC.md
@DESIGN.md
@resources/css/app.css
@.claude/skills/dloazur-frontend-stack/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: White override + missing @theme nuances + ink-600 remap</name>
  <files>resources/css/app.css, resources/views/admin/agenda/index.blade.php</files>
  <read_first>
    - resources/css/app.css (the full @theme block, to find existing lagon/warn/ink ramps and the white token)
    - resources/views/admin/agenda/index.blade.php (lines 29-31 chip, 59 text-warn-700, 76 text-ink-600)
    - 11-UI-SPEC.md section Color D-02 (white override) + D-05 table (exact OKLCH values for new tokens)
    - 11-FINDINGS.md P1 "Pur #fff" + P1 "Tokens Tailwind v4 non definis" + P3 "Chip count agenda"
    - DESIGN.md section 6 (jamais #000/#fff law)
  </read_first>
  <action>
    1. D-02 white override: add `--color-white: var(--color-sand-50);` to the @theme block in
       resources/css/app.css. This redirects all bg-white/text-white (~90 hits) to warm sable.
       No class-by-class sweep.
    2. D-03 exceptions: verify WhatsApp (--color-whatsapp, hex token) and the QR white card do
       NOT inherit the override. If the QR card uses bg-white literally, scope it: give that card
       an explicit bg-[oklch(1_0_0)] inline (or a dedicated bg-qr-white token) so it stays pure
       white. WhatsApp uses its own token, leave it.
    3. D-04 contrast: sand-50 (~oklch 0.987) vs azure-500/navy-900 keeps WCAG AA. No regression
       expected; note in SUMMARY that text-white on azure/navy was visually confirmed.
    4. D-05 missing nuances (use exact values from UI-SPEC table):
       - Add `--color-lagon-50: oklch(0.965 0.030 202);`
       - Add `--color-warn-700: oklch(0.550 0.110 72);`
       - Do NOT add ink-600. Instead remap agenda/index.blade.php:76 text-ink-600 -> text-ink-700.
    5. P3 agenda chip: agenda/index.blade.php:29-31 azur chip for a neutral count -> bg-sand-100
       text-ink-500 (azure reserved for selection/active).

    All new colors OKLCH only (Forces #5). Never #000/#fff pure (except the explicitly-scoped QR card).
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && grep -q "color-white:.*sand-50" resources/css/app.css && grep -q "color-lagon-50" resources/css/app.css && grep -q "color-warn-700" resources/css/app.css && ! grep -q "text-ink-600" resources/views/admin/agenda/index.blade.php && npx vite build >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - resources/css/app.css @theme contains `--color-white: var(--color-sand-50)`.
    - resources/css/app.css @theme contains `--color-lagon-50` and `--color-warn-700` with the OKLCH values from UI-SPEC.
    - agenda/index.blade.php contains no `text-ink-600` (remapped to text-ink-700) and no `text-warn-700` that lacks a declared token (token now exists).
    - agenda/index.blade.php count chip uses `bg-sand-100 text-ink-500`, not azure.
    - WhatsApp and QR-card whites are not sand-tinted (verified per D-03).
    - npx vite build passes.
  </acceptance_criteria>
  <done>White resolves to sand-50 globally; lagon-50/warn-700 declared; ink-600 remapped; agenda chip neutral; exceptions preserved.</done>
</task>

<task type="auto">
  <name>Task 2: CI guardrail for undeclared Tailwind v4 token classes (D-06)</name>
  <files>bin/check-undeclared-tokens.sh, .github/workflows/tests.yml</files>
  <read_first>
    - resources/css/app.css (to enumerate the declared color tokens + their declared nuance steps)
    - .github/workflows/tests.yml (full, to find where to insert the step)
    - 11-FINDINGS.md P1 "Tokens Tailwind v4 non definis" (the fix recommends a grep CI guardrail)
    - .claude/skills/dloazur-frontend-stack/SKILL.md (Tailwind v4 CSS-first: only declared nuances emit utilities)
  </read_first>
  <action>
    Implements D-06 (FINDINGS recommendation, adopted).

    1. Create bin/check-undeclared-tokens.sh: a shell script that greps resources/views and
       resources/js for color-token utility classes (text-/bg-/border-/ring- prefixed by a known
       brand/semantic token name: sand, azure, navy, lagon, sun, ink, warn, success, danger,
       whatsapp) and flags any nuance step NOT declared in resources/css/app.css @theme. Strip
       comment lines before counting (grep -v of blade {{-- and // ); a known false-positive
       guard. Exit non-zero with the offending file:line list if any undeclared class is found.
       Allow the QR-card scoped exception. Keep the declared-token list sourced by grepping the
       @theme block itself so it stays in sync (do not hardcode a stale list).
    2. Wire it into .github/workflows/tests.yml as a step before or alongside the pest run, so a
       reintroduced phantom token (e.g. text-warn-900) fails CI.
  </action>
  <verify>
    <automated>cd /Users/amnesia/dev/dloazur && test -x bin/check-undeclared-tokens.sh && bin/check-undeclared-tokens.sh && grep -q "check-undeclared-tokens" .github/workflows/tests.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bin/check-undeclared-tokens.sh exists, is executable, and exits 0 on the current (fixed) tree.
    - The script derives the declared-token nuance list from the @theme block (not a hardcoded list).
    - The script strips comment lines so header prose does not self-trigger (no bare ==0 on unfiltered grep).
    - .github/workflows/tests.yml runs the script (grep `check-undeclared-tokens` hits the workflow).
    - Introducing a fake `text-warn-900` somewhere makes the script exit non-zero (spot-check during exec).
  </acceptance_criteria>
  <done>CI guardrail in place; undeclared token classes fail the build; current tree passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No runtime trust boundary crossed. CSS tokens + a CI shell script + a blade class remap.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | n/a | theming / CI script | accept | Cosmetic + build-time guardrail only. No new attack surface, no user input, no network. |
| T-11-SC | Tampering | npm installs | mitigate | No new packages. vite build uses existing lockfile. |
</threat_model>

<verification>
- npx vite build passes; bin/check-undeclared-tokens.sh exits 0.
- Manual: inspect a vitrine card and an admin card in browser, confirm warm sand background (not clinical pure white).
- Manual: confirm philosophie section lagon-50 background now renders, agenda warn text now readable.
</verification>

<success_criteria>
SC-4 satisfied: no pure white, no phantom tokens, CI guardrail prevents recurrence. Contributes to SC-8 (theming >= 3/4).
</success_criteria>

<output>
Create .planning/phases/11-audit-impeccable-ux-ui-wording/11-02-SUMMARY.md when done.
</output>
