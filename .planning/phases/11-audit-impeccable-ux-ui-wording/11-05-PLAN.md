---
phase: 11-audit-impeccable-ux-ui-wording
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/views/livewire/portail/passage-timeline.blade.php
autonomous: true
requirements: [P1-EAU-SAINE-GATE, P2-TEMPORARYURL, P2-PORTAIL-EMPTY, P3-ALT-HERO, P3-CL-LIBRE-UNIT, P3-AVATAR-INITIAL, D-08]

must_haves:
  truths:
    - "The Eau saine badge renders only when measures are in range (phOk && clOk && tacOk), not merely when a passage exists (SC-7, P1)"
    - "A failed temporaryUrl never renders a broken <img src=''>; a fallback message shows instead (P2)"
    - "A client with exactly one passage sees a 1-passage history message, not a bare empty section (P2)"
    - "All portail copy stays vous strict (D-08 hard lock, Forces #4)"
  artifacts:
    - path: "resources/views/livewire/portail/passage-timeline.blade.php"
      provides: "in-range-gated Eau saine badge + guarded photo render + 1-passage state"
      contains: "$phOk"
  key_links:
    - from: "passage-timeline.blade.php Eau saine badge"
      to: "$phOk && $clOk && $tacOk"
      via: "in-range gate already computed in the component"
      pattern: "phOk.*clOk.*tacOk"
---

<objective>
Portail remediation: gate the "Eau saine" badge on the in-range logic already computed
in the component (so a B2B concierge never forwards "Eau saine" over an out-of-range pH),
guard the broken temporaryUrl render (worst failure surface for the "proof" promise),
add the 1-passage history state, and clear the portail P3 polish (hero alt, Cl libre
unit/verdict, operator-initial avatar). All client-facing: vouvoiement stays strict (D-08).

Purpose: SC-7 (Eau saine gated on in-range). PRODUCT.md "la preuve remplace le superlatif".
Output: A portail where every claim is backed by the adjacent proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-FINDINGS.md
@.planning/phases/11-audit-impeccable-ux-ui-wording/11-UI-SPEC.md
@PRODUCT.md
@.claude/skills/dloazur-frontend-stack/SKILL.md

<interfaces>
$phOk, $clOk, $tacOk are already computed in the passage-timeline Livewire component
(per CONTEXT code_context Reusable Assets). Do NOT recompute them.
Photo disk is Cloudflare R2 (memory photos-disk-is-r2-not-scaleway), not Scaleway -
relevant when reasoning about temporaryUrl exceptions.
This is a SINGLE-FILE plan (one blade) - all four findings touch passage-timeline.blade.php.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Eau saine on in-range + guard temporaryUrl + 1-passage state</name>
  <files>resources/views/livewire/portail/passage-timeline.blade.php</files>
  <read_first>
    - resources/views/livewire/portail/passage-timeline.blade.php (lines 30 hero, 37 alt, 64-70/99-100/114-115/129-130 Eau saine badge + measure tiles, 115/130 Cl libre unit, 168-176 avatar, 180/193 photo alt, 183-196 temporaryUrl/img, 208 history count>1)
    - 11-UI-SPEC.md sections "Portail Eau saine gate", "Portail @if temporaryUrl guard", "Portail 1-passage history state", and the P3 portail items in FINDINGS
    - 11-FINDINGS.md P1 "Eau saine asserte sans lien a la preuve", P2 temporaryUrl, P2 portail empty-states, P3 alt hero, P3 Cl libre unit, P3 avatar P
    - memory: photos-disk-is-r2-not-scaleway
  </read_first>
  <action>
    All edits in passage-timeline.blade.php; all copy stays vous (D-08, Forces #4).

    1. P1 Eau saine gate (UI-SPEC): replace the badge guard @if ($lastPassage) at lines
       64-70/99-100/114-115/129-130 with @if ($lastPassage && $phOk && $clOk && $tacOk). Out of
       range -> no badge (or neutral state), never a green "Eau saine" over a bad measure. Use the
       already-computed $phOk/$clOk/$tacOk; do NOT recompute.
    2. P2 temporaryUrl guard (UI-SPEC): wrap the photo at lines 183-196 in
       @if ($firstPhotoUrl) <img ...> @else <p class="text-ink-500 text-sm">Photo non disponible
       pour ce passage.</p> @endif. No more <img src=""> on Storage/R2 exception.
    3. P2 1-passage state (UI-SPEC): at line 208 (branch count() > 1), when count() === 1 render
       "Votre premier passage apparait ci-dessus. L'historique se remplira a chaque entretien."
       isEmpty() keeps the existing empty state.
    4. P3 alt hero (UI-SPEC): line 37 alt="Votre piscine" -> dated alt when $heroPhotoUrl is the
       real photo (mirror the dated alt at :193).
    5. P3 Cl libre unit (UI-SPEC): at :115,130 keep the unit (mg/L) always visible; move the
       verdict ("ideal") into a separate element (dot/check), so the unit does not disappear when
       in range. Three tiles stay consistent.
    6. P3 avatar (UI-SPEC): :168-176 hardcoded "P" avatar -> initial derived from the operator
       name; consider "Le mot de Pierre" label. Keep vous in surrounding copy.
  </action>
  <verify>
    <automated>grep -q "phOk" resources/views/livewire/portail/passage-timeline.blade.php && grep -Eq "firstPhotoUrl" resources/views/livewire/portail/passage-timeline.blade.php && grep -q "Photo non disponible" resources/views/livewire/portail/passage-timeline.blade.php && grep -q "premier passage" resources/views/livewire/portail/passage-timeline.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The Eau saine badge guard is @if ($lastPassage && $phOk && $clOk && $tacOk) (grep phOk/clOk/tacOk together near the badge).
    - The first-photo render is wrapped in @if ($firstPhotoUrl) with an @else "Photo non disponible pour ce passage." - no <img src=""> path remains.
    - count() === 1 shows the "Votre premier passage apparait ci-dessus..." message; isEmpty() unchanged.
    - Hero alt is dated when the real photo is shown.
    - Cl libre tile keeps mg/L always; verdict is a separate element.
    - Avatar initial is derived from the operator name, not a hardcoded "P".
    - Every string remains vous (no tu anywhere in this file).
  </acceptance_criteria>
  <done>Eau saine gated on in-range; no broken img; 1-passage state present; portail P3 polish landed; vous intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R2 signed URL to client browser | temporaryUrl for the passage photo is rendered to the magic-link-authenticated client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | temporaryUrl render | mitigate | The @if guard only changes whether the existing temporaryUrl is rendered or a fallback text shows; it does NOT widen scope, lengthen expiry, or expose the raw R2 path. No new signed-URL surface. |
| T-11-05-02 | Tampering | client-facing register | mitigate | vouvoiement preserved (D-08 hard lock). No copy weakens the proof claim; gating tightens it. |
| T-11-SC | Tampering | npm installs | mitigate | No new packages; single blade file. |
</threat_model>

<verification>
- Manual: a passage with an out-of-range pH shows NO "Eau saine" badge.
- Manual: force a temporaryUrl failure -> "Photo non disponible pour ce passage." renders, no broken image icon.
- Manual: a client with exactly one passage sees the 1-passage history message.
- npx vite build passes.
</verification>

<success_criteria>
SC-7 satisfied (Eau saine gated on in-range). Portail P2/P3 fixes landed; vouvoiement intact (D-08).
</success_criteria>

<output>
Create .planning/phases/11-audit-impeccable-ux-ui-wording/11-05-SUMMARY.md when done.
</output>
