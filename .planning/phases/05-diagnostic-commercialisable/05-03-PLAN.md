---
phase: 05-diagnostic-commercialisable
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - database/migrations/2026_05_30_000010_add_lead_columns_to_diagnostics_table.php
  - app/Models/Diagnostic.php
  - app/Livewire/DiagnosticWizard.php
  - resources/views/livewire/diagnostic-wizard.blade.php
  - app/Mail/DiagnosticLead.php
  - resources/views/emails/diagnostic-lead.blade.php
  - config/diagnostic-tree.php
  - tests/Feature/DiagnosticWizardTest.php
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-06]
must_haves:
  truths:
    - "A user completes a 2-step chemistry wizard (pool info then measurements) with numeric validation and the volume reaches the dose engine"
    - "The filter-type select is a mandatory constrained choice (sable/verre/cartouche/diatomees) before any product reco; logged-in clients are pre-filled from their pool (override allowed); free-text pool values are non-authoritative hints"
    - "The wizard is action-aware: a gesture marked already-tried-and-failed is never re-proposed and routes to chlore-lock/metals/calcaire instead"
    - "Computing the plan persists a Diagnostic row server-side on completion with mesures + recommandations; disclaimer_accepted_at is non-null on any dosing row (D-04)"
    - "A logged-out diagnostic persists with client_id = null; a logged-in diagnostic sets client_id to the authenticated client"
    - "A lead form (Prenom + Commune required, Email/Site web optional) persists onto the diagnostic as additive columns (no separate Lead model) and notifies Pierre by mail, ContactForm pattern (D-03)"
    - "A one-tap WhatsApp link to 0696 94 00 54 is pre-filled with a rich diagnostic summary"
  artifacts:
    - path: "database/migrations/2026_05_30_000010_add_lead_columns_to_diagnostics_table.php"
      provides: "Additive prenom/commune/email/site_web columns on diagnostics"
      contains: "Schema::table"
    - path: "app/Mail/DiagnosticLead.php"
      provides: "Pierre lead-notification mailer (follows ContactMessage)"
      min_lines: 20
    - path: "app/Livewire/DiagnosticWizard.php"
      provides: "computeAndPersist() + submitLead() server actions; DoseEngine call; session PDF-gate seeding"
      min_lines: 90
    - path: "tests/Feature/DiagnosticWizardTest.php"
      provides: "DIAG-03 gate, Req5 anon/auth persistence, Req6 lead, DIAG-06 WhatsApp (fully implemented)"
  key_links:
    - from: "app/Livewire/DiagnosticWizard.php"
      to: "app/Services/Diagnostic/DoseEngine.php"
      via: "DoseEngine::compute() in computeAndPersist()"
      pattern: "DoseEngine::compute"
    - from: "app/Livewire/DiagnosticWizard.php"
      to: "app/Models/Diagnostic.php"
      via: "Diagnostic::create() on completion"
      pattern: "Diagnostic::create"
    - from: "app/Livewire/DiagnosticWizard.php"
      to: "app/Mail/DiagnosticLead.php"
      via: "Mail::to(config('contact.recipient'))->send()"
      pattern: "DiagnosticLead"
---

<objective>
Build brick 3 — the conversion core: the water-chemistry wizard (pool info + measurements), the action-aware "qu'as-tu deja essaye ?" differentiator, server-side dose computation + persistence, lead capture, and the basic pre-filled WhatsApp hand-off. After this plan, an anonymous or logged-in visitor can run a full measured diagnostic, get server-computed prudent doses gated behind the disclaimer, leave their coordinates (notifying Pierre), and one-tap WhatsApp Pierre with a summary.

This slice wires the Plan 01 Livewire component to the Plan 02 DoseEngine, adds the additive lead columns (D-03), and fully implements DiagnosticWizardTest.

Purpose: deliver the qualified-lead machine (the north-star) — DIAG-01 wizard + DIAG-02 wiring + DIAG-03 enforcement + DIAG-06 basic escalation + persistence (Req5/Req6/Req7).
Output: 2-step wizard UI (S3) + action-aware tree wiring + computeAndPersist/submitLead actions + lead migration + DiagnosticLead mailer + filled DiagnosticWizardTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diagnostic-commercialisable/05-SPEC.md
@.planning/phases/05-diagnostic-commercialisable/05-CONTEXT.md
@.planning/phases/05-diagnostic-commercialisable/05-RESEARCH.md
@.planning/phases/05-diagnostic-commercialisable/05-PATTERNS.md
@.planning/phases/05-diagnostic-commercialisable/05-FLOCULANT-BRANCH-SPEC.md
@.planning/phases/05-diagnostic-commercialisable/05-BLUEPRINT-APP.md
@.planning/phases/05-diagnostic-commercialisable/05-VALIDATION.md
@.planning/phases/05-diagnostic-commercialisable/05-01-SUMMARY.md
@.planning/phases/05-diagnostic-commercialisable/05-02-SUMMARY.md

<interfaces>
<!-- Reused contracts extracted from the codebase + prior plans. Use directly. -->

DoseEngine (Plan 02, app/Services/Diagnostic/DoseEngine.php):
  DoseEngine::compute(array $mesures, float $volume): array — ordered recommendation cards (param/current/target/product/dose/note + safety ref). Server-only. Call from computeAndPersist().

DiagnosticWizard (Plan 01, app/Livewire/DiagnosticWizard.php) — already has: WithRateLimiting + UsesSpamProtection traits, HoneypotData $extraFields, mount(), $disclaimerAccepted flag + acceptDisclaimer() server guard, tree config surfaced to the view, Alpine-only step nav. GROW it here — do not recreate.

Diagnostic model (app/Models/Diagnostic.php) — fillable currently: client_id, piscine_id, volume_m3, type_probleme, mesures, recommandations, disclaimer_accepted_at, created_via. ADD the 4 lead columns to fillable here. casts already map mesures/recommandations to array.

ContactForm submit guard chain (app/Livewire/ContactForm.php) — the canonical 5-step order to mirror for submitLead() AND reuse steps 1-3 in computeAndPersist():
  1. rateLimit(5,60) then addError('throttle') on TooManyRequestsException
  2. protectAgainstSpam() in try/catch then silent return on bot
  3. validate()
  4. side-effect (Mail / Diagnostic::create) in try/catch then Log::error + addError on failure
  5. success state + reset

ContactMessage mailer (app/Mail/ContactMessage.php) — Mailable + Queueable,SerializesModels; promoted readonly ctor props; envelope() with from + replyTo; content() returns a Blade view. DiagnosticLead follows this; replyTo only when the diagnostic email is present (email optional).

Auth guard: auth('clients')->id() — null when anonymous (Req5). config/auth.php defines the clients guard.

Persistence shape (PATTERNS, D-04/D-06):
  Diagnostic::create([... 'disclaimer_accepted_at' => now(), 'created_via' => 'wizard' or 'depannage' ...]);
  session()->put('diagnostic_ids', array_merge(session('diagnostic_ids', []), [$id])); // seeds the Plan 05 PDF session-gate

config/contact.php — recipient = config('contact.recipient'); whatsapp_number = config('contact.whatsapp_number', '596696940054'); rate_limit 5/60.

Existing migration idiom: database/migrations/2026_05_28_000009_create_diagnostics_table.php (anonymous-class extends Migration). New file uses Schema::table() additive (D-03), down() drops the 4 columns.

piscines.filtration is a bare nullable string (free text, max 30) — NOT a clean enum. Treat the pool value as a non-authoritative prefill hint; the wizard's constrained filter-type select is authoritative (CONTEXT D-08 gap + PATTERNS Filter-type normalization).
</interfaces>
</context>

<ui_design_context>
This slice builds UI surface S3 (water-chemistry wizard, register product) and S7 (lead-capture form, register product), plus the action-aware multiselect feeding S5 — governed by 05-UI-SPEC.md. Read before building any view:
@.planning/phases/05-diagnostic-commercialisable/05-UI-SPEC.md
@DESIGN.md
@PRODUCT.md
@.impeccable/design.json
@resources/css/app.css
@mockups/diagnostic-dloazur.html
You MAY invoke the project impeccable skill (impeccable craft / impeccable shape on the wizard step shell + lead form) to build to the contract; at minimum apply the 05-UI-SPEC.md S3/S7 rules + DESIGN.md @theme tokens. REUSE the signature stepper primitive (design.json stepper-plus/minus), the input primitive, and the ContactForm field/error/honeypot markup before creating partials. Do NOT run ui-ux-pro-max (not installed).
</ui_design_context>

<tasks>

<task type="auto">
  <name>Task 1: Chemistry wizard UI (S3) + action-aware tree wiring + lead-form UI (S7)</name>
  <files>resources/views/livewire/diagnostic-wizard.blade.php, app/Livewire/DiagnosticWizard.php, config/diagnostic-tree.php</files>
  <read_first>
    - app/Livewire/DiagnosticWizard.php (the Plan 01 component — grow it; do not recreate)
    - resources/views/livewire/diagnostic-wizard.blade.php (the Plan 01 view — add S3 wizard steps + S7 lead form to it)
    - resources/views/livewire/piscine-form.blade.php (numeric input + the filtration select idiom — extend options to sable/verre/cartouche/diatomees per FLOCULANT-BRANCH-SPEC) and its mount() prefill shape (PiscineForm loads the client's pool fields)
    - resources/views/livewire/contact-form.blade.php (honeypot directive, wire:loading button swap, error display, success state — the S7 lead form mirrors this 1:1)
    - 05-UI-SPEC.md S3 (2 steps: pool-info with size-mode toggle Volume m3 OR surface+profondeur then live-derived volume, the constrained filter-type select mandatory before any product reco, « Piscine au sel ? » oui/non+ppm; measurements pH/chlore/TAC/stabilisant/sel each a stepper or numeric field with « ex: 7,4 » helper + is_numeric validation; step indicator 1/2; Alpine drives step nav, only « Calculer mon plan d'action » is a wire:click; stepper :active scale .95 only) and S7 (« Vos coordonnees » Prenom req/Commune req/Email opt/Site web opt, shown AFTER value delivered) and the Copywriting Contract (exact CTA + error copy)
    - 05-BLUEPRINT-APP.md section 6 (action-aware table: pruning + redirect of tried-and-failed gestures) and 05-UI-SPEC.md « Qu'as-tu déjà essayé ? » multiselect contract
    - 05-FLOCULANT-BRANCH-SPEC.md section 2 (filter-type is a mandatory input before any product recommendation; logged-in prefill + override)
  </read_first>
  <action>
    Extend resources/views/livewire/diagnostic-wizard.blade.php with surface S3: the chemistry-flow Step 1 (pool info) — a size-mode toggle (Volume m3 OR surface+profondeur with a live-derived volume readout), the constrained filter-type select (sable/verre/cartouche/diatomees) marked mandatory before any product reco, and « Piscine au sel ? » (oui/non + ppm); Step 2 (measurements) — pH, chlore libre, alcalinite (TAC), stabilisant, taux de sel as steppers or numeric fields with inputmode="decimal", the « ex: 7,4 » helper, comma-decimal accepted, tabular-nums, plus the optional chlore total + TH fields (audit P1 intake). Add the « Qu'as-tu déjà tenté ? » multiselect-chip node and wire it so a tried-and-failed gesture is pruned/redirected per BLUEPRINT section 6 (the routing data already lives in config/diagnostic-tree.php from Plan 01; here you finalize the chip options and the prune/redirect Alpine logic, and add any missing tried-action redirect leaves to the tree config). Add surface S7: the lead-capture form (« Vos coordonnees » — Prenom req, Commune req, Email opt, Site web opt) mirroring contact-form.blade.php exactly incl. the visually-hidden honeypot div and the wire:loading button swap, shown after the plan is delivered.
    Extend app/Livewire/DiagnosticWizard.php with the wizard input properties + #[Validate] attributes: lead fields mirror ContactForm (prenom/commune required string max:80, email nullable email max:160, siteWeb nullable url max:255); measurements follow PiscineForm numeric idiom (volume nullable numeric min:1 max:1000; ph/chlore/alcalinite/stabilisant/selPpm each nullable numeric); filtration nullable in the set sable,verre,cartouche,diatomees; sizeMode volume or surface; sel bool. Add a mesures() accessor assembling the measurement array for DoseEngine. For the logged-in path, extend mount() with the PiscineForm prefill shape (load the client's pool, prefill filtration as a non-authoritative hint with override). Keep step/mode/node/tried purely in Alpine (no Livewire binding). Do NOT add the compute/persist/lead server actions here — Task 2 owns them; this task delivers the inputs, validation rules, and UI only.
  </action>
  <acceptance_criteria>
    - UI-SPEC S3/S7 contract: product register (sand surfaces; azure-500 only on the single primary action + the stepper plus and selected ring per UI-SPEC Color); the result plan stays an ordered differentiated list (not an identical card grid); numeric helper « ex: 7,4 » present; step indicator 1/2; filter-type select offers exactly sable/verre/cartouche/diatomees and is required before any product reco; French verb+noun CTAs (« Continuer », « Calculer mon plan d'action », « Envoyer mes coordonnees »); error copy matches the Copywriting Contract (« La valeur doit etre un nombre. Exemple : 7,4 », « Indique ton prenom », « Indique ta commune »); OKLCH only, no #000/#fff; no em dashes / gradient text / side-stripe borders; stepper :active scale .95 is the only scale; prefers-reduced-motion honored.
    - DIAG-02 invariant holds: grep finds no dose arithmetic (a number then asterisk then volume/m3) in resources/js or the wizard Blade — the derived-volume readout is allowed (surface times depth is geometry, not a dose) but NO dose coefficient appears client-side.
    - The « déjà tenté ? » multiselect prunes/redirects a tried gesture (e.g. selecting « Chlore choc » on eau-verte routes toward chlore-lock/metals, never re-proposing a shock) — verified by a manual walk + the tree config carrying the redirect leaves.
    - Validation: #[Validate] attrs present for all inputs; filtration constrained to the 4 canonical values; lead fields mirror ContactForm exactly.
    - ./vendor/bin/pest stays green (no regressions; DiagnosticWizardTest still incomplete until Task 2).
  </acceptance_criteria>
  <verify>
    <automated>./vendor/bin/pest 2>&1 | tail -5; echo "filter options present:"; grep -nE "sable|verre|cartouche|diatom" resources/views/livewire/diagnostic-wizard.blade.php | head</automated>
  </verify>
  <done>The 2-step chemistry wizard (S3) + the action-aware multiselect + the lead form (S7) render to the UI-SPEC contract; all inputs carry #[Validate] attrs; filtration is constrained to the 4 canonical values; no dose math client-side; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Lead migration + DiagnosticLead mailer + computeAndPersist/submitLead/WhatsApp + fill DiagnosticWizardTest</name>
  <files>database/migrations/2026_05_30_000010_add_lead_columns_to_diagnostics_table.php, app/Models/Diagnostic.php, app/Mail/DiagnosticLead.php, resources/views/emails/diagnostic-lead.blade.php, app/Livewire/DiagnosticWizard.php, tests/Feature/DiagnosticWizardTest.php</files>
  <read_first>
    - database/migrations/2026_05_28_000009_create_diagnostics_table.php (the anonymous-class migration idiom; use Schema::table additive, after('created_via'))
    - 05-RESEARCH.md Schema Extension (the exact 4 columns: prenom string 80, commune string 80, email string 160, site_web string 255, all nullable) and Pattern 4 (disclaimer server guard) and Pattern 5 (WhatsApp wa.me/596696940054 + encodeURIComponent) and Pattern 7 (Mail::to(config('contact.recipient'))->send(new DiagnosticLead(...)))
    - 05-PATTERNS.md (the computeAndPersist persistence block incl. session diagnostic_ids seeding for the Plan 05 PDF gate; the ContactMessage->DiagnosticLead mailer mapping; the additive-migration + Diagnostic::$fillable note)
    - app/Mail/ContactMessage.php + resources/views/emails/contact-message.blade.php (the mailer + email-view to clone; optional-field @if pattern for the optional email/site web)
    - app/Livewire/ContactForm.php (the 5-step submit guard chain to reuse)
    - tests/Feature/DiagnosticWizardTest.php (the Plan 01 stub — replace its markTestIncomplete bodies with real assertions) and tests/Feature/ContactFormTest.php (Livewire::test, Mail::fake/assertSent, assertHasErrors, honeypot trip, rate-limit key, route-200)
    - 05-VALIDATION.md (the DIAG-03 / Req5 / Req6 / DIAG-06 rows to assert) and 05-UI-SPEC.md S6 (the rich WhatsApp pre-fill context list: symptome, mesures, filtre+volume, actions tentees, diagnostic, confidence, coordinates — basic version here, full escalation engine is Plan 04)
  </read_first>
  <action>
    Create the additive migration database/migrations/2026_05_30_000010_add_lead_columns_to_diagnostics_table.php using Schema::table(): add nullable prenom(80), commune(80), email(160), site_web(255) after created_via; down() drops the 4. Add the 4 names to Diagnostic::$fillable (model otherwise reused as-is). Create app/Mail/DiagnosticLead.php cloning ContactMessage (Mailable + Queueable,SerializesModels; promoted readonly ctor taking the Diagnostic or its lead fields + summary; envelope subject « Nouveau diagnostic — Dlo Azur Piscines », from config mail.from, replyTo only when email present; content returns view emails.diagnostic-lead). Create resources/views/emails/diagnostic-lead.blade.php cloning contact-message.blade.php (inline-CSS doc, #0080ff header, .field/.label/.value blocks, @if around optional email/site web) rendering Prenom, Commune, Email?, Site web?, and the diagnostic summary (symptome/mesures/actions tentees/diagnostic).
    Extend app/Livewire/DiagnosticWizard.php with the two server actions. computeAndPersist(): reuse ContactForm steps 1-3 (rate-limit, honeypot, validate), then enforce the disclaimer server guard (if not $disclaimerAccepted addError('disclaimer', « Accepte d'abord les conditions pour voir les recommandations de dosage. ») and return), then $recs = DoseEngine::compute($this->mesures(), (float) $this->volume) wrapped in try/catch (Log::error + addError on failure), then Diagnostic::create([... client_id => auth('clients')->id() (null anon), piscine_id, volume_m3 => volume or null, mesures => mesures(), recommandations => recs, disclaimer_accepted_at => now(), created_via => 'wizard' or 'depannage' ...]); store savedDiagnosticId and session()->put('diagnostic_ids', merge with [$id]) (seeds the Plan 05 PDF gate). submitLead(): mirror the ContactForm 5-step chain 1:1, persisting the 4 lead fields onto the existing diagnostic row (update) and dispatching Mail::to(config('contact.recipient', 'contact@dloazurpiscines.com'))->send(new DiagnosticLead(...)). Expose a computed WhatsApp summary string (symptome + mesures + actions tentees + diagnostic) for the Alpine :href wa.me/596696940054 pre-fill (encodeURIComponent in the view, Pattern 5; the rich escalation engine is Plan 04 — here the summary is the basic version).
    Replace the DiagnosticWizardTest stub bodies with real assertions: disclaimer gate (call computeAndPersist without accept then assertHasErrors(['disclaimer']) and Diagnostic count 0); a persisted dosing row has non-null disclaimer_accepted_at; anonymous completion creates a row with client_id null storing mesures+recommandations; logged-in completion (actingAs a client on the clients guard) sets client_id; lead submit with Prenom+Commune persists the lead and Mail::assertSent(DiagnosticLead::class), missing required fields assertHasErrors, honeypot trip sends nothing; the computed WhatsApp summary contains the number 596696940054 and a non-empty message. Use uses(RefreshDatabase::class) (already on the file from Plan 01).
  </action>
  <acceptance_criteria>
    - php artisan migrate runs the additive migration; diagnostics has prenom/commune/email/site_web (verify via schema or a tinker describe); Diagnostic::$fillable includes the 4.
    - ./vendor/bin/pest --filter DiagnosticWizard GREEN: disclaimer gate rejects + 0 rows; persisted dosing row has non-null disclaimer_accepted_at; anon client_id null with mesures+recommandations; logged-in sets client_id; lead persists + DiagnosticLead sent; required-field errors; honeypot sends nothing; WhatsApp summary has 596696940054 + non-empty text.
    - DIAG-03 enforced server-side (not just client): computeAndPersist returns an error and persists nothing when disclaimerAccepted is false.
    - D-06 seed present: grep shows session diagnostic_ids written on persist (the Plan 05 PDF gate depends on it).
    - DIAG-02 invariant still holds (no dose math in resources/js); ./vendor/bin/pest full suite green.
  </acceptance_criteria>
  <verify>
    <automated>php artisan migrate 2>&1 | tail -4; ./vendor/bin/pest --filter DiagnosticWizard 2>&1 | tail -10; ./vendor/bin/pest 2>&1 | tail -4</automated>
  </verify>
  <done>Lead columns migrated + in fillable; DiagnosticLead mailer + email view clone ContactMessage; computeAndPersist enforces the disclaimer server-side, calls DoseEngine, persists with client_id semantics + session seed; submitLead persists the lead + notifies Pierre; the WhatsApp link is pre-filled; DiagnosticWizardTest fully implemented and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor browser to Livewire server actions | Untrusted measurement + lead input crosses to computeAndPersist/submitLead; route is public, no auth |
| lead form to Pierre mailbox | Bot/spam abuse vector; PII (email, commune) collected |
| disclaimer client gate to server persistence | DIAG-03 boundary — server must re-check before returning/persisting doses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Spoofing | bot spam on the lead form | mitigate | Reuse ContactForm honeypot (spatie/laravel-honeypot, visually-hidden + aria-hidden) + WithRateLimiting (5/60s) verbatim; honeypot trip sends nothing (asserted in DiagnosticWizardTest). |
| T-05-08 | Tampering | disclaimer bypass to obtain doses | mitigate | computeAndPersist re-checks $disclaimerAccepted server-side and persists nothing + returns an error when false (RESEARCH Pattern 4); persisted dosing rows always carry non-null disclaimer_accepted_at (D-04, asserted). |
| T-05-09 | Information Disclosure | PII (lead email/commune) handling | mitigate | EU-hosted (Laravel Cloud Frankfurt); the diagnostic row IS the lead (D-03, no separate store); minimal fields; notification via existing Mail pattern. |
| T-05-10 | Tampering | XSS via measurement/lead input in the email or summary | mitigate | Blade auto-escape {{ }} in emails/diagnostic-lead.blade.php; never {!! !!}; WhatsApp message is encodeURIComponent-encoded. |
| T-05-11 | Information Disclosure | dose formulas in client bundle | mitigate | computeAndPersist is a wire:click server call (DoseEngine in PHP); no coefficient client-side (DIAG-02 smoke grep). |
| T-05-SC | Tampering | npm/composer installs | accept | No new packages (RESEARCH Package Legitimacy Audit: zero new deps). No install task. |
</threat_model>

<verification>
- ./vendor/bin/pest --filter DiagnosticWizard green: DIAG-03 gate, Req5 anon/auth persistence, Req6 lead + Pierre mail, honeypot, DIAG-06 WhatsApp summary.
- php artisan migrate applies the additive lead columns; Diagnostic::$fillable updated.
- DIAG-03 enforced server-side (compute returns error + persists nothing without accept).
- D-06 session seed written on persist (Plan 05 PDF gate dependency).
- DIAG-02 invariant: no dose math in resources/js.
- Full suite green.
</verification>

<success_criteria>
- DIAG-01 (wizard complete): 2-step chemistry wizard with numeric validation, constrained filter-type, action-aware « déjà tenté ? » pruning/redirect.
- DIAG-02 (wired): computeAndPersist calls the server DoseEngine; doses never client-side.
- DIAG-03 (enforced): disclaimer re-checked server-side; dosing rows carry non-null disclaimer_accepted_at.
- DIAG-06 (basic): one-tap pre-filled WhatsApp summary to 0696 94 00 54 (rich escalation engine layered in Plan 04).
- Req5/Req6: anon + logged-in persistence; lead capture + Pierre notification.
- DiagnosticWizardTest fully implemented and green.
</success_criteria>

<output>
Create `.planning/phases/05-diagnostic-commercialisable/05-03-SUMMARY.md` when done.
</output>
