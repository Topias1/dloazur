---
phase: 05-diagnostic-commercialisable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/vitrine.php
  - app/Http/Controllers/DiagnosticController.php
  - resources/views/vitrine/diagnostic.blade.php
  - app/Livewire/DiagnosticWizard.php
  - resources/views/livewire/diagnostic-wizard.blade.php
  - config/diagnostic-tree.php
  - resources/views/layouts/partials/nav.blade.php
  - resources/views/vitrine/eau-verte-urgence.blade.php
  - tests/Unit/DecisionTreeTest.php
  - tests/Feature/DiagnosticWizardTest.php
  - tests/Feature/DiagnosticPdfTest.php
  - tests/Feature/DiagnosticRouteTest.php
autonomous: true
requirements: [DIAG-01, DIAG-03]
must_haves:
  truths:
    - "A visitor reaches /diagnostic without auth and sees a brand landing with two entry tiles (symptom flow / chemistry flow)"
    - "A visitor picks a symptom, traverses branching questions client-side (no per-step network round-trip), and reaches an ordered action-plan leaf"
    - "Each of the 8 top-level symptom problems reaches at least one leaf; the electrolyser sub-tree exposes its 5 documented fault leaves"
    - "The eau-trouble cartridge path never emits the word floculant"
    - "A legal disclaimer is presented as an inline step before any dosing/chemical step is shown; dosing is unreachable until explicitly accepted"
    - "The vitrine nav and /services/eau-verte-urgence both link to /diagnostic"
  artifacts:
    - path: "config/diagnostic-tree.php"
      provides: "Versioned decision tree (question nodes + result leaves) incl. floculant filter-type sub-branch and action-aware leaves"
      contains: "'version'"
    - path: "app/Livewire/DiagnosticWizard.php"
      provides: "Full-page Livewire component owning state; Alpine owns step navigation (D-01)"
      min_lines: 40
    - path: "resources/views/livewire/diagnostic-wizard.blade.php"
      provides: "Symptom-flow + disclaimer-gate UI surfaces S2/S4/S5 (Alpine-driven)"
    - path: "resources/views/vitrine/diagnostic.blade.php"
      provides: "Brand landing S1 wrapping the Livewire component"
    - path: "app/Http/Controllers/DiagnosticController.php"
      provides: "GET /diagnostic show() method"
      exports: ["show"]
    - path: "tests/Unit/DecisionTreeTest.php"
      provides: "DIAG-01 tree-completeness + floculant-branch assertions"
  key_links:
    - from: "routes/vitrine.php"
      to: "DiagnosticController::show"
      via: "Route::get('/diagnostic')"
      pattern: "diagnostic.*DiagnosticController"
    - from: "resources/views/livewire/diagnostic-wizard.blade.php"
      to: "config/diagnostic-tree.php"
      via: "tree data surfaced to Alpine (no formulas in JS)"
      pattern: "diagnostic.*tree|@js|Alpine"
    - from: "resources/views/layouts/partials/nav.blade.php"
      to: "route('diagnostic')"
      via: "nav link"
      pattern: "route\\('diagnostic'\\)"
---

<objective>
Ship the walking-skeleton-style first vertical slice of the diagnostic (brick 1 of the 7-brick build order in 05-EXPERT-ARBITRATION.md): a public, indexable `/diagnostic` route with a brand landing, the "Dépannage rapide" symptom decision-tree flow, and the inline disclaimer gate — reaching a safe ordered action plan with zero dosing math. This is demoable on its own: a visitor lands, picks a symptom, accepts the disclaimer, and gets a safe step-by-step plan, with a one-tap path to Pierre.

This plan also lays the Nyquist Wave-0 foundation: it creates FOUR of the five Pest test files as stubs (DoseEngineTest is owned by the parallel Plan 02, the TDD plan) so no later plan is blocked on test-file creation, and fully implements the decision-tree completeness tests.

Purpose: open the acquisition tap (the highest-value early conversion surface) and lock the design contract + decision-tree data layer that bricks 3-6 build on.
Output: `/diagnostic` route + landing + symptom-flow Livewire/Alpine UI + versioned decision-tree config + 4 Pest test files (DecisionTree fully implemented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diagnostic-commercialisable/05-SPEC.md
@.planning/phases/05-diagnostic-commercialisable/05-CONTEXT.md
@.planning/phases/05-diagnostic-commercialisable/05-RESEARCH.md
@.planning/phases/05-diagnostic-commercialisable/05-PATTERNS.md
@.planning/phases/05-diagnostic-commercialisable/05-FLOCULANT-BRANCH-SPEC.md
@.planning/phases/05-diagnostic-commercialisable/05-DIAGNOSTIC-EXPERT-AUDIT.md
@.planning/phases/05-diagnostic-commercialisable/05-VALIDATION.md

<interfaces>
<!-- Reused contracts extracted from the codebase. Use directly — no exploration needed. -->

Diagnostic model (app/Models/Diagnostic.php) — reuse as-is, do NOT modify in this plan:
  fillable: client_id, piscine_id, volume_m3, type_probleme, mesures, recommandations, disclaimer_accepted_at, created_via
  casts: mesures->array, recommandations->array, disclaimer_accepted_at->datetime
  relations: client(): BelongsTo, piscine(): BelongsTo

ContactForm (app/Livewire/ContactForm.php) — pattern source for the Livewire component skeleton:
  traits: WithRateLimiting, UsesSpamProtection
  public HoneypotData $extraFields; mount() initializes new HoneypotData()
  render(): view('livewire.contact-form')

VitrineController (app/Http/Controllers/VitrineController.php) — show() analog:
  eauVerteUrgence()/contact() return view(...) with title/description/canonical/ogImage vars

routes/vitrine.php — /contact is OUTSIDE the cache.headers:vitrine group (stateful Livewire). /diagnostic follows the same placement (no cache middleware, no auth).

Auth guard: config/auth.php defines a clients guard (lines 47, 76) — use auth('clients') for logged-in detection (this plan: symptom flow only, no persistence yet).

@theme tokens (resources/css/app.css): azure-50..950, navy-50..950, lagon-300..700, sun-300..500, sand-50..300, ink-400..950, success oklch(0.700 0.150 155), warn oklch(0.800 0.130 80), --warn-bg oklch(0.965 0.045 85), danger oklch(0.620 0.210 25). WhatsApp green = #25D366 (only sanctioned hex).

Existing reusable components: x-button, x-cta-whatsapp, x-icon.{arrow-right,calendar,check,drop,shield,sparkle,star,sun,whatsapp}, x-picture, x-seo.meta (verify exact path resources/views/components/seo/meta.blade.php).
</interfaces>
</context>

<ui_design_context>
This slice builds UI surfaces S1 (landing, register brand), S2 (symptom tree, register product), S4 (disclaimer gate, register product), and the minimal S5 (result leaf) — all governed by 05-UI-SPEC.md. Read before building any view:
@.planning/phases/05-diagnostic-commercialisable/05-UI-SPEC.md
@DESIGN.md
@PRODUCT.md
@.impeccable/design.json
@resources/css/app.css
@mockups/diagnostic-dloazur.html
You MAY invoke the project impeccable skill (impeccable shape / impeccable craft on /diagnostic) to build to the contract; at minimum apply the 05-UI-SPEC.md surface rules + DESIGN.md @theme tokens. Reuse the Component Sources reuse list (x-button, x-cta-whatsapp, icons, layout shell, SEO meta) before creating partials. Do NOT run ui-ux-pro-max (catalog-only, not installed).
</ui_design_context>

<tasks>

<task type="auto">
  <name>Task 0: Create 4 Wave-0 Pest test files (stubs) and fully implement DecisionTreeTest</name>
  <files>tests/Unit/DecisionTreeTest.php, tests/Feature/DiagnosticWizardTest.php, tests/Feature/DiagnosticPdfTest.php, tests/Feature/DiagnosticRouteTest.php</files>
  <read_first>
    - .planning/phases/05-diagnostic-commercialisable/05-VALIDATION.md (the per-task verification map — every row keyed by requirement + target file)
    - tests/Feature/ContactFormTest.php (canonical Livewire/Pest idiom: Livewire::test()->set()->call()->assertHasErrors(); Mail::fake(); rate-limit key; this->get('/route')->assertStatus(200))
    - tests/Feature/GoogleReviewsServiceTest.php (Pest it()/expect() idiom; tests/Unit/ExampleTest.php for Unit location)
    - .planning/phases/05-diagnostic-commercialisable/05-FLOCULANT-BRANCH-SPEC.md section 8 (the floculant Pest acceptance list)
    - .planning/phases/05-diagnostic-commercialisable/05-DIAGNOSTIC-EXPERT-AUDIT.md section 9 (the expert Pest acceptance list)
  </read_first>
  <action>
    Create FOUR Pest files so later plans (03/04/05) inherit them rather than creating them (Nyquist Dimension 8). NOTE: tests/Unit/DoseEngineTest.php is NOT created here — it is owned and created by the parallel Plan 02 (the TDD plan) so the two Wave-1 plans share no files. For DiagnosticWizardTest, DiagnosticPdfTest, DiagnosticRouteTest: write the describe/it shells with explicit test names drawn from 05-VALIDATION.md plus the two expert acceptance lists, each body marked markTestIncomplete('Plan 0X — brief') so the suite stays green and the named gaps are visible. Tag each incomplete it with the owning plan in the message (03 = wizard persistence/lead/WhatsApp; 04 = escalation/confidence rich-context; 05 = PDF + session-gate + public route).
    Fully IMPLEMENT DecisionTreeTest now (it asserts only on config('diagnostic-tree.*'), which Task 2 produces): assert every one of the 8 top-level symptom problems reaches at least one result leaf; assert the electrolyser sub-tree exposes its 5 fault leaves (electro-debit, electro-entartree, electro-usee, electro-panne, electro-sel-bas); assert the cartridge filter path of the eau-trouble branch contains no occurrence of the substring floculant (per FLOCULANT-BRANCH-SPEC section 8); assert the tree carries a version key. Use plain config() reads, no RefreshDatabase, place in tests/Unit/ per the PATTERNS analog.
    DiagnosticWizardTest and DiagnosticPdfTest use uses(RefreshDatabase::class) (they persist rows in later plans). DecisionTreeTest is pure — no RefreshDatabase.
  </action>
  <acceptance_criteria>
    - ./vendor/bin/pest --filter DecisionTree is initially RED (config absent) until Task 2 lands; after Task 2 it passes with all completeness + floculant-substring + 5-electrolyser-leaf assertions.
    - ./vendor/bin/pest green overall: the three stub files report markTestIncomplete (not failures), suite is not red.
    - Each of the 4 files exists at its 05-VALIDATION.md target path; grep confirms the named it(...) strings for: disclaimer-gate reject + non-null disclaimer_accepted_at (DiagnosticWizardTest), session-gated PDF enumeration rejected (DiagnosticPdfTest), /diagnostic reachable without auth (DiagnosticRouteTest).
    - No watch-mode flags; feedback under 60s.
  </acceptance_criteria>
  <verify>
    <automated>./vendor/bin/pest 2>&1 | tail -6</automated>
  </verify>
  <done>The 4 test files exist at their VALIDATION target paths (DoseEngineTest belongs to Plan 02); DecisionTreeTest fully implemented (will pass after Task 2); the other 3 are green stubs with named incomplete tests tagged by owning plan; full suite not red.</done>
</task>

<task type="auto">
  <name>Task 1: Public /diagnostic route + DiagnosticController::show + brand landing (S1)</name>
  <files>routes/vitrine.php, app/Http/Controllers/DiagnosticController.php, resources/views/vitrine/diagnostic.blade.php, resources/views/layouts/partials/nav.blade.php, resources/views/vitrine/eau-verte-urgence.blade.php</files>
  <read_first>
    - routes/vitrine.php (place /diagnostic BELOW the cache.headers:vitrine group, alongside /contact — stateful Livewire, no cache, no auth; SPEC Req 9 + PATTERNS Public-route placement)
    - app/Http/Controllers/VitrineController.php (the show()/eauVerteUrgence() view-with-SEO-vars idiom to mirror — title/description/canonical/ogImage)
    - resources/views/vitrine/contact.blade.php (the full-page-Livewire-wrapper + class_exists guard + WhatsApp fallback shape to copy structurally)
    - 05-UI-SPEC.md S1 (brand landing: drenched marine/azure, Display title, two entry tiles min h-15 « Trouver mon problème » / « Analyser mon eau », .rise entrance, prefers-reduced-motion) and the Copywriting Contract (the exact CTA labels)
    - resources/views/layouts/partials/nav.blade.php — locate the real vitrine nav partial (RESEARCH Open Question 4: verify which file holds the nav before editing; check resources/views/layouts/ and partials/). Add the « Diagnostic piscine gratuit » entry pointing to route('diagnostic').
    - resources/views/vitrine/eau-verte-urgence.blade.php — locate the actual view backing route('services.eau-verte-urgence') (VitrineController::eauVerteUrgence); add the high-intent CTA « Ma piscine est verte ? Diagnostic gratuit » to route('diagnostic').
  </read_first>
  <action>
    Add the route to routes/vitrine.php OUTSIDE the cache group (mirror the /contact placement): Route::get('/diagnostic', [DiagnosticController::class, 'show'])->name('diagnostic'). Do NOT add the PDF route here (Plan 05 owns it). Create app/Http/Controllers/DiagnosticController.php with a show(): View method returning view('vitrine.diagnostic', [...]) carrying title « Diagnostic piscine gratuit · Dlo Azur Piscines », a meta description, canonical = url('/diagnostic'), and ogImage — mirroring VitrineController's SEO-var shape so the page is indexable (extends 999.1). Create resources/views/vitrine/diagnostic.blade.php extending layouts.app: a brand landing hero (S1 — drenched marine/azure ground, Display Fredoka title e.g. « Ta piscine te pose problème ? », one-line promise, two large entry tiles min h-15 wired to set the Alpine mode and advance) wrapping the diagnostic-wizard Livewire component behind the same class_exists(\App\Livewire\DiagnosticWizard::class) guard + WhatsApp fallback as contact.blade.php. Add the nav link and the eau-verte CTA. Use x-seo.meta for the indexable meta. Apply only @theme tokens; the warm sun accent is allowed solely on the « Ma piscine est verte ? » tile per UI-SPEC Color.
  </action>
  <acceptance_criteria>
    - php artisan route:list --name=diagnostic shows GET /diagnostic with no auth middleware and no cache.headers:vitrine middleware (verify it is NOT inside the cache group).
    - UI-SPEC S1 contract: landing uses only @theme tokens (no arbitrary px/hex except #25D366); OKLCH only, no #000/#fff; azure-500 only on the reserved-for list (UI-SPEC Color); Display title is Fredoka (font-display), one per page; entry-tile labels are the exact French verb+noun CTAs from the Copywriting Contract (« Trouver mon problème », « Analyser mon eau »); no gradient text, no side-stripe borders, no em dashes; motion is transform+opacity .rise with prefers-reduced-motion honored.
    - grep for route('diagnostic') in the real nav partial and the eau-verte view both return a hit.
    - The route returns 200 for an anonymous GET (curl -I or tinker check); DiagnosticRouteTest stays incomplete here (Plan 05 implements it).
  </acceptance_criteria>
  <verify>
    <automated>php artisan route:list --name=diagnostic 2>&1 | head; grep -rn "route('diagnostic')" resources/views/layouts resources/views/vitrine/eau-verte-urgence.blade.php 2>&1 | head</automated>
  </verify>
  <done>/diagnostic is publicly reachable (no auth, no cache middleware), renders the brand S1 landing to the UI-SPEC contract, hosts the Livewire component behind a guard, and is linked from the vitrine nav + eau-verte-urgence page.</done>
</task>

<task type="auto">
  <name>Task 2: Decision-tree config + symptom-flow Livewire/Alpine UI + inline disclaimer gate (S2, S4, minimal S5)</name>
  <files>config/diagnostic-tree.php, app/Livewire/DiagnosticWizard.php, resources/views/livewire/diagnostic-wizard.blade.php</files>
  <read_first>
    - 05-RESEARCH.md Full Decision Tree Reference (the verbatim node table: start, green-1, cloudy-1, brown-1/2, clear-1, electro-1/2/3/4 + the auto-green conditional node) and Pattern 3 (the config array shape: questions + results keys) and Pattern 1 (Alpine step nav, keep step in Alpine only) and Pattern 4 (server disclaimer guard)
    - 05-FLOCULANT-BRANCH-SPEC.md (the eau-trouble sub-branch: filter-type node sable/verre to floculant choc, cartouche to clarifiant [word floculant forbidden], diatomées to nettoyage+clarifiant; pH gate; supersedes the empty floculant leaf and CONTEXT D-08)
    - 05-DIAGNOSTIC-EXPERT-AUDIT.md sections 4-5 (decision-tree corrections: green-1 must test stabilisant to chlore-lock/manque-stab leaves; eau-boueuse/pollution-organique route clarification via the floculant sub-tree; cloudy-1 bifurcation eau-calcaire; electrolyser entry on sel OR selPpm over 1000; algues-installees reformulated) and section 6 (the systematic EPI/ne-jamais-melanger safety block + delai-baignade text on every chemical step)
    - 05-BLUEPRINT-APP.md section 6 (action-aware « qu'as-tu déjà essayé ? » table — a failed gesture routes to chlore-lock/metals/calcaire/resistant-algae, never re-proposed)
    - config/contact.php (the return-array banner-comment idiom + env-overridable values; tie the file header to DIAG-01/D-02)
    - resources/views/livewire/contact-form.blade.php (honeypot directive, wire:loading button swap, WhatsApp link) and resources/views/livewire/piscine-form.blade.php (numeric input + filter-type select idiom — extend the option set to sable/verre/cartouche/diatomées)
    - 05-UI-SPEC.md S2 (one question per screen, big h-13+ option tiles, emoji anchors, multiselect chips, horizontal slide motion), S4 (inline disclaimer step — NOT a modal — before any chemical output), S5 gabarit (Diagnostic statement to Pourquoi to Safety block ambre to Etapes ordonnees to Dosage-later to Quand appeler Pierre)
  </read_first>
  <action>
    Create config/diagnostic-tree.php returning a versioned PHP array ('version' => 1 + banner comment tying it to DIAG-01/D-02) with questions and results keys. Transcribe the full node set from RESEARCH Full Decision Tree Reference VERBATIM, then layer the authoritative corrections: replace the eau-trouble path with the FLOCULANT-BRANCH-SPEC filter-type sub-branch (sable/verre to floculant-choc sequence; cartouche to clarifiant sequence with the word floculant entirely absent; diatomées to filter-clean + clarifiant; insert the pH-gate step when pH outside [7.0,7.4]); add the green-1 stabilisant leaves (chlore-lock surstabilisation when stab over ~80; manque-de-stabilisant when stab under ~20-30) per the audit; route eau-boueuse + pollution-organique clarification into the floculant sub-tree (floculant last, after chlore drop); add the cloudy-1 eau-calcaire bifurcation; reformulate algues-installees. Every result leaf that contains a chemical gesture MUST embed the systematic safety block text (EPI gants+lunettes, ne jamais melanger, produit dans l'eau jamais l'inverse, delai baignade) and end dosed/chemical guidance with a re-test reminder — baseline copy from the audit section 6 (Pierre signs off pre-launch). Add an action-aware tried-signal model in the tree per BLUEPRINT section 6 so a tried-and-failed gesture is pruned/redirected (the routing data lives in config; the UI wiring of the multiselect lands here).
    Create app/Livewire/DiagnosticWizard.php: mirror ContactForm's skeleton (use WithRateLimiting, UsesSpamProtection; public HoneypotData $extraFields; mount() init; render() to view('livewire.diagnostic-wizard')). Add the symptom-flow state needed now: a bool $disclaimerAccepted and an array of tried-actions, plus a server acceptDisclaimer() action and the disclaimer server-guard shape (RESEARCH Pattern 4) — even though full persistence/dose-compute is Plan 03, the guard method and $disclaimerAccepted flag are defined here so the gate is enforceable. Surface the tree config to the view (pass config('diagnostic-tree') into render) so Alpine can traverse it — NO dose formula or coefficient anywhere in the Blade-emitted JS (DIAG-02 invariant; the symptom tree carries only static leaf text).
    Create resources/views/livewire/diagnostic-wizard.blade.php implementing surfaces S2 (symptom tree: x-data with step/mode/node/tried, one question per screen, big option tiles with the mockup emoji anchors, the « Qu'as-tu déjà tenté ? » multiselect-chip node, back affordance, horizontal-slide motion), S4 (inline disclaimer step shown before any chemical leaf, explicit affirm + « J'ai compris, voir les recommandations » — never a modal), and the minimal S5 leaf render (Diagnostic statement Fredoka headline to Pourquoi to ambre Safety block to ordered Etapes to a « Demander une intervention à Pierre » WhatsApp seam reusing x-cta-whatsapp). Keep step/node/mode/tried purely in Alpine (RESEARCH Pitfall 1 — never bind to Livewire); guard the Alpine root against re-render reset with wire:ignore.self / x-cloak. Handle the auto-green conditional node (chlore OK + pH over 7.6 to ph-calc, else algues-installees) in Alpine over the server-provided node data.
  </action>
  <acceptance_criteria>
    - ./vendor/bin/pest --filter DecisionTree GREEN: all 8 top-level problems reach at least one leaf; electrolyser sub-tree exposes its 5 fault leaves; version key present; the cartridge path contains zero floculant substring (FLOCULANT-BRANCH-SPEC section 8 / VALIDATION row).
    - grep -n floculant config/diagnostic-tree.php shows it ONLY in the sable/verre + diatomées branches, NEVER in the cartouche branch (manually confirm the cartouche sub-array).
    - DIAG-02 invariant: grep for dose arithmetic (a number followed by an asterisk then volume/m3) in resources/js/ and the wizard view returns nothing — no dose arithmetic in client JS/Blade-JS (the symptom flow has no doses; guards the invariant early).
    - UI-SPEC S2/S4/S5 contract: product register (sand surfaces, azure only on the single primary action + selected-option ring per UI-SPEC Color); disclaimer is an inline progressive step, not a modal (UI-SPEC S4); the leaf plan is an ordered differentiated list, not an identical card grid (UI-SPEC S5); the safety block uses the ambre/--warn-bg styling, never red; French verb+noun CTAs (« J'ai compris, voir les recommandations », « Continuer », « Demander une intervention à Pierre »); OKLCH only, no #000/#fff; no em dashes / no gradient text / no side-stripe borders; motion transform+opacity, prefers-reduced-motion honored.
    - Disclaimer gate: the acceptDisclaimer()/server-guard shape exists in DiagnosticWizard (grep for disclaimerAccepted) so Plan 03's compute path can reject when unaccepted.
  </acceptance_criteria>
  <verify>
    <automated>./vendor/bin/pest --filter DecisionTree 2>&1 | tail -8; echo "floculant occurrences in tree:"; grep -c floculant config/diagnostic-tree.php</automated>
  </verify>
  <done>config/diagnostic-tree.php holds the full, expert-corrected tree (floculant filter-type sub-branch, green-1 stabilisant leaves, eau-calcaire bifurcation, safety blocks); DecisionTreeTest is green; the symptom flow runs client-side via Alpine to a safe leaf; the inline disclaimer gate exists and is server-enforceable; no dose math in client JS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor browser to /diagnostic | Untrusted input (symptom selections, tried-actions) crosses here; route is fully public, no auth |
| client-delivered JS to dose logic | DIAG-02 invariant boundary — no formula/coefficient may cross into resources/js or Blade-emitted JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | dose formulas in client bundle | mitigate | Symptom flow carries only static leaf text from config/diagnostic-tree.php; no arithmetic in JS. Smoke grep in Task 2 verify. (DoseEngine itself lands server-only in Plan 02.) |
| T-05-02 | Tampering | disclaimer bypass on the chemical path | mitigate | Inline gate (Alpine hides) + server-side disclaimerAccepted guard defined in DiagnosticWizard (RESEARCH Pattern 4); full enforcement on persist lands in Plan 03 but the guard shape is created here so it cannot be skipped. |
| T-05-03 | Tampering | XSS via leaf content / future user input rendered in views | mitigate | Blade auto-escape (double-brace) only in the wizard view; never the raw-output directive. Tree content is static, authored, not user-supplied. |
| T-05-SC | Tampering | npm/composer installs | accept | No new packages this plan (RESEARCH Package Legitimacy Audit: zero new deps). No install task; nothing to vet. |
</threat_model>

<verification>
- ./vendor/bin/pest (full suite) green: DecisionTreeTest passes; the three stub files are markTestIncomplete, not failures.
- ./vendor/bin/pest --filter DecisionTree green: 8 top-level problems reach leaves, 5 electrolyser fault leaves present, cartridge path has no floculant, version key present.
- php artisan route:list --name=diagnostic: GET /diagnostic, no auth, no cache middleware.
- grep confirms route('diagnostic') in the nav partial + eau-verte view.
- Anonymous GET /diagnostic returns 200 and renders the S1 brand landing.
- No dose arithmetic in resources/js or the wizard Blade (DIAG-02 invariant smoke grep).
</verification>

<success_criteria>
- DIAG-01 (partial — symptom flow): all 8 top-level problems reach a leaf; electrolyser sub-tree exposes 5 fault leaves; floculant filter-type sub-branch correct (cartouche never says floculant); action-aware "déjà tenté?" multiselect wired.
- DIAG-03 (partial — gate present): inline disclaimer step before any chemical leaf; server guard shape exists.
- Req 9: /diagnostic publicly reachable, indexable, linked from nav + eau-verte-urgence.
- Nyquist Dimension 8: 4 Pest files exist (DoseEngineTest owned by Plan 02); DecisionTreeTest fully implemented and green; no later plan blocked on test-file creation.
- UI surfaces S1/S2/S4/minimal-S5 built to the 05-UI-SPEC.md contract.
</success_criteria>

<output>
Create `.planning/phases/05-diagnostic-commercialisable/05-01-SUMMARY.md` when done.
</output>
