---
phase: 05-diagnostic-commercialisable
plan: 04
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - app/Livewire/DiagnosticWizard.php
  - resources/views/livewire/diagnostic-wizard.blade.php
  - config/diagnostic-tree.php
  - tests/Feature/DiagnosticWizardTest.php
autonomous: true
requirements: [DIAG-06, DIAG-01]
must_haves:
  truths:
    - "A contextualized escalation engine fires preemptively when the diagnosis is hors-DIY (acide chlorhydrique, 230V, equipment, major imbalance) and reactively after a failed re-test"
    - "The escalation peak (S6) presents a one-gesture WhatsApp CTA pre-filled with rich context: symptome, mesures (+ fiabilite), filtre + volume, actions tentees + resultats, diagnostic, confidence, coordinates"
    - "A confidence index (eleve / moyen / indicatif) is shown on every diagnostic result with a one-line explanation"
    - "An over-escalation guard prevents shouting 'appelle Pierre' on an easy DIY case (honesty preserves trust)"
    - "The results screen offers a Telecharger le rapport (PDF) link to the diagnostic.pdf route (provided by Plan 05)"
  artifacts:
    - path: "app/Livewire/DiagnosticWizard.php"
      provides: "Escalation classification (preemptive/reactive) + confidence computation server-side"
      min_lines: 110
    - path: "resources/views/livewire/diagnostic-wizard.blade.php"
      provides: "S6 escalation recap card + rich pre-filled WhatsApp builder; S5 confidence chip; PDF download link"
    - path: "tests/Feature/DiagnosticWizardTest.php"
      provides: "DIAG-06 escalation-trigger + rich-context assertions (extended)"
  key_links:
    - from: "resources/views/livewire/diagnostic-wizard.blade.php"
      to: "wa.me/596696940054"
      via: "Alpine :href with encodeURIComponent(rich summary)"
      pattern: "wa\\.me/596696940054"
    - from: "app/Livewire/DiagnosticWizard.php"
      to: "config/diagnostic-tree.php"
      via: "leaf escalation flags drive preemptive escalation"
      pattern: "escalad|hors.?diy|preempt"
    - from: "resources/views/livewire/diagnostic-wizard.blade.php"
      to: "route('diagnostic.pdf', ...)"
      via: "PDF download link (guarded with Route::has)"
      pattern: "diagnostic\\.pdf"
---

<objective>
Build bricks 4 + 5 — the escalation engine and the confidence index, the two cheap "intelligence" multipliers that turn raw captures into qualified leads (the north-star). Escalation fires at the pic d'intention: preemptively when the case is hors-DIY (acide chlorhydrique, 230V, equipment fault, major imbalance), reactively after a failed re-test. The one-gesture WhatsApp hand-off (S6) carries rich context so Pierre arrives with zero questions to re-ask. The confidence index (S5) is an honest signal (eleve / moyen / indicatif) that builds trust and invites measurement when low.

This slice grows the Plan 03 wizard with the escalation classifier + confidence computation + the S6 escalation peak UI, upgrades the basic WhatsApp pre-fill into the full rich-context builder (DIAG-06), and adds the « Telecharger le rapport (PDF) » link to the results screen (the diagnostic.pdf route is created by the parallel Plan 05 — the link is guarded with Route::has so order is safe). It owns the wizard view in Wave 3; Plan 05 owns the controller/PDF/tests with zero shared files.

Purpose: deepen DIAG-06 from a basic link (Plan 03) into the contextualized escalation engine the locked persona converts on; add the confidence index (CDC section 5.5).
Output: escalation classifier + confidence computation + S6 escalation recap card + rich WhatsApp builder + PDF download link + extended DiagnosticWizardTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-diagnostic-commercialisable/05-SPEC.md
@.planning/phases/05-diagnostic-commercialisable/05-CONTEXT.md
@.planning/phases/05-diagnostic-commercialisable/05-BLUEPRINT-APP.md
@.planning/phases/05-diagnostic-commercialisable/05-CDC-SAFE-DIAGNOSTIC.md
@.planning/phases/05-diagnostic-commercialisable/05-DIAGNOSTIC-EXPERT-AUDIT.md
@.planning/phases/05-diagnostic-commercialisable/05-VALIDATION.md
@.planning/phases/05-diagnostic-commercialisable/05-03-SUMMARY.md

<interfaces>
<!-- Reused contracts from prior plans. Use directly. -->

DiagnosticWizard (Plan 03, app/Livewire/DiagnosticWizard.php) — already has: computeAndPersist() (DoseEngine call + persistence + session seed + savedDiagnosticId), submitLead() (lead + Pierre mail), $disclaimerAccepted guard, the basic WhatsApp summary string. GROW it here with escalation classification + confidence — do not recreate.

config/diagnostic-tree.php (Plans 01+03) — result leaves; some leaves are inherently hors-DIY (electro-entartree acide, electro-panne coffret, electro-usee, major-imbalance). Add/read an escalation flag per leaf to drive preemptive escalation.

WhatsApp: wa.me/596696940054 (international form of 0696 94 00 54). Alpine :href + encodeURIComponent (RESEARCH Pattern 5, Pitfall 4 — test accents).

PDF route (Plan 05, same wave): route('diagnostic.pdf', $savedDiagnosticId) — guard the link with @if(Route::has('diagnostic.pdf')) so the wizard view never breaks if rendered before Plan 05 registers the route.

contact.whatsapp_number config key = 596696940054.
</interfaces>
</context>

<ui_design_context>
This slice builds UI surface S6 (one-gesture rich escalation, register BRAND — the conversion peak) and the confidence chip + PDF link on S5 (register product) — governed by 05-UI-SPEC.md. Read before building any view:
@.planning/phases/05-diagnostic-commercialisable/05-UI-SPEC.md
@DESIGN.md
@PRODUCT.md
@.impeccable/design.json
@resources/css/app.css
@mockups/diagnostic-dloazur.html
You MAY invoke the project impeccable skill (impeccable craft / impeccable polish on the escalation peak) to build to the contract; at minimum apply the 05-UI-SPEC.md S6/S5 rules + DESIGN.md @theme tokens. REUSE x-cta-whatsapp + x-icon.whatsapp (channel-correct green + glyph), the card primitive, and the status-chip styling for the confidence chip. Do NOT run ui-ux-pro-max (not installed).
</ui_design_context>

<tasks>

<task type="auto">
  <name>Task 1: Escalation classifier + confidence index (server-side) + tree escalation flags</name>
  <files>app/Livewire/DiagnosticWizard.php, config/diagnostic-tree.php</files>
  <read_first>
    - app/Livewire/DiagnosticWizard.php (the Plan 03 component — grow it; do not recreate)
    - 05-BLUEPRINT-APP.md section 5 (escalation engine: preemptive triggers = equipment / risky manip / major imbalance; reactive = plan executed + re-test no improvement, 2 failed attempts force escalation; the rich context list) and section 6 (action-aware table feeding the rich context)
    - 05-CDC-SAFE-DIAGNOSTIC.md section 5.5 (confidence index: eleve = based on measurements; moyen = refine by measuring stabilisant/TAC; indicatif = visual diagnosis without measurement) and section 5.7 (lead qualification a/b/c/d) and section 3 guard-rail 2 (do not over-escalate on easy DIY)
    - 05-DIAGNOSTIC-EXPERT-AUDIT.md section 6 (the particulier/pro limit: acide chlorhydrique, 230V, coffret diagnosis are pro-redirect by default — these are the preemptive escalation cases)
    - config/diagnostic-tree.php (the leaves — read which are hors-DIY; add an escalation flag/level per leaf)
    - 05-VALIDATION.md (the DIAG-06 escalation rows to assert)
  </read_first>
  <action>
    Add escalation flags to config/diagnostic-tree.php result leaves: mark hors-DIY leaves (electro-entartree acide path, electro-panne, electro-usee, major-imbalance / large-dose cases) with an escalation level (preemptive) and a short reason, per the audit section 6 particulier/pro limit and BLUEPRINT section 5. Extend app/Livewire/DiagnosticWizard.php with: (a) an escalation classifier method that returns preemptive when the reached leaf carries the hors-DIY flag OR the dose plan implies a risky manip / major imbalance (acide, large drain, electrical), reactive when a re-test-failed signal is present (the re-test loop lands in Plan 06; here expose the reactive entry hook + a property the loop can set), and none otherwise; (b) a confidence computation returning eleve / moyen / indicatif from the available inputs (measurements present and complete = eleve; partial measurements = moyen; purely visual symptom path, no measurements = indicatif) per CDC section 5.5; (c) an over-escalation guard so an easy DIY leaf (balanced water, simple symptom) never surfaces a preemptive escalation (CDC guard-rail 2). Assemble the rich-context payload (symptome, mesures + reliability note, filtre + volume, actions tentees + results, diagnostic, confidence, coordinates if provided) as a server-built string/array the view will encode for WhatsApp. Keep all of this server-side (it is classification logic, not dose math, but still belongs server-side for consistency and testability).
  </action>
  <acceptance_criteria>
    - The escalation classifier returns preemptive for a hors-DIY leaf (e.g. electro-entartree acide) and none for an easy DIY leaf (e.g. balanced-water / simple algues-parois) — asserted in DiagnosticWizardTest (Task 2) or a focused unit assertion.
    - The confidence index returns indicatif on the no-measurement symptom path, eleve on a complete measurement set, moyen on partial — per CDC section 5.5.
    - Over-escalation guard: no preemptive escalation on an easy DIY case (CDC guard-rail 2) — verified by an assertion.
    - The rich-context payload includes symptome, mesures, actions tentees, diagnostic, confidence (DIAG-06 / BLUEPRINT section 5 list).
    - DIAG-02 invariant holds (classification is not dose math; no coefficients added to JS).
  </acceptance_criteria>
  <verify>
    <automated>./vendor/bin/pest --filter DiagnosticWizard 2>&1 | tail -8</automated>
  </verify>
  <done>Tree leaves carry escalation flags; the wizard computes escalation (preemptive/reactive/none) with an over-escalation guard, computes the confidence index, and assembles the rich-context payload — all server-side.</done>
</task>

<task type="auto">
  <name>Task 2: S6 escalation peak UI + rich WhatsApp builder + S5 confidence chip + PDF link + extend DiagnosticWizardTest</name>
  <files>resources/views/livewire/diagnostic-wizard.blade.php, tests/Feature/DiagnosticWizardTest.php</files>
  <read_first>
    - resources/views/livewire/diagnostic-wizard.blade.php (the Plan 03 view — add the S6 escalation card + confidence chip + the PDF link; upgrade the WhatsApp :href to the rich payload)
    - 05-UI-SPEC.md S6 (BRAND register — committed/warm marine or azure card framing the WhatsApp green CTA; one gesture; context recap of what Pierre receives; preemptive vs reactive entry; a single restrained attention cue, no pulsing/looping/scale) and S5 confidence chip (success/warn chips with one-line explanation) and the Copywriting Contract (« Demander une intervention a Pierre », « Telecharger le rapport (PDF) », the confidence copy, the hard-stop redirect copy) and the Safety/escalation copy block
    - resources/views/components/cta-whatsapp.blade.php + resources/views/components/icon/whatsapp.blade.php (the channel-correct CTA to reuse)
    - 05-RESEARCH.md Pattern 5 + Pitfall 4 (Alpine :href wa.me/596696940054 + encodeURIComponent; test accented chars)
    - tests/Feature/DiagnosticWizardTest.php (extend with escalation + rich-context assertions) and tests/Feature/ContactFormTest.php (Livewire test idiom)
  </read_first>
  <action>
    Extend resources/views/livewire/diagnostic-wizard.blade.php with surface S6: an escalation recap card (BRAND register — marine/azure card, warm, framing the WhatsApp-green CTA « Demander une intervention a Pierre ») that appears at the escalation moment — preemptively when the classifier returns preemptive (collapsing the DIY steps and surfacing the recap of what Pierre will receive), and as an always-available secondary "appeler Pierre" seam from any plan screen. Upgrade the WhatsApp :href to encode the full rich-context payload from Task 1 via encodeURIComponent (Pattern 5). Add the hard-stop / pro-redirect danger-tinted callout (full ring, no side stripe) for the acide-chlorhydrique / 230V cases per the Copywriting Contract. Add the S5 confidence chip to the result screen: a success chip for « Confiance elevee », a warn chip for « Confiance moyenne » / « Indicatif », each with its one-line explanation (CDC section 5.5 copy). Add the « Telecharger le rapport (PDF) » link on the results screen pointing to route('diagnostic.pdf', $savedDiagnosticId), wrapped in @if(Route::has('diagnostic.pdf')) so it is safe if Plan 05's route is not yet registered (product register, exact Copywriting Contract CTA). Use the restrained one-time attention cue on the escalation CTA (a soft shadow bloom on reveal) — never pulsing/looping/scale.
    Extend tests/Feature/DiagnosticWizardTest.php: assert the WhatsApp deep link contains 596696940054 AND the rich context (symptome + mesures + actions tentees + diagnostic) — not just a static string; assert a hors-DIY leaf surfaces the preemptive escalation and an easy DIY leaf does not (over-escalation guard); assert the confidence index value for indicatif (no-measurement path) vs eleve (full measurements). Keep uses(RefreshDatabase::class).
  </action>
  <acceptance_criteria>
    - UI-SPEC S6 contract: BRAND register escalation peak (marine/azure card + WhatsApp green CTA); one gesture (single wa.me link, no long form); the CTA is « Demander une intervention a Pierre »; the attention cue is a single non-looping shadow bloom, no pulsing/scale; OKLCH only (sole hex #25D366 for the channel); no em dashes / gradient text / side-stripe borders (the hard-stop callout uses a full ring, not a left stripe).
    - UI-SPEC S5 confidence chip: success chip for eleve, warn chip (never red) for moyen/indicatif, each with the CDC one-line explanation.
    - The results screen has the « Telecharger le rapport (PDF) » link to route('diagnostic.pdf', ...) wrapped in @if(Route::has('diagnostic.pdf')).
    - DIAG-06: ./vendor/bin/pest --filter DiagnosticWizard asserts the WhatsApp link carries 596696940054 + the rich context (symptome/mesures/actions/diagnostic); preemptive escalation fires on a hors-DIY leaf and not on an easy DIY leaf.
    - WhatsApp message is encodeURIComponent-encoded (accents safe); grep finds wa.me/596696940054 in the view.
    - ./vendor/bin/pest full suite green.
  </acceptance_criteria>
  <verify>
    <automated>./vendor/bin/pest --filter DiagnosticWizard 2>&1 | tail -10; echo "whatsapp + pdf link present:"; grep -nE "wa.me/596696940054|diagnostic.pdf" resources/views/livewire/diagnostic-wizard.blade.php | head</automated>
  </verify>
  <done>The S6 escalation peak renders to the BRAND contract with a one-gesture rich-context WhatsApp CTA; the hard-stop redirect callout is present for risky cases; the S5 confidence chip shows eleve/moyen/indicatif; the guarded PDF download link is on the results screen; DiagnosticWizardTest asserts the rich-context link + escalation triggers + over-escalation guard; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic result to WhatsApp deep link | Diagnostic summary (incl. any user input) is serialized into a wa.me URL opened by the device |
| escalation classification to UI | Server-decided escalation level drives what the user is told (must not over-escalate or hide pro-redirect on risky cases) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | XSS / broken link via user input in the WhatsApp summary | mitigate | encodeURIComponent on the full message (RESEARCH Pitfall 4); Blade auto-escape for any rendered recap; never the raw-output directive. |
| T-05-13 | Safety/Repudiation | hiding the pro-redirect on acide/230V to inflate leads, or over-escalating easy DIY | mitigate | Hard-stop danger callout is mandatory on acide-chlorhydrique/230V leaves (audit section 6 / Copywriting Contract); over-escalation guard prevents preemptive escalation on easy DIY (CDC guard-rail 2) — both asserted. |
| T-05-14 | Information Disclosure | dose formulas in client bundle | mitigate | Escalation/confidence is classification, not dose math; doses still come from the Plan 02 server DoseEngine; DIAG-02 smoke grep unchanged. |
| T-05-SC | Tampering | npm/composer installs | accept | No new packages (RESEARCH Package Legitimacy Audit: zero new deps). No install task. |
</threat_model>

<verification>
- ./vendor/bin/pest --filter DiagnosticWizard green: rich-context WhatsApp link (596696940054 + symptome/mesures/actions/diagnostic), preemptive escalation on hors-DIY leaf, no escalation on easy DIY (guard), confidence index eleve/moyen/indicatif.
- S6 escalation peak built to the BRAND contract; S5 confidence chip to the product contract.
- Hard-stop pro-redirect callout present for acide/230V.
- The guarded « Telecharger le rapport (PDF) » link is on the results screen.
- DIAG-02 invariant unchanged (no dose math client-side).
- Full suite green.
</verification>

<success_criteria>
- DIAG-06 (full): contextualized one-gesture WhatsApp escalation, preemptive (hors-DIY) + reactive hook, rich pre-filled context (symptome, mesures, actions tentees, diagnostic, confidence, coordinates).
- Confidence index (CDC section 5.5) on every result with honest level + explanation.
- Over-escalation guard (CDC guard-rail 2) and hard-stop pro-redirect (audit section 6) both enforced.
- The PDF download link (guarded) is wired on the results screen.
- DiagnosticWizardTest extended and green.
</success_criteria>

<output>
Create `.planning/phases/05-diagnostic-commercialisable/05-04-SUMMARY.md` when done.
</output>
