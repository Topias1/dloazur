---
phase: 07-espace-admin-retours-pierre
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - database/migrations/2026_06_03_000003_create_passage_produit_table.php
  - app/Models/Passage.php
  - app/Models/Produit.php
  - app/Http/Controllers/Api/PassageProduitController.php
  - app/Http/Controllers/Admin/PassageCreateController.php
  - resources/views/admin/passages/create.blade.php
  - resources/js/passage-form.js
  - routes/api.php
  - tests/Feature/Api/PassageProduitSyncTest.php
autonomous: true
requirements: [admin-5]

must_haves:
  truths:
    - "Pierre coche les produits utilisés (quantité optionnelle) dans la saisie offline du passage"
    - "La consommation chimie traverse le MÊME chemin de synchro offline que le passage (Alpine + IndexedDB, jamais Livewire)"
    - "Au retour réseau, le pivot passage_produit est rempli avec prix_snapshot du produit au moment de la synchro"
  artifacts:
    - path: "database/migrations/2026_06_03_000003_create_passage_produit_table.php"
      provides: "Table pivot passage_produit {passage_id, produit_id, quantite nullable, prix_snapshot}"
      contains: "passage_produit"
    - path: "app/Models/Passage.php"
      provides: "Relation produits() BelongsToMany withPivot"
      contains: "belongsToMany"
    - path: "app/Http/Controllers/Api/PassageProduitController.php"
      provides: "Endpoint sync pivot avec prix_snapshot"
      exports: ["store"]
    - path: "resources/js/passage-form.js"
      provides: "État produits + _toPayload extension + _syncProduits offline"
      contains: "_syncProduits"
    - path: "resources/views/admin/passages/create.blade.php"
      provides: "Mini-sélecteur produits utilisés (Alpine x-for)"
      contains: "produitsDisponibles"
  key_links:
    - from: "resources/js/passage-form.js"
      to: "/api/passages/produits"
      via: "fetch POST après sync passage OK"
      pattern: "passages/produits"
    - from: "app/Http/Controllers/Api/PassageProduitController.php"
      to: "passage_produit pivot"
      via: "$passage->produits()->sync avec prix_snapshot"
      pattern: "prix_snapshot"
---

<objective>
Modéliser et capturer la **consommation chimie** (admin-5, partie schéma + saisie). Aujourd'hui `actions` est un simple tableau de textes ; aucune table ne relie produit + quantité + prix à un passage. On crée le pivot `passage_produit`, on ajoute un mini-sélecteur « produits utilisés » dans la saisie offline, et on synchronise la consommation par le **même chemin offline** que le passage (Alpine + IndexedDB + endpoint API) — sans refaire le bug admin-2.

Purpose: Donner au récap mensuel (Plan 07-04) une source de données réelle. Sélecteur léger : produits pré-listés depuis la table `produits`, quantité optionnelle pour minimiser la saisie terrain. `prix_snapshot` = prix HT brut au moment du passage (aucun calcul TVA — franchise 293 B, Phase 3).

Output: migration pivot + relations Passage/Produit + PassageProduitController + route API + état/payload/sync JS + section Blade + test de synchro pivot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-espace-admin-retours-pierre/07-CONTEXT.md
@.planning/phases/07-espace-admin-retours-pierre/07-PATTERNS.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- État réel vérifié. CONTRAINTE DURE : saisie offline = Alpine + IndexedDB, JAMAIS Livewire. -->

Produit model (app/Models/Produit.php) : $fillable = sku, libelle, description, prix_ht, unite, actif. `prix_ht` casté decimal:2. `actif` casté boolean. C'est `prix_ht` qui alimente prix_snapshot.

Passage model (app/Models/Passage.php) : $fillable inclut notes_privees (ajouté Plan 07-01). Relations existantes : client(), piscine(), photos(). Ajouter produits() BelongsToMany.

PassageController (analog pour PassageProduitController) : namespace App\Http\Controllers\Api, validation Request, JsonResponse. Bindings nommés si SQL brut. Ici on peut utiliser l'API Eloquent `->produits()->sync($map)` (pas de SQL brut nécessaire).

Routes API (routes/api.php) : `Route::post('passages', ...)` et `Route::post('passages/{uuid}/photos', ...)->whereUuid('uuid')`. La route photos est contrainte par `whereUuid` donc `passages/produits` ne sera PAS capturé comme {uuid} — mais par prudence déclarer `passages/produits` AVANT la route {uuid}/photos.

passage-form.js (resources/js/passage-form.js) état réel :
- initialData destructuré (clientId, piscineId, clients) — ligne 39-46.
- `notesPrivees: ''` ligne 74.
- `_toPayload()` ligne 291, retourne déjà `notes_privees` ligne 306.
- `$watch('notesPrivees', () => this._debouncedSave())` ligne 130 — pattern de watch debounced.
- `_uploadPassage(item)` ligne 363 : sur succès appelle `markStatus('passages', item.id, 'synced')` (lignes 377/383). C'est ici qu'il faut appeler `_syncProduits(item)` après le markStatus synced.
- Import depuis offline-queue.js : upsertPassage, savePhoto, getPassagesByStatus, getPhotosByPassage, markStatus, countPendingAll.

create.blade.php : x-data="passageForm({...})". Section « Actions menées » utilise `<template x-for="action in actionsAvailable">` avec label/checkbox custom — analog pour la section produits.

PassageCreateController (app/Http/Controllers/Admin/PassageCreateController.php) : injecte `$clients` server-side (offline-safe). Ajouter `$produits` de la même façon.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pivot passage_produit + relations Passage/Produit</name>
  <files>database/migrations/2026_06_03_000003_create_passage_produit_table.php, app/Models/Passage.php, app/Models/Produit.php</files>
  <read_first>
    - database/migrations/2026_05_28_000003_create_produits_table.php (analog create-table)
    - database/migrations/2026_05_28_000005_create_passages_table.php (style foreignId->constrained, lignes 16-17)
    - app/Models/Passage.php (où ajouter la relation produits())
    - app/Models/Produit.php (où ajouter la relation passages())
  </read_first>
  <action>
    Créer `2026_06_03_000003_create_passage_produit_table.php` : `Schema::create('passage_produit', ...)` avec `$table->id();`, `$table->foreignId('passage_id')->constrained('passages')->cascadeOnDelete();`, `$table->foreignId('produit_id')->constrained('produits')->cascadeOnDelete();`, `$table->decimal('quantite', 8, 2)->nullable();`, `$table->decimal('prix_snapshot', 10, 2)->nullable();` (prix HT au moment du passage), `$table->timestamps();`, `$table->unique(['passage_id', 'produit_id']);`. `down()` : `Schema::dropIfExists('passage_produit');`.
    Dans `app/Models/Passage.php`, ajouter `produits(): BelongsToMany` → `belongsToMany(Produit::class, 'passage_produit')->withPivot(['quantite', 'prix_snapshot'])->withTimestamps();`. Importer `Illuminate\Database\Eloquent\Relations\BelongsToMany`.
    Dans `app/Models/Produit.php`, ajouter `passages(): BelongsToMany` → `belongsToMany(Passage::class, 'passage_produit')->withPivot(['quantite', 'prix_snapshot'])->withTimestamps();`. Importer BelongsToMany.
  </action>
  <verify>
    <automated>php artisan migrate --env=testing 2>&1 | grep -qi "passage_produit\|DONE\|Nothing" && grep -q "belongsToMany" app/Models/Passage.php && grep -q "belongsToMany" app/Models/Produit.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration crée `passage_produit` avec colonnes passage_id, produit_id, quantite (nullable), prix_snapshot (nullable)
    - Migration contient `unique(['passage_id', 'produit_id'])`
    - `Passage::produits()` retourne un BelongsToMany avec `withPivot(['quantite', 'prix_snapshot'])`
    - `Produit::passages()` retourne un BelongsToMany symétrique
    - `php artisan migrate --pretend` n'erreure pas
  </acceptance_criteria>
  <done>Le pivot passage_produit existe et les deux relations BelongsToMany exposent quantite + prix_snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PassageProduitController + route API + test de synchro</name>
  <files>app/Http/Controllers/Api/PassageProduitController.php, routes/api.php, tests/Feature/Api/PassageProduitSyncTest.php</files>
  <read_first>
    - app/Http/Controllers/Api/PassageController.php (analog : validation, JsonResponse, structure)
    - routes/api.php (ordre des routes : déclarer produits avant {uuid}/photos)
    - app/Models/Produit.php (prix_ht source de prix_snapshot)
  </read_first>
  <behavior>
    - Test 1 : POST /api/passages/produits avec `{passage_client_uuid: <uuid d'un passage existant>, produits: [{produit_id: X, quantite: 2.5}]}` → 200 ET `assertDatabaseHas('passage_produit', ['passage_id' => ..., 'produit_id' => X, 'quantite' => 2.5, 'prix_snapshot' => <prix_ht de X>])`.
    - Test 2 (quantité optionnelle) : un produit sans quantité → ligne pivot créée avec quantite NULL, prix_snapshot quand même renseigné.
    - Test 3 (idempotence sync) : un second POST avec une liste différente remplace l'ensemble du pivot du passage (sync, pas attach) sans dupliquer.
    - Test 4 (validation) : un produit_id inexistant → 422.
  </behavior>
  <action>
    Créer `PassageProduitController` (namespace App\Http\Controllers\Api, extends Controller, méthode `store(Request): JsonResponse`). Validation : `passage_client_uuid` required uuid exists:passages,client_uuid ; `produits` nullable array ; `produits.*.produit_id` required integer exists:produits,id ; `produits.*.quantite` nullable numeric min:0 max:9999. Résoudre `$passage = Passage::where('client_uuid', $data['passage_client_uuid'])->firstOrFail();`. Construire le map de sync : pour chaque item, `$sync[$item['produit_id']] = ['quantite' => $item['quantite'] ?? null, 'prix_snapshot' => Produit::find($item['produit_id'])?->prix_ht];`. Appeler `$passage->produits()->sync($sync);`. Retourner `response()->json(['ok' => true], 200);`.
    Dans `routes/api.php` : importer `PassageProduitController`, ajouter `Route::post('passages/produits', [PassageProduitController::class, 'store'])->name('passages.produits.store');` AVANT la route `passages/{uuid}/photos`.
    Créer le test `tests/Feature/Api/PassageProduitSyncTest.php` couvrant les 4 comportements.
  </action>
  <verify>
    <automated>php artisan test --filter=PassageProduitSync 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list --path=api/passages/produits` liste la route POST nommée `api.passages.produits.store`
    - `php artisan test --filter=PassageProduitSync` sort en code 0
    - Le controller lit `prix_snapshot` depuis `Produit::find(...)->prix_ht`
    - Le controller utilise `->produits()->sync(...)` (remplace, idempotent — pas attach)
    - La route produits est déclarée avant `passages/{uuid}/photos` dans routes/api.php
  </acceptance_criteria>
  <done>POST /api/passages/produits remplit le pivot avec prix_snapshot, quantité optionnelle, idempotent, validé — prouvé par test vert.</done>
</task>

<task type="auto">
  <name>Task 3: Sélecteur produits offline (Blade + Alpine + sync JS)</name>
  <files>app/Http/Controllers/Admin/PassageCreateController.php, resources/views/admin/passages/create.blade.php, resources/js/passage-form.js</files>
  <read_first>
    - app/Http/Controllers/Admin/PassageCreateController.php (injection server-side de $clients — répliquer pour $produits)
    - resources/views/admin/passages/create.blade.php (section Actions menées = analog x-for/checkbox ; x-data passageForm)
    - resources/js/passage-form.js (état, _toPayload ligne 291-308, $watch ligne 130, _uploadPassage ligne 363 markStatus synced)
    - CLAUDE.md (contrainte : offline = Alpine + IndexedDB, JAMAIS Livewire)
  </read_first>
  <action>
    Dans `PassageCreateController::create()`, charger les produits actifs server-side (donc offline-safe) : `$produits = Produit::where('actif', true)->orderBy('libelle')->get(['id', 'libelle', 'prix_ht']);` et les passer au `compact(...)` de la vue. Importer `App\Models\Produit`.
    Dans `create.blade.php` : injecter les produits dans le x-data Alpine via `produits: @js($produits ?? [])` (ajout au paramètre objet de `passageForm({...})`). Ajouter une `<section>` « Produits utilisés <span>(optionnel)</span> » entre la section Actions et la section Notes, reproduisant le pattern `<template x-for="p in produitsDisponibles" :key="p.id">` avec un label/checkbox custom identique aux actions ; afficher `p.libelle` ; un `<input type="number" inputmode="decimal" min="0" step="0.1">` lié à `produitQuantites[p.id]` visible seulement quand le produit est coché (`x-show="isProduitSelected(p.id)"` + `x-cloak` + `@click.stop`). Style design system register product (mêmes classes que la section actions : `bg-azure-50 ring-azure-200` à l'état coché).
    Dans `resources/js/passage-form.js` :
    - État (après notesPrivees ligne 74) : `produitsDisponibles: initialData.produits ?? []`, `produitIds: []`, `produitQuantites: {}`.
    - Helpers (près des helpers actions) : `toggleProduit(id)` (ajoute/retire l'id de produitIds, nettoie produitQuantites à la dé-sélection), `isProduitSelected(id)`.
    - `_toPayload()` : ajouter en fin d'objet retourné `produits: this.produitIds.map((id) => ({ produit_id: id, quantite: this._num(this.produitQuantites[id] ?? '') }))`.
    - `init()` : ajouter `this.$watch('produitIds', () => this._debouncedSave()); this.$watch('produitQuantites', () => this._debouncedSave());` (persistance IndexedDB du brouillon).
    - `_uploadPassage(item)` : après le `markStatus('passages', item.id, 'synced')` du chemin succès, appeler `await this._syncProduits(item);` (avant l'upload photos).
    - Ajouter la méthode `_syncProduits(item)` : parser `item.payload_json`, lire `payload.produits ?? []`, si vide return ; sinon `fetch('/api/passages/produits', { method:'POST', headers:this._headers(true), credentials:'same-origin', body: JSON.stringify({ passage_client_uuid: item.client_uuid, produits }) })`. Échec = soft : `console.warn(...)`, NE bloque PAS la synchro principale du passage (le passage reste synced).
    NE JAMAIS utiliser Livewire pour cette saisie — Alpine + IndexedDB uniquement (contrainte CF-02). La consommation chimie suit exactement le même chemin offline que le passage.
  </action>
  <verify>
    <automated>grep -q "_syncProduits" resources/js/passage-form.js && grep -q "produitsDisponibles" resources/views/admin/passages/create.blade.php && grep -q "Produit::where" app/Http/Controllers/Admin/PassageCreateController.php && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `PassageCreateController` charge `Produit::where('actif', true)` et passe `$produits` à la vue
    - `create.blade.php` injecte `@js($produits` dans le x-data passageForm et contient une section avec `x-for="p in produitsDisponibles"`
    - `passage-form.js` : `_toPayload()` retourne un tableau `produits` de `{produit_id, quantite}`
    - `passage-form.js` : `_syncProduits` est appelé dans `_uploadPassage` APRÈS markStatus synced, et l'échec est soft (console.warn, pas de throw bloquant)
    - `passage-form.js` ne référence AUCUN composant Livewire (grep Livewire absent du fichier)
    - `npx vite build` se termine sans erreur (le JS compile)
  </acceptance_criteria>
  <done>Pierre coche des produits (quantité optionnelle) dans la saisie offline ; au retour réseau la consommation se synchronise via /api/passages/produits sur le même chemin offline que le passage, sans bloquer la synchro principale.</done>
</task>

</tasks>

<verification>
- `php artisan test --filter=PassageProduitSync` vert (pivot + prix_snapshot + idempotence + validation).
- `npx vite build` compile le JS modifié.
- `grep Livewire resources/js/passage-form.js` ne renvoie rien (contrainte offline respectée).
- La route `api/passages/produits` est déclarée avant `passages/{uuid}/photos`.
</verification>

<success_criteria>
- Pivot passage_produit modélise produit + quantité (optionnelle) + prix_snapshot HT.
- Sélecteur produits léger dans la saisie, offline-first (Alpine + IndexedDB, jamais Livewire).
- La chimie traverse le même chemin de synchro que le passage (ne reproduit pas le bug admin-2).
- Aucun calcul TVA (franchise 293 B — Phase 3).
</success_criteria>

<output>
Créer `.planning/phases/07-espace-admin-retours-pierre/07-03-SUMMARY.md` à la fin.
Documenter : schéma du pivot, endpoint de sync, et le point d'insertion de _syncProduits dans le cycle offline.
</output>
