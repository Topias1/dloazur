---
phase: 07-espace-admin-retours-pierre
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - app/Http/Controllers/Admin/RecapMensuelController.php
  - resources/views/admin/recap/index.blade.php
  - routes/admin.php
autonomous: true
requirements: [admin-5]

must_haves:
  truths:
    - "Pierre ouvre une page 'Récap mensuel par client' montrant, par client sur un mois, le nb de passages et la chimie consommée"
    - "Le récap est navigable par mois/année"
    - "Un bouton 'Générer la facture' est présent mais inerte (teaser Phase 3), aucune logique de facturation déclenchée"
  artifacts:
    - path: "app/Http/Controllers/Admin/RecapMensuelController.php"
      provides: "Agrégation passages + produits par client sur un mois"
      exports: ["index"]
    - path: "resources/views/admin/recap/index.blade.php"
      provides: "Vue récap mensuel + bouton facture inerte"
      contains: "Générer la facture"
    - path: "routes/admin.php"
      provides: "Route GET admin/recap nommée admin.recap.index"
      contains: "recap"
  key_links:
    - from: "app/Http/Controllers/Admin/RecapMensuelController.php"
      to: "passage_produit pivot"
      via: "passages.with(produits) agrégés par client/mois"
      pattern: "produits"
    - from: "resources/views/admin/recap/index.blade.php"
      to: "(inerte)"
      via: "bouton teaser sans action vers Phase 3"
      pattern: "Générer la facture"
---

<objective>
Construire la page **« Récap mensuel par client »** (admin-5, partie restitution) — le « cahier de fin de mois » de Pierre numérisé : par client, sur un mois donné, le nombre de passages + la chimie consommée (depuis le pivot `passage_produit` rempli par Plan 07-03). Le bouton « Générer la facture » est présent mais **inerte** — un teaser qui relie la Phase 3 (admin-4), sans aucune logique de facturation/PDF/Odoo dans cette phase.

Purpose: Donner à Pierre la vue de synthèse qu'il décrit, prête à alimenter la facturation Phase 3. AUCUN calcul TVA ici (franchise 293 B — la mention/total est Phase 3). `prix_snapshot` reste un prix HT brut affiché tel quel.

Output: `RecapMensuelController` (agrégation Eloquent par client/mois) + vue Blade (tableau récap + bouton inerte) + route admin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-espace-admin-retours-pierre/07-CONTEXT.md
@.planning/phases/07-espace-admin-retours-pierre/07-PATTERNS.md
@DESIGN.md

<interfaces>
<!-- Patterns réels. Register product. Scope fence : AUCUNE facturation/PDF/Odoo/TVA. -->

DashboardController (analog controller read-only) : namespace App\Http\Controllers\Admin, retourne view + compact, aucun Livewire.

Pivot passage_produit (créé Plan 07-03) : relation `Passage::produits()` BelongsToMany avec `withPivot(['quantite', 'prix_snapshot'])`. La chimie consommée d'un passage = `$passage->produits` avec `$produit->pivot->quantite` et `$produit->pivot->prix_snapshot`.

Client model : relation `passages()` HasMany. `name` = nom du client.
Passage : colonne `visited_at` (datetime) pour le filtrage par mois.

Layout admin (resources/views/admin/dashboard.blade.php) : `@extends('layouts.admin')` + sections + `<x-admin.mobile-bottom-nav />`.
Pattern tableau/liste : resources/views/admin/clients/show.blade.php (section passages, dl/dd, cards rounded-2xl).
Route admin existante : préfixe admin/, name admin., pattern `Route::get('clients', [ClientController::class, 'index'])->name('clients.index');`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecapMensuelController (agrégation par client/mois)</name>
  <files>app/Http/Controllers/Admin/RecapMensuelController.php, routes/admin.php</files>
  <read_first>
    - app/Http/Controllers/Admin/DashboardController.php (analog controller read-only)
    - app/Models/Client.php (relation passages, champ name)
    - app/Models/Passage.php (relation produits ajoutée Plan 07-03, colonne visited_at)
    - routes/admin.php (imports en tête + pattern d'enregistrement de route)
  </read_first>
  <action>
    Créer `RecapMensuelController` (namespace App\Http\Controllers\Admin, extends Controller, `index(Request): View`) :
    - `$mois = $request->integer('mois', now()->month);` et `$annee = $request->integer('annee', now()->year);`.
    - `$debut = Carbon::create($annee, $mois, 1)->startOfMonth();` et `$fin = $debut->copy()->endOfMonth();`.
    - Requête : `Client::query()->withCount(['passages as nb_passages' => fn ($q) => $q->whereBetween('visited_at', [$debut, $fin])])->having('nb_passages', '>', 0)->with(['passages' => fn ($q) => $q->whereBetween('visited_at', [$debut, $fin])->with('produits')])->orderBy('name')->get();` → `$clients`.
    - Retourner `view('admin.recap.index', compact('clients', 'mois', 'annee', 'debut'));`.
    Importer Client, Carbon, View, Request.
    Dans `routes/admin.php` : importer `RecapMensuelController`, ajouter `Route::get('recap', [RecapMensuelController::class, 'index'])->name('recap.index');`.
  </action>
  <verify>
    <automated>php artisan route:list --name=admin.recap 2>&1 | grep -q recap && php -l app/Http/Controllers/Admin/RecapMensuelController.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list --name=admin.recap` liste GET `admin/recap` → `admin.recap.index`
    - Le controller filtre les passages par `whereBetween('visited_at', [$debut, $fin])`
    - Le controller eager-load `produits` sur les passages (`->with('produits')`)
    - Le controller n'inclut AUCUN calcul TVA, AUCune génération PDF, AUCun appel Odoo
    - `php -l` : aucune erreur de syntaxe
  </acceptance_criteria>
  <done>GET admin/recap?mois=X&annee=Y retourne les clients ayant ≥1 passage sur le mois, avec leurs passages et la chimie consommée chargés.</done>
</task>

<task type="auto">
  <name>Task 2: Vue récap mensuel + bouton facture inerte</name>
  <files>resources/views/admin/recap/index.blade.php</files>
  <read_first>
    - resources/views/admin/clients/show.blade.php (layout wrapper + pattern table/dl)
    - resources/views/admin/dashboard.blade.php (sections layout admin)
    - DESIGN.md (tokens, register product, classes rounded-2xl/ring/azure)
  </read_first>
  <action>
    Créer `resources/views/admin/recap/index.blade.php` avec le wrapper `@extends('layouts.admin')` (title « Récap mensuel · Dlo Azur », sections sidebar/topbar via `<x-admin.sidebar>` / `<x-admin.topbar>`, `<x-admin.mobile-bottom-nav />` en fin de main, conteneur `px-5 sm:px-8 py-7 max-w-3xl space-y-6`).
    - En-tête : titre + un sélecteur de période simple (mois/année) sous forme de petit formulaire GET vers `route('admin.recap.index')` (deux `<select>` mois + année, soumission au change ou bouton « Voir »). Afficher le mois courant lisible (ex. « Juin 2026 ») depuis `$debut->locale('fr')->isoFormat('MMMM YYYY')`.
    - Pour chaque client de `$clients` : une card (`rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-6`) avec le nom du client, le nombre de passages (`$client->nb_passages`), et une liste « Chimie consommée » agrégeant les produits utilisés sur le mois : itérer les passages → produits, afficher `produit->libelle`, la quantité (`pivot->quantite` ou « — » si null), et le prix_snapshot HT (`pivot->prix_snapshot` affiché brut, sans TVA). Agréger par produit (sommer les quantités quand renseignées) — un total HT brut par produit acceptable. Si aucun produit : « Aucune chimie enregistrée ce mois ».
    - Bouton **inerte** « Générer la facture » : un `<span>` non cliquable (style `bg-sand-200 text-ink-400 cursor-not-allowed`) avec mention `(bientôt — Phase facturation)`. AUCUN href actif, aucune action. C'est un teaser vers la Phase 3.
    - État vide global : si `$clients` est vide, message « Aucun passage sur cette période ».
    AUCUN affichage de TVA, AUCun total TTC — uniquement des montants HT bruts (franchise 293 B, Phase 3 traite la facture).
  </action>
  <verify>
    <automated>grep -q "Générer la facture" resources/views/admin/recap/index.blade.php && grep -q "cursor-not-allowed" resources/views/admin/recap/index.blade.php && php artisan view:cache 2>&1 | tail -2 && php artisan view:clear >/dev/null 2>&1; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - La vue affiche `$client->nb_passages` et la chimie consommée par client (libelle + quantité + prix_snapshot HT)
    - Le bouton « Générer la facture » est un élément inerte (cursor-not-allowed, pas de href actif, pas de form submit vers une route de facturation)
    - La vue contient un sélecteur de période (mois/année) pointant vers `route('admin.recap.index')`
    - La vue ne contient AUCUNE chaîne « TVA », aucun total TTC
    - La vue a un état vide explicite quand aucun client/passage
    - `php artisan view:cache` compile la vue sans erreur
  </acceptance_criteria>
  <done>La page récap mensuel montre, par client et par mois, le nb de passages + la chimie consommée (HT brut), avec un bouton « Générer la facture » inerte qui annonce la Phase 3.</done>
</task>

</tasks>

<verification>
- `php artisan route:list --name=admin.recap` montre la route.
- `php artisan view:cache` compile la vue récap.
- `grep -i tva resources/views/admin/recap/index.blade.php` ne renvoie rien (scope fence : pas de TVA).
- Le bouton « Générer la facture » est inerte (cursor-not-allowed, aucun href/action).
</verification>

<success_criteria>
- Page « Récap mensuel par client » : nb passages + chimie consommée sur le mois.
- Navigable par mois/année.
- Bouton « Générer la facture » inerte (teaser Phase 3) — aucune logique de facturation/PDF/Odoo.
- Aucun calcul TVA (franchise 293 B).
- UI conforme au design system impeccable (register product).
</success_criteria>

<output>
Créer `.planning/phases/07-espace-admin-retours-pierre/07-04-SUMMARY.md` à la fin.
Documenter : route ajoutée, forme du sélecteur de période, méthode d'agrégation de la chimie, confirmation que le bouton facture est inerte.
</output>
