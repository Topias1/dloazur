---
phase: 07-espace-admin-retours-pierre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_03_000001_add_notes_privees_to_passages.php
  - app/Models/Passage.php
  - app/Http/Controllers/Api/PassageController.php
  - tests/Feature/Api/PassageNotesPriveesTest.php
autonomous: true
requirements: [admin-2]

must_haves:
  truths:
    - "Une note interne saisie offline persiste en base après synchro (POST /api/passages)"
    - "La note interne n'apparaît JAMAIS dans la timeline du portail client"
    - "La colonne notes_privees existe sur la table passages"
  artifacts:
    - path: "database/migrations/2026_06_03_000001_add_notes_privees_to_passages.php"
      provides: "Colonne notes_privees nullable text sur passages"
      contains: "notes_privees"
    - path: "app/Models/Passage.php"
      provides: "notes_privees dans $fillable"
      contains: "notes_privees"
    - path: "app/Http/Controllers/Api/PassageController.php"
      provides: "Upsert qui persiste notes_privees (INSERT + ON CONFLICT SET + binding)"
      contains: "notes_privees"
    - path: "tests/Feature/Api/PassageNotesPriveesTest.php"
      provides: "Test d'invariant : persiste après sync ET absent du portail"
      contains: "notes_privees"
  key_links:
    - from: "app/Http/Controllers/Api/PassageController.php"
      to: "passages.notes_privees"
      via: "INSERT column list + ON CONFLICT SET + bindings dict"
      pattern: "notes_privees"
    - from: "app/Livewire/Portail/PassageTimeline.php"
      to: "portail view"
      via: "notes_privees absent de la query et de la vue (invariant)"
      pattern: "notes_privees"
---

<objective>
Corriger le bug de perte de donnée `notes_privees` (admin-2, priorité haute). La note interne est aujourd'hui saisie dans le formulaire, envoyée par le JS offline, validée par l'API — puis **silencieusement effacée à la synchro** car la colonne est absente de la migration, de `$fillable`, et des trois listes de l'upsert (INSERT, ON CONFLICT SET, bindings).

Purpose: Restaurer une fonctionnalité saluée par Pierre puis cassée — une vraie perte de donnée terrain. Verrouiller l'invariant de vie privée : la note interne persiste côté serveur ET reste invisible côté portail client.

Output: migration add-column + `$fillable` + correction upsert (3 emplacements) + test Pest d'invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-espace-admin-retours-pierre/07-CONTEXT.md
@.planning/phases/07-espace-admin-retours-pierre/07-PATTERNS.md

<interfaces>
<!-- État réel du code (vérifié 2026-06-03). L'exécuteur n'a pas à explorer. -->

PassageController::store() (app/Http/Controllers/Api/PassageController.php) :
- La règle de validation `'notes_privees' => ['nullable', 'string', 'max:2000']` est DÉJÀ présente (ligne 48). Ne pas la dupliquer.
- INSERT column list (lignes 61-66) : se termine par `actions, notes,` — `notes_privees` MANQUE.
- VALUES list (lignes 67-72) : `:actions, :notes,` — `:notes_privees` MANQUE.
- ON CONFLICT DO UPDATE SET (lignes 73-88) : a `notes = EXCLUDED.notes,` puis `synced_at` — `notes_privees = EXCLUDED.notes_privees` MANQUE.
- Bindings dict (lignes 90-109) : a `'notes' => $data['notes'] ?? null,` — `'notes_privees' => ...` MANQUE.
- L'upsert utilise DB::affectingStatement() avec bindings nommés (PAS d'interpolation string).

Passage model (app/Models/Passage.php) :
- $fillable (lignes 16-34) liste `'notes'` puis `'pdf_path'`. `notes_privees` MANQUE.
- $casts (lignes 36-48) : pas de cast nécessaire pour notes_privees (texte simple).

Le JS `resources/js/passage-form.js` envoie DÉJÀ `notes_privees` dans `_toPayload()` (ligne 306). Aucune modif JS dans ce plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration notes_privees + $fillable Passage</name>
  <files>database/migrations/2026_06_03_000001_add_notes_privees_to_passages.php, app/Models/Passage.php</files>
  <read_first>
    - database/migrations/2026_05_30_000010_add_lead_columns_to_diagnostics_table.php (analog add-column pattern exact)
    - database/migrations/2026_05_28_000005_create_passages_table.php (schéma actuel passages, pour le `->after()`)
    - app/Models/Passage.php (état actuel de $fillable)
  </read_first>
  <action>
    Créer la migration `2026_06_03_000001_add_notes_privees_to_passages.php` suivant le pattern add-column de `add_lead_columns_to_diagnostics_table.php` : `Schema::table('passages', ...)`, ajouter `$table->text('notes_privees')->nullable()->after('notes');`. La méthode `down()` appelle `$table->dropColumn(['notes_privees']);`.
    Dans `app/Models/Passage.php`, ajouter `'notes_privees'` dans `$fillable` immédiatement après `'notes'` (avant `'pdf_path'`). Aucun cast à ajouter (texte simple).
  </action>
  <verify>
    <automated>php artisan migrate --env=testing 2>&1 | grep -qi "notes_privees\|DONE\|Nothing" && grep -q "'notes_privees'" app/Models/Passage.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier migration contient `Schema::table('passages'` et `text('notes_privees')->nullable()->after('notes')`
    - La migration `down()` contient `dropColumn(['notes_privees'])`
    - `app/Models/Passage.php` $fillable contient `'notes_privees'` placé après `'notes'`
    - `php artisan migrate --pretend` n'erreure pas sur cette migration
  </acceptance_criteria>
  <done>La colonne notes_privees existe sur passages après migration ET est mass-assignable via le modèle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix upsert PassageController (3 emplacements) + test d'invariant</name>
  <files>app/Http/Controllers/Api/PassageController.php, tests/Feature/Api/PassageNotesPriveesTest.php</files>
  <read_first>
    - app/Http/Controllers/Api/PassageController.php (les 3 emplacements à corriger : INSERT/VALUES, ON CONFLICT SET, bindings)
    - app/Livewire/Portail/PassageTimeline.php (INVARIANT — query du portail, notes_privees doit y rester absent ; NE PAS modifier)
    - resources/views/livewire/portail/passage-timeline.blade.php (vue portail — notes_privees ne doit jamais y figurer)
    - tests/Feature (chercher un test existant POST /api/passages comme analog de structure : actingAs, postJson, assertDatabaseHas)
  </read_first>
  <behavior>
    - Test 1 (persistance) : POST /api/passages avec un client_uuid neuf + `notes_privees: "code portail 1234"` → réponse 200 ET `assertDatabaseHas('passages', ['client_uuid' => ..., 'notes_privees' => 'code portail 1234'])`.
    - Test 2 (invariant vie privée) : après ce POST, rendre la timeline portail du client (Livewire test sur PassageTimeline OU assertion sur le HTML rendu de la vue portail) → la chaîne `code portail 1234` est ABSENTE de la sortie. Ne jamais exposer notes_privees au portail.
    - Test 3 (idempotence préservée) : un second POST avec le même client_uuid et une note modifiée met à jour notes_privees (status reste draft) — le fix ne casse pas l'ON CONFLICT existant.
  </behavior>
  <action>
    Dans `PassageController::store()` corriger les 3 emplacements (la validation ligne 48 reste inchangée) :
    1. INSERT column list : ajouter `notes_privees` après `notes,` dans la liste des colonnes ; ajouter `:notes_privees` après `:notes,` dans la liste VALUES.
    2. ON CONFLICT DO UPDATE SET : ajouter la ligne `notes_privees = EXCLUDED.notes_privees,` juste après `notes = EXCLUDED.notes,` (avant `synced_at = :synced_at2`).
    3. Bindings dict : ajouter `'notes_privees' => $data['notes_privees'] ?? null,` juste après `'notes' => $data['notes'] ?? null,`.
    Conserver l'usage de `DB::affectingStatement()` et des bindings nommés — aucune interpolation string.
    Créer le test `tests/Feature/Api/PassageNotesPriveesTest.php` couvrant les 3 comportements ci-dessus. Pour Test 2, requêter/rendre la timeline portail réelle (PassageTimeline) afin de prouver l'invariant sur le vrai chemin de lecture client, pas seulement une assertion de DB.
  </action>
  <verify>
    <automated>php artisan test --filter=PassageNotesPrivees 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=PassageNotesPrivees` sort en code 0 (tous verts)
    - `grep -c notes_privees app/Http/Controllers/Api/PassageController.php` ≥ 5 (validation + colonne INSERT + VALUES + SET + binding)
    - Le test contient un `assertDatabaseHas('passages', [... 'notes_privees' => ...])`
    - Le test prouve l'absence de la valeur notes_privees dans la sortie rendue de PassageTimeline (assertDontSee ou équivalent)
    - `app/Livewire/Portail/PassageTimeline.php` n'a PAS été modifié (invariant : aucune addition de notes_privees)
  </acceptance_criteria>
  <done>La note interne persiste après synchro POST /api/passages ET n'apparaît jamais dans la timeline portail client, prouvé par test vert.</done>
</task>

</tasks>

<verification>
- `php artisan migrate --pretend` passe la nouvelle migration sans erreur.
- `php artisan test --filter=PassageNotesPrivees` vert (persistance + invariant + idempotence).
- `grep -n notes_privees app/Livewire/Portail/PassageTimeline.php` ne renvoie rien (invariant respecté).
</verification>

<success_criteria>
- notes_privees persiste à la synchro (migration + $fillable + upsert 3 emplacements).
- notes_privees reste invisible côté portail client, prouvé par un test Pest sur PassageTimeline.
- Aucune régression sur l'idempotence client_uuid de l'upsert.
</success_criteria>

<output>
Créer `.planning/phases/07-espace-admin-retours-pierre/07-01-SUMMARY.md` à la fin.
Documenter : nom exact de la migration, emplacements corrigés, et confirmation de l'invariant portail.
</output>
