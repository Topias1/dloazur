---
phase: 07-espace-admin-retours-pierre
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - database/migrations/2026_06_03_000002_add_frequence_to_piscines.php
  - app/Models/Piscine.php
  - app/Http/Controllers/Admin/AgendaController.php
  - resources/views/admin/agenda/index.blade.php
  - resources/views/components/admin/sidebar.blade.php
  - resources/views/components/admin/mobile-bottom-nav.blade.php
  - routes/admin.php
  - tests/Feature/AgendaTest.php
autonomous: true
requirements: [admin-1]

must_haves:
  truths:
    - "Pierre voit la liste des piscines/clients attendus aujourd'hui, dérivée d'une fréquence portée par la piscine (zéro saisie de RDV)"
    - "Chaque ligne de l'agenda mène en un clic à la saisie pré-remplie (?client_id=X)"
    - "Les passages récents avec une note interne remontent comme flags 'à revoir' dans l'agenda"
    - "L'agenda est accessible depuis la navigation admin (sidebar desktop + nav mobile), pas seulement par URL directe"
  artifacts:
    - path: "database/migrations/2026_06_03_000002_add_frequence_to_piscines.php"
      provides: "Colonne frequence_jour nullable string sur piscines"
      contains: "frequence_jour"
    - path: "app/Http/Controllers/Admin/AgendaController.php"
      provides: "Query piscines du jour (dérivée frequence_jour) + flags à revoir (notes_privees)"
      exports: ["index"]
    - path: "resources/views/admin/agenda/index.blade.php"
      provides: "Vue agenda du jour + liens vers saisie pré-remplie"
      contains: "admin.passages.create"
    - path: "resources/views/components/admin/sidebar.blade.php"
      provides: "Lien de navigation 'Mon agenda' vers admin.agenda.index"
      contains: "admin.agenda.index"
    - path: "routes/admin.php"
      provides: "Route GET admin/agenda nommée admin.agenda.index"
      contains: "agenda"
    - path: "tests/Feature/AgendaTest.php"
      provides: "Test Feature prouvant la dérivation frequence_jour→piscines du jour + invariant note non divulguée"
      contains: "frequence_jour"
  key_links:
    - from: "resources/views/admin/agenda/index.blade.php"
      to: "admin.passages.create"
      via: "route() avec client_id"
      pattern: "admin\\.passages\\.create"
    - from: "app/Http/Controllers/Admin/AgendaController.php"
      to: "piscines.frequence_jour"
      via: "where(frequence_jour, today)"
      pattern: "frequence_jour"
    - from: "resources/views/components/admin/sidebar.blade.php"
      to: "admin.agenda.index"
      via: "route() dans la liste de nav"
      pattern: "admin\\.agenda\\.index"
---

<objective>
Construire l'**agenda du jour** admin (admin-1) — la porte d'entrée que Pierre décrit : « voir mes piscines du jour → cliquer le client → saisir ». L'agenda est **DÉRIVÉ d'une fréquence/jour de passage** portée par la piscine (décision verrouillée : zéro CRUD de RDV pour un pro solo). Y faire remonter les flags « à revoir » issus des notes internes (lien avec admin-2). **Le câbler dans la navigation admin** (sidebar + nav mobile) pour qu'il soit atteignable, et le **prouver par un test Feature**.

Purpose: Réutiliser le chemin de saisie existant (`?client_id=X`, auto-sélection piscine) ; ne pas réinventer la saisie. L'agenda déduit la tournée du jour de la cadence de chaque piscine.

Output: migration `frequence_jour` + `$fillable` Piscine + `AgendaController` read-only (pattern DashboardController) + vue Blade + route admin + entrées de navigation (sidebar/mobile) + test Feature AgendaTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-espace-admin-retours-pierre/07-CONTEXT.md
@.planning/phases/07-espace-admin-retours-pierre/07-PATTERNS.md
@PRODUCT.md
@DESIGN.md

<interfaces>
<!-- Patterns réels vérifiés. Suivre le design system impeccable, register product. -->

DashboardController (app/Http/Controllers/Admin/DashboardController.php) = analog : namespace App\Http\Controllers\Admin, extends Controller, retourne view('admin.…', compact(...)). Read-only, AUCUN Livewire.

Route admin existante (routes/admin.php) — déjà importées : ClientController, DashboardController, PassageCreateController, PostController. Préfixe `admin/`, name `admin.`, middleware web+auth (déclaré dans bootstrap/app.php). Pattern d'enregistrement : `Route::get('clients', [ClientController::class, 'index'])->name('clients.index');`

Saisie pré-remplie : route nommée `admin.passages.create` accepte `?client_id=X` (PassageCreateController auto-sélectionne la piscine si le client n'en a qu'une — D-64). Lien Blade existant (clients/show.blade.php) : `route('admin.passages.create', ['client_id' => $client->id])`.

Piscine model (app/Models/Piscine.php) : $fillable se termine par `'notes'`. Relation `client()` BelongsTo existe. Ajouter `frequence_jour` après `'notes'`.

notes_privees existe sur passages (créé par Plan 07-01) — c'est la source des flags « à revoir ».

Layout admin (resources/views/admin/dashboard.blade.php) : `@extends('layouts.admin')` + sections title/sidebar/topbar/main + `<x-admin.mobile-bottom-nav />` en fin de main.

Sidebar admin (resources/views/components/admin/sidebar.blade.php) : `<nav class="space-y-1 text-sm font-medium">` contient une liste de liens `<a href="{{ route('admin.xxx') }}" @class([...])>` avec un `<svg>` 20×20 puis le libellé. Les liens actifs utilisent le pattern `request()->routeIs('admin.xxx.*')` pour `bg-white/10 text-white` + `aria-current="page"`. Insérer le nouveau lien « Mon agenda » juste APRÈS le lien « Tableau de bord » (c'est la porte d'entrée quotidienne).

Nav mobile admin (resources/views/components/admin/mobile-bottom-nav.blade.php) : `<nav class="... grid grid-cols-4 h-18 ...">` avec 4 cellules (Tableau de bord, Clients, Passages, Factures-greyed). Chaque item actif = `<a href="{{ route('admin.xxx') }}" class="flex flex-col items-center justify-center gap-1 ...">` + svg 22×22 + libellé `text-[11px]`. Pour ajouter Agenda sans déborder : passer la grille à `grid-cols-5` et insérer la cellule « Agenda » juste après « Accueil ».
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration frequence_jour + $fillable Piscine</name>
  <files>database/migrations/2026_06_03_000002_add_frequence_to_piscines.php, app/Models/Piscine.php</files>
  <read_first>
    - database/migrations/2026_05_30_000010_add_lead_columns_to_diagnostics_table.php (analog add-column exact)
    - app/Models/Piscine.php (état actuel de $fillable, ligne 14-23)
  </read_first>
  <action>
    Créer la migration `2026_06_03_000002_add_frequence_to_piscines.php` : `Schema::table('piscines', ...)`, ajouter `$table->string('frequence_jour', 16)->nullable()->after('notes');`. Choix retenu (Claude's Discretion verrouillée ici) : un champ string simple stockant le jour de la semaine en français minuscule (`lundi`, `mardi`, …, `dimanche`) tel que produit par `Carbon::now()->locale('fr')->isoFormat('dddd')`, pour matcher directement la query de l'agenda. `down()` appelle `dropColumn(['frequence_jour'])`.
    Dans `app/Models/Piscine.php`, ajouter `'frequence_jour'` dans `$fillable` après `'notes'`.
  </action>
  <verify>
    <automated>php artisan migrate --env=testing 2>&1 | grep -qi "frequence\|DONE\|Nothing" && grep -q "'frequence_jour'" app/Models/Piscine.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration contient `Schema::table('piscines'` et `string('frequence_jour', 16)->nullable()->after('notes')`
    - Migration `down()` contient `dropColumn(['frequence_jour'])`
    - `app/Models/Piscine.php` $fillable contient `'frequence_jour'`
    - `php artisan migrate --pretend` n'erreure pas
  </acceptance_criteria>
  <done>La colonne frequence_jour existe sur piscines et est mass-assignable, format jour-fr minuscule aligné sur la query agenda.</done>
</task>

<task type="auto">
  <name>Task 2: AgendaController + vue + route + entrées de navigation (sidebar & mobile)</name>
  <files>app/Http/Controllers/Admin/AgendaController.php, resources/views/admin/agenda/index.blade.php, routes/admin.php, resources/views/components/admin/sidebar.blade.php, resources/views/components/admin/mobile-bottom-nav.blade.php</files>
  <read_first>
    - app/Http/Controllers/Admin/DashboardController.php (analog controller read-only complet)
    - resources/views/admin/dashboard.blade.php (layout wrapper admin)
    - resources/views/admin/clients/show.blade.php (pattern card dl/dd + lien "Nouveau passage")
    - routes/admin.php (où enregistrer la route + imports en tête de fichier)
    - resources/views/components/admin/sidebar.blade.php (liste de nav, pattern @class + request()->routeIs)
    - resources/views/components/admin/mobile-bottom-nav.blade.php (grid-cols-4, cellules item)
  </read_first>
  <action>
    Créer `AgendaController` (namespace App\Http\Controllers\Admin, extends Controller, méthode `index(Request): View`) :
    - Jour courant : `$today = Carbon::now()->locale('fr')->isoFormat('dddd');` (ex. 'lundi').
    - `$piscinesAujourdhui` : `Piscine::where('frequence_jour', $today)->with(['client:id,name'])->orderBy('nom')->get();` (les piscines attendues aujourd'hui, dérivées de la fréquence).
    - `$aRevoir` : `Passage::whereNotNull('notes_privees')->where('visited_at', '>=', Carbon::now()->subDays(7))->with(['client:id,name', 'piscine:id,nom'])->orderByDesc('visited_at')->get();` (flags « à revoir » issus des notes internes — lien admin-2). Décision : afficher le texte de la note car c'est l'espace admin privé de Pierre (jamais le portail).
    - Retourner `view('admin.agenda.index', compact('piscinesAujourdhui', 'aRevoir', 'today'))`.
    Créer la vue `resources/views/admin/agenda/index.blade.php` avec le wrapper `@extends('layouts.admin')` (title « Mon agenda du jour · Dlo Azur », sections sidebar/topbar via les composants `<x-admin.sidebar>` / `<x-admin.topbar>`, `<x-admin.mobile-bottom-nav />` en fin de main). Deux blocs cards (style `rounded-2xl bg-white ring-1 ring-navy-900/8 ...` du design system, register product) :
      1. « Aujourd'hui » : pour chaque piscine, nom client + nom piscine + un lien CTA vers `route('admin.passages.create', ['client_id' => $piscine->client_id])` (style bouton azure-500 du pattern clients/show). État vide : message « Aucune piscine prévue aujourd'hui ».
      2. « À revoir » : pour chaque passage de `$aRevoir`, badge + client/piscine + date + texte de la note, avec lien vers la saisie du même client. État vide : « Rien à revoir ».
    Enregistrer dans `routes/admin.php` : importer `AgendaController` en tête, ajouter `Route::get('agenda', [AgendaController::class, 'index'])->name('agenda.index');`.

    CÂBLAGE NAVIGATION (sinon l'agenda est inaccessible depuis le back-office) :
    - `resources/views/components/admin/sidebar.blade.php` : dans le `<nav>`, juste APRÈS le bloc « Tableau de bord », insérer un lien « Mon agenda » sur le même pattern que les autres : `<a href="{{ route('admin.agenda.index') }}" @class([...])>` avec état actif basé sur `request()->routeIs('admin.agenda.*')` (`bg-white/10 text-white` + `aria-current="page"`, sinon `hover:bg-white/8 hover:text-white`), un `<svg width="20" height="20" ...>` (icône calendrier — réutiliser le path calendrier du lien Passages : `M8 2v4M16 2v4M3 10h18M5 4h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2Z`), libellé « Mon agenda ».
    - `resources/views/components/admin/mobile-bottom-nav.blade.php` : passer la grille de `grid-cols-4` à `grid-cols-5`, et insérer une cellule « Agenda » juste après « Accueil », sur le même pattern : `<a href="{{ route('admin.agenda.index') }}" class="flex flex-col items-center justify-center gap-1 {{ request()->routeIs('admin.agenda.*') ? 'text-azure-600' : 'text-ink-400' }}" @if(request()->routeIs('admin.agenda.*')) aria-current="page" @endif>` + svg 22×22 (même path calendrier) + `<span class="text-[11px] font-semibold">Agenda</span>`.
  </action>
  <verify>
    <automated>php artisan route:list --name=admin.agenda 2>&1 | grep -q agenda && php -l app/Http/Controllers/Admin/AgendaController.php && grep -q "admin.agenda.index" resources/views/components/admin/sidebar.blade.php && grep -q "admin.agenda.index" resources/views/components/admin/mobile-bottom-nav.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list --name=admin.agenda` liste la route GET `admin/agenda` → `admin.agenda.index`
    - `AgendaController::index` requête `Piscine::where('frequence_jour', $today)` (grep `frequence_jour` dans le controller ≥ 1)
    - `AgendaController::index` construit `$aRevoir` avec `whereNotNull('notes_privees')`
    - La vue `agenda/index.blade.php` contient `route('admin.passages.create'` avec `client_id`
    - La vue contient un état vide explicite pour chacun des deux blocs (pas de page blanche si aucune donnée)
    - `resources/views/components/admin/sidebar.blade.php` contient un lien `route('admin.agenda.index')` (grep `admin.agenda.index` ≥ 1)
    - `resources/views/components/admin/mobile-bottom-nav.blade.php` contient un lien `route('admin.agenda.index')` ET la grille est `grid-cols-5` (plus de cellule orpheline)
    - `php -l` sur le controller : aucune erreur de syntaxe
  </acceptance_criteria>
  <done>Une requête GET admin/agenda affiche les piscines du jour (dérivées de frequence_jour) avec liens vers la saisie pré-remplie et la section « à revoir » ; l'agenda est atteignable en un clic depuis la sidebar desktop ET la nav mobile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test Feature AgendaTest (dérivation jour + invariant note privée)</name>
  <files>tests/Feature/AgendaTest.php</files>
  <read_first>
    - tests/Feature/DashboardStatsTest.php (pattern Pest admin : actingAs($admin)->get('/admin'), factories Client/Piscine/Passage, assertSee/assertDontSee)
    - tests/Feature/PassageCreateViewTest.php (analog : seed Piscine + Client, assertions sur la vue admin)
    - app/Http/Controllers/Admin/AgendaController.php (la logique sous test — créé Task 2)
  </read_first>
  <behavior>
    - Test 1 (dérivation jour) : créer une Piscine `frequence_jour='lundi'` (client A) et une Piscine `frequence_jour='mardi'` (client B) ; figer la date au lundi via `Carbon::setTestNow(Carbon::parse('2026-06-08'))` (un lundi — vérifier le jour réel à l'écriture du test, sinon ajuster) ; `actingAs($admin)->get('/admin/agenda')` → `assertOk()`, `assertSee` le client A (lundi), `assertDontSee` le client B (mardi).
    - Test 2 (flag à revoir) : un Passage récent (`visited_at` dans les 7 jours) avec `notes_privees` non nul → l'agenda `assertSee` le client du passage dans le bloc « À revoir ».
    - Test 3 (état vide) : aucune piscine du jour ni note → `assertSee('Aucune piscine prévue aujourd'hui')` et `assertSee('Rien à revoir')`.
    - Penser à `Carbon::setTestNow(null)` en fin de test (ou `afterEach`).
  </behavior>
  <action>
    Créer `tests/Feature/AgendaTest.php` (Pest, `uses(RefreshDatabase::class)` selon la convention du fichier analog). Utiliser les factories existantes (User pour l'admin, Client, Piscine, Passage) ; créer l'admin via la factory User comme dans DashboardStatsTest. Implémenter les 3 comportements ci-dessus. Geler le temps avec `Carbon::setTestNow()` pour rendre la dérivation `frequence_jour → jour courant` déterministe — c'est le cœur de ce que ce test prouve.
  </action>
  <verify>
    <automated>php artisan test --filter=AgendaTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=AgendaTest` sort en code 0
    - Le test fige le temps (`Carbon::setTestNow`) et prouve qu'une piscine `frequence_jour='lundi'` apparaît un lundi et qu'une piscine d'un autre jour n'apparaît pas (`assertDontSee`)
    - Le test couvre le bloc « À revoir » (passage récent porteur d'une note interne)
    - Le test couvre les deux états vides
  </acceptance_criteria>
  <done>La dérivation frequence_jour→piscines du jour et le flag « à revoir » sont prouvés automatiquement par un test Feature vert.</done>
</task>

</tasks>

<verification>
- `php artisan route:list --name=admin.agenda` montre la route.
- `php artisan migrate --pretend` passe la migration frequence_jour.
- La vue agenda lie vers `admin.passages.create` avec client_id (réutilise le chemin existant, zéro nouvelle logique de saisie).
- La sidebar ET la nav mobile exposent un lien `admin.agenda.index` (l'agenda est atteignable depuis le back-office).
- `php artisan test --filter=AgendaTest` vert (dérivation du jour prouvée).
</verification>

<success_criteria>
- Agenda du jour dérivé de frequence_jour (zéro CRUD de RDV).
- Chaque ligne mène à la saisie pré-remplie existante.
- Flags « à revoir » (notes internes) remontés dans l'agenda.
- Agenda câblé dans la navigation admin (sidebar + mobile), pas seulement par URL.
- UI conforme au design system impeccable (register product).
</success_criteria>

<output>
Créer `.planning/phases/07-espace-admin-retours-pierre/07-02-SUMMARY.md` à la fin.
Documenter : format retenu pour frequence_jour, route ajoutée, entrées de navigation (sidebar + mobile, grid-cols-5), comportement des deux blocs (aujourd'hui / à revoir), couverture du test AgendaTest.
</output>
