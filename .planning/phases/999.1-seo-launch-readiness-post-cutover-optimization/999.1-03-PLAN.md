---
phase: 999.1-seo-launch-readiness-post-cutover-optimization
plan: 03
type: execute
wave: 2
depends_on: ["999.1-02", "999.1-05"]
files_modified:
  - app/Support/SchemaOrg/BreadcrumbSchema.php
  - app/Http/Controllers/VitrineController.php
  - routes/vitrine.php
  - resources/views/layouts/app.blade.php
  - resources/views/vitrine/services/entretien-recurrent.blade.php
  - resources/views/vitrine/services/analyse-eau.blade.php
  - resources/views/vitrine/services/spa.blade.php
  - resources/views/vitrine/services/eau-verte-urgence.blade.php
  - tests/Feature/Seo/VitrinePagesTest.php
  - tests/Feature/Seo/BreadcrumbTest.php
autonomous: true
requirements: [SITE-07]
must_haves:
  truths:
    - "Routes /services/entretien-recurrent, /services/analyse-eau, /services/spa return 200"
    - "Each new service page and the expanded eau-verte page emits a BreadcrumbList JSON-LD matching its visible breadcrumb"
    - "eau-verte-urgence is expanded to 450+ words with causes/DIY/protocol/FAQ sections"
    - "Service pages cover spa as well as pools (D-11)"
  artifacts:
    - path: "app/Support/SchemaOrg/BreadcrumbSchema.php"
      provides: "BreadcrumbList JSON-LD builder"
      contains: "breadcrumbList"
    - path: "resources/views/vitrine/services/spa.blade.php"
      provides: "Spa maintenance service page (D-11)"
      min_lines: 30
    - path: "resources/views/vitrine/services/entretien-recurrent.blade.php"
      provides: "Recurring maintenance service page"
      min_lines: 30
    - path: "resources/views/vitrine/services/analyse-eau.blade.php"
      provides: "Water analysis service page"
      min_lines: 30
  key_links:
    - from: "routes/vitrine.php"
      to: "app/Http/Controllers/VitrineController.php"
      via: "named routes services.entretien-recurrent / services.analyse-eau / services.spa"
      pattern: "services\\.(entretien-recurrent|analyse-eau|spa)"
    - from: "app/Http/Controllers/VitrineController.php"
      to: "app/Support/SchemaOrg/BreadcrumbSchema.php"
      via: "per-method DI, passes $breadcrumbJsonLd to view"
      pattern: "BreadcrumbSchema"
    - from: "resources/views/layouts/app.blade.php"
      to: "$breadcrumbJsonLd"
      via: "renders breadcrumb JSON-LD slot + forwards $type to <x-seo.meta>"
      pattern: "breadcrumbJsonLd"
---

<objective>
Build the dedicated service-page architecture (D-10, D-11): a `BreadcrumbSchema` builder, layout wiring for `$breadcrumbJsonLd` and `$type`, four `VitrineController` methods + routes, three NEW service Blade pages (entretien-recurrent, analyse-eau, spa), and the expansion of the thin `/services/eau-verte-urgence` page to 450+ words with FAQ (D-13). Content is authored from the Zyro harvest (plan 02) with `[À compléter — Pierre ADAM]` placeholders where facts are missing (D-09).

Purpose: fixes the audit "page-type mismatch (single-URL vitrine)" and "eau-verte too thin" findings, and adds BreadcrumbList structured data on sub-pages. The spa page is mandatory — the live Zyro site markets "piscines et spa" (D-11).
Output: BreadcrumbSchema + 4 service pages (3 new, 1 expanded) + controller/route/layout wiring + Pest tests.

Interface-first ordering: Task 1 creates the BreadcrumbSchema contract + layout slot; Task 2 wires the controller/routes; Task 3 authors the Blade pages against the established props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-CONTEXT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md
@resources/views/vitrine/services/eau-verte-urgence.blade.php
@DESIGN.md

<interfaces>
<!-- Verified from codebase -->
VitrineController (final class extends Controller, namespace App\Http\Controllers):
- `home(LocalBusinessSchema $schema): View` — per-method DI pattern to copy.
- `eauVerteUrgence(): View` returns `view('vitrine.services.eau-verte-urgence', ['title','description','canonical'=>url('/services/eau-verte-urgence'),'ogImage'=>asset(...)])`.

routes/vitrine.php: new routes go INSIDE the existing `Route::middleware('cache.headers:vitrine')->group(...)`. Naming convention `services.{slug}`.

layouts/app.blade.php (lines 7-17): currently calls `<x-seo.meta :title :description :image :canonical />` (NO :type) and renders `@if(isset($jsonLd) && $jsonLd) {!! $jsonLd !!} @endif`. There is `@stack('head')` at line 20. NEEDS: add `:type="$type ?? 'website'"` to the meta call, and add a breadcrumb slot `@if(isset($breadcrumbJsonLd) && $breadcrumbJsonLd) {!! $breadcrumbJsonLd !!} @endif`.

<x-seo.meta> already accepts a `type` prop (default 'website') and emits og:type from it (meta.blade.php:6).

BreadcrumbSchema (NEW) — analog app/Support/SchemaOrg/LocalBusinessSchema.php (same namespace, `final class`, `toScript()`):
- `public function toScript(array $crumbs): string` where each crumb is `['name' => ..., 'url' => ...]`.
- Builds `Schema::breadcrumbList()->itemListElement([Schema::listItem()->position($i+1)->name(...)->item(...)])->toScript()`.

<x-picture> component (from plan 05): `<x-picture src="assets/..." alt="..." class="..." loading="eager|lazy" />` — use for inline images; og:image stays .jpg.

Tailwind tokens (UI-SPEC / DESIGN.md): dark hero `bg-navy-900`; body bg `bg-sand-50`; headings `text-ink-950 font-display font-bold`; body `text-ink-700 leading-relaxed max-w-[65ch]`; primary CTA `bg-azure-500 text-white hover:bg-azure-600`; WhatsApp CTA `bg-[#25D366]`; kicker `text-xs font-bold uppercase tracking-[0.18em] text-lagon-600` (or lagon-400 on dark). Touch targets min-h-[44px]. h1 `clamp(2.6rem,5vw,4rem)`, h2 `clamp(1.875rem,3vw,2.5rem)`. Exactly 2 weights (400/700), 4 size roles.
WhatsApp number: `https://wa.me/596696940054`. tel: `+596696940054`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BreadcrumbSchema + wire layout for breadcrumb + type</name>
  <files>app/Support/SchemaOrg/BreadcrumbSchema.php, resources/views/layouts/app.blade.php, tests/Feature/Seo/BreadcrumbTest.php</files>
  <read_first>
    - app/Support/SchemaOrg/LocalBusinessSchema.php (analog: namespace, final class, toScript convention)
    - resources/views/layouts/app.blade.php (the layout being edited — lines 7-20: meta call, $jsonLd slot, @stack)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§BreadcrumbSchema, §layouts/app.blade.php EXTEND)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md (§Pattern 2 — breadcrumbList API)
  </read_first>
  <action>
    Create `app/Support/SchemaOrg/BreadcrumbSchema.php`: `namespace App\Support\SchemaOrg; use Spatie\SchemaOrg\Schema;` `final class BreadcrumbSchema` with `public function toScript(array $crumbs): string`. Iterate `$crumbs` (each `['name'=>string,'url'=>string]`), build `Schema::listItem()->position($index+1)->name($crumb['name'])->item($crumb['url'])` into an array, return `Schema::breadcrumbList()->itemListElement($items)->toScript()`. Then edit `resources/views/layouts/app.blade.php`: add `:type="$type ?? 'website'"` to the existing `<x-seo.meta>` call; immediately after the existing `$jsonLd` slot add `@if(isset($breadcrumbJsonLd) && $breadcrumbJsonLd) {!! $breadcrumbJsonLd !!} @endif`. Then create `tests/Feature/Seo/BreadcrumbTest.php` (Pest) — for now assert the BreadcrumbSchema builder: call `(new BreadcrumbSchema)->toScript([['name'=>'Accueil','url'=>url('/')],['name'=>'Services','url'=>url('/services')]])` and assert output contains `"@type":"BreadcrumbList"`, `ListItem`, `"position":1`, `"position":2`, and the names. (Route-level breadcrumb assertions are added in Task 3.)
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=Breadcrumb 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/Support/SchemaOrg/BreadcrumbSchema.php` exists with `toScript(array $crumbs): string` and uses `Schema::breadcrumbList()`.
    - `grep -q ':type=' resources/views/layouts/app.blade.php` succeeds (meta call now forwards type).
    - `grep -q 'breadcrumbJsonLd' resources/views/layouts/app.blade.php` succeeds.
    - `./vendor/bin/pest --filter=Breadcrumb` passes; output asserts `BreadcrumbList` + positions.
  </acceptance_criteria>
  <done>BreadcrumbSchema builds valid JSON-LD; layout forwards $type and renders the breadcrumb slot.</done>
</task>

<task type="auto">
  <name>Task 2: Add VitrineController methods + routes for 3 new service pages</name>
  <files>app/Http/Controllers/VitrineController.php, routes/vitrine.php, tests/Feature/Seo/VitrinePagesTest.php</files>
  <read_first>
    - app/Http/Controllers/VitrineController.php (the file being extended — copy the eauVerteUrgence() + home() DI shape)
    - routes/vitrine.php (the cache.headers:vitrine group to extend)
    - app/Support/SchemaOrg/BreadcrumbSchema.php (from Task 1 — the dependency to inject)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§VitrineController EXTEND, §routes/vitrine.php EXTEND — exact method/route shapes)
  </read_first>
  <action>
    In `app/Http/Controllers/VitrineController.php` add `use App\Support\SchemaOrg\BreadcrumbSchema;` and three methods, each taking `(BreadcrumbSchema $breadcrumb): View` (per-method DI matching `home()`): `entretienRecurrent()`, `analyseEau()`, `spa()`. Each returns its view with `title`, `description`, `canonical` (`url('/services/{slug}')`), `ogImage` (`asset('assets/brand/photos/{relevant}.jpg')` — use `.jpg` for og:image), and `breadcrumbJsonLd` => `$breadcrumb->toScript([['name'=>'Accueil','url'=>url('/')],['name'=>'Services','url'=>url('/services')],['name'=>'{label}','url'=>url('/services/{slug}')]])`. Titles per UI-SPEC h1 pattern, e.g. "Entretien régulier de piscine en Martinique · Dlo Azur Piscines", "Analyse de l'eau de piscine en Martinique · Dlo Azur Piscines", "Entretien de spa en Martinique · Dlo Azur Piscines". Also update `eauVerteUrgence()` to take `(BreadcrumbSchema $breadcrumb)` and pass `breadcrumbJsonLd` (Accueil › Services › Eau verte urgence). In `routes/vitrine.php`, inside the existing `cache.headers:vitrine` group, add: `services/entretien-recurrent` → `entretienRecurrent` name `services.entretien-recurrent`; `services/analyse-eau` → `analyseEau` name `services.analyse-eau`; `services/spa` → `spa` name `services.spa`. Then create `tests/Feature/Seo/VitrinePagesTest.php` asserting each new route returns 200 and the response contains the page-specific canonical and `"@type":"BreadcrumbList"`. (Views are stubbed in Task 3 — to make routes resolvable, create minimal placeholder views first OR order Task 3 before running the route test; create the three Blade files as minimal `@extends('layouts.app') @section('content') ... @endsection` stubs in this task so routes resolve, then flesh them out in Task 3.)
  </action>
  <verify>
    <automated>php artisan route:list --json 2>/dev/null | grep -o "services.entretien-recurrent\|services.analyse-eau\|services.spa" | sort -u | wc -l | grep -q 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list` includes named routes `services.entretien-recurrent`, `services.analyse-eau`, `services.spa`.
    - `grep -q 'BreadcrumbSchema' app/Http/Controllers/VitrineController.php` succeeds.
    - `eauVerteUrgence` signature now injects `BreadcrumbSchema` and passes `breadcrumbJsonLd`.
    - GET `/services/spa`, `/services/analyse-eau`, `/services/entretien-recurrent` return 200 (stub views resolve).
  </acceptance_criteria>
  <done>Three new routes resolve to controller methods that pass breadcrumb JSON-LD; eau-verte also wired.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 3 new service pages + expand eau-verte to 450+ words with FAQ</name>
  <files>resources/views/vitrine/services/entretien-recurrent.blade.php, resources/views/vitrine/services/analyse-eau.blade.php, resources/views/vitrine/services/spa.blade.php, resources/views/vitrine/services/eau-verte-urgence.blade.php, tests/Feature/Seo/VitrinePagesTest.php</files>
  <read_first>
    - resources/views/vitrine/services/eau-verte-urgence.blade.php (analog + the page being expanded — copy its structure)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§new service pages skeleton, §eau-verte EXPAND, §FAQ accordion Alpine pattern, §Shared Patterns / Tailwind tokens)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md (§Copywriting Contract service pages, §Layout pattern, §FAQ accordion, §Accessibility)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md (real body copy + spa wording to author from)
    - DESIGN.md (OKLCH tokens, typography rules)
  </read_first>
  <action>
    Flesh out the three stub views and expand eau-verte. Each page: `@extends('layouts.app')`, `@push('head') @if(isset($breadcrumbJsonLd)) {!! $breadcrumbJsonLd !!} @endif @endpush`, `@section('content')`. Structure per UI-SPEC Layout pattern: (1) navy-900 hero band with kicker (`text-lagon-400` uppercase Inter 700 0.75rem tracking-[0.18em]), h1 Display (Fredoka 700, `clamp(2.6rem,5vw,4rem)`, `text-sand-50`), subtitle (Inter 400, `text-sand-100/80`), CTA row ("Demander un devis" azure-500 + "Nous contacter par WhatsApp" `bg-[#25D366]` → `https://wa.me/596696940054`), and a visible breadcrumb `<nav aria-label="Fil d'Ariane">` (min-h-[44px], `›` separators in muted, current item non-link `aria-current="page"`); (2) content body `bg-sand-50` `max-w-3xl` with h2 sections (each preceded by a `text-lagon-600` kicker) and body prose `text-ink-700 leading-relaxed max-w-[65ch]`, using `<x-picture>` for any inline images; (3) CTA band (azure-600 or navy-800). Author real French long-form copy from ZYRO-CONTENT-HARVEST.md; where a fact is missing leave an HTML-comment placeholder `<!-- [À compléter — Pierre ADAM] -->` (NOT visible text — avoids AI-filler, D-09). Keep spa.blade.php focused on spa maintenance (D-11). For `eau-verte-urgence.blade.php`: expand to 450+ words with sections "Causes de l'eau verte", "Checklist DIY", "Quand appeler le pro", "Protocole 5 étapes" (use `lagon-500` step-number circles), "Paramètres avant/après", and an FAQ block (2-3 questions) built with Alpine `x-data="{ open: false }" / @click / x-show / x-collapse` — trigger is a `<button>` (min-h-[44px], `:aria-expanded`), NOT a div; chevron `azure-500` rotates 180° on open; respect `prefers-reduced-motion`. NO Livewire (CLAUDE.md lock). Use `sun-500` warm CTA at most once on the eau-verte page only. Finally, extend `tests/Feature/Seo/VitrinePagesTest.php`: assert `/services/spa` body mentions "spa"; assert `/services/eau-verte-urgence` response word count in the main content is materially larger (assert it contains the section headings "Protocole" and at least one FAQ question string); assert each page emits exactly one `<h1>` and a `BreadcrumbList`.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=Vitrine 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `./vendor/bin/pest --filter=Vitrine` passes.
    - `grep -qi spa resources/views/vitrine/services/spa.blade.php` succeeds.
    - eau-verte page contains the headings "Protocole" and at least one FAQ `<button` with `x-data`/`x-collapse` (Alpine, no Livewire — `grep -L "wire:" resources/views/vitrine/services/eau-verte-urgence.blade.php` shows no Livewire directive).
    - Each new service page extends `layouts.app` and pushes `$breadcrumbJsonLd` to head.
    - No service page uses a bordered left-callout; kicker labels use `lagon` tokens; CTAs use `azure-500`/`#25D366` tokens.
  </acceptance_criteria>
  <done>Four service pages (3 new + expanded eau-verte) are live, on-brand, fact-gated, and breadcrumb-marked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→public service pages | New public Blade pages, no auth, no user input (fixed-slug routes). |
| server→crawler | BreadcrumbList JSON-LD rendered to crawlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-03-01 | Tampering (XSS) | Service-page Blade output | mitigate | All copy is static Blade; dynamic interpolation uses `{{ }}` auto-escaping. Breadcrumb JSON-LD uses `BreadcrumbSchema->toScript()` (Spatie JSON_HEX_TAG escaping) — never hand-concatenated. No user input reaches these views. |
| T-9991-03-02 | Tampering | FAQ accordion (Alpine) | accept | Client-side toggle only; no data submission, no network call (deliberately not Livewire). Inert UI state. |
| T-9991-03-03 | Information disclosure | `[À compléter]` placeholders | mitigate | Placeholders are HTML comments (not rendered text) — no half-finished/fabricated facts shipped to users or crawlers (D-09). |
| T-9991-03-SC | Tampering | composer/npm installs | accept | Zero new packages (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `./vendor/bin/pest --filter=Vitrine` and `--filter=Breadcrumb` pass; full suite green.
- New routes return 200; each emits one h1 + a BreadcrumbList.
- eau-verte expanded to 450+ words with FAQ; spa page present (D-11).
- Manual phase gate: Google Rich Results Test on a service URL shows BreadcrumbList.
</verification>

<success_criteria>
- 4 service pages (entretien-recurrent, analyse-eau, spa NEW; eau-verte expanded) live with breadcrumb JSON-LD and on-brand UI.
- BreadcrumbSchema + layout wiring (type + breadcrumb slot) in place for downstream city/blog pages.
</success_criteria>

<output>
Create `.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-03-SUMMARY.md` when done.
</output>
