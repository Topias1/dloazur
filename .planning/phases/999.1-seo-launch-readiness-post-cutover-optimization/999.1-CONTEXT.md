# Phase 999.1: SEO launch-readiness & post-cutover optimization - Context

**Gathered:** 2026-05-29
**Status:** Ready for planning

<domain>
## Phase Boundary

**999.1 delivers post-cutover SEO growth for the Laravel vitrine** — schema enrichment, content depth, dedicated service/city pages, image optimization, `llms.txt`, and the GBP/reviews dependency. It makes `dloazurpiscines.com` actually *rank* once the Laravel app is live on the domain.

**Scope re-split decided during discussion (see D-07):** The 5 CRITICAL **cutover-blockers** are NOT in 999.1. They gate the Phase 1 launch (an invisible-site risk if deferred) and belong in **Phase 1's cutover checklist** (where they already overlap "Phase 1 cutover carry-overs" and `CutoverReadinessTest.php` exists). 999.1 is purely the post-cutover growth phase and stays backlog until cutover is done.

**Important framing (from discussion):** `dloazurpiscines.com` today is still the **old Zyro/Hostinger website-builder site** (verified 2026-05-29: `x-powered-by: HostingerWebsiteBuilder`, `platform: hostinger`, zyrosite assets). The new Laravel vitrine is on staging, `noindex`. **No SEO is implemented on the Zyro site** — it's a no-code builder, throwaway, and under a hard no-touch constraint (memory `never-touch-prod-zyro`). The Zyro site is instead used as a **content source** to optimize the new vitrine (D-08).

</domain>

<decisions>
## Implementation Decisions

### Schema.org type & enrichment
- **D-01:** Switch homepage JSON-LD `@type` from `Schema::plumber()` (D-26) → **`["LocalBusiness","HomeAndConstructionBusiness"]`**. Semantically correct for a *pisciniste d'entretien* (pool cleaning ≠ plumbing), and aligns with the future GBP entity category. Verify `spatie/laravel-schema-org` multi-type syntax. **This supersedes D-26.**
- **D-02:** Add `founder` → `Schema::person()->name('Pierre ADAM')` now — zero risk, entity-graph value.
- **D-03:** Add `email` → `contact@dloazurpiscines.com` now. Confirmed publicly displayed on the live site, so on-page/markup consistency holds (Google requires the marked-up email be visible on the page).
- **D-04:** **Do NOT** add `streetAddress`/`postalCode` — SAB hides its address; marking up a non-visible address is a Google policy violation. Omit until/unless Pierre chooses to display a public address.
- **D-05:** **Do NOT** add `aggregateRating` until real GBP reviews exist. Self-controlled review markup on `LocalBusiness` is prohibited (2024 policy); fabricated ratings risk a manual action stripping ALL rich results. Gated on GBP+reviews (D-09).
- **D-06:** `hasOfferCatalog` → `OfferCatalog` is a **second wave** addition (after GBP is live and the service list is stable). Primary value is AI/LLM entity grounding, not Google rich results.

### Phase scope & sequencing
- **D-07:** **999.1 = post-cutover growth ONLY.** The 5 CRITICAL cutover-blockers move to **Phase 1's cutover checklist**. This is a ROADMAP edit to be executed via `/gsd-phase` as a follow-up (noted here per user direction, not performed by this discuss command). Blockers being moved:
  - Production env must emit **no** `x-robots-tag: noindex` (verify `curl -I https://dloazurpiscines.com` post-cutover).
  - `public/robots.txt` literal `Sitemap: ${APP_URL}/sitemap.xml` → interpolate via route/`url()` or hardcode.
  - `/mentions-legales` real 14-digit SIRET (Pierre supplies; a `596 17806 7639`-ish number appears on the Zyro site but is NOT a clean SIRET — Pierre must confirm).
  - Drive canonical/og/schema URLs from `config('app.url')`; set `APP_URL=https://dloazurpiscines.com` in prod. (NOTE: `components/seo/meta.blade.php` already resolves canonical/og:url via `url()->current()` and emits `twitter:card` + favicon — those audit items are **already fixed**; verify prod `APP_URL` is the remaining gap.)
  - Add security headers (HSTS, X-Content-Type-Options, X-Frame-Options/CSP frame-ancestors, Permissions-Policy).

### Page architecture
- **D-10:** Fix the single-URL intent mismatch with **dedicated service pages FIRST, then 4 hand-written city hubs**. Reject programmatic/templated city pages — for a 4-commune solo operator they trip Google's scaled-content-abuse / doorway-page rules (post-Aug-2025 enforcement).
- **D-11:** Service pages must cover **spa** as well as pools — the live Zyro site markets "entretien et nettoyage de piscines **et spa**". Current service set: regular maintenance (entretien récurrent), green-water emergency (eau verte), water analysis, + spa.
- **D-12:** 4 city hubs: **Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets** (matches existing `areaServed`). Each gated on **≥1 real operator-supplied fact per commune** (local water specifics, a real job) — no publish without a genuine local detail. City pages link → service pages, not the reverse.
- **D-13:** Expand `/services/eau-verte-urgence` from 222w → **450w+** (causes, DIY checklist, when-to-call-pro, 5-step protocol, before/after params, FAQ). Expand thin `/realisations` (~108–145w) with 2–3 written case studies.

### Content authorship & GBP/reviews ownership
- **D-08:** **Mine the live Zyro site (rendered crawl) for real content** — full body copy, services list (incl. spa), testimonials, NAP, email, SIRET candidate, and the **URL inventory** (the URL list also feeds the cutover 301 redirect map). This is a concrete planning task, not background.
- **D-09:** Claude **drafts** the French long-form copy from harvested Zyro content + Pierre-supplied specifics; placeholders where facts are missing (avoids the AI-filler signal the audit flagged). **GBP creation + review solicitation = a tracked Pierre-action dependency** (zero-dev, out of code scope) — it gates D-05 (`aggregateRating`) and any on-page review display.

### Claude's Discretion
- The scope re-split (D-07) was delegated ("you decide") — adopted the blockers→Phase-1 / 999.1=growth split as the lower-risk option.
- Remaining MEDIUM/LOW audit items (FAQ content/PAA, price-range signal, WebP/AVIF conversion, blog real dates + `lastmod`, `BreadcrumbList`, `og:type: article` on blog, demote "Espace client" nav / promote "Demander un devis", `llms.txt`, `twitter:card` already done, Martinique-specific copy, operator credentials) are in-scope for 999.1 growth — planner to sequence by severity.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase definition & audit source (the authoritative WHAT)
- `.planning/ROADMAP.md` §"Phase 999.1" (lines ~133–175) — the full SEO audit findings grouped by severity (CRITICAL/HIGH/MEDIUM/LOW). This is the canonical scope list.
- `.planning/REQUIREMENTS.md` — `SITE-07` (SEO local Martinique: meta, sitemap, structured data).
- Memory `seo-cutover-gotchas` — cutover blockers + highest-ROI GBP note (not in-repo; recall by name).
- Memory `never-touch-prod-zyro` — hard no-touch constraint on the live Zyro site.
- Memory `pierre-statut-fiscal` — auto-entrepreneur, franchise en base TVA (affects legal page / SIRET / no VAT).

### Existing SEO code to modify
- `app/Support/SchemaOrg/LocalBusinessSchema.php` — JSON-LD builder; currently `Schema::plumber()` (D-01 changes this). Already has `areaServed` (4 cities + Martinique), geo, opening hours, `priceRange '€€'`, `telephone`, empty `sameAs: []` (→ GBP).
- `resources/views/components/seo/meta.blade.php` — canonical/og:url via `url()->current()`, `twitter:card`, favicon — **already correct**; do not redo.
- `public/robots.txt` — has the `${APP_URL}` literal bug (→ Phase 1 cutover).
- `app/Http/Controllers/SitemapController.php` — sitemap (uses `route()` per memory; needs `lastmod` on static URLs, blog real dates).
- `app/Http/Controllers/VitrineController.php` — vitrine page controller (where service/city routes are added).
- `resources/views/vitrine/services/eau-verte-urgence.blade.php` — thin page to expand (D-13).
- `resources/views/vitrine/realisations.blade.php` — thin page to expand (D-13).

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `LocalBusinessSchema::toScript()` — extend in place for D-01..D-06; `spatie/laravel-schema-org` already a dependency.
- `<x-seo.meta>` Blade component — already handles per-page title/description/canonical/og/twitter; new service/city pages pass props through it.
- `spatie/laravel-medialibrary` (in stack) — image conversions; candidate for WebP/AVIF generation.
- `SitemapController` — extend for new service/city URLs + `lastmod`.

### Established Patterns
- Vitrine pages are Blade under `resources/views/vitrine/` with partials in `vitrine/partials/`; routes via `VitrineController`. New pages follow this, NOT Livewire.
- URLs already resolve via `url()`/`route()`/`config('app.url')` in most places — prod `APP_URL` is the single switch (Phase 1).

### Integration Points
- New service/city routes in `routes/web.php` (existing 301 redirects live there too — extend for the Zyro→Laravel 301 map).
- Schema script injected in `resources/views/layouts/app.blade.php`.

</code_context>

<specifics>
## Specific Ideas

- Live Zyro positioning to preserve/improve: *"Entretien et nettoyage de piscines et spa … experts locaux … eau parfaite toute l'année … solutions sur mesure."* Keep the spa + "experts locaux" + year-round angles.
- Verified live NAP: phone `+596 696 94 00 54`, email `contact@dloazurpiscines.com`.
- Highest local-SEO ROI is GBP (zero-dev, Pierre-owned) — flag prominently in planning even though it's not a code task.

</specifics>

<deferred>
## Deferred Ideas

- **5 CRITICAL cutover-blockers** → moved to **Phase 1 cutover checklist** (D-07), not deferred-lost. Roadmap edit via `/gsd-phase` is a tracked follow-up.
- **301 redirect map (Zyro URLs → Laravel routes)** and **GSC property migration** at cutover — execution belongs with the Phase 1 cutover, but the URL inventory is harvested here (D-08). Cross-reference for Phase 1.

</deferred>

---

*Phase: 999.1-seo-launch-readiness-post-cutover-optimization*
*Context gathered: 2026-05-29*
