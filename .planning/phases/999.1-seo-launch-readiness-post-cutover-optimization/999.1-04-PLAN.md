---
phase: 999.1-seo-launch-readiness-post-cutover-optimization
plan: 04
type: execute
wave: 3
depends_on: ["999.1-03"]
files_modified:
  - app/Http/Controllers/VitrineController.php
  - routes/vitrine.php
  - resources/views/vitrine/zones/fort-de-france.blade.php
  - resources/views/vitrine/zones/le-lamentin.blade.php
  - resources/views/vitrine/zones/schoelcher.blade.php
  - resources/views/vitrine/zones/les-trois-ilets.blade.php
  - tests/Feature/Seo/CityHubTest.php
autonomous: true
requirements: [SITE-07]
must_haves:
  truths:
    - "Routes /zones/fort-de-france, /zones/le-lamentin, /zones/schoelcher, /zones/les-trois-ilets return 200"
    - "Each city hub emits a BreadcrumbList JSON-LD and links to service pages (city→service direction)"
    - "Each city hub retains a [FAIT LOCAL REQUIS ...] placeholder gate until Pierre supplies a real local fact (no programmatic/templated pages)"
  artifacts:
    - path: "resources/views/vitrine/zones/fort-de-france.blade.php"
      provides: "Fort-de-France city hub"
      min_lines: 30
    - path: "resources/views/vitrine/zones/les-trois-ilets.blade.php"
      provides: "Les Trois-Îlets city hub"
      min_lines: 30
  key_links:
    - from: "routes/vitrine.php"
      to: "app/Http/Controllers/VitrineController.php"
      via: "named routes zones.{slug}"
      pattern: "zones\\.(fort-de-france|le-lamentin|schoelcher|les-trois-ilets)"
    - from: "resources/views/vitrine/zones/fort-de-france.blade.php"
      to: "resources/views/vitrine/services/spa.blade.php"
      via: "service links (city→service)"
      pattern: "route\\('services\\."
---

<objective>
Author 4 hand-written city-hub pages (D-12): Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets — each with a navy hero, a gated local-fact paragraph, and links to the service pages (city→service direction only, never the reverse). Add `VitrineController` methods + `zones.{slug}` routes + BreadcrumbList JSON-LD. Each page keeps a `<!-- [FAIT LOCAL REQUIS: ...] -->` placeholder as a HARD publish gate until Pierre supplies a genuine local fact.

Purpose: fixes the audit "page-type mismatch" with a small, fact-gated set of city pages. CRITICAL: D-10/D-12 + Google's doorway/scaled-content policy forbid programmatic/templated near-identical city pages. Exactly 4 pages, each must differ by a real local fact — no two pages may differ only by the city token.
Output: 4 city-hub Blade pages + controller/route wiring + Pest test.

This depends on plan 03 (shared `VitrineController`/`routes/vitrine.php`, the `BreadcrumbSchema`, and the service routes the city pages link to).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-CONTEXT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md
@resources/views/vitrine/services/eau-verte-urgence.blade.php

<interfaces>
<!-- From plan 03 (already shipped in Wave 2) -->
BreadcrumbSchema: `(BreadcrumbSchema $breadcrumb)->toScript([['name'=>...,'url'=>...], ...])`.
VitrineController per-method DI pattern: `public function fortDeFrance(BreadcrumbSchema $breadcrumb): View`.
Service routes available to link to: `services.entretien-recurrent`, `services.eau-verte-urgence`, `services.analyse-eau`, `services.spa`.
routes/vitrine.php: add inside the existing `cache.headers:vitrine` group; naming `zones.{slug}`.
layouts/app.blade.php already renders `$breadcrumbJsonLd` and forwards `$type` (plan 03 Task 1).
<x-picture> available for inline images; og:image stays .jpg.

UI-SPEC City Hub contract:
- h1 pattern "Pisciniste à [Commune] · Dlo Azur Piscines".
- Kicker `[COMMUNE]` in lagon-400 (on navy hero).
- CTAs: "Demander un devis gratuit" (azure-500) + "Appeler Pierre" (tel:+596696940054, navy-900 button).
- Local-fact intro pattern: "À [Commune], [fait spécifique] Pierre intervient sur [type de piscines]."
- Hard gate placeholder: `<!-- [FAIT LOCAL REQUIS: spécificité eau/chantier à {commune}] -->` — DO NOT publish (i.e. do NOT remove the placeholder) without a real Pierre fact.
- Service list links city→service (NOT reverse). NO FAQ on city hubs.
- 4 communes: Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets (match schema areaServed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 VitrineController city methods + zones routes</name>
  <files>app/Http/Controllers/VitrineController.php, routes/vitrine.php</files>
  <read_first>
    - app/Http/Controllers/VitrineController.php (extend — copy the service-method DI shape from plan 03)
    - routes/vitrine.php (the cache.headers:vitrine group)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§VitrineController zone methods, §routes/vitrine.php — exact shapes)
  </read_first>
  <action>
    Add four methods to `VitrineController`, each `(BreadcrumbSchema $breadcrumb): View`: `fortDeFrance()`, `leLamentin()`, `schoelcher()`, `lesTroisIlets()`. Each returns its `vitrine.zones.{slug}` view with `title` ("Pisciniste à [Commune] · Dlo Azur Piscines"), `description` (commune-specific), `canonical` (`url('/zones/{slug}')`), `ogImage` (`asset('assets/brand/og-default.jpg')` — .jpg), and `breadcrumbJsonLd` => `$breadcrumb->toScript([['name'=>'Accueil','url'=>url('/')],['name'=>'[Commune]','url'=>url('/zones/{slug}')]])` (2-level trail: Accueil › Commune). In `routes/vitrine.php`, inside the existing `cache.headers:vitrine` group, add `zones/fort-de-france`→`fortDeFrance` name `zones.fort-de-france`; `zones/le-lamentin`→`leLamentin` name `zones.le-lamentin`; `zones/schoelcher`→`schoelcher` name `zones.schoelcher`; `zones/les-trois-ilets`→`lesTroisIlets` name `zones.les-trois-ilets`. Create the four Blade files as minimal `@extends('layouts.app')@section('content')...@endsection` stubs so routes resolve (fleshed out in Task 2).
  </action>
  <verify>
    <automated>php artisan route:list --json 2>/dev/null | grep -o "zones.fort-de-france\|zones.le-lamentin\|zones.schoelcher\|zones.les-trois-ilets" | sort -u | wc -l | grep -q 4 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list` includes the 4 `zones.*` named routes.
    - All 4 zone methods inject `BreadcrumbSchema` and pass `breadcrumbJsonLd`.
    - GET each `/zones/{slug}` returns 200 (stub views resolve).
  </acceptance_criteria>
  <done>Four city routes resolve to controller methods passing 2-level breadcrumb JSON-LD.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 4 city-hub pages (fact-gated, city→service links)</name>
  <files>resources/views/vitrine/zones/fort-de-france.blade.php, resources/views/vitrine/zones/le-lamentin.blade.php, resources/views/vitrine/zones/schoelcher.blade.php, resources/views/vitrine/zones/les-trois-ilets.blade.php, tests/Feature/Seo/CityHubTest.php</files>
  <read_first>
    - resources/views/vitrine/services/eau-verte-urgence.blade.php (structural analog — hero + body + CTA band)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§zones/*.blade.php — hero/CTA/local-fact-gate/service-links patterns)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md (§City Hub Pages copy + layout differences, §Accessibility)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md (§Pierre-Action Facts Required — which local facts are still placeholders)
    - DESIGN.md (tokens)
  </read_first>
  <action>
    Flesh out each city page: `@extends('layouts.app')`, `@push('head') @if(isset($breadcrumbJsonLd)) {!! $breadcrumbJsonLd !!} @endif @endpush`, `@section('content')`. Navy-900 hero band with kicker `[COMMUNE]` (`text-lagon-400`), h1 "Pisciniste à [Commune] · Dlo Azur Piscines" (Fredoka 700, `clamp(2.6rem,5vw,4rem)`, `text-sand-50`), subtitle, CTA row "Demander un devis gratuit" (azure-500) + "Appeler Pierre" (`tel:+596696940054`, navy-900 button), and a visible 2-level breadcrumb nav (Accueil › Commune, min-h-[44px], `aria-current="page"` on the commune). Body (`bg-sand-50`, `max-w-3xl`): a local-fact intro paragraph using the pattern "À [Commune], [fait] Pierre intervient sur [type]." — but KEEP the `<!-- [FAIT LOCAL REQUIS: spécificité eau/chantier à {commune}] -->` HTML-comment placeholder in place where the genuine fact is not yet supplied (do NOT invent a local fact; this is the D-12 hard publish gate). Add a service list linking city→service: `route('services.entretien-recurrent')`, `route('services.eau-verte-urgence')`, `route('services.analyse-eau')`, `route('services.spa')`. NO FAQ (UI-SPEC). NO `sun-500` on city hubs. Each page MUST be genuinely distinct (different copy, different local angle) — two pages differing only by the city token violate D-10. Then create `tests/Feature/Seo/CityHubTest.php`: each of the 4 routes returns 200, emits one `<h1>`, emits `"@type":"BreadcrumbList"`, links to at least one `services.*` route, and STILL contains the `FAIT LOCAL REQUIS` placeholder (asserting the gate is intact / unpublished until Pierre fills it). Assert no two city pages have identical body content.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=CityHub 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `./vendor/bin/pest --filter=CityHub` passes.
    - Each `/zones/{slug}` returns 200, has exactly one h1, emits a BreadcrumbList, and links to a `services.*` route.
    - `grep -rl "FAIT LOCAL REQUIS" resources/views/vitrine/zones/` lists all 4 files (gate intact until Pierre supplies facts).
    - No two city Blade files are byte-identical except for the commune token (test asserts distinct body content).
    - No `wire:` directive in any zone file (no Livewire).
  </acceptance_criteria>
  <done>Four distinct, fact-gated city hubs link to services, carry BreadcrumbList, and hold the publish gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→public city pages | New public Blade pages, no auth, fixed-slug routes, no user input. |
| server→crawler | BreadcrumbList JSON-LD + page copy consumed by crawlers; doorway/scaled-content policy is the real risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-04-01 | Repudiation/Reputation | City pages as doorway/scaled content | mitigate | D-12 hard gate: exactly 4 hand-written pages, each gated on a real per-commune fact; test asserts distinct body content + intact `FAIT LOCAL REQUIS` placeholder. No programmatic/templated generation (RESEARCH Pitfall 2). |
| T-9991-04-02 | Tampering (XSS) | City-page Blade output | mitigate | Static copy + `{{ }}` escaping; breadcrumb via `BreadcrumbSchema->toScript()` (JSON_HEX_TAG). No user input. |
| T-9991-04-03 | Information disclosure | local-fact placeholders | mitigate | Gate placeholders are HTML comments; no fabricated local facts shipped (D-09/D-12). |
| T-9991-04-SC | Tampering | composer/npm installs | accept | Zero new packages. |
</threat_model>

<verification>
- `./vendor/bin/pest --filter=CityHub` passes; full suite green.
- 4 city routes return 200, each with one h1 + BreadcrumbList + service links + intact local-fact gate.
- Pages are genuinely distinct (no doorway pattern).
</verification>

<success_criteria>
- 4 hand-written, fact-gated city hubs linking city→service, with BreadcrumbList and on-brand navy heros.
- Publish gate (`FAIT LOCAL REQUIS`) intact until Pierre supplies real local facts.
</success_criteria>

<output>
Create `.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-04-SUMMARY.md` when done.
</output>
