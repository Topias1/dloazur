---
phase: 999.1-seo-launch-readiness-post-cutover-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/SchemaOrg/LocalBusinessSchema.php
  - tests/Feature/Seo/LocalBusinessSchemaTest.php
autonomous: true
requirements: [SITE-07]
must_haves:
  truths:
    - "Home page JSON-LD emits @type as an array [\"LocalBusiness\",\"HomeAndConstructionBusiness\"]"
    - "(D-02) Home page JSON-LD includes founder Pierre ADAM"
    - "(D-03) Home page JSON-LD includes email contact@dloazurpiscines.com"
    - "(D-04) Home page JSON-LD omits streetAddress and postalCode (SAB hides address)"
    - "Home page JSON-LD does NOT include aggregateRating, streetAddress, or postalCode"
  artifacts:
    - path: "app/Support/SchemaOrg/LocalBusinessSchema.php"
      provides: "Multi-type LocalBusiness+HomeAndConstructionBusiness JSON-LD builder"
      contains: "MultiTypedEntity"
    - path: "tests/Feature/Seo/LocalBusinessSchemaTest.php"
      provides: "Schema assertions for D-01..D-05"
      contains: "LocalBusiness"
  key_links:
    - from: "app/Http/Controllers/VitrineController.php"
      to: "app/Support/SchemaOrg/LocalBusinessSchema.php"
      via: "home() injects LocalBusinessSchema, renders $jsonLd in layout"
      pattern: "LocalBusinessSchema"
---

<objective>
Rewrite the home-page JSON-LD builder from `Schema::plumber()` (D-26) to a multi-type `["LocalBusiness","HomeAndConstructionBusiness"]` entity, adding `founder` (Pierre ADAM) and `email`, while deliberately omitting `aggregateRating`, `streetAddress`, and `postalCode`.

Purpose: `Plumber` is semantically wrong for a pisciniste d'entretien (pool cleaning ≠ plumbing). The correct multi-type aligns with the future GBP entity category and AI/LLM entity grounding. Implements D-01 (supersedes D-26), D-02, D-03, and enforces the prohibitions D-04 and D-05.
Output: Rewritten `LocalBusinessSchema.php` + a new Pest test that locks the multi-type array, founder, email, and the absence of rating/street/postal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-CONTEXT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md

<interfaces>
<!-- Verified from vendor/spatie/schema-org/src/MultiTypedEntity.php and Schema.php -->
Package is `spatie/schema-org` 4.0.2 (CLAUDE.md/CONTEXT call it `spatie/laravel-schema-org` — same library).

MultiTypedEntity API:
- `new \Spatie\SchemaOrg\MultiTypedEntity()`
- `->localBusiness(callable $fn)` — $fn receives a `\Spatie\SchemaOrg\LocalBusiness`
- `->homeAndConstructionBusiness()` — adds the second @type, no props needed
- `->toScript()` — emits `<script type="application/ld+json">` with `@type: ["LocalBusiness","HomeAndConstructionBusiness"]`, JSON_HEX_TAG + JSON_UNESCAPED_UNICODE
- GOTCHA: `->add()` (or adding the same class twice) throws `TypeAlreadyInMultiTypedEntity`; use the chained method form.

LocalBusiness props available: name, image, url, telephone, email, priceRange, founder, sameAs, address, geo, areaServed, openingHoursSpecification.
Schema facade helpers: Schema::person(), Schema::postalAddress(), Schema::geoCoordinates(), Schema::city(), Schema::administrativeArea(), Schema::openingHoursSpecification().

Current builder (the rewrite target) — app/Support/SchemaOrg/LocalBusinessSchema.php:
- Class `final class LocalBusinessSchema`, namespace `App\Support\SchemaOrg`, method `public function toScript(): string`.
- Currently `Schema::plumber()->name(...)->image(...)->url(...)->telephone('+596696940054')->priceRange('€€')->sameAs([])->address(...)->geo(...)->areaServed([...])->openingHoursSpecification([...])`.
- Docblock says "Plumber sub-type (D-26)".

Consumer: VitrineController::home(LocalBusinessSchema $schema) passes `'jsonLd' => $schema->toScript()` to the home view; layouts/app.blade.php renders `{!! $jsonLd !!}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing schema test (RED)</name>
  <files>tests/Feature/Seo/LocalBusinessSchemaTest.php</files>
  <read_first>
    - app/Support/SchemaOrg/LocalBusinessSchema.php (current builder to be replaced)
    - tests/Feature/SeoTest.php (existing SEO assertion conventions — how the home route + JSON-LD is asserted)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md (§Validation Architecture, §Pattern 1)
  </read_first>
  <behavior>
    - Test: home route ('/') response contains a JSON-LD script whose decoded `@type` is exactly the array `["LocalBusiness","HomeAndConstructionBusiness"]` (order-insensitive; assert both members present and it is an array, not a string).
    - Test: decoded JSON-LD has `founder.name` == "Pierre ADAM".
    - Test: decoded JSON-LD has `email` == "contact@dloazurpiscines.com".
    - Test: decoded JSON-LD does NOT contain key `aggregateRating` (D-05).
    - Test: decoded JSON-LD `address` has NO `streetAddress` and NO `postalCode` keys; `addressLocality` == "Fort-de-France", `addressRegion` == "Martinique", `addressCountry` == "FR" (D-04).
    - Test: `@type` is NOT the string "Plumber".
  </behavior>
  <action>
    Create `tests/Feature/Seo/LocalBusinessSchemaTest.php` (Pest). Create the `tests/Feature/Seo/` directory if absent. GET `/`, extract the `<script type="application/ld+json">` block (the one produced by `LocalBusinessSchema`, i.e. containing "Dlo Azur Piscines"), `json_decode` it, and assert the behaviors above. Use `expect()` Pest assertions. Reference the JSON-LD by locating the script tag content and decoding — the home view renders it via `{!! $jsonLd !!}` in the layout head. The test MUST fail initially because the current builder emits `@type: "Plumber"` and lacks founder/email.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=LocalBusinessSchema 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Seo/LocalBusinessSchemaTest.php` exists.
    - `./vendor/bin/pest --filter=LocalBusinessSchema` runs and FAILS (RED) — the assertions reference the not-yet-implemented multi-type/founder/email.
    - Test file references the strings `LocalBusiness`, `HomeAndConstructionBusiness`, `founder`, `contact@dloazurpiscines.com`, and asserts absence of `aggregateRating` and `streetAddress`.
  </acceptance_criteria>
  <done>Test exists and fails for the right reason (current Plumber builder does not satisfy the new assertions).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite LocalBusinessSchema to MultiTypedEntity (GREEN)</name>
  <files>app/Support/SchemaOrg/LocalBusinessSchema.php</files>
  <read_first>
    - app/Support/SchemaOrg/LocalBusinessSchema.php (the file being rewritten in place)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§LocalBusinessSchema rewrite target — exact body)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md (§Pattern 1 — MultiTypedEntity, including the `TypeAlreadyInMultiTypedEntity` gotcha)
  </read_first>
  <action>
    Rewrite `toScript()` to build a `new \Spatie\SchemaOrg\MultiTypedEntity()`. Inside `->localBusiness(function (\Spatie\SchemaOrg\LocalBusiness $biz) { ... })` set: name 'Dlo Azur Piscines'; image `asset('assets/brand/photos/hero-pierre-piscine.jpg')`; url `url('/')`; telephone '+596696940054'; email 'contact@dloazurpiscines.com' (D-03); priceRange '€€'; founder `Schema::person()->name('Pierre ADAM')` (D-02); sameAs [] (GBP URL added later per D-09); address via `Schema::postalAddress()->addressCountry('FR')->addressRegion('Martinique')->addressLocality('Fort-de-France')` (NO streetAddress/postalCode per D-04); geo `Schema::geoCoordinates()->latitude(14.6037)->longitude(-61.0594)`; areaServed the 4 cities + Martinique administrativeArea (unchanged); openingHoursSpecification (unchanged from current builder — Mon-Fri 08:00-17:00, Sat 09:00-12:00). Then chain `->homeAndConstructionBusiness()` for the second @type (D-01). Return `$mte->toScript()`. Add imports `use Spatie\SchemaOrg\MultiTypedEntity;` (keep `use Spatie\SchemaOrg\Schema;`). Update the class docblock from "Plumber sub-type (D-26)" to "LocalBusiness+HomeAndConstructionBusiness multi-type (D-01, supersedes D-26)". Do NOT add aggregateRating (D-05) or hasOfferCatalog (D-06 second-wave). Do NOT use `->add()`; use the chained method form to avoid `TypeAlreadyInMultiTypedEntity`.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=LocalBusinessSchema 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `./vendor/bin/pest --filter=LocalBusinessSchema` passes (GREEN).
    - `grep -q "MultiTypedEntity" app/Support/SchemaOrg/LocalBusinessSchema.php` succeeds.
    - `grep -q "homeAndConstructionBusiness" app/Support/SchemaOrg/LocalBusinessSchema.php` succeeds.
    - `grep -q "Pierre ADAM" app/Support/SchemaOrg/LocalBusinessSchema.php` succeeds.
    - `grep -c "Schema::plumber" app/Support/SchemaOrg/LocalBusinessSchema.php` returns 0.
    - `grep -c "aggregateRating\|streetAddress\|postalCode" app/Support/SchemaOrg/LocalBusinessSchema.php` returns 0.
  </acceptance_criteria>
  <done>Builder emits multi-type JSON-LD with founder + email, no rating/street/postal; the Task 1 test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→Googlebot/AI-crawler | Rendered JSON-LD `<script>` is consumed by external crawlers; all values are hardcoded (no user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-01 | Tampering | JSON-LD `<script>` in `LocalBusinessSchema::toScript()` | mitigate | Use `MultiTypedEntity::toScript()` (JSON_HEX_TAG + JSON_UNESCAPED_UNICODE, verified BaseType.php:163); never hand-concatenate JSON-LD. All values are static literals — no attacker-controlled input. |
| T-9991-02 | Information disclosure | Marked-up `email` / address fields | accept | Email is intentionally public (displayed on site per D-03). Address is locality-only (D-04) — no street/postal disclosed. No PII beyond already-public NAP. |
| T-9991-SC | Tampering | composer/npm installs | accept | Zero new packages this plan (RESEARCH §Package Legitimacy Audit — `spatie/schema-org` already in composer.lock, vetted Phase 1). |
</threat_model>

<verification>
- `./vendor/bin/pest --filter=LocalBusinessSchema` passes.
- `./vendor/bin/pest` full suite stays green (no regression to existing `SeoTest`).
- Manual (phase gate, not this plan): Google Rich Results Test on staging home URL shows the multi-type LocalBusiness.
</verification>

<success_criteria>
- Home JSON-LD `@type` is the array `["LocalBusiness","HomeAndConstructionBusiness"]`.
- `founder` (Pierre ADAM) and `email` present; `aggregateRating`/`streetAddress`/`postalCode` absent.
- No reference to `Schema::plumber()` remains.
</success_criteria>

<output>
Create `.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-01-SUMMARY.md` when done.
</output>
