---
phase: 999.1-seo-launch-readiness-post-cutover-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md
autonomous: true
requirements: [SITE-07]
must_haves:
  truths:
    - "Real body copy, services list (incl. spa), testimonials, NAP, and email from the live Zyro site are captured in a single harvest document"
    - "A URL inventory of the live Zyro site is captured for the cutover 301 map"
    - "Missing facts (per-commune local facts, SIRET, real dates, case-study measures) are listed explicitly as Pierre-action gaps"
  artifacts:
    - path: ".planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md"
      provides: "Harvested Zyro content + URL inventory + Pierre-fact gap list feeding plans 03/04"
      min_lines: 40
  key_links:
    - from: ".planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md"
      to: "resources/views/vitrine/services/spa.blade.php"
      via: "content source for service/city page authoring (plans 03/04)"
      pattern: "spa|entretien|eau verte"
---

<objective>
Harvest the live Zyro/Hostinger site (`https://dloazurpiscines.com`, read-only crawl) for real marketing content: full body copy, the services list (explicitly including spa), testimonials, NAP, email, SIRET candidate, and the URL inventory. Produce a single `ZYRO-CONTENT-HARVEST.md` that downstream content plans (03 service pages, 04 city hubs, realisations expansion) draw from — avoiding the AI-filler signal the audit flagged (D-08, D-09).

Purpose: D-08 makes this a concrete planning input, not background. Plans 03/04 must author from real harvested copy + Pierre facts, with explicit placeholders where facts are missing. This plan also feeds the Phase-1 cutover 301 redirect map.
Output: `ZYRO-CONTENT-HARVEST.md` in the phase directory.

CRITICAL CONSTRAINT (memory `never-touch-prod-zyro`): READ-ONLY. No push, deploy, DNS, or any write to `https://dloazurpiscines.com`. Crawl/fetch GET only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-CONTEXT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md

<interfaces>
<!-- Known facts from CONTEXT.md / UI-SPEC.md to confirm/extend, not re-derive -->
Verified live NAP: phone `+596 696 94 00 54`, email `contact@dloazurpiscines.com`.
Live Zyro positioning: "Entretien et nettoyage de piscines et spa … experts locaux … eau parfaite toute l'année … solutions sur mesure."
Existing partial inventory: `.planning/phases/01-vitrine-fondations/ZYRO-URL-INVENTORY.md` (read it; extend, don't duplicate).
SIRET candidate seen on Zyro: a `596 17806 7639`-ish number — NOT a clean 14-digit SIRET; flag as Pierre-action (Phase 1 cutover concern, do NOT use in code here).
Service set to confirm: entretien récurrent, eau verte urgence, analyse de l'eau, + spa (D-11).
4 communes (city hubs): Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets (D-12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crawl the live Zyro site (read-only) and capture content + URL inventory</name>
  <files>.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md</files>
  <read_first>
    - .planning/phases/01-vitrine-fondations/ZYRO-URL-INVENTORY.md (existing partial inventory — extend it, do not duplicate)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-CONTEXT.md (D-08 scope, verified NAP, Zyro positioning)
  </read_first>
  <action>
    READ-ONLY crawl of `https://dloazurpiscines.com` (GET only; no writes, no DNS, no deploy). Fetch the homepage and each discoverable internal page (follow nav + footer links). For each page capture: URL, page title/H1, the full rendered body copy (verbatim where reusable), any testimonials/avis with attribution, the services described (confirm spa is marketed alongside piscines per D-11), the NAP (confirm phone `+596 696 94 00 54` and email `contact@dloazurpiscines.com`), and any SIRET/RCS-looking number (record as a candidate only — flag it Pierre-action, do NOT treat as confirmed). Build a URL inventory table (Zyro URL to suggested Laravel route) for the cutover 301 map. Write everything to `ZYRO-CONTENT-HARVEST.md` with sections: `## URL Inventory`, `## Body Copy by Page`, `## Services (incl. spa)`, `## Testimonials`, `## NAP and Identity`, `## SIRET candidate (Pierre-action, NOT for code here)`. If a fetch is blocked or a page is JS-rendered and unreadable, note the gap rather than inventing content (avoids AI-filler — D-09).
  </action>
  <verify>
    <automated>test -f .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md && grep -qi spa .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md && grep -q "URL Inventory" .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ZYRO-CONTENT-HARVEST.md` exists with the six named sections.
    - The document mentions `spa` and includes a `## URL Inventory` table.
    - NAP captured matches phone `+596 696 94 00 54` and email `contact@dloazurpiscines.com` (or the gap is explicitly noted if the page was unreadable).
    - No write operation was performed against the live site (read-only crawl only).
  </acceptance_criteria>
  <done>Harvest document exists with real content, URL inventory, and explicit gap notes; no live-site writes occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Compile the Pierre-action fact gap list</name>
  <files>.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md</files>
  <read_first>
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md (§Pierre-Action Dependencies — the canonical gate list)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md (the harvest from Task 1)
  </read_first>
  <action>
    Append a `## Pierre-Action Facts Required` section to `ZYRO-CONTENT-HARVEST.md` enumerating every fact the content plans (03/04) cannot author without Pierre: (1) at least one real local fact per commune for Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets (water specifics / a real job / access constraint) — hard gate per D-12; list ALL FOUR communes by name (plan 04's D-12 gate depends on the gap list being complete — a missing commune means a city page can publish without its required local fact); (2) 2-3 real case-study facts for /realisations (commune, pool type, before/after pH/chlore/TAC measures) per D-13; (3) the real publish date for the migrated Zyro blog article; (4) a price-range figure ("à partir de X€") if any page is to display one. For each gap, mark which plan/page it blocks and note that the executor must leave the `[À compléter — Pierre ADAM]` / `[FAIT LOCAL REQUIS ...]` placeholder rather than inventing the fact. Explicitly cross-reference the SIRET as a Phase-1 cutover item (NOT touched in 999.1).
  </action>
  <verify>
    <automated>F=.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md; grep -q "Pierre-Action Facts Required" "$F" && grep -qi "fort-de-france" "$F" && grep -qi "le lamentin" "$F" && grep -qi "schoelcher" "$F" && grep -qi "les trois-îlets\|les trois-ilets" "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Pierre-Action Facts Required` section present.
    - Lists all 4 communes by name (Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets) as needing a local fact each — verified by the grep gate (plan 04 D-12 completeness dependency).
    - Lists case-study facts, blog date, and price-range as gaps; cross-references SIRET to Phase-1 cutover.
  </acceptance_criteria>
  <done>The gap list exists and downstream plans know exactly which placeholders block publish; all 4 communes covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| our-tooling→live Zyro prod site | Read-only HTTP GET crawl of a hard no-touch external site. The risk is an accidental write/deploy/DNS action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-02-01 | Tampering | Live Zyro prod site (`dloazurpiscines.com`) | mitigate | HARD CONSTRAINT (memory `never-touch-prod-zyro`): GET-only crawl, no push/deploy/DNS/write. Executor must not use any mutating HTTP method or hosting CLI against the live host. |
| T-9991-02-02 | Information disclosure | `llms.txt`/harvest content (downstream) | accept | Only public marketing copy is harvested; no admin/auth URLs are recorded in the inventory. |
| T-9991-02-SC | Tampering | composer/npm installs | accept | Zero installs — pure crawl + markdown authoring. |
</threat_model>

<verification>
- `ZYRO-CONTENT-HARVEST.md` exists with all named sections and the Pierre-action gap list (all 4 communes).
- No mutating operation was performed against the live Zyro site.
</verification>

<success_criteria>
- Real Zyro body copy, services (incl. spa), testimonials, NAP, URL inventory, and Pierre-fact gaps (all 4 communes) are captured in one document for plans 03/04 to author from.
</success_criteria>

<output>
Create `.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-02-SUMMARY.md` when done.
</output>
