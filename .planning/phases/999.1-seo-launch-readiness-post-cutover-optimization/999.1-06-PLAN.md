---
phase: 999.1-seo-launch-readiness-post-cutover-optimization
plan: 06
type: execute
wave: 4
depends_on: ["999.1-03", "999.1-04"]
files_modified:
  - public/llms.txt
  - routes/web.php
  - app/Http/Controllers/BlogController.php
  - app/Http/Controllers/SitemapController.php
  - resources/content/blog/les-3-etapes-indispensables-pour-un-entretien-de-piscine-parfait-en-martinique.md
  - resources/content/blog/de-la-passion-a-lentrepreneuriat-lhistoire-de-dlo-azur-piscines.md
  - resources/views/vitrine/realisations.blade.php
  - resources/views/layouts/app.blade.php
  - tests/Feature/Seo/LlmsTxtTest.php
  - tests/Feature/Seo/SitemapSeoTest.php
  - tests/Feature/BlogTest.php
autonomous: true
requirements: [SITE-07]
must_haves:
  truths:
    - "/llms.txt returns 200 with a text content-type and lists only public marketing URLs"
    - "Blog show pages emit og:type=article and an enriched Article JSON-LD (image, dateModified, mainEntityOfPage, publisher)"
    - "Sitemap includes the new service + city URLs and lastmod on static URLs"
    - "(D-13) /realisations has 2-3 written case studies (fact-gated) and the nav demotes 'Espace client' / promotes 'Demander un devis'"
  artifacts:
    - path: "public/llms.txt"
      provides: "AI-crawler discoverability file"
      contains: "Dlo Azur Piscines"
    - path: "tests/Feature/Seo/LlmsTxtTest.php"
      provides: "/llms.txt 200 assertion"
      contains: "llms"
  key_links:
    - from: "routes/web.php"
      to: "public/llms.txt"
      via: "kernel route serving the static file (mirror robots.txt)"
      pattern: "llms"
    - from: "app/Http/Controllers/SitemapController.php"
      to: "routes/vitrine.php"
      via: "Url::create(route('services.spa')) etc + setLastModificationDate"
      pattern: "services\\.|zones\\."
    - from: "app/Http/Controllers/BlogController.php"
      to: "resources/views/layouts/app.blade.php"
      via: "passes 'type' => 'article'; layout forwards $type to <x-seo.meta>"
      pattern: "'type' => 'article'"
---

<objective>
Ship the remaining MEDIUM/LOW discoverability items: `/llms.txt` (AI crawlers), blog `og:type=article` + enriched Article JSON-LD, real blog dates, sitemap `lastmod` + the new service/city URLs, `/realisations` case studies (D-13, fact-gated), and the nav tweak (demote "Espace client", promote "Demander un devis").

Purpose: closes the audit's MEDIUM/LOW backlog. Runs last (Wave 4) because the sitemap must reference the routes created in plans 03/04, and the nav edit shares `layouts/app.blade.php` with plan 03 (serialized by wave).
Output: llms.txt + route + blog enrichment + sitemap update + realisations expansion + nav tweak + Pest tests.

Task split note: the sitemap extension, the /realisations content, and the nav demotion are three independent deliverables with separate rollback boundaries, so they are split into Task 3 (sitemap) and Task 4 (realisations + nav) rather than bundled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md
@.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md

<interfaces>
<!-- Verified from codebase -->
robots.txt kernel route (analog) — routes/web.php lines 26-30:
`Route::get('/robots.txt', fn() => response(file_get_contents(public_path('robots.txt')), 200, ['Content-Type' => ...]))->name('robots');`

BlogController::show() returns view('blog.show', ['post','title','description','articleJsonLd' => $this->buildArticleSchema($post)]).
buildArticleSchema() currently: `Schema::article()->headline(...)->datePublished($post['date']->toIso8601String())->author(Schema::person()->name($post['author']))->toScript()`.
Post array shape: title, date (Carbon), excerpt, author, slug; likely also 'cover' (verify in BlogRepository) — fall back to `asset('assets/brand/og-default.jpg')` if absent.

layouts/app.blade.php already forwards `$type` to `<x-seo.meta>` (plan 03 Task 1). The nav lives in this file: line 54 `Espace client` link → `route('portail.magic-link.request')`; nav also has a tel link (line 55). "Demander un devis" should point to `route('contact')`.

SitemapController::index() builds `Sitemap::create()->add(Url::create(route('home'))->setPriority(1.0)->setChangeFrequency('weekly'))->add(Url::create(route('services'))->setPriority(0.8))...`. Blog URLs already get `setLastModificationDate($post['date'])`. New routes from plans 03/04: services.entretien-recurrent, services.analyse-eau, services.spa, services.eau-verte-urgence, zones.fort-de-france, zones.le-lamentin, zones.schoelcher, zones.les-trois-ilets.

Blog placeholder dates: `resources/content/blog/les-3-etapes...md` and `de-la-passion...md` both have `date: 2025-01-01` (placeholder). The bienvenue post already has real 2026-05-28. Real dates are a Pierre-fact — if not supplied, set a defensible real date and note the dependency (do NOT keep 2025-01-01).

realisations.blade.php: existing page, has `@include('vitrine.partials.realisations-grid')`, a "Plus de chantiers" photo grid, and `final-cta`. Add case studies section before final-cta.
<x-seo.meta> accepts `type` prop (default 'website').
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public/llms.txt + serving route + test</name>
  <files>public/llms.txt, routes/web.php, tests/Feature/Seo/LlmsTxtTest.php</files>
  <read_first>
    - routes/web.php (lines 23-30: the robots.txt kernel route to mirror)
    - public/robots.txt (the static-file pattern)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§public/llms.txt — content + route)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md (§/llms.txt — structure, tone, do-NOT-include)
  </read_first>
  <action>
    Create `public/llms.txt` as plain markdown: a `# Dlo Azur Piscines` title, a `>` one-line description ("Pisciniste d'entretien en Martinique — service-area business, Fort-de-France et communes limitrophes."), and a `## Pages` list of public marketing URLs only (Accueil, Services + the 4 service sub-pages, the 4 zones pages, Réalisations, Blog, Contact). Use absolute `https://dloazurpiscines.com/...` URLs (this file is post-cutover, prod host is the live domain). Do NOT include admin, auth, or client-portal routes (UI-SPEC). Factual tone, no marketing fluff. Add a kernel route in `routes/web.php` mirroring the robots.txt route: `Route::get('/llms.txt', fn() => response(file_get_contents(public_path('llms.txt')), 200, ['Content-Type' => 'text/plain; charset=UTF-8']))->name('llms');`. Create `tests/Feature/Seo/LlmsTxtTest.php` asserting GET `/llms.txt` returns 200, content-type starts with `text/`, body contains "Dlo Azur Piscines", and body does NOT contain "/admin" or "/login".
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=Llms 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `public/llms.txt` exists, plain markdown, lists only public URLs (no /admin, /login, portal routes).
    - GET `/llms.txt` returns 200 with a `text/*` content-type (test asserts).
    - `grep -q 'llms' routes/web.php` succeeds; route named `llms`.
    - `./vendor/bin/pest --filter=Llms` passes.
  </acceptance_criteria>
  <done>/llms.txt is served, public-only, and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Blog og:type=article + enriched Article schema + real dates</name>
  <files>app/Http/Controllers/BlogController.php, resources/content/blog/les-3-etapes-indispensables-pour-un-entretien-de-piscine-parfait-en-martinique.md, resources/content/blog/de-la-passion-a-lentrepreneuriat-lhistoire-de-dlo-azur-piscines.md, tests/Feature/BlogTest.php</files>
  <read_first>
    - app/Http/Controllers/BlogController.php (the file being edited — show() + buildArticleSchema())
    - tests/Feature/BlogTest.php (existing blog test to extend)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md (§Blog og:type fix, §Blog real dates — verified Article props)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§BlogController EXTEND)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md (§Pierre-Action Facts Required — real blog date)
  </read_first>
  <action>
    In `BlogController::show()` add `'type' => 'article'` to the view payload (the layout already forwards `$type` to `<x-seo.meta>` after plan 03). Enrich `buildArticleSchema()`: add `->dateModified($post['date']->toIso8601String())`, `->image($post['cover'] ?? asset('assets/brand/og-default.jpg'))`, `->mainEntityOfPage(url('/blog/'.$post['slug']))`, `->publisher(Schema::organization()->name('Dlo Azur Piscines'))`. (Verify the `cover`/image key in BlogRepository; fall back to og-default.jpg.) Replace the placeholder `date: 2025-01-01` in both migrated Zyro blog markdown files with a defensible real publish date — if Pierre has not supplied the exact date (see ZYRO-CONTENT-HARVEST.md gap list), use the article's known migration/publish date rather than 2025-01-01, and note the Pierre-fact dependency in the SUMMARY. Extend `tests/Feature/BlogTest.php`: assert a blog show response contains `og:type` content `article`, the Article JSON-LD contains `"publisher"` and `"dateModified"`, and assert NO blog markdown frontmatter still contains `2025-01-01`.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=Blog 2>&1 | tail -12 && ! grep -rq "2025-01-01" resources/content/blog/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Blog show emits `og:type` = `article` (test asserts).
    - Article JSON-LD includes `publisher`, `dateModified`, `image`, `mainEntityOfPage`.
    - No blog markdown frontmatter contains `2025-01-01` (`grep -rq "2025-01-01" resources/content/blog/` returns nonzero/empty).
    - `./vendor/bin/pest --filter=Blog` passes.
  </acceptance_criteria>
  <done>Blog pages are article-typed with enriched schema and real dates.</done>
</task>

<task type="auto">
  <name>Task 3: Sitemap — append new service/city URLs + lastmod on static URLs</name>
  <files>app/Http/Controllers/SitemapController.php, tests/Feature/Seo/SitemapSeoTest.php</files>
  <read_first>
    - app/Http/Controllers/SitemapController.php (the file being extended — Url::create pattern)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§SitemapController EXTEND)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-RESEARCH.md (§Sitemap lastmod for static URLs)
  </read_first>
  <action>
    In `SitemapController::index()` append `Url::create(route('...'))->setPriority(...)->setLastModificationDate(now())` for the new routes: services.eau-verte-urgence, services.entretien-recurrent, services.analyse-eau, services.spa (priority 0.8), zones.fort-de-france, zones.le-lamentin, zones.schoelcher, zones.les-trois-ilets (priority 0.7). Also add `->setLastModificationDate(now())` to the existing static URLs (services, realisations) that currently lack lastmod. Do NOT touch the nav or realisations content here — this task owns the sitemap only (clean rollback boundary). Create `tests/Feature/Seo/SitemapSeoTest.php`: GET `/sitemap.xml` returns 200, body contains the 4 service URLs and 4 zone URLs and `<lastmod>` on at least the static service URLs.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=SitemapSeo 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `/sitemap.xml` includes all 4 `/services/*` and all 4 `/zones/*` URLs plus `<lastmod>` on static service URLs (test asserts).
    - `./vendor/bin/pest --filter=SitemapSeo` passes.
    - This task modifies only `SitemapController.php` and its test (no nav/realisations changes).
  </acceptance_criteria>
  <done>Sitemap covers the new service/city URLs with lastmod; isolated to the sitemap controller.</done>
</task>

<task type="auto">
  <name>Task 4: /realisations case studies + nav demote Espace client / promote Demander un devis</name>
  <files>resources/views/vitrine/realisations.blade.php, resources/views/layouts/app.blade.php, tests/Feature/Seo/SitemapSeoTest.php</files>
  <read_first>
    - resources/views/vitrine/realisations.blade.php (the page being expanded — structure + final-cta include)
    - resources/views/layouts/app.blade.php (nav at lines 35-95: Espace client link line 54, mobile nav lines 79-95)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-PATTERNS.md (§realisations EXPAND case-study card)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-UI-SPEC.md (§/realisations Case Studies copy, §Color rules; nav demote note)
    - .planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/ZYRO-CONTENT-HARVEST.md (§Pierre-Action Facts Required — case study facts)
  </read_first>
  <action>
    (a) In `resources/views/vitrine/realisations.blade.php` add a case-studies `<section>` before the `final-cta` include: a `text-lagon-600` kicker "CHANTIERS", an h2 "Réalisations récentes", and 2-3 case-study `<article>` cards using h3 pattern "[Type] · [Commune] — [Problème résolu]" with body "commune + problème → protocole → paramètres avant/après → résultat" and a `text-success` "Eau saine" outcome chip. Author from ZYRO-CONTENT-HARVEST.md; KEEP the `<!-- [CHANTIER RÉEL REQUIS — Pierre ADAM doit fournir: commune, type, mesures avant/après] -->` HTML-comment placeholder where real measures are missing (do NOT invent). No bordered left-callouts (use ring/tinted bg). (b) In `layouts/app.blade.php` nav: demote "Espace client" (make it visually secondary — e.g. a quiet text link, lower emphasis) and add/promote a primary "Demander un devis" CTA → `route('contact')` styled `bg-azure-500 text-white` (min-h-[44px]); apply to BOTH the desktop nav (line ~54 area) and the mobile nav list (lines ~79-95). Do not remove the Espace client link (clients still need it) — just reduce its prominence relative to the devis CTA. (c) Extend `tests/Feature/Seo/SitemapSeoTest.php` (or add a focused assertion group) covering this task's deliverables: GET `/realisations` 200 and body contains "CHANTIERS"; the layout renders a "Demander un devis" link → `route('contact')` and still renders the Espace client link.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter=SitemapSeo 2>&1 | tail -12 && grep -q "Demander un devis" resources/views/layouts/app.blade.php && grep -q "CHANTIER RÉEL REQUIS\|CHANTIERS" resources/views/vitrine/realisations.blade.php && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `/realisations` contains a "CHANTIERS" case-studies section with 2-3 `<article>` cards and an intact `CHANTIER RÉEL REQUIS` placeholder where measures are missing.
    - `layouts/app.blade.php` nav contains a primary "Demander un devis" CTA → `route('contact')` in both desktop and mobile nav; "Espace client" demoted but still present.
    - `./vendor/bin/pest --filter=SitemapSeo` passes; full suite green.
    - This task does NOT modify `SitemapController.php` (sitemap is Task 3's boundary).
  </acceptance_criteria>
  <done>Realisations has fact-gated case studies; nav promotes the devis CTA while keeping Espace client; independent of the sitemap change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/llms.txt, /sitemap.xml | Public static/generated files served to crawlers; risk is leaking non-public URLs. |
| server→crawler | Blog Article JSON-LD + sitemap consumed by crawlers; no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-06-01 | Information disclosure | `/llms.txt` content | mitigate | Only intentionally-public marketing URLs listed; test asserts absence of `/admin` and `/login`. No internal/auth/portal paths (RESEARCH §Security Domain). |
| T-9991-06-02 | Tampering (XSS) | Blog Article JSON-LD + realisations Blade | mitigate | `Schema::article()->toScript()` (JSON_HEX_TAG); realisations copy is static `{{ }}`-escaped Blade. Post fields originate from in-repo markdown, not user input. |
| T-9991-06-03 | Information disclosure | sitemap URLs | accept | Sitemap lists only public marketing routes (no admin/portal). Existing pattern from Phase 1. |
| T-9991-06-SC | Tampering | composer/npm installs | accept | Zero new packages (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `./vendor/bin/pest --filter=Llms`, `--filter=Blog`, `--filter=SitemapSeo` all pass; full suite green.
- `/llms.txt` 200 text, public-only; blog article-typed; sitemap covers new URLs + lastmod; realisations case studies present; nav devis CTA promoted.
</verification>

<success_criteria>
- /llms.txt served (public-only), blog og:type=article + enriched schema + real dates, sitemap with new URLs + lastmod, /realisations fact-gated case studies, nav demotes Espace client / promotes Demander un devis.
</success_criteria>

<output>
Create `.planning/phases/999.1-seo-launch-readiness-post-cutover-optimization/999.1-06-SUMMARY.md` when done.
</output>
