---
phase: 06-blog-admin-crud
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - routes/admin.php
  - app/Http/Controllers/Admin/PostController.php
  - app/Livewire/PostIndex.php
  - resources/views/admin/blog/index.blade.php
  - resources/views/admin/blog/create.blade.php
  - resources/views/admin/blog/edit.blade.php
  - resources/views/livewire/post-index.blade.php
  - resources/views/components/admin/status-badge.blade.php
  - resources/views/components/admin/sidebar.blade.php
  - tests/Feature/PostAdminListTest.php
autonomous: true
requirements: [CONTENT-01]
must_haves:
  truths:
    - "An authenticated operator sees /admin/blog listing ALL posts (drafts + published)"
    - "Each list row shows a status badge (Publié / Brouillon) and cover thumbnail or drop fallback"
    - "The empty state appears when no posts exist; search filters by title"
    - "An unauthenticated request to /admin/blog redirects to login"
    - "The sidebar shows an active Blog nav item"
  artifacts:
    - path: "app/Http/Controllers/Admin/PostController.php"
      provides: "thin GET controllers index/create/show/edit"
      contains: "view('admin.blog.index')"
    - path: "app/Livewire/PostIndex.php"
      provides: "paginated list of ALL posts with title search"
      contains: "WithPagination"
    - path: "resources/views/components/admin/status-badge.blade.php"
      provides: "reusable Publié/Brouillon chip"
      contains: "Brouillon"
  key_links:
    - from: "routes/admin.php"
      to: "app/Http/Controllers/Admin/PostController.php"
      via: "blog.index/create/show/edit routes"
      pattern: "PostController"
    - from: "resources/views/admin/blog/index.blade.php"
      to: "app/Livewire/PostIndex.php"
      via: "<livewire:post-index />"
      pattern: "livewire:post-index"
---

<objective>
Admin-CRUD shell slice: the read-only admin surface for blog posts. Add the `/admin/blog` route group (mirroring Clients CRUD: thin GET controllers + Livewire list), a `PostIndex` Livewire list that shows ALL posts (drafts included — admin sees everything, `scopePublished` is public-only), the three `admin/blog/*` thin views, a reusable `<x-admin.status-badge>` chip, and the active Blog sidebar nav item. Built to the 06-UI-SPEC contract (Surface 1 + badge component + sidebar).

Purpose: After this slice Pierre can navigate to /admin/blog and see his article list with statuses — the shell the editor (Plan 04) plugs into.
Output: blog routes, PostController, PostIndex + view, status-badge component, sidebar nav, three admin views, list test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-blog-admin-crud/06-CONTEXT.md
@.planning/phases/06-blog-admin-crud/06-PATTERNS.md
@.planning/phases/06-blog-admin-crud/06-UI-SPEC.md

<interfaces>
<!-- Admin route group (routes/admin.php) registered under middleware(['web','auth'])->prefix('admin')->name('admin.') -->
<!-- Clients route shape to mirror (routes/admin.php:24-27): index/create/{client}/{client}/edit -->
<!-- Post model (Plan 01): Post::query(), $post->status, $post->title, $post->date, $post->getFirstMediaUrl('cover','thumbnail') -->
<!-- ClientIndex (app/Livewire/ClientIndex.php): WithPagination, public string $search, updatedSearch()->resetPage(), pgsql ILIKE branch, paginate(25) -->
</interfaces>

<ui_design_context>
Build to the contract in 06-UI-SPEC.md (Surface 1 — Liste des articles, Composant — Badge statut, Sidebar entrée Blog) and DESIGN.md @theme tokens.
- Mirror `resources/views/admin/clients/index.blade.php` and `resources/views/livewire/client-index.blade.php` for the shell (header, search, list card, empty state, pagination) — consistency screen-to-screen IS the affordance for Pierre.
- Tokens only from DESIGN.md @theme: cards `rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs`, primary `bg-azure-500 hover:bg-azure-600`, labels/meta `ink-900/ink-500`, `font-display` (Fredoka) for h1 only.
- Status badge: chip `rounded-full px-2.5 py-0.5 text-xs`, Publié = success/10 bg + dark-green text + success dot, Brouillon = sand-100 + ink-500 + ink-400 dot. Labels exactly "Publié" / "Brouillon".
- Cover thumbnail `h-11 w-14 rounded-lg object-cover ring-1 ring-navy-900/8`; fallback `bg-azure-50` block with `<x-icon.drop>` — never a fake image.
- Sidebar: active item (not greyed), feather file-text icon, label "Blog", aria-current when active.
- FR copy from the UI-SPEC copywriting table; no em dash; WCAG AA (badge green dark enough for 4.5:1).
</ui_design_context>
</context>

<phase_goal_user_story>
**As a** Pierre (operator), **I want to** open /admin/blog and see all my articles with their draft/published status, **so that** I have a familiar place (mirroring my Clients list) to manage blog content before writing.
</phase_goal_user_story>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Blog routes + PostController + active sidebar nav item</name>
  <files>routes/admin.php, app/Http/Controllers/Admin/PostController.php, resources/views/components/admin/sidebar.blade.php, tests/Feature/PostAdminListTest.php</files>
  <read_first>
    - routes/admin.php (full — Clients route block lines 24-27 to mirror; add `use App\Http\Controllers\Admin\PostController;`)
    - app/Http/Controllers/Admin/ClientController.php (exact analog — copy structure, swap Client→Post, view names)
    - resources/views/components/admin/sidebar.blade.php (Clients @class item lines 43-56 — copy pattern; insert Blog item after Passages)
    - 06-PATTERNS.md §"routes/admin.php" + §"PostController" + §"sidebar.blade.php" + 06-UI-SPEC.md §Sidebar
  </read_first>
  <behavior>
    - GET /admin/blog while authenticated → 200.
    - GET /admin/blog while unauthenticated → redirect to login (302).
    - Sidebar renders a Blog link to route('admin.blog.index') with aria-current when on a blog route.
  </behavior>
  <action>
    Add to routes/admin.php (after Clients block) the four blog routes mirroring Clients exactly: `blog` → index, `blog/create` → create, `blog/{post}` → show, `blog/{post}/edit` → edit, all `[PostController::class, ...]` with names blog.index/create/show/edit. Use `{post}` id-binding (admin internal; public uses slug). Import PostController. Create `App\Http\Controllers\Admin\PostController` mirroring ClientController: index() returns view('admin.blog.index'); create() returns view('admin.blog.create'); show(Post $post) returns view('admin.blog.show', compact('post')); edit(Post $post) returns view('admin.blog.edit', compact('post')). Insert the Blog sidebar item in components/admin/sidebar.blade.php between the Passages item and the greyed future items, copying the Clients @class pattern (routeIs('admin.blog.*'), aria-current), feather file-text icon (20×20 stroke 2), label "Blog". Create tests/Feature/PostAdminListTest.php with the auth/redirect behaviors (use a seeded auth user via actingAs).
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PostAdminListTest.php --filter "admin blog"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PostController::class, 'index'" routes/admin.php` and the other three route names present.
    - `grep -n "route('admin.blog.index')" resources/views/components/admin/sidebar.blade.php` matches; `aria-current` present in the Blog item.
    - PostAdminListTest: authenticated 200 + unauthenticated 302-to-login behaviors pass (GREEN).
  </acceptance_criteria>
  <done>Blog routes registered under admin auth group; PostController mirrors ClientController; active Blog sidebar item; auth gate verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PostIndex Livewire list + status-badge component + admin views</name>
  <files>app/Livewire/PostIndex.php, resources/views/livewire/post-index.blade.php, resources/views/admin/blog/index.blade.php, resources/views/admin/blog/create.blade.php, resources/views/admin/blog/edit.blade.php, resources/views/components/admin/status-badge.blade.php, tests/Feature/PostAdminListTest.php</files>
  <read_first>
    - app/Livewire/ClientIndex.php (exact analog — WithPagination, search, updatedSearch, pgsql ILIKE, paginate(25))
    - resources/views/livewire/client-index.blade.php (full — header, search, list card, empty state, pagination)
    - resources/views/admin/clients/{index,create,edit}.blade.php (thin view analogs)
    - 06-PATTERNS.md §"PostIndex" + §"post-index.blade.php" + §"admin/blog/*" views + §"Blade View Shell"
    - 06-UI-SPEC.md Surface 1 (list cards, états vide/recherche, pagination) + Composant Badge statut + Copywriting table
  </read_first>
  <behavior>
    - PostIndex render lists ALL posts (drafts + published), ordered by date desc — NOT filtered by status.
    - Search filters by title (ILIKE on pgsql / LIKE on sqlite).
    - Each row shows the status badge with label "Publié" or "Brouillon".
    - Empty state appears when zero posts; "Nouvel article" CTA links to admin.blog.create.
  </behavior>
  <action>
    Create `App\Livewire\PostIndex` mirroring ClientIndex: `use WithPagination`, `public string $search='';`, `updatedSearch()` resets page, render() builds `Post::query()->when($search, fn($q)=>$q->where('title',$likeOp,'%'.$search.'%'))->orderByDesc('date')->paginate(25)` with the driver-aware ILIKE/LIKE branch — NO status filter (admin sees drafts). render() → view('livewire.post-index'). Create `resources/views/components/admin/status-badge.blade.php` as a Blade component taking `$status`: chip `rounded-full px-2.5 py-0.5 text-xs inline-flex items-center gap-1.5`; Publié → success/10 bg, dark-green text (oklch ~0.42 0.12 155 for AA), success dot, label "Publié"; Brouillon → sand-100 bg, ink-500 text, ink-400 dot, label "Brouillon". Create post-index.blade.php mirroring client-index: header "Blog" + "Nouvel article" CTA, optional search ("Rechercher un article…"), list cards (cover thumbnail via getFirstMediaUrl or x-icon.drop fallback, title, date d/m/Y meta, `<x-admin.status-badge :status="$post->status" />`, chevron) linking to admin.blog.edit, empty state + no-result state + pagination — all FR copy per UI-SPEC. Create the three thin admin/blog views: index embeds `<livewire:post-index />`, create embeds `<livewire:post-form />`, edit embeds `<livewire:post-form :postId="$post->id" />` (post-form arrives in Plan 04 — these views reference it now; the create/edit GET will render an empty/missing component until Plan 04, acceptable as the shell). Use the layouts/admin shell (@extends, sidebar, topbar, mobile-bottom-nav). Extend PostAdminListTest: assert list shows a seeded published + draft post with correct badge labels, and empty-state copy when none.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PostAdminListTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'WithPagination' app/Livewire/PostIndex.php` matches; render has NO `where('status'` filter (admin sees all).
    - `grep -n 'orderByDesc' app/Livewire/PostIndex.php` matches with `'date'`.
    - status-badge component contains both "Publié" and "Brouillon" labels.
    - PostAdminListTest: list shows both statuses with correct badge labels + empty-state behaviors pass (GREEN).
  </acceptance_criteria>
  <done>PostIndex lists all posts with status badges; reusable status-badge chip; three admin views wired to layout; UI-SPEC Surface 1 met; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/blog | Authenticated operator only; auth middleware on route group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | unauthenticated access to /admin/blog | mitigate | routes inherit middleware(['web','auth']) from the admin group; test asserts 302-to-login for guests |
| T-06-08 | Injection | search term in PostIndex query | mitigate | Eloquent parameter binding (when/where with bound value); ILIKE pattern parameterized, not concatenated into SQL |
| T-06-SC | Tampering | composer/npm installs | accept | this plan adds NO new package |
</threat_model>

<verification>
- `./vendor/bin/pest --filter "PostAdmin"` green.
- Manual (deferred to Plan 04 human-verify): visual list + badge rendering against UI-SPEC.
</verification>

<success_criteria>
- /admin/blog shows all posts with Publié/Brouillon badges; auth-gated; search by title.
- Sidebar has an active Blog item; views follow the Clients shell and UI-SPEC tokens/copy.
</success_criteria>

<output>
Create `.planning/phases/06-blog-admin-crud/06-03-SUMMARY.md` when done
</output>
