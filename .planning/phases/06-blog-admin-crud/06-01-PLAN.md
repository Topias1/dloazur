---
phase: 06-blog-admin-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - phpunit.xml
  - app/Models/Post.php
  - database/migrations/2026_05_30_000001_create_posts_table.php
  - database/seeders/PostMigrationSeeder.php
  - app/Support/BlogRepository.php
  - tests/Feature/PostModelTest.php
  - tests/Feature/PostMigrationSeederTest.php
autonomous: true
requirements: [CONTENT-01]
must_haves:
  truths:
    - "A Post can be created and queried from the posts table"
    - "Post::published() returns only status=published rows"
    - "Slug auto-generates from title via Str::slug on create"
    - "PostMigrationSeeder lands the 3 canonical articles and is safe to re-run (3 rows after 2 runs)"
  artifacts:
    - path: "app/Models/Post.php"
      provides: "Eloquent Post model with HasMedia, scopePublished, slug auto-gen"
      contains: "scopePublished"
    - path: "database/migrations/2026_05_30_000001_create_posts_table.php"
      provides: "posts table with string(16) status + compound index"
      contains: "string('status', 16)"
    - path: "database/seeders/PostMigrationSeeder.php"
      provides: "Idempotent migration of the 3 .md articles"
      contains: "updateOrCreate"
  key_links:
    - from: "database/seeders/PostMigrationSeeder.php"
      to: "app/Support/BlogRepository.php::parse"
      via: "public parse() reuse"
      pattern: "->parse\\("
---

<objective>
Foundation slice: the thinnest DB-backed data layer that proves real read/write for blog posts. Introduce the `Post` Eloquent model (`HasMedia`, `scopePublished`, slug auto-gen), the `posts` migration (project `string(16)` status convention), make `BlogRepository::parse()` public, and an idempotent `PostMigrationSeeder` that migrates the 3 canonical `.md` articles byte-for-byte. Also lock the test environment to `BLOG_SOURCE=files` so the existing `BlogTest` stays green while later waves flip the public read path to DB.

Purpose: Every later slice (public cutover, admin CRUD, editor) depends on this model + table existing. This is the contract layer.
Output: `Post` model, `posts` migration, public `parse()`, `PostMigrationSeeder`, two new Pest test files, `phpunit.xml` env lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-blog-admin-crud/06-CONTEXT.md
@.planning/phases/06-blog-admin-crud/06-RESEARCH.md
@.planning/phases/06-blog-admin-crud/06-PATTERNS.md

<interfaces>
<!-- BlogRepository::parse() return shape (app/Support/BlogRepository.php:129-140) — the Post columns must cover every key. -->
parse() returns: title (string), slug (string), date (Carbon), show_date (bool), excerpt (string), author (string), cover (string|null), body (string), reading_time (int), filepath (string).
parse() is currently `private` at app/Support/BlogRepository.php:108 — make it `public`.

<!-- Project status-column convention (database/migrations/2026_05_28_000005_create_passages_table.php:19) -->
$table->string('status', 16)->default('draft');  // NOT ->enum()

<!-- AdminSeeder idempotent pattern: User::updateOrCreate([key], [attrs]); standalone, NOT registered in DatabaseSeeder. -->
</interfaces>
</context>

<phase_goal_user_story>
**As a** the platform (data layer), **I want to** persist and query blog posts in Postgres with a published scope and migrated legacy content, **so that** Pierre's future admin actions and the public blog read path have a real DB to operate on.
</phase_goal_user_story>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Test scaffolds + phpunit BLOG_SOURCE lock + posts migration + public parse()</name>
  <files>phpunit.xml, database/migrations/2026_05_30_000001_create_posts_table.php, app/Support/BlogRepository.php, tests/Feature/PostModelTest.php, tests/Feature/PostMigrationSeederTest.php</files>
  <read_first>
    - phpunit.xml (lines 23-42 — env block; add BLOG_SOURCE before any DB read path is flipped)
    - database/migrations/2026_05_28_000005_create_passages_table.php (analog: string(16) status + compound index)
    - app/Support/BlogRepository.php:108 (parse() — change `private` → `public`; do NOT alter its body or return shape)
    - 06-PATTERNS.md §"database/migrations/*_create_posts_table.php" + §"PostMigrationSeeder" (analog file:line)
    - 06-RESEARCH.md Pattern 4 (schema) + Open Question 3 (test env BLOG_SOURCE=files)
  </read_first>
  <behavior>
    - PostModelTest: a Post created with only a title gets a Str::slug-derived slug.
    - PostModelTest: Post::published() excludes draft rows, includes published rows.
    - PostMigrationSeederTest: running the seeder once yields exactly 3 posts with the 3 canonical slugs; running it twice still yields 3 (idempotent upsert keyed on slug).
    - PostMigrationSeederTest: migrated posts preserve title, author (default "Pierre ADAM"), date, show_date, status='published'.
  </behavior>
  <action>
    Add `<env name="BLOG_SOURCE" value="files"/>` to the phpunit.xml env block so the existing file-backed BlogTest keeps passing after later waves default the app to DB. Create the `posts` migration (timestamp prefix `2026_05_30_000001`) with columns: id; string title; string slug unique; text body; text excerpt nullable; string('status', 16) default 'draft'; string author default 'Pierre ADAM'; date date nullable; boolean show_date default true; timestamps; compound index ['status','date']. Use `->enum()` is FORBIDDEN — match the passages `->string('status', 16)` convention. Change `BlogRepository::parse()` from `private` to `public` (no body change). Create the two Pest test files in tests/Feature/ with the behaviors above; these are RED scaffolds — they may reference Post and PostMigrationSeeder which Tasks 1-2 implement. Use `RefreshDatabase` (sqlite :memory:). For the seeder test, point at the real resource_path('content/blog') files.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/BlogTest.php</automated>
    <automated>grep -n 'string(.status., 16)' database/migrations/2026_05_30_000001_create_posts_table.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'public function parse' app/Support/BlogRepository.php` matches (visibility changed).
    - `grep -c 'enum(' database/migrations/2026_05_30_000001_create_posts_table.php` returns 0.
    - `grep -n 'BLOG_SOURCE' phpunit.xml` matches with value "files".
    - Existing `./vendor/bin/pest tests/Feature/BlogTest.php` stays fully green (no regression from the env lock).
    - `tests/Feature/PostModelTest.php` and `tests/Feature/PostMigrationSeederTest.php` exist and contain the four behaviors above.
  </acceptance_criteria>
  <done>posts migration uses string(16) status + compound index; parse() is public; phpunit locks BLOG_SOURCE=files; existing BlogTest green; two RED test scaffolds exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Post model — HasMedia, scopePublished, slug auto-gen</name>
  <files>app/Models/Post.php</files>
  <read_first>
    - app/Models/Client.php (analog: namespace, $fillable, $casts, HasFactory)
    - 06-PATTERNS.md §"app/Models/Post.php" (file:line excerpts: fillable, casts, scopePublished, booted slug hook, medialibrary registration)
    - 06-RESEARCH.md Pattern 2 (medialibrary registerMediaCollections + nested registerMediaConversions + singleFile + nonQueued) + Pattern 4 (scopePublished, booted hook)
  </read_first>
  <behavior>
    - Post::published() (scope) compiles to where status='published'.
    - Creating a Post with title only sets slug = Str::slug(title).
    - getFirstMediaUrl('cover','thumbnail') is callable (HasMedia wired); returns '' when no cover.
  </behavior>
  <action>
    Create `App\Models\Post` extending Model implements `Spatie\MediaLibrary\HasMedia`, using `InteractsWithMedia` and `HasFactory`. $fillable: title, slug, body, excerpt, status, author, date, show_date. $casts: date => 'date', show_date => 'boolean'. Add `scopePublished(Builder $query)` returning `where('status','published')`. Add `booted()` with a `creating` hook that sets slug from `Str::slug($post->title)` only when slug empty. Add `registerMediaCollections()` declaring a `cover` collection: `->singleFile()->useDisk('s3')` with a nested `registerMediaConversions` adding a `thumbnail` conversion `->width(1200)->height(630)->nonQueued()` (no queue dependency on serverless). Do NOT add getRouteKeyName — admin routes use id binding; the public blog.show route resolves slug via BlogRepository, not model binding.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PostModelTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'implements HasMedia' app/Models/Post.php` matches.
    - `grep -n 'function scopePublished' app/Models/Post.php` matches.
    - `grep -n "addMediaConversion('thumbnail')" app/Models/Post.php` and `->nonQueued()` both present.
    - PostModelTest slug-auto-gen and scopePublished behaviors pass (GREEN).
  </acceptance_criteria>
  <done>Post model exists with HasMedia/cover collection, scopePublished, and slug auto-gen; PostModelTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent PostMigrationSeeder for the 3 canonical articles</name>
  <files>database/seeders/PostMigrationSeeder.php</files>
  <read_first>
    - database/seeders/AdminSeeder.php (analog: standalone, updateOrCreate, production-safe, NOT registered in DatabaseSeeder)
    - database/seeders/DatabaseSeeder.php (confirm it is env-gated; do NOT register the new seeder here)
    - app/Support/BlogRepository.php:108 (now public parse() — reuse it)
    - 06-PATTERNS.md §"PostMigrationSeeder" + 06-RESEARCH.md Pattern 6
    - 06-CONTEXT.md <specifics> (the 3 exact canonical slugs)
  </read_first>
  <behavior>
    - run() upserts exactly 3 posts keyed on slug (the 3 canonical slugs).
    - Re-running run() leaves 3 rows (no duplicates).
    - Each migrated post: status='published', author defaults 'Pierre ADAM' when absent, date + show_date preserved from front matter.
  </behavior>
  <action>
    Create `Database\Seeders\PostMigrationSeeder` extending Seeder, standalone (NOT registered in DatabaseSeeder — mirror AdminSeeder). In run(): instantiate `new BlogRepository()`, iterate the 3 resource_path('content/blog') files for the canonical slugs (bienvenue-dlo-azur, de-la-passion-a-lentrepreneuriat-lhistoire-de-dlo-azur-piscines, les-3-etapes-indispensables-pour-un-entretien-de-piscine-parfait-en-martinique). For each, call the now-public `parse($path)` to get the array, then `Post::updateOrCreate(['slug' => $parsed['slug']], [...])` mapping title, body, excerpt, author, date (use $parsed['date']->toDateString() since column is a date cast), show_date, status => 'published'. Cover stays null (the 3 articles have none; SEO og-default fallback covers it). Add a docblock noting: run via `php artisan db:seed --class=PostMigrationSeeder --force`; idempotent via slug upsert; production-safe.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/PostMigrationSeederTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'updateOrCreate' database/seeders/PostMigrationSeeder.php` matches with `['slug' =>` key.
    - `grep -c 'PostMigrationSeeder' database/seeders/DatabaseSeeder.php` returns 0 (not registered).
    - PostMigrationSeederTest idempotency (run twice → 3 rows) and field-preservation behaviors pass (GREEN).
  </acceptance_criteria>
  <done>Seeder migrates the 3 canonical articles idempotently, preserves all parsed fields, status=published; PostMigrationSeederTest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeder → filesystem | Reads trusted in-repo .md files; no untrusted input |
| migration → Postgres | DDL only; project-controlled schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | slug uniqueness | mitigate | unique index on slug column; duplicate Str::slug collision handled in PostForm (Plan 04) |
| T-06-02 | Elevation | PostMigrationSeeder unintended prod run | accept | idempotent upsert keyed on slug is safe to re-run; not registered in env-gated DatabaseSeeder; run explicitly with --force |
| T-06-SC | Tampering | composer/npm installs | mitigate | this plan adds NO new package; easymde install + legitimacy checkpoint lives in Plan 04 |
</threat_model>

<verification>
- `./vendor/bin/pest --filter "Post|Blog"` green (PostModelTest, PostMigrationSeederTest, BlogTest all pass).
- Migration applies cleanly: status is varchar(16), compound index on (status,date) present.
</verification>

<success_criteria>
- Post model + posts migration exist; scopePublished and slug auto-gen verified by test.
- PostMigrationSeeder migrates 3 canonical articles idempotently (verified: run twice → 3 rows).
- parse() public; phpunit BLOG_SOURCE=files; existing BlogTest green.
</success_criteria>

<output>
Create `.planning/phases/06-blog-admin-crud/06-01-SUMMARY.md` when done
</output>
