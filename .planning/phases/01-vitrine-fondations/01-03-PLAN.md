---
phase: 01-vitrine-fondations
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - routes/vitrine.php
  - app/Http/Controllers/VitrineController.php
  - app/Http/Controllers/SitemapController.php
  - app/Support/SchemaOrg/LocalBusinessSchema.php
  - resources/views/vitrine/home.blade.php
  - resources/views/vitrine/services.blade.php
  - resources/views/vitrine/realisations.blade.php
  - resources/views/vitrine/contact.blade.php
  - resources/views/vitrine/mentions-legales.blade.php
  - resources/views/vitrine/cgv.blade.php
  - resources/views/vitrine/confidentialite.blade.php
  - resources/views/vitrine/partials/hero.blade.php
  - resources/views/vitrine/partials/services-grid.blade.php
  - resources/views/vitrine/partials/how-it-works.blade.php
  - resources/views/vitrine/partials/hospitality.blade.php
  - resources/views/vitrine/partials/realisations-grid.blade.php
  - resources/views/vitrine/partials/pierre.blade.php
  - resources/views/vitrine/partials/espace-client-teaser.blade.php
  - resources/views/vitrine/partials/testimonials.blade.php
  - resources/views/vitrine/partials/final-cta.blade.php
  - resources/views/components/icon/sun.blade.php
  - resources/views/components/icon/star.blade.php
  - resources/views/components/icon/sparkle.blade.php
  - resources/views/components/icon/shield.blade.php
  - resources/views/components/icon/calendar.blade.php
  - tests/Feature/HomePageTest.php
  - tests/Feature/SeoTest.php
  - tests/Feature/StaticPagesTest.php
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-06
  - SITE-07

must_haves:
  truths:
    - "A visitor opening / sees the full home page transposed 1:1 from mockups/v1/vitrine.html — hero, services grid, how-it-works, hospitality, realisations, pierre bio, espace client teaser, testimonials, final CTA, footer"
    - "WhatsApp CTA is reachable from the topbar, the hero, the hospitality section's partner CTA fallback, the final CTA section, and the mobile floating FAB (md:hidden)"
    - "Pages /services, /realisations, /mentions-legales, /cgv, /confidentialite return 200 with their own <title> and meta description"
    - "GET /sitemap.xml returns 200 + Content-Type application/xml + body contains <urlset> with entries for /, /services, /realisations, /contact, /mentions-legales, /cgv, /confidentialite (blog entries arrive in Plan 04)"
    - "GET / contains a <script type=\"application/ld+json\"> with a valid Plumber LocalBusiness schema (name, image, address Martinique, geo Fort-de-France, telephone +596696940054, areaServed including Fort-de-France/Le Lamentin/Schoelcher/Les Trois-Îlets, openingHoursSpecification)"
    - "Every page has <meta name=description>, OG title/description/image/type/locale, twitter:card, theme-color #0080ff, canonical URL"
    - "All photos use the .photo-grade CSS filter and have descriptive French alt text; no stock tropical imagery (PRODUCT.md anti-references)"
    - "resources/views/vitrine/partials/testimonials.blade.php places a guarded `@if(class_exists(\\App\\Livewire\\GoogleReviews::class)) <livewire:google-reviews /> @endif` block (D-28 amended — Plan 04 supplies the component; this plan opens the seam)"
  artifacts:
    - path: resources/views/vitrine/home.blade.php
      provides: "Full home page transposed 1:1 from mockups/v1/vitrine.html using Plan 01 layouts/app.blade.php"
      contains: "<x-layouts.app>, <x-vitrine.hero>, <x-vitrine.services-grid>, wa.me/596696940054, application/ld+json"
    - path: app/Support/SchemaOrg/LocalBusinessSchema.php
      provides: "Builder that returns the Spatie\\SchemaOrg\\Plumber graph for the home page (D-26)"
      contains: "Schema::plumber(), addressRegion('Martinique'), latitude(14.6037), longitude(-61.0594), areaServed, openingHoursSpecification"
    - path: app/Http/Controllers/SitemapController.php
      provides: "Dynamic /sitemap.xml via spatie/laravel-sitemap covering vitrine + blog (Plan 04 will plug blog posts)"
      contains: "Sitemap::create(), Url::create('/'), Url::create('/services'), Url::create('/realisations'), Url::create('/contact')"
    - path: routes/vitrine.php
      provides: "Public vitrine routes — home + service + realisations + contact (view shell only, Plan 04 binds Livewire form) + 3 legal pages + sitemap.xml + named routes for navigation/footer/sitemap"
      contains: "Route::get('/', [VitrineController::class, 'home'])->name('home'), Route::get('/services'), Route::get('/realisations'), Route::get('/contact'), Route::get('/mentions-legales'), Route::get('/cgv'), Route::get('/confidentialite'), Route::get('/sitemap.xml', [SitemapController::class, 'index'])"
    - path: tests/Feature/HomePageTest.php
      provides: "Asserts every block from UI-SPEC §Page Structure exists on the home + WhatsApp CTAs are present + section order is preserved"
      contains: "->get('/')->assertOk(), assertSeeText('Votre piscine'), assertSeeText('claire toute l\\'année'), assertSee('wa.me/596696940054'), assertSeeInOrder([...sections in vitrine.html order...])"
    - path: tests/Feature/SeoTest.php
      provides: "Asserts sitemap.xml renders + LocalBusiness JSON-LD parses with required Plumber fields"
      contains: "->get('/sitemap.xml')->assertOk()->assertHeader('Content-Type', 'application/xml'), json_decode of <script type=application/ld+json> with @type Plumber, areaServed, geo"
  key_links:
    - from: "resources/views/vitrine/partials/testimonials.blade.php"
      to: "Plan 04 app/Livewire/GoogleReviews.php"
      via: "Guarded `@if(class_exists(\\App\\Livewire\\GoogleReviews::class)) <livewire:google-reviews /> @endif` — Plan 03 opens the seam; Plan 04 supplies the component (D-28 amended). Symmetric to the contact form guard pattern used elsewhere in this phase."
      pattern: "livewire:google-reviews|App\\\\Livewire\\\\GoogleReviews"
    - from: "resources/views/vitrine/home.blade.php"
      to: "Plan 04 /contact Livewire form"
      via: "anchor #contact in home links to /contact route; home itself contains the final CTA section but the form lives at /contact"
      pattern: "href=.{0,5}#contact|href=.{0,5}/contact"
    - from: "app/Http/Controllers/SitemapController.php"
      to: "Plan 04 BlogRepository"
      via: "Plan 04 will inject BlogRepository here to append blog post URLs to the sitemap; for now SitemapController emits only vitrine routes and is dependency-injection-ready"
      pattern: "BlogRepository|app\\(BlogRepository::class\\)"
---

<objective>
**Transpose the full vitrine 1:1 from `mockups/v1/vitrine.html` into Blade.** SITE-01, SITE-02, SITE-03 (home + services + realisations + supporting pages), SITE-06 (WhatsApp CTA everywhere), SITE-07 (sitemap.xml + LocalBusiness JSON-LD + OG meta + canonical + theme-color). Pages: home, services, realisations, contact (view shell only — Plan 04 injects the Livewire form), mentions-legales, cgv, confidentialite. SEO: dynamic sitemap, structured data via `spatie/schema-org` Plumber sub-type per D-26, meta tags + OG per UI-SPEC §SEO/Metadata Contracts.

Mockup mapping is **non-negotiable** — every section, every component, every copy line in the mockup ends up rendered. No redesign, no rewording, no skipping.

Output: a fully styled public vitrine running on the Walking Skeleton, no backend feature work beyond view rendering + SEO data builders.
</objective>

<note>
**Cross-plan mutation by Plan 06:** Plan 06 (Wave 3) will wrap the route groups in `routes/vitrine.php` with the `cache.headers:vitrine` middleware (and the `/sitemap.xml` route with `cache.headers:sitemap`). The middleware only adds response headers (`Cache-Control`, `Vary`), so no Plan 03 test breaks. Plan 03 ships `routes/vitrine.php` in its final route-definition form; Plan 06 surgically wraps the existing `Route::get(...)` calls in `Route::middleware('cache.headers:...')->group(...)` blocks. Coordinate via the file ownership note in Plan 06 Task 1.
</note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vitrine-fondations/01-CONTEXT.md
@.planning/phases/01-vitrine-fondations/01-RESEARCH.md
@.planning/phases/01-vitrine-fondations/01-UI-SPEC.md
@.planning/phases/01-vitrine-fondations/SKELETON.md
@.planning/phases/01-vitrine-fondations/01-01-SUMMARY.md
@PRODUCT.md
@DESIGN.md
@mockups/v1/vitrine.html
@mockups/v1/app.css

<interfaces>
<!-- Source-of-truth section order from UI-SPEC §Page Structure & Sections, must match mockups/v1/vitrine.html: -->
<!-- 1. <header> topbar — already in resources/views/layouts/app.blade.php from Plan 01 -->
<!-- 2. <main id="top"> hero -->
<!-- 3. #services — services grid -->
<!-- 4. "Comment ça marche" — 3-step process -->
<!-- 5. #hospitality — navy-drenched B2B section -->
<!-- 6. #realisations — photo grid (2+4 asymmetric) -->
<!-- 7. #pierre — bio section -->
<!-- 8. Espace client teaser -->
<!-- 9. Testimonials — 2 blockquotes -->
<!-- 10. #contact — final CTA (azure-600 bg) -->
<!-- 11. <footer> — navy-950 with QR code -->
<!-- 12. Floating WhatsApp FAB (mobile, md:hidden) -->

<!-- Copy contract from UI-SPEC §Copywriting Contract — vitrine table — use these strings VERBATIM, do not paraphrase. The mockup contains them already; if mockup and UI-SPEC ever disagree, UI-SPEC wins. -->

LocalBusiness JSON-LD contract (Plumber sub-type per D-26 + RESEARCH Pattern 5):
- @type: "Plumber"
- name: "Dlo Azur Piscines"
- image: asset('assets/brand/photos/hero-pierre-piscine.jpg')
- url: url('/')
- telephone: "+596696940054"
- priceRange: "€€"
- address: PostalAddress { addressCountry: 'FR', addressRegion: 'Martinique', addressLocality: 'Fort-de-France' }
- geo: GeoCoordinates { latitude: 14.6037, longitude: -61.0594 }
- areaServed: [City('Fort-de-France'), City('Le Lamentin'), City('Schoelcher'), City('Les Trois-Îlets'), AdministrativeArea('Martinique')]
- openingHoursSpecification:
  - { dayOfWeek: ['Monday','Tuesday','Wednesday','Thursday','Friday'], opens: '08:00', closes: '17:00' }
  - { dayOfWeek: 'Saturday', opens: '09:00', closes: '12:00' }
- sameAs: [TBD: Pierre's Google Business URL — leave empty array if not provided yet]

Sitemap entries Phase 1 (from D-27 + RESEARCH Pattern 6):
- / (priority 1.0, changefreq weekly)
- /services (priority 0.8)
- /realisations (priority 0.8)
- /contact (priority 0.7)
- /mentions-legales (priority 0.3)
- /cgv (priority 0.3)
- /confidentialite (priority 0.3)
- /blog (priority 0.6) — Plan 04 confirms this entry plus per-article entries

SEO meta requirements per UI-SPEC §SEO/Metadata Contracts:
- Every page: <title> unique, <meta name=description> unique, canonical URL, theme-color #0080ff, favicon link
- Home only: LocalBusiness JSON-LD in <head>
- All pages: OG title/description/image/type=website/locale=fr_MQ, twitter:card=summary_large_image

Photo placeholders (per UI-SPEC §Photo Treatment — Pierre to provide real photos; for now ship with safe placeholders + clear TODO in src):
- public/assets/brand/photos/hero-pierre-piscine.jpg
- public/assets/brand/photos/pierre-portrait.jpg
- public/assets/brand/photos/entretien-dos-logo.jpg
- public/assets/brand/photos/villa-hospitality.jpg
- public/assets/brand/photos/avant-apres.jpg
- public/assets/brand/photos/piscine-propre.jpg
- public/assets/brand/photos/piscine-hors-sol.jpg
- public/assets/brand/photos/montage-hors-sol.jpg
- public/assets/brand/photos/balai-detail.jpg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write HomePageTest + SeoTest + StaticPagesTest (RED) per UI-SPEC contract</name>
  <read_first>
    - .planning/phases/01-vitrine-fondations/01-UI-SPEC.md (§Page Structure & Sections + §Copywriting Contract + §SEO/Metadata Contracts + §Accessibility Contract)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (§Phase Requirements → Test Map for SITE-01..03, SITE-06, SITE-07)
    - mockups/v1/vitrine.html (read full file — every section's id, every CTA target, every section's expected copy)
    - tests/Pest.php (confirm RefreshDatabase is in Feature suite — though this plan does not actually need DB)
    - tests/Feature/SkeletonSmokeTest.php (style reference)
  </read_first>
  <behavior>
    HomePageTest (covers SITE-01, SITE-06):
    - Test 1 (`home renders 200 with required title and meta description`): GET / returns 200; body contains '<title>Dlo Azur Piscines · Entretien de piscines en Martinique</title>'; body contains '<meta name="description" content="Entretien, dépannage et analyse de l\'eau de votre piscine en Martinique. Un service à taille humaine. Devis gratuit, réponse rapide sur WhatsApp.">'.
    - Test 2 (`home contains the hero H1 copy`): assertSeeText('Votre piscine'); assertSeeText('claire toute l\'année.').
    - Test 3 (`home contains primary + secondary hero CTAs`): assertSee('Demander un devis gratuit'); assertSee('Nous écrire').
    - Test 4 (`home contains all 3 trust bullets`): assertSeeText('Photos à chaque passage'); assertSeeText('Réponse rapide'); assertSeeText('Suivi en ligne de vos interventions').
    - Test 5 (`home contains every section in mockup order`): assertSeeInOrder([id="services", id="hospitality", id="realisations", id="pierre", id="contact"]).
    - Test 6 (`home has at least 3 WhatsApp CTAs`): substring count of 'wa.me/596696940054' is ≥ 3 (topbar + hero + final CTA at minimum; FAB and footer push count higher).
    - Test 7 (`home has the WhatsApp FAB markup with md:hidden`): assertSee('class="fixed bottom-5 right-5 h-14 w-14') AND assertSee('md:hidden') — robust to slight class re-ordering, so split into two assertSee calls.
    - Test 8 (`home declares lang=fr and includes skip link`): assertSee('lang="fr"'); assertSeeText('Aller au contenu principal').
    - Test 9 (`home includes section copy "Un entretien complet"`): assertSeeText('Un entretien complet, pensé pour le climat antillais.').
    - Test 10 (`home includes hospitality CTA "Devenir partenaire"`): assertSeeText('Devenir partenaire').
    - Test 11 (`home includes Pierre bio H2`): assertSeeText('Dlo, c\'est l\'eau. Azur, c\'est sa couleur.').
    - Test 12 (`home includes the footer wordmark`): assertSeeText('© 2026 Dlo Azur Piscines · Pierre ADAM').

    SeoTest (covers SITE-07):
    - Test 13 (`sitemap returns valid XML`): GET /sitemap.xml returns 200 + header Content-Type 'application/xml' + body starts with '<?xml version="1.0"'.
    - Test 14 (`sitemap contains all vitrine routes`): body contains '<loc>' values for url('/'), url('/services'), url('/realisations'), url('/contact'), url('/mentions-legales'), url('/cgv'), url('/confidentialite').
    - Test 15 (`home embeds LocalBusiness JSON-LD with Plumber type`): body contains '<script type="application/ld+json">'; extract the JSON; assert ['@context'] === 'https://schema.org' AND ['@type'] === 'Plumber'.
    - Test 16 (`LocalBusiness JSON-LD has required fields`): from the extracted JSON: name === 'Dlo Azur Piscines', telephone === '+596696940054', address.addressRegion === 'Martinique', geo.latitude === 14.6037 (or float), priceRange === '€€'.
    - Test 17 (`LocalBusiness JSON-LD areaServed includes 4 cities + Martinique area`): from areaServed array, count entries with @type='City' === 4 (Fort-de-France, Le Lamentin, Schoelcher, Les Trois-Îlets) AND count with @type='AdministrativeArea' === 1.
    - Test 18 (`LocalBusiness JSON-LD has openingHoursSpecification for Mon-Fri + Sat`): the array has at least 2 entries with dayOfWeek covering Monday-Friday plus Saturday.
    - Test 19 (`every page has canonical + theme-color #0080ff`): for routes '/', '/services', '/realisations', '/contact', '/mentions-legales', '/cgv', '/confidentialite', body contains '<meta name="theme-color" content="#0080ff">' AND '<link rel="canonical"'.

    StaticPagesTest (covers SITE-02, SITE-03 + legal pages):
    - Test 20 (`services page renders 200 with own title`): GET /services returns 200; body contains '<title>Services' OR matching pattern from UI-SPEC.
    - Test 21 (`realisations page renders 200 with own title`): GET /realisations returns 200; body contains '<title>Réalisations' OR matching pattern.
    - Test 22 (`legal pages return 200`): GET /mentions-legales, /cgv, /confidentialite all return 200.
    - Test 23 (`contact page renders 200 with form shell ready for Plan 04`): GET /contact returns 200; body contains 'Nous contacter' H1; body contains '<livewire:' marker or the placeholder where Plan 04 will inject the form.
    - Test 24 (`robots.txt is reachable`): GET /robots.txt returns 200; body contains 'Sitemap:'.

    Total: 24 failing tests. Run all 3 files to confirm RED state.
  </behavior>
  <action>
    Create the 3 test files in `tests/Feature/`. Each uses Pest syntax. Use `$this->get($url)` and chain `->assertOk()`, `->assertSee()`, `->assertSeeText()`, `->assertSeeInOrder()`, `->assertHeader('Content-Type', 'application/xml')`. For JSON-LD parsing in SeoTest, extract the JSON via a regex: `preg_match('#<script type="application/ld\+json">(.+?)</script>#s', $response->getContent(), $m)` then `json_decode($m[1], true)` and run assertions on the array.

    For section-order assertion (Test 5), use `assertSeeInOrder(['id="services"', 'id="hospitality"', 'id="realisations"', 'id="pierre"', 'id="contact"'])` — this is robust to whitespace/attribute reordering as long as IDs appear in the documented order.

    Run `./vendor/bin/pest tests/Feature/HomePageTest.php tests/Feature/SeoTest.php tests/Feature/StaticPagesTest.php` — every test must fail (RED) because routes/vitrine.php is still the empty stub from Plan 01.

    Commit message: `test(01-03): RED — vitrine pages + SEO + static pages`.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/HomePageTest.php tests/Feature/SeoTest.php tests/Feature/StaticPagesTest.php 2>&amp;1 | grep -qE 'Tests:.+(failed|errors)'</automated>
  </verify>
  <done>24 failing tests written, covering SITE-01..03, SITE-06, SITE-07.</done>
  <acceptance_criteria>
    - File tests/Feature/HomePageTest.php exists with at least 12 distinct `it(...)` or `test(...)` blocks
    - File tests/Feature/SeoTest.php exists with at least 7 distinct test blocks
    - File tests/Feature/StaticPagesTest.php exists with at least 5 distinct test blocks
    - HomePageTest contains substrings: `assertSee('wa.me/596696940054')`, `assertSeeText('Votre piscine')`, `assertSeeText('Aller au contenu principal')`, `assertSeeInOrder`
    - SeoTest contains substrings: `application/ld\+json` (regex), `'@type' => 'Plumber'` or `=== 'Plumber'`, `'/sitemap.xml'`, `assertHeader('Content-Type', 'application/xml')`
    - Command `./vendor/bin/pest tests/Feature/HomePageTest.php tests/Feature/SeoTest.php tests/Feature/StaticPagesTest.php` exits non-zero (RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transpose mockup → Blade + wire SitemapController + LocalBusinessSchema builder + register vitrine routes (GREEN)</name>
  <read_first>
    - mockups/v1/vitrine.html (FULL file — every section, every icon, every copy line)
    - .planning/phases/01-vitrine-fondations/01-UI-SPEC.md (Component Inventory + Copywriting Contract + Accessibility Contract)
    - tests/Feature/HomePageTest.php (the contract from Task 1)
    - tests/Feature/SeoTest.php (the contract from Task 1)
    - tests/Feature/StaticPagesTest.php (the contract from Task 1)
    - resources/views/layouts/app.blade.php (Plan 01 — vitrine layout, what slot/sections it exposes)
    - resources/views/components/cta-whatsapp.blade.php (Plan 01 — reuse for every WhatsApp CTA)
    - resources/views/components/icon/*.blade.php (Plan 01 — reuse drop/whatsapp/arrow-right/check)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (§Pattern 5 LocalBusiness JSON-LD code; §Pattern 6 Sitemap dynamic code)
  </read_first>
  <action>
    1. **`routes/vitrine.php`** — Replace stub with 7 GET routes + 1 sitemap route:
       ```
       Route::get('/', [VitrineController::class, 'home'])->name('home');
       Route::get('/services', [VitrineController::class, 'services'])->name('services');
       Route::get('/realisations', [VitrineController::class, 'realisations'])->name('realisations');
       Route::get('/contact', [VitrineController::class, 'contact'])->name('contact');
       Route::get('/mentions-legales', [VitrineController::class, 'mentionsLegales'])->name('legal.mentions');
       Route::get('/cgv', [VitrineController::class, 'cgv'])->name('legal.cgv');
       Route::get('/confidentialite', [VitrineController::class, 'confidentialite'])->name('legal.confidentialite');
       Route::get('/sitemap.xml', [SitemapController::class, 'index'])->name('sitemap');
       ```
       **IMPORTANT — coordinate with Plan 01:** The Walking Skeleton home route in `routes/web.php` points to `HomeController::class`. Replace that line in `routes/web.php` so the root binding sources from VitrineController instead, OR (cleaner) delete `HomeController::class` route from `routes/web.php` and rely on `routes/vitrine.php` `Route::get('/')`. Either way, `tests/Feature/SkeletonSmokeTest.php` must continue to pass after this change because its assertions are subsets of `HomePageTest`'s assertions. Delete `app/Http/Controllers/HomeController.php` and `resources/views/skeleton-home.blade.php` once the new home route renders. Note: this technically touches `routes/web.php` (Plan 01 ownership) — but it's a single-line delete and is the cleanest seam between skeleton and vitrine. Document this in SUMMARY.

    2. **`app/Http/Controllers/VitrineController.php`** — One controller, 7 methods (home, services, realisations, contact, mentionsLegales, cgv, confidentialite). Each method returns `view('vitrine.{name}', [...$seoVars])` where `$seoVars` is an array with `title`, `description`, `ogImage`, `canonical`. The `home` method additionally builds the LocalBusiness JSON-LD via `app(LocalBusinessSchema::class)->toScript()` and passes it as `$jsonLd` to the view.

    3. **`app/Http/Controllers/SitemapController.php`** — `index()` method per RESEARCH Pattern 6:
       ```
       $sitemap = Sitemap::create()
           ->add(Url::create(route('home'))->setPriority(1.0)->setChangeFrequency('weekly'))
           ->add(Url::create(route('services'))->setPriority(0.8))
           ->add(Url::create(route('realisations'))->setPriority(0.8))
           ->add(Url::create(route('contact'))->setPriority(0.7))
           ->add(Url::create(route('legal.mentions'))->setPriority(0.3))
           ->add(Url::create(route('legal.cgv'))->setPriority(0.3))
           ->add(Url::create(route('legal.confidentialite'))->setPriority(0.3));
       // Plan 04 hook: append blog URLs if BlogRepository binding exists in the container
       if (app()->bound(\App\Support\BlogRepository::class)) {
           $sitemap->add(Url::create(route('blog.index'))->setPriority(0.6));
           foreach (app(\App\Support\BlogRepository::class)->all() as $post) {
               $sitemap->add(Url::create(route('blog.show', ['slug' => $post['slug']]))->setLastModificationDate($post['date'])->setPriority(0.5));
           }
       }
       return response($sitemap->render(), 200, ['Content-Type' => 'application/xml']);
       ```
       The `app()->bound()` check gracefully degrades if Plan 04 hasn't shipped yet (BlogRepository not registered). Plan 04 will register BlogRepository as a singleton in its own service provider; if absent in Phase 1 tests, the sitemap simply omits blog entries.

    4. **`app/Support/SchemaOrg/LocalBusinessSchema.php`** — Builder class with a single public method `toScript(): string`. Implementation per RESEARCH Pattern 5 — return the JSON-LD <script> tag string for the Plumber graph. Use config + helpers (`asset()`, `url()`, `config('app.url')`) rather than hardcoded URLs so staging/prod URLs differ correctly.

    5. **`resources/views/vitrine/home.blade.php`** — The full home transposition. Structure:
       ```
       <x-layouts.app :title="$title" :description="$description" :canonical="$canonical" :ogImage="$ogImage" :jsonLd="$jsonLd">
           @include('vitrine.partials.hero')
           @include('vitrine.partials.services-grid')
           @include('vitrine.partials.how-it-works')
           @include('vitrine.partials.hospitality')
           @include('vitrine.partials.realisations-grid')
           @include('vitrine.partials.pierre')
           @include('vitrine.partials.espace-client-teaser')
           @include('vitrine.partials.testimonials')
           @include('vitrine.partials.final-cta')
           {{-- floating WhatsApp FAB --}}
           <a href="https://wa.me/596696940054" aria-label="Nous écrire sur WhatsApp" class="fixed bottom-5 right-5 h-14 w-14 rounded-full bg-[#25D366] shadow-lg md:hidden flex items-center justify-center text-white">
               <x-icon.whatsapp size="22" />
           </a>
       </x-layouts.app>
       ```
       The layout (`<x-layouts.app>`) must accept a `:jsonLd` prop (Plan 01 created the layout with this slot in mind, but if missing, add `@if(isset($jsonLd)) {!! $jsonLd !!} @endif` in the head). Document the slot in the SUMMARY.

       Each partial file under `resources/views/vitrine/partials/` is the **exact 1:1 transposition of its mockup section** — copy the HTML from `mockups/v1/vitrine.html`, convert inline CDN Tailwind classes (which are already Tailwind 4-compatible since the design uses standard utilities + the custom OKLCH tokens defined in Plan 01's @theme), and:
       - Replace `<img src="../../assets/brand/photos/...">` with `<img src="{{ asset('assets/brand/photos/...') }}">`
       - Replace any `href="portail.html"` with `href="{{ route('home') }}#contact"` (portail is Phase 2)
       - Replace `href="passage.html"` (if any) with `href="#"` (Phase 2)
       - Replace inline `<svg>` icon blocks with `<x-icon.whatsapp />`, `<x-icon.drop />`, `<x-icon.check />`, `<x-icon.arrow-right />`, etc. — wherever a Plan 01 component matches. For SVGs not yet componentized (sun, star, sparkle, shield, calendar), create them in `resources/views/components/icon/` as part of this task (Task 2 additions to artifacts).
       - Apply French alt text per UI-SPEC §Photo Treatment (describe what's pictured, not "photo of pool").
       - Add `class="photo-grade"` to every content `<img>` (PRODUCT.md filter applied via Plan 01's @layer base CSS).

    6. **Other view files** (each `<x-layouts.app>` based, passing the SEO vars):
       - `resources/views/vitrine/services.blade.php` — extends services-grid partial into a full page; add the "Comment ça marche" partial again for repeat exposure; standard footer
       - `resources/views/vitrine/realisations.blade.php` — extends realisations-grid partial, add 6-8 more placeholder photo slots
       - `resources/views/vitrine/contact.blade.php` — Heading "Nous contacter" (UI-SPEC §Copywriting Contract → Contact form), `<div class="max-w-lg mx-auto py-16"><h1>Nous contacter</h1><livewire:contact-form /></div>` — the `<livewire:contact-form />` tag is a placeholder; Plan 04 registers the component; if executed before Plan 04, Livewire renders a "component not found" error → guard with `@if(class_exists(\App\Livewire\ContactForm::class)) <livewire:contact-form /> @else <p class="text-ink-500">Formulaire en cours de chargement…</p> @endif`. Plan 04 will replace the @if block with the direct livewire tag.
       - `resources/views/vitrine/mentions-legales.blade.php` — static legal text (SIRET placeholder, RCS placeholder, hébergeur Laravel Cloud info per RGPD requirement, DPO contact = contact@dloazurpiscines.com). Use placeholder TODO comments for Pierre to fill in actual SIRET/RCS values before cutover.
       - `resources/views/vitrine/cgv.blade.php` — static CGV placeholder
       - `resources/views/vitrine/confidentialite.blade.php` — static privacy notice (EU hosting Frankfurt + Paris, no third-party cookies in Phase 1, contact form data not persisted per D-13)

    7. **Run tests until green**: `./vendor/bin/pest tests/Feature/HomePageTest.php tests/Feature/SeoTest.php tests/Feature/StaticPagesTest.php --ci`. Iterate until all 24 tests pass.

    8. **Visual sanity check (manual but not gated)**: run `php artisan serve` and visit `/`, `/services`, `/realisations`, `/contact`, `/sitemap.xml` in a browser. Confirm OKLCH tokens render (Fredoka loaded, azure-500 buttons visible).

    Commit message: `feat(01-03): GREEN — vitrine 1:1 transposition + sitemap + LocalBusiness JSON-LD`.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/HomePageTest.php tests/Feature/SeoTest.php tests/Feature/StaticPagesTest.php tests/Feature/SkeletonSmokeTest.php --ci</automated>
  </verify>
  <done>All 24 vitrine tests pass + the original SkeletonSmokeTest still passes (no regression on the home route).</done>
  <acceptance_criteria>
    - All 7 vitrine view files exist under resources/views/vitrine/
    - All 9 partial files exist under resources/views/vitrine/partials/
    - File app/Http/Controllers/VitrineController.php has at least 7 public methods named home, services, realisations, contact, mentionsLegales, cgv, confidentialite
    - File app/Http/Controllers/SitemapController.php contains substrings `Sitemap::create()`, `Url::create`, `setPriority`, `app()->bound(\App\Support\BlogRepository::class)`
    - File app/Support/SchemaOrg/LocalBusinessSchema.php contains substrings `Schema::plumber()`, `latitude(14.6037)`, `longitude(-61.0594)`, `addressRegion('Martinique')`, `'Fort-de-France'`, `'Le Lamentin'`, `'Schoelcher'`, `'Les Trois-Îlets'`, `dayOfWeek`
    - File routes/vitrine.php contains all 8 Route::get definitions named home/services/realisations/contact/legal.mentions/legal.cgv/legal.confidentialite/sitemap
    - File resources/views/vitrine/home.blade.php contains substrings: `Demander un devis gratuit`, `claire toute l'année`, `Photos à chaque passage`, `wa.me/596696940054`, `id="services"`, `id="hospitality"`, `id="realisations"`, `id="pierre"`, `id="contact"`, `md:hidden`
    - File resources/views/vitrine/partials/hero.blade.php contains `min-h-[92vh]` AND `class="photo-grade"` AND `from-navy-950`
    - File resources/views/vitrine/contact.blade.php contains substring `Nous contacter` AND substring `livewire:contact-form` (with the @if class_exists guard)
    - Command `./vendor/bin/pest tests/Feature/HomePageTest.php tests/Feature/SeoTest.php tests/Feature/StaticPagesTest.php --ci` exits 0
    - Command `./vendor/bin/pest tests/Feature/SkeletonSmokeTest.php --ci` still exits 0 (no regression)
    - Command `curl -sI http://127.0.0.1:8000/sitemap.xml` (against `php artisan serve`) returns `Content-Type: application/xml`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 03 — Public-facing vitrine + SEO data)

| Boundary | Description |
|----------|-------------|
| Visitor (anonymous browser) → Laravel public routes | Untrusted; no input fields on vitrine pages (contact form is Plan 04) |
| Visitor → /sitemap.xml | Same; output is read-only XML, no user input crosses |
| Crawler bots (Googlebot, Bingbot) → robots.txt + sitemap.xml + JSON-LD | Trust verified by IP/UA cross-checks at Google's end; we expose only canonical URLs we own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering / XSS | Blade view rendering user-controllable strings | mitigate | All view variables use Blade's `{{ }}` auto-escape; raw `{!! $jsonLd !!}` is bounded to the LocalBusinessSchema builder output (server-controlled, constant) — never user input |
| T-3-02 | Information Disclosure | Stack traces or env values leaking via debug routes | mitigate | Plan 01 enforces `APP_DEBUG=false` in production; vitrine routes do not call dd() or var_dump() |
| T-3-03 | Tampering | Open-redirect via canonical or OG URL | mitigate | Canonical URLs use `url()` / `route()` helpers backed by APP_URL env var; never user-controlled |
| T-3-04 | Spoofing | Fake LocalBusiness data on a clone site | accept | JSON-LD is publicly visible by design; risk is reputational rather than security |
| T-3-05 | DoS | Sitemap generation on every request when blog grows | accept Phase 1 | Phase 1 emits ~7 static URLs + (if Plan 04 lands) ~5 blog URLs — under 1 KB XML, sub-ms render. Plan 06 may add HTTP cache headers (`Cache-Control: public, max-age=3600`) — deferred to Plan 06 |
| T-3-06 | Information Disclosure | Anti-references — accidentally using stock tropical imagery | mitigate | UI-SPEC §Photo Treatment requires real photos; Plan 03 ships with placeholder paths + clear comment that Pierre must replace before cutover; Plan 06 cutover validation includes a manual review of every photo asset |
| T-3-07 | Tampering | SEO poisoning via path traversal in sitemap | mitigate | SitemapController accepts no user input — all URLs come from internal `route()` calls |
| T-3-08 | Repudiation | Logging of public page hits | accept | Laravel Cloud access logs cover this; no per-page audit log needed |
| T-3-SC | Tampering | spatie/laravel-sitemap + spatie/schema-org install | mitigate | Both pre-cleared by Plan 01's front-loaded slopcheck; no new package installs in Plan 03 |
</threat_model>

<verification>
- All 24 Pest tests across HomePageTest/SeoTest/StaticPagesTest pass green
- Previous Pest tests from Plan 01 + Plan 02 still pass (zero regression)
- Visiting `/` in a browser renders the full vitrine (manual sanity — not gated)
- View-source on `/` contains exactly one `<script type="application/ld+json">` block with valid Plumber JSON
- `curl /sitemap.xml` returns 200 + `Content-Type: application/xml` + `<urlset>` + at least 7 `<loc>` entries
</verification>

<success_criteria>
1. SITE-01: Home page renders the hero + services + how-it-works + hospitality + realisations + pierre bio + espace teaser + testimonials + final CTA + footer + WhatsApp FAB.
2. SITE-02: `/services` page renders with own title + meta description.
3. SITE-03: `/realisations` page renders with own title + photo grid placeholders.
4. SITE-06: WhatsApp CTA `wa.me/596696940054` appears at minimum in topbar, hero, final CTA, footer, and mobile FAB (≥ 5 occurrences in source).
5. SITE-07 (partial — JSON-LD + sitemap + meta): home has LocalBusiness Plumber JSON-LD with all required fields; `/sitemap.xml` returns valid XML covering all vitrine routes; every page has unique title + description + canonical + theme-color #0080ff.
6. Mockup transposition is 1:1 — no redesign, no copy edits beyond UI-SPEC variants.
</success_criteria>

<output>
Create `.planning/phases/01-vitrine-fondations/01-03-SUMMARY.md` documenting:
- Final list of vitrine routes + their names
- Confirmation that the Walking Skeleton home stub (HomeController + skeleton-home.blade.php) has been replaced by VitrineController::home + vitrine/home.blade.php
- Photo placeholder paths still needing real assets from Pierre
- The exact LocalBusiness JSON-LD output as logged by `php artisan tinker` → `app(LocalBusinessSchema::class)->toScript()`
- Confirmation that the contact view uses the @if(class_exists ContactForm) guard so it does not crash before Plan 04
</output>
