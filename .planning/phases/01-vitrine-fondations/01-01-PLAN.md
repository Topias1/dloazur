---
phase: 01-vitrine-fondations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - package.json
  - package-lock.json
  - vite.config.js
  - bootstrap/app.php
  - bootstrap/providers.php
  - artisan
  - .env.example
  - .env.testing
  - .gitignore
  - phpunit.xml
  - tests/Pest.php
  - tests/TestCase.php
  - tests/Feature/SkeletonSmokeTest.php
  - tests/Feature/HealthCheckTest.php
  - app/Providers/AppServiceProvider.php
  - app/Http/Controllers/HomeController.php
  - app/Http/Controllers/HealthController.php
  - config/app.php
  - config/filesystems.php
  - config/mail.php
  - config/services.php
  - resources/css/app.css
  - resources/js/app.js
  - resources/views/layouts/app.blade.php
  - resources/views/layouts/admin.blade.php
  - resources/views/layouts/auth.blade.php
  - resources/views/layouts/blog.blade.php
  - resources/views/components/button.blade.php
  - resources/views/components/icon/whatsapp.blade.php
  - resources/views/components/icon/drop.blade.php
  - resources/views/components/icon/arrow-right.blade.php
  - resources/views/components/icon/check.blade.php
  - resources/views/components/cta-whatsapp.blade.php
  - resources/views/components/seo/meta.blade.php
  - resources/views/skeleton-home.blade.php
  - routes/web.php
  - routes/vitrine.php
  - routes/blog.php
  - routes/admin.php
  - public/robots.txt
  - public/assets/brand/.gitkeep
  - .github/workflows/tests.yml
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/phases/01-vitrine-fondations/CLOUD-PROVISIONING.md
autonomous: false
requirements:
  - SITE-01
  - AUTH-01
user_setup:
  - service: laravel-cloud
    why: "EU Central (Frankfurt) project + Postgres + auto-deploy on push main"
    env_vars:
      - name: APP_KEY
        source: "Generated locally via php artisan key:generate, copied to Laravel Cloud secrets UI"
      - name: DB_CONNECTION
        source: "Laravel Cloud auto-injects when Postgres resource attached"
      - name: APP_URL
        source: "Laravel Cloud project URL (e.g., dloazur-staging.laravel.cloud) or preprod.dloazurpiscines.com"
    dashboard_config:
      - task: "Create Laravel Cloud project, region EU Central (Frankfurt)"
        location: "https://cloud.laravel.com → New Project"
      - task: "Attach managed Postgres resource"
        location: "Laravel Cloud project → Resources → Add Postgres"
      - task: "Connect GitHub repo, branch main = production environment, auto-deploy enabled"
        location: "Laravel Cloud project → Environments → Connect GitHub"
      - task: "Set environment secrets (APP_KEY, OPERATOR_INITIAL_PASSWORD placeholder for Plan 05)"
        location: "Laravel Cloud project → Environment → Secrets"
  - service: scaleway
    why: "Object Storage Paris (fr-par) for photo uploads (Phase 2 usage, wired in Phase 1)"
    env_vars:
      - name: SCALEWAY_ACCESS_KEY_ID
        source: "Scaleway console → IAM → API keys"
      - name: SCALEWAY_SECRET_ACCESS_KEY
        source: "Scaleway console → IAM → API keys (shown once)"
      - name: SCALEWAY_BUCKET
        source: "Scaleway console → Object Storage → Bucket name (dlo-azur-piscines-staging)"
      - name: SCALEWAY_REGION
        source: "Static: fr-par"
      - name: SCALEWAY_ENDPOINT
        source: "Static: https://s3.fr-par.scw.cloud"
    dashboard_config:
      - task: "Create Scaleway account if not exists"
        location: "https://console.scaleway.com"
      - task: "Create bucket dlo-azur-piscines-staging in fr-par region, visibility=private"
        location: "Scaleway console → Object Storage → Create bucket"

must_haves:
  truths:
    - "Repository preserves all .planning/, mockups/, PRODUCT.md, DESIGN.md, CLAUDE.md, docs/, assets/ files exactly as they were before bootstrap"
    - "composer install completes with 0 errors and produces a working vendor/ directory"
    - "php artisan migrate runs cleanly against a local Postgres and reports the users + cache + jobs tables created (business tables come from Plan 02)"
    - "GET / returns 200 with response body containing '<title>Dlo Azur Piscines · Entretien de piscines en Martinique</title>' and string 'wa.me/596696940054'"
    - "GET /up returns 200 with JSON body containing 'db: ok' (proves DB connection alive)"
    - "GitHub Actions workflow tests.yml runs Pest --ci against Postgres 16 service and exits 0"
    - "Laravel Cloud staging deployment is reachable over HTTPS and returns the home page styled with Fredoka + Inter and azure-500 brand color"
    - "CLAUDE.md and PROJECT.md reflect Laravel 13.x (not 11.x) and document Tailwind v4 CSS-first (@theme in app.css)"
  artifacts:
    - path: composer.json
      provides: "Laravel 13 framework + Pest 4 + DomPDF baseline (Phase 3 dependency installed early per RESEARCH.md) + Mailgun mailer transport + all Phase 1 Spatie packages installed upfront to avoid lock conflicts in Wave 2 parallel plans"
      contains: "laravel/framework, pestphp/pest, pestphp/pest-plugin-laravel, laravel/fortify, spatie/laravel-markdown, spatie/laravel-sitemap, spatie/schema-org, spatie/laravel-honeypot, danharrin/livewire-rate-limiting, dompdf/dompdf, livewire/livewire, symfony/mailgun-mailer, symfony/http-client, spatie/yaml-front-matter"
    - path: package.json
      provides: "Tailwind v4 + Vite + Laravel Vite plugin + typography plugin"
      contains: "tailwindcss, @tailwindcss/vite, @tailwindcss/typography, vite, laravel-vite-plugin, alpinejs"
    - path: resources/css/app.css
      provides: "Tailwind v4 @theme block transposing all OKLCH tokens from mockups/v1/theme.js + custom utilities from mockups/v1/app.css"
      contains: "@import \"tailwindcss\";, @theme {, --color-azure-500: oklch(0.615 0.211 256), --color-navy-900, --color-sand-50, --color-lagon-400, --font-display: \"Fredoka\", --font-sans: \"Inter\", --spacing-13, --spacing-15, --spacing-18, --breakpoint-xs"
    - path: resources/views/layouts/app.blade.php
      provides: "Vitrine layout shell — Fredoka+Inter, sand-50 body, fixed nav placeholder, footer with WhatsApp QR + legal links, meta+OG slots"
      contains: "@vite, x-seo.meta, lang=\"fr\""
    - path: resources/views/layouts/admin.blade.php
      provides: "Back-office shell — sidebar navy + topbar + slot main, used by Plan 05"
      contains: "lg:grid lg:grid-cols-[16rem_1fr]"
    - path: resources/views/layouts/auth.blade.php
      provides: "Auth split layout — brand panel + form panel, used by Plan 05 Fortify views"
      contains: "lg:grid lg:grid-cols-2, bg-navy-900"
    - path: resources/views/components/cta-whatsapp.blade.php
      provides: "Reusable WhatsApp CTA component with brand color #25D366 — used everywhere"
      contains: "bg-[#25D366], wa.me/596696940054"
    - path: routes/web.php
      provides: "Root route + dynamic require of routes/vitrine.php, routes/blog.php, routes/admin.php via bootstrap/app.php"
      contains: "HealthController, HomeController"
    - path: routes/vitrine.php
      provides: "Empty stub registered — Plan 03 fills it with /services, /realisations, /contact, /mentions-legales, /cgv, /confidentialite"
      contains: "use Illuminate\\Support\\Facades\\Route"
    - path: routes/blog.php
      provides: "Empty stub registered — Plan 04 fills it with /blog and /blog/{slug}"
      contains: "use Illuminate\\Support\\Facades\\Route"
    - path: routes/admin.php
      provides: "Empty stub registered — Plan 05 fills it with /admin (auth middleware)"
      contains: "use Illuminate\\Support\\Facades\\Route"
    - path: bootstrap/app.php
      provides: "Laravel 13 application bootstrap with route partition registration"
      contains: "withRouting, vitrine.php, blog.php, admin.php, withExceptions"
    - path: app/Providers/AppServiceProvider.php
      provides: "Boot-time configuration: URL::forceHttps in prod, Vite prefetch concurrency, locale fr"
      contains: "URL::forceHttps, App::environment('production')"
    - path: config/app.php
      provides: "Timezone America/Martinique (UTC-4 no DST) + locale fr + faker_locale fr_FR"
      contains: "'timezone' => 'America/Martinique', 'locale' => 'fr'"
    - path: config/filesystems.php
      provides: "Scaleway S3 disk wired (no uploads Phase 1 but ready for Phase 2)"
      contains: "'scaleway', 's3', 'endpoint', 'use_path_style_endpoint' => true, 'fr-par'"
    - path: config/mail.php
      provides: "Mailgun EU transport configured (used by Plan 04 contact form)"
      contains: "'mailgun' => [, 'endpoint' => env('MAILGUN_ENDPOINT', 'api.eu.mailgun.net')"
    - path: tests/Pest.php
      provides: "Pest 4 bootstrap with Feature/Unit tagging + RefreshDatabase in Feature suite"
      contains: "uses(Tests\\TestCase::class, RefreshDatabase::class)->in('Feature')"
    - path: tests/Feature/SkeletonSmokeTest.php
      provides: "Home renders, WhatsApp CTA present, Fredoka link present in head"
      contains: "->get('/')->assertOk(), assertSeeText('Dlo Azur Piscines'), assertSee('wa.me/596696940054'), Fredoka"
    - path: tests/Feature/HealthCheckTest.php
      provides: "GET /up returns 200 with db:ok JSON, proves DB connection"
      contains: "->getJson('/up')->assertOk()->assertJson(['db' => 'ok'])"
    - path: .github/workflows/tests.yml
      provides: "CI pipeline: PHP 8.3 + Node 22 + Postgres 16 service + composer install + npm ci + npm run build + pest --ci"
      contains: "php-version: '8.3', node-version: '22', postgres:16, pest --ci"
    - path: CLAUDE.md
      provides: "Updated stack table — Laravel 13 (override), Tailwind 4 CSS-first @theme path"
      contains: "Laravel | 13.x, Tailwind CSS | 4.x"
    - path: .planning/PROJECT.md
      provides: "Updated Constraints + Key Decisions to reflect Laravel 13 override"
      contains: "Laravel 13"
    - path: .planning/phases/01-vitrine-fondations/CLOUD-PROVISIONING.md
      provides: "Step-by-step handover doc for the human-action items: Laravel Cloud account, Scaleway bucket, Mailgun DNS"
      contains: "Laravel Cloud, Scaleway, Mailgun"
  key_links:
    - from: "resources/views/skeleton-home.blade.php"
      to: "resources/css/app.css design tokens"
      via: "@vite + Tailwind v4 @theme compilation"
      pattern: "bg-sand-50|text-ink-700|font-display"
    - from: "routes/web.php"
      to: "Plan 03/04/05 route files"
      via: "bootstrap/app.php ->withRouting registering vitrine.php + blog.php + admin.php"
      pattern: "vitrine.php|blog.php|admin.php"
    - from: "config/mail.php"
      to: "Plan 04 ContactForm Livewire"
      via: "MAIL_MAILER=mailgun env var, used by Mail::to()"
      pattern: "MAILGUN_ENDPOINT|api.eu.mailgun.net"
    - from: "config/filesystems.php"
      to: "Phase 2 spatie/medialibrary uploads"
      via: "disk 'scaleway' definition (S3 driver + custom endpoint)"
      pattern: "scaleway|s3.fr-par.scw.cloud"
---

<objective>
**Walking Skeleton + scaffold.** Bootstrap Laravel 13 in a planning-bearing repo (`/tmp` create-project + rsync workaround), wire Tailwind v4 CSS-first tokens transposed from `mockups/v1/theme.js`, install Pest 4, install ALL Phase 1 Composer + npm packages upfront (so Wave 2 plans never touch `composer.json`/`package.json` and avoid merge conflicts), build the foundational Blade layouts (vitrine + admin + auth + blog) using OKLCH tokens, register route partition files (`web.php`, `vitrine.php`, `blog.php`, `admin.php`), serve a styled placeholder home at `/`, wire health-check route `/up` that pings the DB, configure GitHub Actions CI, provision Laravel Cloud staging + Scaleway bucket + Mailgun DNS, and deploy. End state = the smallest end-to-end vertical slice: visitor → DNS → Laravel Cloud edge → routing → controller → Blade view + Vite build → DB ping → 200 OK.

Purpose: prove the full stack is alive before any feature work. Every subsequent plan extends one layer without touching the skeleton's architectural decisions.

Output: Working Laravel 13 app with home page styled per design system, CI green, Laravel Cloud staging URL reachable, `SKELETON.md` documenting the contract. CLAUDE.md and PROJECT.md updated to reflect the Laravel 13 stack override.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vitrine-fondations/01-CONTEXT.md
@.planning/phases/01-vitrine-fondations/01-RESEARCH.md
@.planning/phases/01-vitrine-fondations/01-UI-SPEC.md
@.planning/phases/01-vitrine-fondations/01-VALIDATION.md
@.planning/phases/01-vitrine-fondations/SKELETON.md
@CLAUDE.md
@PRODUCT.md
@DESIGN.md
@mockups/v1/vitrine.html
@mockups/v1/theme.js
@mockups/v1/app.css

<interfaces>
<!-- Tailwind v4 CSS-first @theme contract — extracted from mockups/v1/theme.js + app.css -->
<!-- The executor MUST transpose every OKLCH token from mockups/v1/theme.js into the @theme block of resources/css/app.css. -->
<!-- Reference signatures only — do not invent values. -->

From mockups/v1/theme.js (Tailwind v3 JS config, target v4 CSS @theme):
- spacing custom: 13:'3.25rem', 15:'3.75rem', 18:'4.5rem'  →  --spacing-13, --spacing-15, --spacing-18
- screens custom: xs:'400px'  →  --breakpoint-xs
- colors: azure (50..950), navy (50..950), lagon (300..700), sun (300..500), sand (50..300), ink (400..950), success/warn/danger/whatsapp
- fontFamily: display:Fredoka, sans:Inter  →  --font-display, --font-sans
- boxShadow: xs/sm/md/lg marine-tinted  →  --shadow-xs, --shadow-sm, --shadow-md, --shadow-lg
- borderRadius overrides: xl:0.875rem, 2xl:1.25rem, 3xl:1.75rem  →  --radius-xl, --radius-2xl, --radius-3xl
- maxWidth: content:75rem  →  --container-content
- transitionTimingFunction: out-quint:'cubic-bezier(0.22, 1, 0.36, 1)'  →  --ease-out-quint

From mockups/v1/app.css (custom utilities to preserve in @layer base / @layer components of resources/css/app.css):
- :root CSS custom properties block (already mostly redundant with @theme, but keep --warn-bg which is not in theme.js)
- body { background: var(--sand-50); color: var(--ink-700); font-family: 'Inter' }
- :focus-visible { outline: 2px solid var(--azure-500); offset 2px; radius 6px }
- ::selection { background: var(--lagon-300); color: var(--navy-900) }
- @keyframes rise + .rise utility
- .photo-grade { filter: saturate(1.05) contrast(1.02) }
- .photo-duotone { filter: saturate(0.9) brightness(0.96) }
- .ripple { radial-gradient + repeating-linear-gradient — exact gradient strings preserved }
- @media (prefers-reduced-motion: reduce) clamp animation-duration to 0.001ms
- Skip the .phone / .phone-scroll / .notch / .lift-up rules — Phase 2 only (mobile passage capture)
- Skip Fredoka/Inter weight 500 — UI-SPEC §Typography locks to Fredoka 600/700 + Inter 400/600 only

From mockups/v1/vitrine.html (foundational <head> + skip link + top bar + footer markup to extract into layouts/app.blade.php):
- <html lang="fr"> attribute mandatory
- Skip link: <a href="#top" class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:rounded-xl focus:bg-azure-500 focus:text-white focus:font-bold">Aller au contenu principal</a>
- Topbar: fixed top-0 inset-x-0 z-40, max-w-content, h-15, sand-50/85 backdrop-blur-md ring-1 ring-navy-900/10 shadow-sm
- Logo SVG (drop motif): width 30, height 36, viewBox 0 0 28 34, fill currentColor, secondary stroke oklch(0.987 0.005 85)
- Footer: bg-navy-950 text-navy-100 with 4-col grid md:grid-cols-[1.4fr_1fr_1fr_auto]

WhatsApp brand contract (UI-SPEC + RESEARCH):
- Color: #25D366 (intouchable, never replace with token)
- Phone: 0696 94 00 54 → URL: https://wa.me/596696940054
- Icon: inline SVG (extracted to resources/views/components/icon/whatsapp.blade.php)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Stack override decision — confirm Laravel 13 + Tailwind v4 CSS-first</name>
  <read_first>
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (Pitfall 1 + Pitfall 2)
    - .planning/phases/01-vitrine-fondations/01-CONTEXT.md (D-01, D-05)
    - .planning/phases/01-vitrine-fondations/SKELETON.md (Architectural Decisions)
    - CLAUDE.md (current stack table — needs update)
  </read_first>
  <decision>Confirm two stack overrides before scaffold: (a) Laravel 13.x instead of CONTEXT.md D-01 "Laravel 11"; (b) Tailwind v4 CSS-first @theme directive instead of CONTEXT.md D-05 "tailwind.config.js" wording.</decision>
  <context>
    Laravel 11 reached end of security support on 2026-03-12 — today is 2026-05-28. RESEARCH.md Pitfall #1 documents this. Laravel 13 security supported until 2028-03-17. All locked Phase 1 packages (Fortify, Livewire 3, Pest 4, Spatie suite) support Laravel 11/12/13 indifferently per Packagist verification.

    Tailwind v4 (released Jan 2025) deprecates `tailwind.config.js` for new projects in favor of CSS-first `@theme` directive in `resources/css/app.css`. RESEARCH.md Pitfall #2 documents this. Result is functionally identical — only the file target changes.

    Both overrides are pre-decided in the planning_context stack_override_critical block (from the orchestrator) and in SKELETON.md. This checkpoint exists to record the override decision in the implementation log so future agents understand why CLAUDE.md's older "Laravel 11" reference was changed.
  </context>
  <options>
    <option id="confirm-overrides">
      <name>Confirm both overrides (recommended — matches RESEARCH.md + SKELETON.md)</name>
      <pros>Avoids shipping a project on a version with no security support; uses the current standard Tailwind v4 idiom.</pros>
      <cons>Diverges from CONTEXT.md D-01/D-05 literal wording — but both decisions were made before Laravel 11 EOL date passed and before Tailwind v4 release.</cons>
    </option>
    <option id="laravel-12">
      <name>Use Laravel 12 instead of 13 (still supported until 2027-02)</name>
      <pros>More conservative — Laravel 12 has more community documentation than freshly-released 13.</pros>
      <cons>Earlier EOL (Feb 2027); 13 is the current default for new projects per laravel.com/docs/13.x.</cons>
    </option>
    <option id="abort">
      <name>Stop and re-discuss with operator</name>
      <pros>Forces explicit user re-confirmation if the override feels too large.</pros>
      <cons>Blocks all Phase 1 work; the override is already documented in SKELETON.md as the contract.</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — decision checkpoint, no automated gate. Resume signal is the operator response.</automated>
  </verify>
  <done>Operator selects confirm-overrides, laravel-12, or abort. Selection is logged in the SUMMARY of this plan.</done>
  <acceptance_criteria>
    - Operator response captured in the conversation thread
    - The chosen Laravel major version is recorded in this plan's SUMMARY
    - If `abort` selected, Plan 01 stops and the orchestrator is notified for replan
  </acceptance_criteria>
  <resume-signal>Reply "confirm-overrides" (default, recommended), "laravel-12", or "abort"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bootstrap Laravel scaffold via /tmp workaround + install ALL Phase 1 packages upfront</name>
  <read_first>
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (Pattern 7 — Bootstrap on non-empty repo; Pitfall 6 + Pitfall 7)
    - .planning/phases/01-vitrine-fondations/SKELETON.md (Stack Touched section)
    - .gitignore (current minimal one — must be merged with Laravel's)
    - index.html (root, to be deleted — Pitfall 7)
  </read_first>
  <action>
    Per D-01 (overridden to Laravel 13 by Task 1) and RESEARCH §Pattern 7:

    1. Run `cd /tmp && rm -rf dloazur-skeleton && composer create-project laravel/laravel:^13.0 dloazur-skeleton --prefer-dist --no-interaction`.
    2. Rsync the skeleton into the repo without touching planning artefacts: `rsync -av --ignore-existing /tmp/dloazur-skeleton/ /Users/amnesia/dev/dloazur/`. `--ignore-existing` preserves `.planning/`, `mockups/`, `PRODUCT.md`, `DESIGN.md`, `CLAUDE.md`, `docs/`, `assets/`, `.impeccable/`, `.agents/`, `.claude/`, `.nojekyll`, `tools/`, `skills-lock.json`, and existing `.gitignore`.
    3. Delete `/Users/amnesia/dev/dloazur/index.html` (Pitfall 7 — conflicts with Laravel `public/index.html` and will confuse routing). Mockups remain reachable via `python3 -m http.server` + `/mockups/v1/`.
    4. Merge the Laravel skeleton `.gitignore` lines into the existing `/Users/amnesia/dev/dloazur/.gitignore` (vendor/, node_modules/, /public/build, .env, .env.backup, .phpunit.result.cache, Homestead.json, Homestead.yaml, auth.json, .phpactor.json) — do not overwrite the existing entries; append the missing Laravel ones.
    5. Configure local environment: `cp .env.example .env`, `php artisan key:generate`.
    6. Install all Phase 1 Composer packages in ONE require call so the resulting `composer.json` and `composer.lock` are committed once and Wave 2 plans never touch them: `composer require --no-interaction laravel/fortify:^1.37 livewire/livewire:^3.0 spatie/laravel-markdown:^2.8 spatie/laravel-sitemap:^8.1 spatie/schema-org:^4.0 spatie/laravel-honeypot:^4.7 spatie/yaml-front-matter:^2.0 danharrin/livewire-rate-limiting:^2.2 dompdf/dompdf:^3.1 symfony/mailgun-mailer:^7.0 symfony/http-client:^7.0`.
    7. Install Pest 4 dev dependencies in one call: `composer require --dev --no-interaction pestphp/pest:^4.7 pestphp/pest-plugin-laravel:^4.1`. Then `php artisan pest:install` (this generates `tests/Pest.php`, `tests/TestCase.php`, `phpunit.xml`).
    8. Install npm packages in one go: `npm install -D tailwindcss@^4 @tailwindcss/vite@^4 @tailwindcss/typography@^0.5 vite laravel-vite-plugin alpinejs`. Default Laravel skeleton already has `vite` + `laravel-vite-plugin`, this upgrades / pins. `alpinejs` is a runtime dependency, also kept under `-D` per Laravel default.
    9. Verify each package landed in `composer.json` `require` / `require-dev` and `package.json` `devDependencies` — do not commit unless `composer install` and `npm install` complete with zero errors.

    Do NOT yet write any Blade views or modify routes — Task 3 handles structure scaffolding. This task ends with a vanilla Laravel 13 skeleton + all Phase 1 deps installed + planning artefacts intact.

    Per D-30 the deploy pipeline runs `composer install --no-dev` and `npm ci` — verify locally that `composer install` and `npm ci` reproduce identical lock files (commit both `composer.lock` and `package-lock.json`).
  </action>
  <verify>
    <automated>test -d vendor &amp;&amp; test -d node_modules &amp;&amp; test -f composer.lock &amp;&amp; test -f package-lock.json &amp;&amp; grep -q '"laravel/framework"' composer.json &amp;&amp; grep -q '"laravel/fortify"' composer.json &amp;&amp; grep -q '"livewire/livewire"' composer.json &amp;&amp; grep -q '"spatie/laravel-markdown"' composer.json &amp;&amp; grep -q '"spatie/laravel-sitemap"' composer.json &amp;&amp; grep -q '"spatie/schema-org"' composer.json &amp;&amp; grep -q '"spatie/laravel-honeypot"' composer.json &amp;&amp; grep -q '"danharrin/livewire-rate-limiting"' composer.json &amp;&amp; grep -q '"dompdf/dompdf"' composer.json &amp;&amp; grep -q '"symfony/mailgun-mailer"' composer.json &amp;&amp; grep -q '"pestphp/pest"' composer.json &amp;&amp; grep -q '"tailwindcss"' package.json &amp;&amp; grep -q '"@tailwindcss/vite"' package.json &amp;&amp; test -f tests/Pest.php &amp;&amp; test -d .planning &amp;&amp; test -f PRODUCT.md &amp;&amp; test -f DESIGN.md &amp;&amp; test -f mockups/v1/vitrine.html &amp;&amp; ! test -f index.html</automated>
  </verify>
  <done>Laravel 13 skeleton present; all Phase 1 Composer + npm packages installed in single locks; planning artefacts untouched; root-level index.html deleted; .gitignore merged.</done>
  <acceptance_criteria>
    - File composer.json contains the strings: laravel/framework, laravel/fortify, livewire/livewire, spatie/laravel-markdown, spatie/laravel-sitemap, spatie/schema-org, spatie/laravel-honeypot, danharrin/livewire-rate-limiting, dompdf/dompdf, symfony/mailgun-mailer, pestphp/pest, pestphp/pest-plugin-laravel, spatie/yaml-front-matter
    - File package.json contains the strings: tailwindcss, @tailwindcss/vite, @tailwindcss/typography, alpinejs, vite, laravel-vite-plugin
    - File tests/Pest.php exists
    - Directory .planning/ still exists with subdirectories phases/, research/
    - Files PRODUCT.md, DESIGN.md, CLAUDE.md, mockups/v1/vitrine.html, mockups/v1/theme.js, mockups/v1/app.css still exist
    - File index.html in repo root does NOT exist (deleted per Pitfall 7)
    - File .gitignore contains both old line "skills-lock.json" (if present before) AND Laravel-required entries: /vendor, /node_modules, /public/build, .env
    - Command `php artisan --version` returns a string starting with "Laravel Framework 13."
    - Command `composer install --dry-run` reports 0 missing packages
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Tailwind v4 @theme tokens + base layouts + Blade components + route partition + health check + smoke test</name>
  <read_first>
    - mockups/v1/theme.js (full file — every color value, every spacing token, every shadow, every radius override MUST be transposed)
    - mockups/v1/app.css (custom :root vars, .ripple, .photo-grade, .rise, @keyframes, prefers-reduced-motion block)
    - mockups/v1/vitrine.html (extract only the topbar markup, skip link, footer markup, logo SVG — full page is Plan 03)
    - mockups/v1/auth.html lines 14-31 (brand panel layout reused in resources/views/layouts/auth.blade.php)
    - mockups/v1/dashboard.html lines 19-37 (sidebar + topbar shell reused in resources/views/layouts/admin.blade.php)
    - .planning/phases/01-vitrine-fondations/01-UI-SPEC.md (Spacing Scale, Typography, Color sections — confirm 4 type levels, 2 weights per font, OKLCH only)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (Pattern 1 — Tailwind v4 CSS-first example)
    - bootstrap/app.php (default Laravel 13 file — need to extend ->withRouting)
    - routes/web.php (default Laravel 13 placeholder route — replace)
  </read_first>
  <behavior>
    - Test 1 (HealthCheckTest): GET /up returns 200 + JSON { "app": "ok", "db": "ok" }; proves DB connection alive (uses DB::connection()->getPdo()).
    - Test 2 (HealthCheckTest): GET /up with a broken DB connection (via testing trick: rename db connection in config) returns 503 + JSON { "db": "fail" } — gated behind a separate test that swaps the connection config; keep it lightweight.
    - Test 3 (SkeletonSmokeTest): GET / returns 200 with Content-Type text/html and body contains the exact string "<title>Dlo Azur Piscines · Entretien de piscines en Martinique</title>".
    - Test 4 (SkeletonSmokeTest): GET / body contains string "wa.me/596696940054".
    - Test 5 (SkeletonSmokeTest): GET / body contains a stylesheet link generated by @vite, specifically a string matching the pattern resources/css/app.css.
    - Test 6 (SkeletonSmokeTest): GET / body contains lang="fr" attribute on the <html> element.
    - Test 7 (SkeletonSmokeTest): Skip link is the first child of body — assertSeeInOrder(['Aller au contenu principal', '<main', '</main>']).
  </behavior>
  <action>
    Write the failing tests in tests/Feature/SkeletonSmokeTest.php and tests/Feature/HealthCheckTest.php FIRST, then implement until they pass. Implementation steps:

    1. **resources/css/app.css** — Replace the default Tailwind v3 starter content with Tailwind v4 CSS-first config per RESEARCH §Pattern 1. Use the exact OKLCH values from `mockups/v1/theme.js` for every token; do not paraphrase or "round" values.
       - Top: `@import "tailwindcss";` then a Google Fonts import for Fredoka weights 600/700 + Inter weights 400/600 only (UI-SPEC §Typography locks to 2 weights/family).
       - `@source` directives for `../views/**/*.blade.php`, `../**/*.js`, vendor pagination views, storage framework views.
       - `@theme {}` block with every token from theme.js mapped to a `--*` custom property (colors, fonts, spacing 13/15/18, breakpoint xs, shadows xs/sm/md/lg, radius xl/2xl/3xl, container content 75rem, ease out-quint).
       - `@layer base {}` block with body bg/color, :focus-visible outline, ::selection, .photo-grade, .photo-duotone, prefers-reduced-motion clamp.
       - `@layer components {}` block with .ripple gradient (copy exact strings from app.css).
       - `@keyframes rise` and `.rise` animation utility outside any @layer (Tailwind v4 supports raw @keyframes).
       Do NOT include the .phone / .notch / .lift-up rules (Phase 2 only). Do NOT load Inter weight 700 or Fredoka weight 400/500.

    2. **resources/js/app.js** — Import Alpine: `import Alpine from 'alpinejs'; window.Alpine = Alpine; Alpine.start();`. Livewire 3 auto-injects its JS when used.

    3. **vite.config.js** — Add `@tailwindcss/vite` plugin alongside `laravel-vite-plugin`. Default Laravel 13 vite.config.js needs the new plugin import: `import tailwindcss from '@tailwindcss/vite';` and added to plugins array.

    4. **routes/vitrine.php**, **routes/blog.php**, **routes/admin.php** — Each file ships with only `<?php\n\nuse Illuminate\Support\Facades\Route;\n\n// Filled by Plan {0X}` comment. Empty Route::group is fine. These exist so Wave 2 plans can each modify a distinct file.

    5. **bootstrap/app.php** — Extend `->withRouting()` to register the partitions:
       ```
       ->withRouting(
           web: __DIR__.'/../routes/web.php',
           commands: __DIR__.'/../routes/console.php',
           health: '/up',  // Laravel 13 built-in health endpoint
           then: function () {
               Route::middleware('web')->group(base_path('routes/vitrine.php'));
               Route::middleware('web')->group(base_path('routes/blog.php'));
               Route::middleware(['web', 'auth'])->prefix('admin')->name('admin.')->group(base_path('routes/admin.php'));
           },
       )
       ```
       Add `->withExceptions(function ($exceptions) {})` placeholder.

    6. **routes/web.php** — Replace default content with: root home route binding `Route::get('/', [HomeController::class, 'index'])->name('home')` and a `Route::get('/up', [HealthController::class, 'ping'])` (override Laravel's default /up to return JSON with db ping). Plan 06 may add Zyro redirect map here.

    7. **app/Http/Controllers/HomeController.php** — Single `index()` method returning `view('skeleton-home')`. Stays under 30 lines. Plan 03 will replace this with the full vitrine controller — for now it's the skeleton placeholder.

    8. **app/Http/Controllers/HealthController.php** — Single `ping()` method:
       ```
       $db = 'ok';
       try { DB::connection()->getPdo(); } catch (\Throwable $e) { $db = 'fail'; }
       return response()->json(['app' => 'ok', 'db' => $db], $db === 'ok' ? 200 : 503);
       ```

    9. **app/Providers/AppServiceProvider.php** — `boot()` adds `URL::forceHttps()` guarded by `if ($this->app->environment('production'))`. Add `Vite::prefetch(concurrency: 3)` per Laravel 13 default convention.

    10. **config/app.php** — Set `'timezone' => 'America/Martinique'` and `'locale' => 'fr'`, `'faker_locale' => 'fr_FR'`. (Per RESEARCH Pitfall #9 + UI-SPEC `<html lang="fr">` requirement.)

    11. **config/filesystems.php** — Add a `'scaleway'` disk under `disks`:
        ```
        'scaleway' => [
            'driver' => 's3',
            'key' => env('SCALEWAY_ACCESS_KEY_ID'),
            'secret' => env('SCALEWAY_SECRET_ACCESS_KEY'),
            'region' => env('SCALEWAY_REGION', 'fr-par'),
            'bucket' => env('SCALEWAY_BUCKET'),
            'endpoint' => env('SCALEWAY_ENDPOINT', 'https://s3.fr-par.scw.cloud'),
            'use_path_style_endpoint' => true,
            'visibility' => 'private',
            'throw' => false,
        ],
        ```
        Phase 1 wires only — no upload runs until Phase 2.

    12. **config/mail.php** + **config/services.php** — Add Mailgun mailer entry per RESEARCH §Mail Driver Choice. Critical: `MAILGUN_ENDPOINT=api.eu.mailgun.net` (EU region). Set `MAIL_MAILER=log` by default in `.env.example` so local dev never sends real mail; production sets `mailgun` via Laravel Cloud secrets.

    13. **resources/views/layouts/app.blade.php** — Public vitrine layout: `<html lang="fr">`, `<head>` with `<x-seo.meta />` slot include, `@vite(['resources/css/app.css','resources/js/app.js'])`, `<meta name="theme-color" content="#0080ff">`, favicon link. `<body class="bg-sand-50 text-ink-700 antialiased">`. Skip link first. `{{ $slot }}` or `@yield('content')` (use slot for component-style). Footer is `<x-footer />` defined inline for now (the layout itself includes the footer markup transposed from mockups/v1/vitrine.html footer section). Top nav transposed from vitrine.html topbar (lines 28-69 of mockup).

    14. **resources/views/layouts/admin.blade.php** — Skeleton ONLY for Plan 05: `<html lang="fr">`, `<body class="bg-sand-100">`, two-col grid `lg:grid lg:grid-cols-[16rem_1fr]`, slot for sidebar + slot for main. Empty navy-900 sidebar placeholder (Plan 05 fills it). Topbar placeholder. Mobile bottom nav placeholder. Plan 05 binds it via `<x-layout.admin>` usage.

    15. **resources/views/layouts/auth.blade.php** — Split layout from mockups/v1/auth.html lines 14-32: `<html lang="fr">`, `min-h-screen lg:grid lg:grid-cols-2`, brand panel `bg-navy-900` with hero villa-hospitality.jpg + .ripple overlay, form panel right with `<x-slot:form />`. Used by Plan 05.

    16. **resources/views/layouts/blog.blade.php** — Wraps `<x-layouts.app>` with a `<article class="prose prose-lg prose-azure max-w-2xl mx-auto py-16">` shell. Used by Plan 04.

    17. **resources/views/components/cta-whatsapp.blade.php** — Anonymous Blade component: takes optional `:size` prop (sm|md|lg), renders `<a href="https://wa.me/596696940054" class="...bg-[#25D366]...">` with the inline WhatsApp SVG icon. Used by every page.

    18. **resources/views/components/icon/whatsapp.blade.php**, **.../drop.blade.php**, **.../arrow-right.blade.php**, **.../check.blade.php** — Inline SVG components extracted from vitrine.html. Use `@props(['size' => 18])` for sizing.

    19. **resources/views/components/button.blade.php** — `@props(['variant' => 'primary', 'size' => 'md'])` rendering classes per variant: primary = `h-13 px-6 rounded-xl bg-azure-500 text-white font-semibold shadow-md hover:bg-azure-400`; secondary = `h-13 px-6 rounded-xl bg-white/10 ring-1 ring-white/25 backdrop-blur`; whatsapp delegates to `<x-cta-whatsapp />`.

    20. **resources/views/components/seo/meta.blade.php** — `@props(['title', 'description', 'image' => null, 'canonical' => null])` renders `<title>`, `<meta name=description>`, OG title/description/image/type/locale, twitter:card, theme-color, canonical, favicon. Plan 03 + Plan 04 will use this on every page.

    21. **resources/views/skeleton-home.blade.php** — Minimum styled placeholder using `<x-layouts.app :title="config('app.name')" :description="...">` so the GET / response renders the OKLCH skin: a centered sand-50 page with the wordmark Fredoka 700 + the page title from UI-SPEC + a `<x-cta-whatsapp />` button + a one-liner "Site en construction — la vitrine arrive en Plan 03". This is the temporary home that Plan 03 replaces with `resources/views/vitrine/home.blade.php`.

    22. **public/robots.txt** — `User-agent: *\nAllow: /\nDisallow: /admin\nSitemap: ${APP_URL}/sitemap.xml` (literal string `${APP_URL}` placeholder; plan 03 wires the sitemap route).

    23. **public/assets/brand/.gitkeep** — Empty placeholder so the directory is committed (real photos are sourced by Pierre per UI-SPEC §Photo Treatment).

    Run `npm run build` once to confirm Vite compiles Tailwind v4 without errors. Run `php artisan migrate` (default users + cache + jobs tables; Plan 02 adds business tables). Run `./vendor/bin/pest tests/Feature/SkeletonSmokeTest.php tests/Feature/HealthCheckTest.php` — both must exit 0.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter='SkeletonSmokeTest|HealthCheckTest' --exclude-group=skip-ci</automated>
  </verify>
  <done>Vitrine layout shell, route partitions, design tokens compiled, health check route alive, smoke tests green.</done>
  <acceptance_criteria>
    - File resources/css/app.css contains the substrings: `@import "tailwindcss"`, `@theme {`, `--color-azure-500: oklch(0.615 0.211 256)`, `--color-navy-900: oklch(0.232 0.052 251)`, `--color-sand-50: oklch(0.987 0.005 85)`, `--color-lagon-400: oklch(0.788 0.110 204)`, `--font-display: "Fredoka"`, `--font-sans: "Inter"`, `--spacing-13: 3.25rem`, `--spacing-15: 3.75rem`, `--spacing-18: 4.5rem`, `--breakpoint-xs: 400px`, `--container-content: 75rem`
    - File resources/css/app.css contains the substrings: `.ripple {`, `.photo-grade {`, `@keyframes rise`, `prefers-reduced-motion`
    - File resources/css/app.css does NOT contain the substring `#000` or `#fff` (per DESIGN.md rule "no #000 or #fff anywhere")
    - File resources/css/app.css does NOT contain the substring `font-weight: 500` for Inter or Fredoka import URL (UI-SPEC locks to 2 weights/family)
    - File bootstrap/app.php contains the substrings: `routes/vitrine.php`, `routes/blog.php`, `routes/admin.php`
    - Files routes/vitrine.php, routes/blog.php, routes/admin.php all exist and each contains `use Illuminate\Support\Facades\Route`
    - File app/Http/Controllers/HomeController.php contains the substring `view('skeleton-home')`
    - File app/Http/Controllers/HealthController.php contains the substring `DB::connection()->getPdo()`
    - File config/app.php contains the substring `'timezone' => 'America/Martinique'`
    - File config/app.php contains the substring `'locale' => 'fr'`
    - File config/filesystems.php contains the substrings `'scaleway'`, `s3.fr-par.scw.cloud`, `'use_path_style_endpoint' => true`
    - File config/services.php contains the substring `'mailgun'` and references `api.eu.mailgun.net` via env default
    - File resources/views/layouts/app.blade.php contains the substring `lang="fr"`
    - File resources/views/layouts/app.blade.php contains the substring `@vite`
    - File resources/views/layouts/app.blade.php contains the substring `Aller au contenu principal`
    - File resources/views/components/cta-whatsapp.blade.php contains the substring `wa.me/596696940054` and `bg-[#25D366]`
    - File public/robots.txt contains substrings `Disallow: /admin` and `Sitemap:`
    - Command `npm run build` exits 0 and produces files under public/build/
    - Command `php artisan migrate --pretend --no-ansi` reports the default users/cache/jobs tables (Plan 02 adds business tables separately)
    - Command `./vendor/bin/pest --filter='SkeletonSmokeTest|HealthCheckTest'` exits 0 with at least 7 passing assertions
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: GitHub Actions CI baseline + .env.testing + update CLAUDE.md + PROJECT.md + commit + push</name>
  <read_first>
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (GitHub Actions workflow example)
    - .planning/phases/01-vitrine-fondations/SKELETON.md (CI section)
    - CLAUDE.md (current stack table — must update "Laravel | 11.x" → "Laravel | 13.x", add Tailwind 4 note, update RESEARCH-derived recommendations)
    - .planning/PROJECT.md (Constraints + Key Decisions section, update Laravel 11 → Laravel 13)
  </read_first>
  <action>
    1. **.github/workflows/tests.yml** — Create CI workflow per D-29 and RESEARCH §GitHub Actions workflow example. Trigger on push to main + pull_request to main. Single job `pest` on ubuntu-latest. Postgres 16 service with health checks. Steps: checkout → setup-php@v2 (8.3, extensions pdo_pgsql/bcmath/gd/intl, coverage none) → setup-node@v4 (22) → composer install (--prefer-dist --no-progress --optimize-autoloader) → cp .env.example .env → php artisan key:generate → npm ci → npm run build → ./vendor/bin/pest --ci with env DB_CONNECTION=pgsql DB_HOST=127.0.0.1 DB_PORT=5432 DB_DATABASE=testing DB_USERNAME=testing DB_PASSWORD=testing.

    2. **.env.testing** — Pest test env: APP_ENV=testing, APP_KEY=base64:... (generate), DB_CONNECTION=pgsql, DB_HOST=127.0.0.1, DB_PORT=5432, DB_DATABASE=testing, DB_USERNAME=testing, DB_PASSWORD=testing, MAIL_MAILER=array, CACHE_STORE=array, SESSION_DRIVER=array, QUEUE_CONNECTION=sync.

    3. **.env.example** — Update to include all Phase 1 env vars (placeholders only, no secrets): APP_NAME, APP_URL, APP_TIMEZONE=America/Martinique, DB_*, MAIL_MAILER=log, MAIL_FROM_ADDRESS=contact@dloazurpiscines.com, MAIL_FROM_NAME="${APP_NAME}", MAILGUN_DOMAIN, MAILGUN_SECRET, MAILGUN_ENDPOINT=api.eu.mailgun.net, SCALEWAY_ACCESS_KEY_ID, SCALEWAY_SECRET_ACCESS_KEY, SCALEWAY_BUCKET, SCALEWAY_REGION=fr-par, SCALEWAY_ENDPOINT=https://s3.fr-par.scw.cloud, CONTACT_RECIPIENT=contact@dloazurpiscines.com, OPERATOR_EMAIL=pierre@dloazurpiscines.com, OPERATOR_INITIAL_PASSWORD=, OPERATOR_NAME="Pierre ADAM".

    4. **CLAUDE.md update** — Two surgical edits, no other content touched:
       - In the "Constraints" bullet list: change `**Tech stack**: Laravel 11 + Livewire + Alpine.js + Tailwind + PostgreSQL` to `**Tech stack**: Laravel 13 + Livewire 3 + Alpine.js 3 + Tailwind 4 (CSS-first @theme) + PostgreSQL 16` and append a sub-bullet: `- Stack override 2026-05-28 : Laravel 11 EOL passé (sécurité 2026-03-12), Laravel 13 retenu (sécurité jusqu'en 2028-03-17). Tailwind v4 supprime tailwind.config.js — tokens dans resources/css/app.css via @theme.`
       - In the "Recommended Stack > Core Technologies" table: change the `Laravel` row to `13.x` and the `Tailwind CSS` row Purpose column to `Utility CSS, design tokens via @theme CSS-first` and the Why column to `D-01 stack lock; v4 supprime tailwind.config.js`.
       Leave every other section untouched (libraries, alternatives, forbidden packages — unchanged).

    5. **.planning/PROJECT.md update** — Two surgical edits:
       - In "Constraints" bullet list: same Laravel 11 → Laravel 13 substitution with the same override rationale sub-bullet.
       - In "Key Decisions" table: add a new row `| Laravel 13 (stack override 2026-05-28) | Laravel 11 EOL passé, packages cibles supportent 13 indifféremment | Validated — Phase 1 |`.

    6. **.planning/phases/01-vitrine-fondations/CLOUD-PROVISIONING.md** — Step-by-step handover doc for Pierre (and for whoever runs the deploy). Sections: Laravel Cloud account → EU Central project → attach Postgres → connect GitHub repo → secrets (APP_KEY, OPERATOR_INITIAL_PASSWORD, OPERATOR_EMAIL=pierre@dloazurpiscines.com, MAILGUN_DOMAIN=mg.dloazurpiscines.com, MAILGUN_SECRET, MAILGUN_ENDPOINT=api.eu.mailgun.net, SCALEWAY_ACCESS_KEY_ID, SCALEWAY_SECRET_ACCESS_KEY, SCALEWAY_BUCKET=dlo-azur-piscines-staging, SCALEWAY_REGION=fr-par, SCALEWAY_ENDPOINT=https://s3.fr-par.scw.cloud) → build commands (composer install --no-dev --optimize-autoloader, npm ci, npm run build, php artisan config:cache, php artisan route:cache, php artisan view:cache) → deploy commands (php artisan migrate --force, php artisan db:seed --class=PierreSeeder --force — note Plan 05 ships PierreSeeder, Plan 01 leaves the deploy hook in place as a no-op until Plan 05). Scaleway account → bucket `dlo-azur-piscines-staging` fr-par private. Mailgun account → EU region → DNS for mg.dloazurpiscines.com (SPF, DKIM, MX) → flag as blocking item for Plan 04 SITE-05 real-mail send.

    7. Commit (do NOT push from this task — the operator pushes when ready and Laravel Cloud auto-deploys per D-30/D-31):
       Use `git add -A` (include vendor-removed and node_modules-removed paths — but those are in .gitignore now). Commit message:
       `docs(01): Walking Skeleton scaffold (Laravel 13 + Tailwind v4 + Pest 4 + CI)\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
  </action>
  <verify>
    <automated>test -f .github/workflows/tests.yml &amp;&amp; grep -q 'php-version' .github/workflows/tests.yml &amp;&amp; grep -q "'8.3'" .github/workflows/tests.yml &amp;&amp; grep -q 'postgres:16' .github/workflows/tests.yml &amp;&amp; grep -q 'pest --ci' .github/workflows/tests.yml &amp;&amp; test -f .env.testing &amp;&amp; grep -q 'DB_CONNECTION=pgsql' .env.testing &amp;&amp; test -f .env.example &amp;&amp; grep -q 'MAILGUN_ENDPOINT=api.eu.mailgun.net' .env.example &amp;&amp; grep -q 'SCALEWAY_ENDPOINT=https://s3.fr-par.scw.cloud' .env.example &amp;&amp; grep -q 'Laravel 13' CLAUDE.md &amp;&amp; grep -q 'Laravel 13' .planning/PROJECT.md &amp;&amp; test -f .planning/phases/01-vitrine-fondations/CLOUD-PROVISIONING.md</automated>
  </verify>
  <done>CI workflow committed, env templates ready, CLAUDE.md and PROJECT.md reflect Laravel 13 override, handover doc ready.</done>
  <acceptance_criteria>
    - File .github/workflows/tests.yml exists and contains the substrings: `php-version: '8.3'`, `node-version: '22'`, `postgres:16`, `./vendor/bin/pest --ci`
    - File .env.testing exists and contains DB_CONNECTION=pgsql + MAIL_MAILER=array
    - File .env.example contains MAILGUN_ENDPOINT=api.eu.mailgun.net AND SCALEWAY_ENDPOINT=https://s3.fr-par.scw.cloud AND APP_TIMEZONE=America/Martinique
    - File CLAUDE.md contains the substring "Laravel 13" at least once in the Constraints section
    - File CLAUDE.md does NOT contain the unmodified substring "Tech stack**: Laravel 11" anywhere
    - File .planning/PROJECT.md contains the substring "Laravel 13" at least once in Constraints or Key Decisions
    - File .planning/phases/01-vitrine-fondations/CLOUD-PROVISIONING.md exists and contains substrings: "Laravel Cloud", "Scaleway", "Mailgun", "mg.dloazurpiscines.com"
    - Command `git log -1 --format=%s` returns a subject starting with `docs(01)`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Provision Laravel Cloud staging + Scaleway bucket + Mailgun EU DNS — deploy + verify Walking Skeleton live</name>
  <what-built>
    Plan 01 Tasks 2-4 produced a Laravel 13 skeleton that runs locally and passes Pest CI. The remaining step is human-only setup that has no CLI/API substitute (account creation, DNS verification). Once these are done, push to main triggers auto-deploy and the operator visits the staging URL to confirm the page loads.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/01-vitrine-fondations/CLOUD-PROVISIONING.md`. Concretely:

    1. Create Laravel Cloud account at https://cloud.laravel.com (free tier $5 credit / 14 days, then ~4-7€/mo per D-02). Create a new project in EU Central (Frankfurt). Attach a managed Postgres resource. Connect the GitHub repo `Topias1/dloazur`, set branch `main` = environment `staging`, enable auto-deploy.

    2. In the Laravel Cloud project Environment → Secrets, set:
       - APP_KEY (generate locally `php artisan key:generate --show` then paste)
       - APP_URL=https://dloazur-staging.laravel.cloud (or preprod.dloazurpiscines.com if DNS already set)
       - APP_DEBUG=false
       - LOG_CHANNEL=stderr
       - SCALEWAY_* (5 vars from your Scaleway IAM API key — see step 3)
       - MAILGUN_DOMAIN=mg.dloazurpiscines.com, MAILGUN_SECRET=…, MAILGUN_ENDPOINT=api.eu.mailgun.net
       - OPERATOR_INITIAL_PASSWORD=<set a strong random value — used by Plan 05 PierreSeeder>
       Build commands: `composer install --no-dev --optimize-autoloader && npm ci && npm run build && php artisan config:cache && php artisan route:cache && php artisan view:cache`.
       Deploy commands: `php artisan migrate --force` (Plan 05 adds `php artisan db:seed --class=PierreSeeder --force`).

    3. Create Scaleway account at https://console.scaleway.com if not yet existing. Object Storage → create bucket `dlo-azur-piscines-staging` in fr-par region, visibility=private. Create an IAM API key with Object Storage read/write permissions. Paste the 2 keys into Laravel Cloud secrets.

    4. Create Mailgun account at https://www.mailgun.com (Flex plan, $1/1000 emails). Choose EU region. Add domain `mg.dloazurpiscines.com`. Add the SPF, DKIM, MX records as Mailgun displays to your DNS provider (Hostinger for now per user memory). Wait for verification (typically <30 min). Note: Plan 04 SITE-05 contact form send is BLOCKED on this DNS verification — until verified, `MAIL_MAILER=log` is safe.

    5. Push the Plan 01 commit: `git push origin main`. Watch Laravel Cloud build + deploy logs. First deploy will run `php artisan migrate --force` and create the default Laravel tables.

    6. Once deploy is green, visit the URL Laravel Cloud assigns (e.g., https://dloazur-staging.laravel.cloud). Expected outcomes:
       - Home page returns 200 with the placeholder Blade view styled in OKLCH (sand-50 background, Fredoka wordmark in ink-950, azure-500 WhatsApp CTA)
       - The page title in the browser tab is "Dlo Azur Piscines · Entretien de piscines en Martinique"
       - Health check `https://dloazur-staging.laravel.cloud/up` returns `{"app":"ok","db":"ok"}` with status 200
       - View page source in browser — confirm `<html lang="fr">`, `<meta name="theme-color" content="#0080ff">`, the Vite-emitted stylesheet link, and at least one `wa.me/596696940054` href

    7. Capture the live staging URL into the SUMMARY for this plan so Wave 2 plans can reference it for their own deployment verification.
  </how-to-verify>
  <verify>
    <automated>MISSING — provisioning checkpoint, fully human-only (account creation + DNS). Resume signal is operator confirmation of all 7 sub-steps.</automated>
  </verify>
  <done>Laravel Cloud staging URL is live and returns the Walking Skeleton home + /up health check. Operator records the URL.</done>
  <acceptance_criteria>
    - Laravel Cloud project exists in EU Central with Postgres attached
    - GitHub auto-deploy is wired (push to main triggers build)
    - Scaleway bucket `dlo-azur-piscines-staging` exists in fr-par
    - Mailgun domain `mg.dloazurpiscines.com` shows status "Active" (DNS verified) — OR explicitly noted as "still pending DNS, mail in log mode until Plan 04"
    - Staging URL returns 200 on `/` AND `/up`
    - Staging URL is captured in this plan's SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "deployed: &lt;url&gt;" with the live staging URL, or "blocked: &lt;reason&gt;" if any sub-step fails</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 1 — Walking Skeleton)

| Boundary | Description |
|----------|-------------|
| Browser → Laravel Cloud edge | Visitor untrusted; HTTPS terminates at Laravel Cloud, then forwards to Laravel app |
| Laravel app → PostgreSQL | App layer trusted; queries via Eloquent only, never DB::raw() with user input |
| Laravel app → Scaleway S3 | App layer trusted; private bucket, app credentials in env, no public uploads Phase 1 |
| GitHub Actions runner → Postgres test service | Ephemeral, isolated per workflow run, no secret data |
| Local dev → Laravel Cloud secrets | Operator-mediated copy-paste in dashboard UI; secrets never in repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Stack traces leaked to visitors in prod | mitigate | `APP_DEBUG=false` enforced via Laravel Cloud secret, `LOG_CHANNEL=stderr` (logs go to Laravel Cloud dashboard, never response body), bootstrap/app.php `->withExceptions()` placeholder confirms the default exception renderer is active |
| T-1-02 | Tampering | Insecure HTTP fallback | mitigate | `URL::forceHttps()` invoked in `AppServiceProvider::boot()` when `environment('production')`; Laravel Cloud terminates TLS 1.2+ |
| T-1-03 | Information Disclosure | .env secrets in repo | mitigate | `.env` added to .gitignore by Laravel skeleton; `.env.example` committed with placeholders only; all real values set via Laravel Cloud secrets UI (handover doc CLOUD-PROVISIONING.md) |
| T-1-04 | Spoofing | Forged Host header / open redirect via APP_URL mis-set | mitigate | `APP_URL` env var pinned to canonical staging/prod URL in Laravel Cloud secrets; default Laravel does not generate user-controlled redirects in skeleton |
| T-1-05 | Tampering | Supply chain — npm/composer install of malicious packages | mitigate | RESEARCH §Package Legitimacy Audit ran slopcheck 0.6.1 on all 9 Phase 1 packages with [OK] verdict; no [SUS]/[SLOP]; lockfiles (composer.lock, package-lock.json) committed so CI installs identical versions; Dependabot recommended (not configured in Phase 1, deferred) |
| T-1-06 | DoS | Anonymous traffic spike on staging during cold-wake | accept | Laravel Cloud scale-to-zero on staging is expected behavior; production tier will keep min instance warm (Plan 06 cutover decision); RESEARCH Pitfall 10 documents Lighthouse 85 on staging cold acceptable |
| T-1-07 | Information Disclosure | Health check `/up` leaking DB host/version | mitigate | HealthController returns only `{app: ok, db: ok|fail}` strings; no driver name, no version, no host |
| T-1-08 | Repudiation | No deploy/audit log if Laravel Cloud is bypassed | accept | Solo operator project; Laravel Cloud + GitHub Actions logs provide sufficient audit trail; explicit OOS Phase 1 |
| T-1-SC | Tampering | npm/composer installs without legitimacy gate | mitigate | All packages pre-cleared by slopcheck in RESEARCH; T-1-SC stays open across Phase 1 — every later plan that runs `composer require` or `npm install` re-checks against the same audit list. **No new packages added by execution without a slopcheck pass.** Plan 01 already front-loads ALL Phase 1 packages so Wave 2 plans should not need additional installs. |

</threat_model>

<verification>
**Plan-level checks (after Task 5 closes):**
- `./vendor/bin/pest --ci` exits 0 against local Postgres
- `npm run build` produces a public/build/manifest.json without errors
- The Laravel Cloud staging URL captured in Task 5 returns 200 on both `/` and `/up` over HTTPS
- View-source on the staging home contains `<html lang="fr">`, `<title>Dlo Azur Piscines · …</title>`, `wa.me/596696940054`, the Vite-emitted CSS link, `<meta name="theme-color" content="#0080ff">`
- CLAUDE.md and .planning/PROJECT.md both reference "Laravel 13" in their stack constraints
</verification>

<success_criteria>
1. `composer install` reproduces the lockfile with all 9 production Spatie/Fortify/Livewire/Symfony/DomPDF packages + 2 dev Pest packages — exit 0.
2. `npm run build` compiles `resources/css/app.css` (Tailwind v4 @theme) and `resources/js/app.js` (Alpine) into `public/build/` — exit 0.
3. `php artisan migrate` on a clean Postgres database creates the default Laravel tables (users, password_reset_tokens, sessions, cache, jobs) — exit 0. Business tables are added by Plan 02.
4. `GET /` on the staging URL returns 200 + body contains the visible page title from UI-SPEC + WhatsApp link.
5. `GET /up` on the staging URL returns 200 + JSON `{"app":"ok","db":"ok"}`.
6. Pest test suite (`./vendor/bin/pest --ci`) exits 0 — at minimum 7 assertions across SkeletonSmokeTest + HealthCheckTest.
7. GitHub Actions workflow `tests.yml` runs to completion on the first push and is green.
8. CLAUDE.md + PROJECT.md reflect Laravel 13 (no remaining "Laravel 11" in stack constraints).
9. SKELETON.md is committed and signed off as the architectural contract for Phase 2-5.
</success_criteria>

<output>
Create `.planning/phases/01-vitrine-fondations/01-01-SUMMARY.md` when done, including:
- The live staging URL captured in Task 5
- The chosen Laravel major version from Task 1 (default: 13)
- Any pending DNS verifications still in progress (Mailgun)
- A "Skeleton ready" green-light statement so Wave 2 plans can proceed
</output>
