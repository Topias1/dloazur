---
phase: 01-vitrine-fondations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - database/migrations/2026_05_28_000001_create_clients_table.php
  - database/migrations/2026_05_28_000002_create_piscines_table.php
  - database/migrations/2026_05_28_000003_create_produits_table.php
  - database/migrations/2026_05_28_000004_create_contrats_table.php
  - database/migrations/2026_05_28_000005_create_passages_table.php
  - database/migrations/2026_05_28_000006_create_photos_meta_table.php
  - database/migrations/2026_05_28_000007_create_factures_table.php
  - database/migrations/2026_05_28_000008_create_signatures_table.php
  - database/migrations/2026_05_28_000009_create_diagnostics_table.php
  - app/Models/Client.php
  - app/Models/Piscine.php
  - app/Models/Produit.php
  - app/Models/Contrat.php
  - app/Models/Passage.php
  - app/Models/PhotoMeta.php
  - app/Models/Facture.php
  - app/Models/Signature.php
  - app/Models/Diagnostic.php
  - database/factories/ClientFactory.php
  - database/factories/PiscineFactory.php
  - database/factories/PassageFactory.php
  - database/seeders/DatabaseSeeder.php
  - database/seeders/DevDataSeeder.php
  - tests/Feature/MigrationsTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every business table from the roadmap exists in PostgreSQL after php artisan migrate: clients, piscines, produits, contrats, passages, photos_meta, factures, signatures, diagnostics"
    - "Critical forward-compat columns exist in their tables: clients.uuid (UUID v4), passages.client_uuid (UUID, unique), passages.signature_path (nullable), factures.numero (nullable string, unique), factures.odoo_id (nullable bigint), factures.tva_rate (decimal default 8.50)"
    - "Eloquent models exist for every business entity with the correct $fillable and $casts (uuid as casted, json columns cast to array)"
    - "Factories exist for User, Client, Piscine, Passage — sufficient to populate ~3 demo clients in dev (D-09)"
    - "DatabaseSeeder runs only DevDataSeeder when environment is local/testing (D-09 — no business seed data in production)"
    - "Production deploy DOES NOT call DatabaseSeeder by default — only PierreSeeder via the deploy hook (PierreSeeder ships in Plan 05)"
  artifacts:
    - path: database/migrations/2026_05_28_000005_create_passages_table.php
      provides: "Passages table with client_uuid UUID unique (D-08, idempotence Phase 2) + signature_path nullable (D-08, Phase 3) + all measurement columns nullable for Phase 2 fill-in"
      contains: "uuid('client_uuid')->unique(), string('signature_path')->nullable(), decimal('ph_avant'), decimal('ph_apres'), decimal('chlore_libre'), decimal('chlore_total'), decimal('tac'), decimal('th'), decimal('sel_g_l'), json('actions'), text('notes'), string('pdf_path')->nullable(), timestamp('synced_at')->nullable()"
    - path: database/migrations/2026_05_28_000007_create_factures_table.php
      provides: "Factures table with numero (separate from id, nullable until posted) + odoo_id nullable + tva_rate default 8.50 (D-08, Pitfall 4 & 5)"
      contains: "string('numero')->nullable()->unique(), unsignedBigInteger('odoo_id')->nullable(), decimal('tva_rate', 4, 2)->default(8.50), string('statut', 16)->default('brouillon')"
    - path: database/migrations/2026_05_28_000001_create_clients_table.php
      provides: "Clients table with public uuid + magic_link_token + magic_link_expires_at placeholders for Phase 2"
      contains: "uuid('uuid')->unique(), string('magic_link_token')->nullable(), timestamp('magic_link_expires_at')->nullable()"
    - path: app/Models/Passage.php
      provides: "Eloquent model with $fillable, $casts (client_uuid→string, actions→array, visited_at→datetime, synced_at→datetime), belongsTo client/piscine"
      contains: "protected $fillable, protected $casts, belongsTo(Client::class), belongsTo(Piscine::class)"
    - path: database/seeders/DatabaseSeeder.php
      provides: "Production-safe entrypoint — only delegates to DevDataSeeder when APP_ENV in [local, testing]; never touches production data per D-09"
      contains: "App::environment(['local', 'testing']), DevDataSeeder::class"
    - path: database/seeders/DevDataSeeder.php
      provides: "3 demo clients with 1 piscine each + 1 produit + 1 contrat — sufficient for local + CI fixtures, never run in prod"
      contains: "Client::factory()->count(3), Piscine::factory()"
    - path: tests/Feature/MigrationsTest.php
      provides: "Comprehensive schema-check tests covering D-07 and D-08 — every table has every required column with the right type/nullable/default"
      contains: "Schema::hasTable('clients'), Schema::hasColumn('passages', 'client_uuid'), Schema::hasColumn('factures', 'numero'), Schema::hasColumn('factures', 'odoo_id'), Schema::hasColumn('factures', 'tva_rate'), Schema::hasColumn('passages', 'signature_path')"
  key_links:
    - from: "database/migrations/2026_05_28_000005_create_passages_table.php"
      to: "Phase 2 offline-first sync"
      via: "client_uuid unique constraint enables idempotent upsert from offline IndexedDB queue"
      pattern: "uuid\\(.client_uuid.\\)->unique"
    - from: "database/migrations/2026_05_28_000007_create_factures_table.php"
      to: "Phase 3 Odoo integration + CGI numerotation"
      via: "odoo_id (Phase 3 push id) + numero (Phase 3 séquentiel CGI) + tva_rate default 8.50 (TVA Martinique pre-set)"
      pattern: "odoo_id|numero|tva_rate"
    - from: "app/Models/Client.php"
      to: "Phase 2 magic link auth via cesargb/laravel-magiclink"
      via: "magic_link_token + magic_link_expires_at columns present in clients table"
      pattern: "magic_link_token|magic_link_expires_at"
---

<objective>
**Deploy the complete business schema in one migration set so future phases never need to retrofit columns.** Per D-07 every entity (clients, piscines, produits, contrats, passages, photos_meta, factures, signatures, diagnostics) gets a migration + Eloquent model now — even though the actual CRUD/UI for these arrives in Phase 2 (clients/piscines/passages), Phase 3 (produits/contrats/factures/signatures), and Phase 5 (diagnostics). Per D-08 the three critical forward-compat columns (`client_uuid`, `odoo_id`, `signature_path`) are written into the very first deploy so Phase 2's offline idempotence and Phase 3's Odoo bridge land without schema churn. Per D-09 only Pierre's user (Plan 05 PierreSeeder) ships to prod — business demo data is dev/test only via `DevDataSeeder`.

Purpose: satisfy ROADMAP Phase 1 success criterion #5 ("Le schéma de base de données complet est déployé") and remove all schema risk from later phases.

Output: 9 idempotent migrations, 9 Eloquent models, 4 factories (User factory from Laravel 13 default + 3 new ones), a production-safe DatabaseSeeder, and a comprehensive MigrationsTest that verifies every required column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vitrine-fondations/01-CONTEXT.md
@.planning/phases/01-vitrine-fondations/01-RESEARCH.md
@.planning/phases/01-vitrine-fondations/SKELETON.md
@.planning/phases/01-vitrine-fondations/01-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Schema contract — extracted from RESEARCH.md §Code Examples + ARCHITECTURE.md -->
<!-- These are the authoritative migrations. Executor MUST transpose them verbatim into Laravel 13 migration anonymous-class syntax. -->

passages table (D-07, D-08, Pitfall 4-5):
- id: bigint auto-increment PK
- client_uuid: UUID unique  ← D-08 idempotence for Phase 2 offline sync
- piscine_id: nullable foreignId → piscines.id nullOnDelete
- client_id: nullable foreignId → clients.id nullOnDelete
- visited_at: nullable dateTime
- status: string(16) default 'draft'  (draft | synced | archived)
- ph_avant, ph_apres: decimal(4,2) nullable
- chlore_libre, chlore_total: decimal(5,2) nullable
- tac, th: decimal(6,2) nullable
- sel_g_l: decimal(5,2) nullable
- actions: json nullable
- notes: text nullable
- pdf_path: string nullable
- signature_path: string nullable  ← D-08 Phase 3
- synced_at: timestamp nullable
- timestamps()
- index(['client_id', 'visited_at'])

factures table (D-07, D-08, Pitfalls 4 & 5):
- id: bigint PK
- uuid: UUID unique (public-facing)
- numero: string nullable unique  ← Pitfall 5 — CGI séquentiel, populated at 'posted' state
- client_id: foreignId cascadeOnDelete
- contrat_id: nullable foreignId nullOnDelete
- passage_id: nullable foreignId nullOnDelete
- lignes: json nullable
- total_ht, tva, total_ttc: decimal(10,2) default 0
- tva_rate: decimal(4,2) default 8.50  ← Pitfall 4 — TVA Martinique pre-set
- statut: string(16) default 'brouillon'
- odoo_id: unsignedBigInteger nullable  ← D-08 Phase 3
- odoo_synced_at: timestamp nullable
- odoo_sync_error: text nullable
- date_echeance: date nullable
- timestamps()

clients table:
- id: bigint PK
- uuid: UUID unique (public-facing, used in URL slugs e.g. portal magic link)
- name: string
- email: string nullable index
- phone: string(30) nullable
- address: string nullable
- notes: text nullable
- magic_link_token: string nullable  ← Phase 2 cesargb/laravel-magiclink
- magic_link_expires_at: timestamp nullable
- timestamps()

piscines table:
- id: bigint PK
- client_id: foreignId cascadeOnDelete
- nom: string nullable (e.g., "piscine principale", "piscine enfants")
- volume_m3: decimal(6,2) nullable
- type: string nullable (e.g., "enterrée", "hors-sol", "spa")
- filtration: string nullable (e.g., "sable", "cartouche", "diatomées")
- traitement: string nullable (e.g., "chlore", "sel", "brome")
- equipements: json nullable
- notes: text nullable
- timestamps()
- index('client_id')

photos_meta table (Phase 2 wiring for spatie/medialibrary fallback metadata):
- id: bigint PK
- passage_id: foreignId cascadeOnDelete
- disk: string default 'scaleway'
- path: string
- mime_type: string nullable
- size_bytes: unsignedInteger nullable
- width, height: unsignedSmallInteger nullable
- captured_at: timestamp nullable
- timestamps()
- index('passage_id')

produits table (Phase 3):
- id: bigint PK
- sku: string nullable unique
- libelle: string
- description: text nullable
- prix_ht: decimal(10,2) default 0
- unite: string(16) nullable (e.g., "passage", "heure", "litre")
- actif: boolean default true
- timestamps()

contrats table (Phase 3):
- id: bigint PK
- client_id: foreignId cascadeOnDelete
- type: string(16)  (ponctuel | forfait_mensuel | forfait_saisonnier)
- libelle: string nullable
- prix_ht_mensuel: decimal(10,2) nullable
- jour_facturation: smallint nullable (1-28)
- date_debut: date nullable
- date_fin: date nullable
- actif: boolean default true
- timestamps()
- index('client_id')

signatures table (Phase 3 — electronic sign per FACT-07):
- id: bigint PK
- passage_id: foreignId cascadeOnDelete
- client_id: foreignId cascadeOnDelete
- signature_data: text nullable  (base64 SVG or PNG dataURI)
- signed_at: timestamp nullable
- signer_name: string nullable
- ip: string(45) nullable
- user_agent: string nullable
- timestamps()
- index(['passage_id', 'client_id'])

diagnostics table (Phase 5):
- id: bigint PK
- client_id: nullable foreignId nullOnDelete  (anonymous visitor diagnostics allowed)
- piscine_id: nullable foreignId nullOnDelete
- volume_m3: decimal(6,2) nullable
- type_probleme: string(32) nullable  (verte | trouble | marron | electrolyseur)
- mesures: json nullable
- recommandations: json nullable  (calcul côté serveur, DIAG-02)
- disclaimer_accepted_at: timestamp nullable  (DIAG-03)
- created_via: string(16) default 'wizard'  (wizard | api | manual)
- timestamps()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write MigrationsTest (RED) covering every required table + critical column + default</name>
  <read_first>
    - .planning/phases/01-vitrine-fondations/01-CONTEXT.md (D-07, D-08, D-09)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (§Code Examples migrations passages/factures/clients + §Phase Requirements → Test Map row D-07/D-08)
    - tests/Pest.php (verify RefreshDatabase trait is wired by skeleton)
    - tests/Feature/SkeletonSmokeTest.php (use same Pest style)
  </read_first>
  <behavior>
    - Test 1 (`it asserts every business table exists`): Schema::hasTable returns true for: clients, piscines, produits, contrats, passages, photos_meta, factures, signatures, diagnostics. Failure points to the missing migration.
    - Test 2 (`it asserts passages.client_uuid is unique and indexed`): Schema::hasColumn('passages', 'client_uuid') AND the column type resolves to UUID-equivalent (Postgres uuid). Insert two passages with the same client_uuid → second insert throws UniqueConstraintViolationException.
    - Test 3 (`it asserts passages.signature_path is nullable string`): Schema::hasColumn('passages', 'signature_path') AND insert without signature_path succeeds. Insert with a string value succeeds.
    - Test 4 (`it asserts factures.numero is nullable unique string distinct from id`): Schema::hasColumn('factures', 'numero'); two factures with same numero='F2026-0001' → second throws unique violation; two factures with numero=null both insert successfully.
    - Test 5 (`it asserts factures.odoo_id is nullable bigint`): Schema::hasColumn('factures', 'odoo_id'); insert with odoo_id=123456789012 succeeds.
    - Test 6 (`it asserts factures.tva_rate defaults to 8.50`): Insert facture without specifying tva_rate → SELECT tva_rate returns 8.50 (or 8.5 numerically equivalent).
    - Test 7 (`it asserts clients.uuid is unique and is UUID v4 valid`): Schema::hasColumn('clients', 'uuid'); two clients with same uuid → unique violation; the value `Str::uuid()` validates via `\Illuminate\Support\Str::isUuid()`.
    - Test 8 (`it asserts clients.magic_link_token + magic_link_expires_at exist`): Both columns present; nullable.
    - Test 9 (`it asserts photos_meta.disk defaults to 'scaleway'`): Insert without disk → SELECT disk returns 'scaleway'.
    - Test 10 (`it asserts diagnostics.disclaimer_accepted_at exists for DIAG-03`): Schema::hasColumn('diagnostics', 'disclaimer_accepted_at').
    - Test 11 (`it asserts contrats.type accepts the three contract kinds`): Insert with type in ['ponctuel', 'forfait_mensuel', 'forfait_saisonnier'] succeeds.
    - Test 12 (`it asserts DatabaseSeeder is a no-op in production`): Set App::shouldReceive('environment')->andReturn('production'); call DatabaseSeeder::run(); assert no Client rows created. Then switch to 'local' → DevDataSeeder runs → assert at least 1 Client row.

    All tests fail at this stage (no migrations exist yet). Each test commits with prefix `test(01-02): add failing test for {table or column}` if working through TDD discipline; otherwise commit the whole test file at once as `test(01-02): add MigrationsTest covering D-07 and D-08`.
  </behavior>
  <action>
    Create `tests/Feature/MigrationsTest.php` using Pest syntax. Each test uses `RefreshDatabase` trait. For column existence checks, use `Schema::hasColumn('passages', 'client_uuid')`. For default-value checks, insert via DB::table to bypass model defaults, then SELECT to confirm the DB default applied. For unique-violation checks, wrap second insert in `expect(fn () => DB::table(...)->insert(...))->toThrow(\Illuminate\Database\UniqueConstraintViolationException::class)`. For the DatabaseSeeder env-gating test (Test 12), use `App::partialMock` or direct env() override via `putenv('APP_ENV=production')` then restore.

    Per RESEARCH §Phase Requirements → Test Map, this test file is the gate for D-07 + D-08.

    Run the test file once at the end of this task — every test MUST fail (red phase). Implementation comes in Task 2.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/MigrationsTest.php 2>&amp;1 | grep -qE '(FAIL|failed|ERRORS)' &amp;&amp; ./vendor/bin/pest tests/Feature/MigrationsTest.php 2>&amp;1 | grep -qE 'Tests:.+(failed|errors)'</automated>
  </verify>
  <done>Test file exists with 12 named tests, all currently failing because migrations are missing.</done>
  <acceptance_criteria>
    - File tests/Feature/MigrationsTest.php exists
    - File contains the substrings: `Schema::hasTable('clients')`, `Schema::hasTable('piscines')`, `Schema::hasTable('passages')`, `Schema::hasTable('factures')`, `Schema::hasColumn('passages', 'client_uuid')`, `Schema::hasColumn('factures', 'numero')`, `Schema::hasColumn('factures', 'odoo_id')`, `Schema::hasColumn('factures', 'tva_rate')`, `Schema::hasColumn('passages', 'signature_path')`, `Schema::hasColumn('clients', 'magic_link_token')`, `Schema::hasColumn('diagnostics', 'disclaimer_accepted_at')`
    - Command `./vendor/bin/pest tests/Feature/MigrationsTest.php` exits non-zero (RED) and the output mentions all 12 distinct test names
    - File contains the substring `RefreshDatabase`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 9 migrations + 9 Eloquent models + 4 factories + env-gated DatabaseSeeder (GREEN)</name>
  <read_first>
    - tests/Feature/MigrationsTest.php (the test contract written in Task 1 — implementations must make every assertion pass)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (§Code Examples — passages, factures, clients migration snippets)
    - .planning/research/ARCHITECTURE.md (schema design — entity relationships, French column names)
    - mockups/v1/passage.html (Phase 2 reference — confirms measurement field list: pH, chlore libre, chlore total, TAC, TH, sel)
  </read_first>
  <behavior>
    Same 12 tests from Task 1 must now ALL pass (GREEN). Each migration must:
    - Use `php artisan make:migration` naming convention (anonymous class form per Laravel 13 default)
    - Be ordered so FKs resolve: clients first, then piscines (FK→clients), produits (no FK), contrats (FK→clients), passages (FK→clients, piscines), photos_meta (FK→passages), factures (FK→clients, contrats, passages), signatures (FK→passages, clients), diagnostics (FK→clients, piscines)
    - Use `$table->timestamps()` and `$table->timestamps(0, true)` is not needed (Postgres timestamptz handles it)
    - Use `$table->uuid('uuid')->unique()` for public-facing UUIDs (clients) — Laravel 13 supports this directly
    - For `client_uuid` on passages: `$table->uuid('client_uuid')->unique()` (NOT a foreign key — D-08 explicitly says this is for idempotence, not referential integrity, because the row may arrive from offline IndexedDB before the matching client row is reconciled)
  </behavior>
  <action>
    1. **Create 9 migration files** in `database/migrations/` with timestamps `2026_05_28_000001` through `2026_05_28_000009` (so they apply in dependency order and are stable across CI re-runs). Use Laravel 13 anonymous-class migration syntax. Column definitions copied EXACTLY from the `<interfaces>` block above — every column, every nullability, every default, every index. Pay special attention to:
       - `clients.uuid` AND `passages.client_uuid` — both UUID unique, different semantics
       - `factures.tva_rate` decimal(4,2) default 8.50  ← do NOT use `default('8.50')` (string); use `default(8.50)` (float literal)
       - `factures.numero` string nullable unique — separate from id auto-increment (Pitfall 5)
       - `factures.odoo_id` unsignedBigInteger nullable (Phase 3)
       - `passages.signature_path` string nullable (Phase 3)
       - `photos_meta.disk` string default 'scaleway' — confirms the disk wiring from Plan 01

    2. **Create 9 Eloquent models** in `app/Models/` (or use `php artisan make:model -m` if it doesn't try to re-create migrations). Each model:
       - Has `use HasFactory` trait (where a factory will exist or be added)
       - Has `protected $fillable = [...]` listing every non-id non-timestamps column
       - Has `protected $casts = [...]` for JSON columns (cast to 'array'), datetime columns (cast to 'datetime' or 'immutable_datetime'), decimal columns (cast to 'decimal:2' for monetary), uuid columns (cast to 'string')
       - Has belongsTo / hasMany relationships per the FK diagram in the interfaces block
       - For Passage: `belongsTo(Client::class)`, `belongsTo(Piscine::class)`, `hasMany(PhotoMeta::class)`, `hasOne(Signature::class)`
       - For Client: `hasMany(Piscine::class)`, `hasMany(Passage::class)`, `hasMany(Contrat::class)`, `hasMany(Facture::class)`
       - For Facture: `belongsTo(Client::class)`, `belongsTo(Contrat::class)`, `belongsTo(Passage::class)`
       - PhotoMeta → name the table explicitly `protected $table = 'photos_meta';` (Laravel default would pluralize incorrectly to `photo_metas`)

    3. **Create factories** in `database/factories/`:
       - `ClientFactory` — uses fr_FR Faker: `name => fake()->lastName().' '.fake()->firstName()`, email fake, phone fake (Martinique-ish 0696… via custom `fake()->numerify('0696######')`), address `fake()->streetAddress().', '.fake()->randomElement(['Fort-de-France', 'Le Lamentin', 'Schoelcher', 'Les Trois-Îlets'])`, uuid via `Str::uuid()`
       - `PiscineFactory` — `client_id => Client::factory()`, `nom => 'Piscine principale'`, `volume_m3 => fake()->randomFloat(2, 8, 80)`, `type => fake()->randomElement(['enterrée', 'hors-sol', 'spa'])`, `filtration => fake()->randomElement(['sable', 'cartouche', 'diatomées'])`, `traitement => fake()->randomElement(['chlore', 'sel', 'brome'])`
       - `PassageFactory` — `client_uuid => Str::uuid()`, links to client + piscine via factories, sample mesures pH 7.2, chlore libre 1.5, status 'draft'
       - Already-existing `UserFactory` (Laravel default) — verify it still works after the Plan 01 install

    4. **Update `database/seeders/DatabaseSeeder.php`** to env-gate:
       ```
       public function run(): void {
           if (!app()->environment(['local', 'testing'])) {
               $this->command->info('DatabaseSeeder skipped — production environment');
               return;
           }
           $this->call(DevDataSeeder::class);
       }
       ```
       This satisfies D-09 (no business seed data in prod) and Task 1 Test 12.

    5. **Create `database/seeders/DevDataSeeder.php`** that runs only in dev/test:
       - Create 3 Clients via factory (Pierre's demo clients)
       - For each Client, create 1 Piscine via factory
       - Create 5 demo Produits (e.g., "Passage entretien standard" 60.00 HT/passage, "Forfait mensuel 1 passage/sem" 220.00 HT/mois, "Analyse complète" 35.00 HT/unité, "Chlore choc 5kg" 28.00 HT/unité, "Acide chlorhydrique 5L" 12.00 HT/unité)
       - Create 1 Contrat per Client (mix of types per RESEARCH ~50/50)
       - Do NOT seed Passages, Factures, Signatures, Diagnostics in dev — those flows are exercised by their own feature tests

    6. **Run migrations + tests locally**:
       - `php artisan migrate:fresh --seed --env=testing` (uses .env.testing's Postgres)
       - `./vendor/bin/pest tests/Feature/MigrationsTest.php` — all 12 tests must pass GREEN
       - `php artisan migrate:fresh --seed` (local .env) — confirm DevDataSeeder runs locally
       - Verify 3 client rows exist via `php artisan tinker` → `Client::count()` returns 3

    Commit message: `feat(01-02): complete business schema + Eloquent models + dev seeder per D-07/D-08/D-09`.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/MigrationsTest.php --ci</automated>
  </verify>
  <done>All 12 schema tests pass. Migrations are idempotent (re-running migrate:fresh produces identical schema). Dev seeder creates fixtures only in local/testing env.</done>
  <acceptance_criteria>
    - Exactly 9 migration files exist under database/migrations/ matching the pattern 2026_05_28_00000[1-9]_create_*.php
    - Exactly 9 model files exist under app/Models/ named Client.php, Piscine.php, Produit.php, Contrat.php, Passage.php, PhotoMeta.php, Facture.php, Signature.php, Diagnostic.php
    - File app/Models/Passage.php contains substrings: `protected $fillable`, `protected $casts`, `belongsTo(Client::class)`, `belongsTo(Piscine::class)`, `'client_uuid' => 'string'`, `'actions' => 'array'`, `'visited_at' => 'datetime'`, `'synced_at' => 'datetime'`
    - File app/Models/Facture.php contains substrings: `belongsTo(Client::class)`, `'tva_rate' => 'decimal:2'`, `'lignes' => 'array'`, `'odoo_id'`, `'numero'`
    - File app/Models/PhotoMeta.php contains substring `protected $table = 'photos_meta'`
    - File database/factories/ClientFactory.php exists and contains `Str::uuid()` reference
    - File database/factories/PassageFactory.php contains `'client_uuid' => Str::uuid()`
    - File database/seeders/DatabaseSeeder.php contains substring `app()->environment(['local', 'testing'])`
    - File database/seeders/DevDataSeeder.php exists and contains `Client::factory()`
    - Command `php artisan migrate:fresh --env=testing --no-interaction` exits 0 and prints 12 "Done" lines (1 per migration file + 3 Laravel defaults users/cache/jobs)
    - Command `./vendor/bin/pest tests/Feature/MigrationsTest.php --ci` exits 0 with 12 passing tests
    - Command `php artisan db:seed --env=testing` does not error and DevDataSeeder runs (the 3 Client rows can be re-counted)
    - Running `php artisan db:seed` after setting APP_ENV=production via env override (e.g., `APP_ENV=production php artisan db:seed --no-interaction --force`) does NOT create Client rows (production-safe gate)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 02 — Schema layer only)

| Boundary | Description |
|----------|-------------|
| Migration runner (artisan) → Postgres | Runs as the deploy hook in Laravel Cloud; trusted; idempotent migrations |
| Seeder → Postgres | DatabaseSeeder env-gated to never touch production; DevDataSeeder is fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | Demo fixtures leaking into production data | mitigate | DatabaseSeeder explicitly checks `app()->environment(['local', 'testing'])` before delegating to DevDataSeeder (D-09); MigrationsTest Test 12 verifies the gate |
| T-2-02 | Information Disclosure | UUID guessability for client portal access | mitigate | clients.uuid uses `Str::uuid()` (UUID v4, 122 bits of entropy); not used for auth on its own — Phase 2's magic_link_token is a separate secret with expires_at |
| T-2-03 | Repudiation | Missing audit timestamps | accept | All tables have `timestamps()` (created_at + updated_at); Phase 1 does not introduce an audit log (deferred to v2 if needed) |
| T-2-04 | Tampering | SQL injection via migrations | mitigate | All schemas use Laravel Schema builder (Blueprint) — never raw SQL with user input; migrations contain only literal column definitions |
| T-2-05 | DoS | Heavy migration locking production table during deploy | accept | Phase 1 tables are empty at creation (no ALTER on existing data); future migrations adding columns to populated tables must use `->after()` and Laravel Cloud zero-downtime deploys |
| T-2-06 | Tampering | factures.numero collision via concurrent insert | mitigate | unique constraint at DB level; Phase 3 will add the CGI sequence allocator under a transaction lock |
| T-2-SC | Tampering | No new package installs in this plan | mitigate | All required packages installed in Plan 01 (front-loaded); Plan 02 only writes PHP/SQL files |
</threat_model>

<verification>
- `php artisan migrate:fresh --env=testing` runs all 9 business migrations + Laravel defaults → exit 0
- `./vendor/bin/pest tests/Feature/MigrationsTest.php --ci` exit 0 with 12 green tests
- `php artisan db:seed --env=testing --no-interaction` produces 3 Client rows
- `APP_ENV=production php artisan db:seed --no-interaction --force` produces 0 Client rows (env gate proven)
- `git diff --stat` shows changes confined to database/, app/Models/, tests/Feature/MigrationsTest.php (no overlap with Plan 01 / 03 / 04 / 05 ownership)
</verification>

<success_criteria>
1. All 12 MigrationsTest assertions pass.
2. The 3 critical D-08 columns are physically present at the end of Plan 02: `passages.client_uuid` (UUID unique), `passages.signature_path` (nullable), `factures.odoo_id` (nullable bigint).
3. `factures.tva_rate` defaults to 8.50 (Martinique TVA pre-set per Pitfall 4).
4. `factures.numero` is a separate column from `id`, nullable, unique (Pitfall 5 — populated at posted state in Phase 3).
5. DatabaseSeeder is a strict no-op in production (D-09 satisfied).
6. 3 demo clients + their piscines + 5 produits exist after `php artisan db:seed` in local/testing only.
</success_criteria>

<output>
Create `.planning/phases/01-vitrine-fondations/01-02-SUMMARY.md` documenting:
- Final list of 9 migration files in apply order
- Confirmation that critical D-08 columns exist
- Confirmation that DatabaseSeeder env gate verified
- Pest test count and runtime for MigrationsTest
</output>
