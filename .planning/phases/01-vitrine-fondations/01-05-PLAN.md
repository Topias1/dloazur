---
phase: 01-vitrine-fondations
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - routes/admin.php
  - app/Providers/FortifyServiceProvider.php
  - bootstrap/providers.php
  - config/fortify.php
  - database/seeders/PierreSeeder.php
  - app/Http/Controllers/Admin/DashboardController.php
  - app/Livewire/Admin/DashboardStub.php
  - resources/views/auth/login.blade.php
  - resources/views/auth/forgot-password.blade.php
  - resources/views/auth/reset-password.blade.php
  - resources/views/admin/dashboard.blade.php
  - resources/views/livewire/admin/dashboard-stub.blade.php
  - resources/views/components/admin/sidebar.blade.php
  - resources/views/components/admin/topbar.blade.php
  - resources/views/components/admin/mobile-bottom-nav.blade.php
  - resources/views/components/admin/stat-card.blade.php
  - tests/Feature/AuthLoginTest.php
  - tests/Feature/AdminShellTest.php
  - tests/Feature/PierreSeederTest.php
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Pierre is seeded into users via PierreSeeder using updateOrCreate keyed by email — idempotent: running the seeder twice produces exactly 1 row"
    - "GET /login renders the styled Blade view transposed from mockups/v1/auth.html (segmented Pro/Client toggle, brand panel left, form panel right)"
    - "POST /login with Pierre's seeded credentials succeeds and redirects to /admin (D-17, fortify.php 'home' => '/admin')"
    - "POST /login with wrong password returns to /login with the UI-SPEC error 'E-mail ou mot de passe incorrect.'"
    - "Fortify login throttle: 5 attempts/minute keyed by lowercased email + IP (RESEARCH §Pattern 2 + Pitfall 8)"
    - "GET /admin requires authentication — anonymous request returns redirect to /login"
    - "Authenticated GET /admin renders the dashboard stub (D-19): greeting 'Bonjour Pierre,', 4 stat cards with em-dash values, sidebar with Tableau de bord active + Clients/Passages/Factures/Catalogue grisés (aria-disabled, tabindex=-1, 'bientôt' badge per D-18)"
    - "POST /logout (Fortify default route) destroys the session and redirects to /"
    - "Phase 1 Fortify config has resetPasswords feature ON; registerUsers, emailVerification, twoFactorAuthentication all OFF (per CONTEXT.md <deferred>)"
    - "All auth views use the design system tokens (azure-500 buttons, sand-50 inputs, Fredoka headings) per UI-SPEC §Auth page Copywriting Contract"
  artifacts:
    - path: app/Providers/FortifyServiceProvider.php
      provides: "Headless Fortify wiring: views (loginView, requestPasswordResetLinkView, resetPasswordView) + RateLimiter::for('login') with 5/min keyed by email+IP"
      contains: "Fortify::loginView, RateLimiter::for('login'"
    - path: config/fortify.php
      provides: "Phase 1 minimum features: only resetPasswords; home='/admin'; guard='web'"
      contains: "'features' =>, Features::resetPasswords(), 'home' => '/admin'"
    - path: database/seeders/PierreSeeder.php
      provides: "Production-safe idempotent seeder — single user keyed by email from env"
      contains: "User::updateOrCreate(['email' => env('OPERATOR_EMAIL'"
    - path: routes/admin.php
      provides: "GET /admin → DashboardController + POST /admin/* routes are off until Phase 2 (only the GET dashboard for now)"
      contains: "Route::get('/', [DashboardController::class, 'index'])->name('dashboard')"
    - path: app/Http/Controllers/Admin/DashboardController.php
      provides: "Single index() method renders the dashboard view passing Pierre's name (auth()->user())"
      contains: "view('admin.dashboard'"
    - path: resources/views/auth/login.blade.php
      provides: "Fortify login view transposed 1:1 from mockups/v1/auth.html — Pro pane functional, Client pane visible-as-stub (AUTH-02 deferred)"
      contains: "<x-layouts.auth>, segmented toggle, Espace pro, Espace client, action=\"{{ route('login') }}\""
    - path: resources/views/admin/dashboard.blade.php
      provides: "Dashboard stub per D-19 — greeting + 4 stat cards with em-dash values + active sidebar nav with greyed items per D-18"
      contains: "Bonjour Pierre, Clients actifs, Passages cette semaine, À synchroniser, Factures en attente, —, aria-disabled=\"true\""
  key_links:
    - from: "POST /login (Fortify)"
      to: "/admin (DashboardController)"
      via: "config('fortify.home') = '/admin' + Route::get('/', [DashboardController::class, 'index']) inside the admin group prefixed with admin"
      pattern: "fortify\\.home|/admin"
    - from: "PierreSeeder"
      to: "production deploy hook in Laravel Cloud"
      via: "Plan 01 CLOUD-PROVISIONING.md deploy commands run `php artisan db:seed --class=PierreSeeder --force` after migrate"
      pattern: "PierreSeeder"
    - from: "resources/views/auth/login.blade.php"
      to: "mockups/v1/auth.html"
      via: "1:1 design transposition — segmented toggle, brand panel, form panel"
      pattern: "Espace pro|Espace client|segmented"
    - from: "resources/views/admin/dashboard.blade.php"
      to: "resources/views/layouts/admin.blade.php (created by Plan 01)"
      via: "Blade component slot consumption — dashboard.blade.php fills the layout's <x-slot:sidebar> and <x-slot:main> slots; slot names must match exactly between producer (Plan 05) and provider (Plan 01)"
      pattern: "x-slot:sidebar|x-slot:main"
---

<objective>
**AUTH-01 — Pro authentication + back-office shell + dashboard stub.**

Per D-03 we use `laravel/fortify` in headless mode (no scaffold views) bound to Blade templates transposed 1:1 from `mockups/v1/auth.html`. Per D-09 + RESEARCH the sole user is Pierre, seeded idempotently via `PierreSeeder` using `OPERATOR_EMAIL` + `OPERATOR_INITIAL_PASSWORD` env vars. Per D-17 the post-login landing is `/admin` which renders the admin shell (sidebar + topbar + slot main) transposed from `mockups/v1/dashboard.html`. Per D-18 the sidebar nav has Clients / Passages / Factures / Catalogue **grisés with 'bientôt' badges and aria-disabled** — they become functional in Phase 2/3. Per D-19 the dashboard body shows a greeting "Bonjour Pierre," + 4 stat cards with `—` values (canvas reused by Phase 2). Per D-20 logout uses Fortify's default route.

Per CONTEXT.md `<deferred>` Phase 1 Fortify features are **only** `resetPasswords()`. No registerUsers (Pierre seeded, no public signup), no emailVerification (Pierre already verified at seed), no 2FA (single user, no scale yet).

Per RESEARCH §Pattern 2 + Pitfall 8: `RateLimiter::for('login')` throttles 5 attempts/minute by lowercased email + IP. `config/fortify.php` `'home' => '/admin'`. RouteServiceProvider not needed in Laravel 13 — admin route is registered through `routes/admin.php` (Plan 01 wired this in `bootstrap/app.php`).

Output: working login + logout + password reset flow + styled back-office shell ready for Phase 2 to plug Clients/Passages CRUD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vitrine-fondations/01-CONTEXT.md
@.planning/phases/01-vitrine-fondations/01-RESEARCH.md
@.planning/phases/01-vitrine-fondations/01-UI-SPEC.md
@.planning/phases/01-vitrine-fondations/SKELETON.md
@.planning/phases/01-vitrine-fondations/01-01-SUMMARY.md
@.planning/phases/01-vitrine-fondations/01-02-SUMMARY.md
@mockups/v1/auth.html
@mockups/v1/dashboard.html

<interfaces>
<!-- Fortify minimal config per RESEARCH §Pattern 2 + D-03 -->
config/fortify.php:
- 'guard' => 'web'
- 'passwords' => 'users'
- 'username' => 'email'
- 'email' => 'email'
- 'home' => '/admin'                      ← redirect after login (D-17)
- 'prefix' => ''                          ← /login at root, not /admin/login
- 'views' => true                         ← we provide our own Blade
- 'middleware' => ['web']
- 'features' => [
    Features::resetPasswords(),
    // NO registerUsers, NO emailVerification, NO twoFactorAuthentication (Phase 1 scope, CONTEXT.md <deferred>)
  ]

<!-- FortifyServiceProvider boot() per RESEARCH §Pattern 2 -->
Fortify::loginView(fn () => view('auth.login'));
Fortify::requestPasswordResetLinkView(fn () => view('auth.forgot-password'));
Fortify::resetPasswordView(fn ($request) => view('auth.reset-password', ['request' => $request]));

RateLimiter::for('login', function (Request $request) {
    $throttleKey = mb_strtolower((string) $request->input('email')).'|'.$request->ip();
    return Limit::perMinute(5)->by($throttleKey);
});

<!-- PierreSeeder per RESEARCH §Code Examples + D-09 -->
User::updateOrCreate(
    ['email' => env('OPERATOR_EMAIL', 'pierre@dloazurpiscines.com')],
    [
        'name' => env('OPERATOR_NAME', 'Pierre ADAM'),
        'password' => Hash::make(env('OPERATOR_INITIAL_PASSWORD', 'change-me-now')),
        'email_verified_at' => now(),
    ]
);

<!-- UI-SPEC §Auth page Copywriting Contract — strict copy -->
- Brand panel H2: 'Chaque passage, gardé en mémoire.'
- Pro pane H1: 'Bon retour, Pierre.'
- Pro pane sub: 'Connectez-vous pour gérer vos passages.'
- Pro submit CTA: 'Se connecter'
- Remember me label: 'Rester connecté sur ce téléphone'
- Forgot password link: 'Oublié ?'
- Client pane H1: 'Votre espace piscine.'
- Client pane sub: 'Pas de mot de passe : recevez un lien d\'accès sécurisé par e-mail.'
  (Phase 2 — shown as stub in Phase 1; submit button disabled with 'Bientôt disponible')
- Footer note: 'Données hébergées en Europe · Confidentialité'
- Auth error wrong credentials: 'E-mail ou mot de passe incorrect.'
- Auth error account locked: 'Trop de tentatives. Attendez quelques minutes.'

<!-- UI-SPEC §Admin shell stub Copywriting Contract — strict copy -->
- Dashboard greeting: 'Bonjour Pierre,'
- Dashboard sub: 'Tableau de bord opérationnel en Phase 2.'
- Stat card label 1: 'Clients actifs'
- Stat card label 2: 'Passages cette semaine'
- Stat card label 3: 'À synchroniser'
- Stat card label 4: 'Factures en attente'
- All stat values (Phase 1): '—'
- Disabled nav items: each accompanied by a 'bientôt' badge (visually different from active)

<!-- Sidebar nav structure per D-18 + UI-SPEC §Admin shell -->
Active (Phase 1): 'Tableau de bord' → route('admin.dashboard')
Greyed (Phase 2/3):
  - 'Clients' (Phase 2)
  - 'Passages' (Phase 2)
  - 'Factures' (Phase 3)
  - 'Catalogue' (Phase 3)
Each greyed item: aria-disabled="true", tabindex="-1", cursor-default, opacity-60, badge 'bientôt'.

User pill at sidebar bottom: avatar 'P' on bg-azure-500, name 'Pierre ADAM', role 'Pisciniste'.
Logout link inside the user pill or as a separate menu item — routes to POST /logout (Fortify default).

<!-- Topbar contract per UI-SPEC §Admin shell -->
- 'Nouveau passage' primary button → greyed out (disabled) in Phase 1 with cursor-not-allowed, opacity-50; Phase 2 wires the route
- Search input → greyed out (Phase 2 wires actual search)
- Logo link returns to /admin (not vitrine)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write AuthLoginTest + AdminShellTest + PierreSeederTest (RED)</name>
  <read_first>
    - .planning/phases/01-vitrine-fondations/01-CONTEXT.md (D-03, D-09, D-17..D-20 + <deferred> for what's NOT in Fortify)
    - .planning/phases/01-vitrine-fondations/01-UI-SPEC.md (§Auth page + §Admin shell + §Copywriting Contract)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (§Pattern 2 + §Phase Requirements → Test Map AUTH-01)
    - tests/Feature/MigrationsTest.php (Plan 02 — to verify users table is present in test DB)
    - mockups/v1/auth.html (full)
    - mockups/v1/dashboard.html (full)
  </read_first>
  <behavior>
    PierreSeederTest:
    - Test 1 (`pierre seeder creates exactly one user with the env credentials`): Set env('OPERATOR_EMAIL', 'pierre@test.local'); call (new PierreSeeder())->run(); assertDatabaseCount('users', 1); assertDatabaseHas('users', ['email' => 'pierre@test.local', 'name' => env('OPERATOR_NAME', 'Pierre ADAM')]); the password column hashes correctly (Hash::check(env('OPERATOR_INITIAL_PASSWORD'), $user->password) is true).
    - Test 2 (`pierre seeder is idempotent`): call run() twice; assertDatabaseCount('users', 1) — no duplicate.
    - Test 3 (`pierre seeder sets email_verified_at to a non-null timestamp`): after run, $user->email_verified_at is not null (skip Fortify email verification per CONTEXT.md <deferred>).
    - Test 4 (`pierre seeder is callable in production env`): App::environment fake to 'production'; PierreSeeder->run() (not DatabaseSeeder!) executes and creates the row — this is intentional: DatabaseSeeder gates dev fixtures but PierreSeeder must run in prod per Plan 01 CLOUD-PROVISIONING.md deploy hook.

    AuthLoginTest (covers AUTH-01):
    - Test 5 (`GET /login renders the styled view with required copy`): GET /login returns 200; body contains 'Bon retour, Pierre.', 'Se connecter', 'Espace pro', 'Espace client', 'Rester connecté sur ce téléphone', 'Données hébergées en Europe · Confidentialité', 'Chaque passage, gardé en mémoire.'
    - Test 6 (`login form posts to the Fortify route`): GET /login body contains `action="{{ route('login') }}"` rendered as the actual URL (e.g., `action="/login"` or `action="http://localhost/login"`); body contains the `@csrf` token field.
    - Test 7 (`POST /login with valid Pierre credentials redirects to /admin`): seed Pierre first; POST /login with the seeded email + password; assertRedirect('/admin'); $this->assertAuthenticatedAs($pierre).
    - Test 8 (`POST /login with wrong password returns to /login with error`): seed Pierre; POST /login with wrong password; assertRedirect('/login') OR assertSessionHasErrors(['email']); the session error string contains 'E-mail ou mot de passe incorrect.' (rendered from Fortify's failed validation messages, may need to override via lang file).
    - Test 9 (`login throttle kicks in after 5 attempts`): seed Pierre; POST /login with wrong password 6 times from the same IP and same email; the 6th attempt returns 429 OR redirects to /login with assertSessionHasErrors and the error string contains 'Trop de tentatives'. (Fortify uses Laravel's default lang or our override.)
    - Test 10 (`GET /forgot-password renders the password reset request view`): GET /forgot-password returns 200 with a form posting to route('password.email').
    - Test 11 (`POST /logout destroys the session and redirects to /`): seed + login Pierre; POST /logout; assertRedirect('/'); assertGuest().

    AdminShellTest (covers D-17..D-20):
    - Test 12 (`GET /admin without auth redirects to login`): GET /admin without login → assertRedirect('/login').
    - Test 13 (`GET /admin while authenticated returns 200 with the dashboard stub`): seed + actingAs Pierre; GET /admin returns 200; body contains 'Bonjour Pierre,', 'Tableau de bord opérationnel en Phase 2.', 4 stat card labels ('Clients actifs', 'Passages cette semaine', 'À synchroniser', 'Factures en attente'), and 4 em-dash values.
    - Test 14 (`admin sidebar has 4 greyed nav items with bientôt badges`): GET /admin body contains 'Clients' near 'bientôt', similarly for 'Passages', 'Factures', 'Catalogue'; each greyed item has `aria-disabled="true"`.
    - Test 15 (`admin sidebar Tableau de bord is the active item`): body contains 'Tableau de bord' linked to `route('admin.dashboard')` with bg-white/10 (active state class).
    - Test 16 (`admin topbar shows greyed 'Nouveau passage' button`): body contains 'Nouveau passage' AND the button has disabled OR cursor-not-allowed OR aria-disabled marker.
    - Test 17 (`admin shell uses layouts/admin.blade.php`): body contains the substring `lg:grid lg:grid-cols-[16rem_1fr]` (sidebar grid from Plan 01).
    - Test 18 (`admin shell has user pill with Pierre's name + Pisciniste role`): body contains 'Pierre ADAM' AND 'Pisciniste'.

    Total: 18 failing tests. Run all 3 files to confirm RED.
  </behavior>
  <action>
    Create the 3 test files in `tests/Feature/`. Use Pest syntax + `RefreshDatabase` trait + `Tests\TestCase`.

    For PierreSeederTest Test 4, use `App::shouldReceive('environment')` partial mock OR override `app()->detectEnvironment(fn () => 'production')` before calling `(new PierreSeeder())->run()`. The point is to verify PierreSeeder is NOT env-gated (only DatabaseSeeder is).

    For AuthLoginTest Test 9 (throttle), use a `beforeEach` hook to `RateLimiter::clear($throttleKey)` so cross-test isolation holds; throttle uses `mb_strtolower($email).'|'.$ip` as key. Set request IP via `withServerVariables(['REMOTE_ADDR' => '127.0.0.1'])` for stability.

    For Test 8 / Test 9 error strings, override Fortify's default validation message in `lang/fr/auth.php`:
    ```
    return [
        'failed' => 'E-mail ou mot de passe incorrect.',
        'password' => 'E-mail ou mot de passe incorrect.',
        'throttle' => 'Trop de tentatives. Attendez :seconds secondes avant de réessayer.',
    ];
    ```
    The default Fortify messages come from `lang/en/auth.php` (published when Laravel app boots). Publish the French translation file via Laravel 13 default (it ships with limited fr resources) OR create the file manually. The error message string assertion is the gate.

    Run `./vendor/bin/pest tests/Feature/AuthLoginTest.php tests/Feature/AdminShellTest.php tests/Feature/PierreSeederTest.php` — every test must fail (RED).

    Commit message: `test(01-05): RED — auth + admin shell + Pierre seeder per AUTH-01`.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/AuthLoginTest.php tests/Feature/AdminShellTest.php tests/Feature/PierreSeederTest.php 2>&amp;1 | grep -qE 'Tests:.+(failed|errors)'</automated>
  </verify>
  <done>18 failing tests written across 3 files.</done>
  <acceptance_criteria>
    - File tests/Feature/PierreSeederTest.php exists with at least 4 distinct test blocks
    - File tests/Feature/AuthLoginTest.php exists with at least 7 distinct test blocks
    - File tests/Feature/AdminShellTest.php exists with at least 7 distinct test blocks
    - PierreSeederTest contains substrings: `assertDatabaseCount('users', 1)`, `PierreSeeder`, `Hash::check`, `email_verified_at`
    - AuthLoginTest contains substrings: `Bon retour, Pierre.`, `actingAs(`, `assertAuthenticatedAs`, `assertRedirect('/admin')`, `assertSessionHasErrors`, `Trop de tentatives`
    - AdminShellTest contains substrings: `Bonjour Pierre,`, `aria-disabled="true"`, `Clients actifs`, `Passages cette semaine`, `À synchroniser`, `Factures en attente`, `Nouveau passage`, `bientôt`
    - Command `./vendor/bin/pest tests/Feature/AuthLoginTest.php tests/Feature/AdminShellTest.php tests/Feature/PierreSeederTest.php` exits non-zero (RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Fortify headless wiring + Pierre seeder + admin shell views + dashboard stub (GREEN)</name>
  <read_first>
    - tests/Feature/AuthLoginTest.php (the contract from Task 1)
    - tests/Feature/AdminShellTest.php (the contract from Task 1)
    - tests/Feature/PierreSeederTest.php (the contract from Task 1)
    - mockups/v1/auth.html (full file — transpose markup 1:1)
    - mockups/v1/dashboard.html (full file — transpose sidebar + topbar markup 1:1)
    - .planning/phases/01-vitrine-fondations/01-RESEARCH.md (§Pattern 2 Fortify setup + Pitfall 8 home redirect)
    - .planning/phases/01-vitrine-fondations/01-UI-SPEC.md (§Auth page + §Admin shell + §Copywriting Contract)
    - resources/views/layouts/admin.blade.php (Plan 01 — verify the named slots `<x-slot:sidebar>` and `<x-slot:main>` exist before authoring the dashboard consumer)
    - resources/views/layouts/auth.blade.php (Plan 01 — verify the split-layout shell)
    - bootstrap/providers.php (Plan 01 — must add FortifyServiceProvider)
    - bootstrap/app.php (Plan 01 — confirm routes/admin.php registration: `Route::middleware(['web', 'auth'])->prefix('admin')->name('admin.')->group(base_path('routes/admin.php'))`)
  </read_first>
  <action>
    1. **Publish Fortify config + actions** — `php artisan vendor:publish --provider="Laravel\Fortify\FortifyServiceProvider"` (publishes `config/fortify.php` + `app/Actions/Fortify/*.php` (CreateNewUser, UpdateUserPassword, ResetUserPassword, etc.)). Delete the `CreateNewUser` action (we don't register users — D-09). Keep `ResetUserPassword` for password reset feature.

    2. **`config/fortify.php`** — Configure per the interfaces block:
       ```
       return [
           'guard' => 'web',
           'passwords' => 'users',
           'username' => 'email',
           'email' => 'email',
           'home' => '/admin',
           'prefix' => '',
           'domain' => null,
           'middleware' => ['web'],
           'limiters' => [
               'login' => 'login',
           ],
           'views' => true,
           'features' => [
               Features::resetPasswords(),
               // DELIBERATELY absent: registerUsers, emailVerification, twoFactorAuthentication
           ],
       ];
       ```

    3. **`app/Providers/FortifyServiceProvider.php`** — Per RESEARCH §Pattern 2 verbatim. `boot()` binds 3 views + 1 RateLimiter::for('login').

    4. **`bootstrap/providers.php`** — Append `App\Providers\FortifyServiceProvider::class,` to the array.

    5. **`database/seeders/PierreSeeder.php`** — Per RESEARCH §Code Examples verbatim. Use env keys `OPERATOR_EMAIL`, `OPERATOR_NAME`, `OPERATOR_INITIAL_PASSWORD`. Idempotent via `updateOrCreate`. Set `email_verified_at` to `now()` to skip verification.

    6. **`lang/fr/auth.php`** — Create the French auth lang file with overrides for `failed`, `password`, `throttle` keys (see Task 1 action notes). Set `config/app.php`'s `'locale' => 'fr'` is already set by Plan 01 — Laravel will pick up `lang/fr/auth.php` automatically.

    7. **`routes/admin.php`** — Replace Plan 01's empty stub with:
       ```
       use App\Http\Controllers\Admin\DashboardController;
       use Illuminate\Support\Facades\Route;
       Route::get('/', [DashboardController::class, 'index'])->name('dashboard');
       // Phase 2 will add Route::resource('clients', ClientController::class), passages, etc.
       ```
       The `bootstrap/app.php` `withRouting` from Plan 01 wraps this in `middleware(['web', 'auth'])->prefix('admin')->name('admin.')` so the full URL is `/admin` and the route name is `admin.dashboard`.

    8. **`app/Http/Controllers/Admin/DashboardController.php`** — Single `index()` returning `view('admin.dashboard', ['user' => auth()->user()])`. Stays under 20 lines.

    9. **`resources/views/auth/login.blade.php`** — Transpose `mockups/v1/auth.html` 1:1 to Blade. Key changes:
       - Wrap in `<x-layouts.auth>` (Plan 01 split layout)
       - Replace `<form id="pane-pro">` with `<form method="POST" action="{{ route('login') }}" class="space-y-4">` + `@csrf`
       - Replace `<input id="pro-email">` with `<input name="email" id="email" type="email" value="{{ old('email') }}" required autocomplete="email" autofocus class="...">`
       - Replace `<input id="pro-pass">` with `<input name="password" id="password" type="password" required autocomplete="current-password" class="...">`
       - "Remember me" → `<input type="checkbox" name="remember" id="remember" />` with UI-SPEC styled label
       - "Oublié ?" link → `<a href="{{ route('password.request') }}" ...>Oublié ?</a>`
       - Submit button text 'Se connecter'
       - Error display: `@error('email') <p class="text-sm text-danger mt-1">{{ $message }}</p> @enderror` directly under the email field; for throttle errors, render at the form level above the email field
       - Pro / Client segmented toggle: Alpine `x-data="{ tab: 'pro' }"` controlling visibility (per UI-SPEC §Auth segmented toggle). The Client pane shows the magic-link form per UI-SPEC §Copywriting Contract → Client pane H1 'Votre espace piscine.' with a disabled submit "Bientôt disponible" + a paragraph "Disponible dès l'arrivée des fiches clients (Phase 2)."

    10. **`resources/views/auth/forgot-password.blade.php`** — Per UI-SPEC §Auth: same layout, single email input, submit "Recevoir le lien de réinitialisation". POST to `route('password.email')`. Success message via session flash.

    11. **`resources/views/auth/reset-password.blade.php`** — POST to `route('password.update')` with hidden `token` field from `$request->route('token')`, email field, password + password_confirmation. Submit "Réinitialiser le mot de passe".

    12. **`resources/views/components/admin/sidebar.blade.php`** — Anonymous Blade component. Transposed from mockups/v1/dashboard.html sidebar (lines ~22-37). Structure:
        - `<aside class="hidden lg:flex flex-col bg-navy-900 text-navy-100 px-4 py-6 sticky top-0 h-screen">`
        - Logo + brand (links to `route('admin.dashboard')`)
        - Nav `<a href="{{ route('admin.dashboard') }}" @class(['flex items-center gap-3 h-11 px-3 rounded-xl', 'bg-white/10 text-white' => request()->routeIs('admin.dashboard'), 'hover:bg-white/8 hover:text-white' => !request()->routeIs('admin.dashboard')])>Tableau de bord</a>`
        - Greyed items: `<span aria-disabled="true" tabindex="-1" class="flex items-center gap-3 h-11 px-3 rounded-xl text-navy-300 opacity-60 cursor-default">Clients <span class="ml-auto text-[10px] font-semibold px-2 py-0.5 rounded-full bg-white/8 text-navy-200">bientôt</span></span>` (same for Passages, Factures, Catalogue)
        - User pill at bottom with avatar 'P' on `bg-azure-500`, name, role, + logout form: `<form method="POST" action="{{ route('logout') }}" class="ml-auto">@csrf <button type="submit" class="text-navy-300 hover:text-white">Déconnexion</button></form>`

    13. **`resources/views/components/admin/topbar.blade.php`** — Transposed from mockups/v1/dashboard.html topbar (lines ~42-58):
        - `<header class="sticky top-0 z-30 bg-sand-100/90 backdrop-blur border-b border-navy-900/8">`
        - Mobile logo (lg:hidden)
        - Search input (greyed, disabled)
        - Primary CTA 'Nouveau passage' — `<button type="button" disabled aria-disabled="true" class="...cursor-not-allowed opacity-50">Nouveau passage</button>` (no href yet — Phase 2 wires it)

    14. **`resources/views/components/admin/mobile-bottom-nav.blade.php`** — Bottom nav for mobile (md:hidden), 4 items: Tableau de bord (active, links to admin.dashboard), Clients (greyed), Passages (greyed), Factures (greyed). Uses `text-[11px] font-semibold tracking-wide` for labels (the `nav-label` one-off utility from UI-SPEC).

    15. **`resources/views/components/admin/stat-card.blade.php`** — `@props(['label', 'value' => '—', 'state' => 'default'])` — `<div class="rounded-2xl bg-white ring-1 ring-navy-900/8 shadow-xs p-5"><p class="text-sm text-ink-500">{{ $label }}</p><p class="mt-1 font-display font-semibold text-3xl text-ink-950">{{ $value }}</p></div>`.

    16. **`resources/views/admin/dashboard.blade.php`** — Combines the above components inside `<x-layouts.admin>`:
        ```
        <x-layouts.admin :title="'Tableau de bord · Dlo Azur'" :user="$user">
            <x-slot:sidebar>
                <x-admin.sidebar :user="$user" />
            </x-slot:sidebar>
            <x-admin.topbar />
            <div class="px-5 sm:px-8 py-7 space-y-7">
                <div>
                    <h1 class="font-display font-semibold text-2xl sm:text-3xl text-ink-950">Bonjour {{ Str::of($user->name)->before(' ') }},</h1>
                    <p class="text-ink-500 mt-1">Tableau de bord opérationnel en Phase 2.</p>
                </div>
                <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
                    <x-admin.stat-card label="Clients actifs" value="—" />
                    <x-admin.stat-card label="Passages cette semaine" value="—" />
                    <x-admin.stat-card label="À synchroniser" value="—" />
                    <x-admin.stat-card label="Factures en attente" value="—" />
                </div>
            </div>
            <x-admin.mobile-bottom-nav />
        </x-layouts.admin>
        ```

    17. **Run tests until green**: `./vendor/bin/pest tests/Feature/AuthLoginTest.php tests/Feature/AdminShellTest.php tests/Feature/PierreSeederTest.php --ci`. Iterate. Edge cases to watch:
        - Throttle test (Test 9) — needs RateLimiter::clear between unrelated tests; use `uses(...)` in tests/Pest.php to wire a beforeEach hook OR inline in the test.
        - Test 7 redirect — Fortify by default returns 302 to `config('fortify.home')`; the test should `assertRedirect('/admin')` not `'/admin/'`.
        - Test 8 error message — Fortify uses `__('auth.failed')` which resolves via lang files. Confirm `lang/fr/auth.php` is being picked up by setting `config(['app.locale' => 'fr'])` in the test if needed.

    Commit message: `feat(01-05): GREEN — Fortify auth + admin shell + Pierre seeder per AUTH-01`.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/AuthLoginTest.php tests/Feature/AdminShellTest.php tests/Feature/PierreSeederTest.php --ci</automated>
  </verify>
  <done>All 18 tests pass green; Pierre can log in; admin shell renders with greyed Phase 2/3 nav items.</done>
  <acceptance_criteria>
    - File config/fortify.php contains substrings: `'home' => '/admin'`, `Features::resetPasswords()`, AND does NOT contain substrings `Features::registerUsers`, `Features::emailVerification`, `Features::twoFactorAuthentication`
    - File app/Providers/FortifyServiceProvider.php contains substrings: `Fortify::loginView`, `Fortify::requestPasswordResetLinkView`, `Fortify::resetPasswordView`, `RateLimiter::for('login'`, `Limit::perMinute(5)->by`
    - File bootstrap/providers.php contains substring `App\Providers\FortifyServiceProvider::class`
    - File database/seeders/PierreSeeder.php contains substrings: `User::updateOrCreate`, `'email' => env('OPERATOR_EMAIL'`, `Hash::make`, `'email_verified_at' => now()`
    - File lang/fr/auth.php exists and contains substrings: `'failed' => 'E-mail ou mot de passe incorrect.'`
    - File routes/admin.php contains substring `Route::get('/', [DashboardController::class, 'index'])->name('dashboard')`
    - File app/Http/Controllers/Admin/DashboardController.php exists and has method `index`
    - File resources/views/auth/login.blade.php contains substrings: `action="{{ route('login') }}"`, `@csrf`, `name="email"`, `name="password"`, `name="remember"`, `Bon retour, Pierre.`, `Se connecter`, `Espace pro`, `Espace client`, `Données hébergées en Europe · Confidentialité`
    - File resources/views/admin/dashboard.blade.php contains substrings: `Bonjour`, `Tableau de bord opérationnel en Phase 2.`, 4 stat-card uses
    - File resources/views/components/admin/sidebar.blade.php contains substrings: `Tableau de bord`, 4 occurrences of `aria-disabled="true"`, 4 occurrences of `bientôt`
    - File resources/views/components/admin/topbar.blade.php contains substring `Nouveau passage` AND substring `disabled` (or `aria-disabled`)
    - Command `php artisan route:list --columns=method,uri,name | grep -E 'login|admin'` returns at least 3 lines (login GET, login POST, admin.dashboard)
    - Command `./vendor/bin/pest tests/Feature/AuthLoginTest.php tests/Feature/AdminShellTest.php tests/Feature/PierreSeederTest.php --ci` exits 0
    - Running PierreSeeder in test env via `php artisan db:seed --class=PierreSeeder --env=testing` produces exactly 1 user row
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 05 — Authentication + Admin)

| Boundary | Description |
|----------|-------------|
| Anonymous visitor → /login | UNTRUSTED — credentials submitted here are the primary auth attack target |
| Authenticated session → /admin/* | TRUSTED while session valid; CSRF tokens validate each POST; auth middleware on the entire admin group |
| PierreSeeder → users table | Trusted (seeder runs as deploy hook); reads OPERATOR_INITIAL_PASSWORD from secrets, never logs it |
| Env vars (OPERATOR_INITIAL_PASSWORD) → Laravel Cloud Secrets UI | Trusted boundary; only Pierre + planning owner have UI access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Spoofing | Brute-force on /login | mitigate | RateLimiter::for('login') 5 attempts/minute keyed by lowercased email + IP (RESEARCH Pattern 2 + Pitfall 8); attempts 6+ return throttle error; Test 9 enforces this |
| T-5-02 | Spoofing | Credential stuffing across IPs (distributed) | accept Phase 1 | Solo operator with 1 known account = low attractiveness; v2 may add 2FA (CONTEXT.md deferred) or device fingerprinting |
| T-5-03 | Tampering | CSRF on POST /login or /admin actions | mitigate | Laravel default CSRF middleware applies to web group; @csrf directive in every form (Test 6 verifies) |
| T-5-04 | Information Disclosure | Password leak via timing attack on login | mitigate | bcrypt has constant-time comparison; Fortify's failed login returns same message regardless of "user exists" vs "wrong password" — UI-SPEC error string is generic "E-mail ou mot de passe incorrect." |
| T-5-05 | Information Disclosure | Password in logs | mitigate | Laravel default ScrubReferer + Log message ignores `password` and `password_confirmation` fields; PierreSeeder uses Hash::make() at seed time, never logs raw password |
| T-5-06 | Tampering | Password reset token guessing | mitigate | Fortify uses Laravel's signed password reset tokens (64-byte random + HMAC); tokens expire after 60 min default |
| T-5-07 | Repudiation | No audit log of failed login attempts | accept Phase 1 | Laravel Cloud access logs cover this; structured audit log deferred to v2 if compliance needs grow |
| T-5-08 | Tampering | Mass assignment on /login (user-controlled extra fields) | mitigate | Fortify validates only email + password fields; User model has `$fillable` whitelist (already on Plan 02 model) |
| T-5-09 | Spoofing | Session fixation | mitigate | Laravel regenerates session ID on login by default; Test 11 (logout) verifies session is destroyed |
| T-5-10 | Information Disclosure | OPERATOR_INITIAL_PASSWORD leak via .env in repo | mitigate | .env is in .gitignore (Plan 01); .env.example uses placeholder only; production value set via Laravel Cloud Secrets UI (CLOUD-PROVISIONING.md); recommend Pierre force-reset to a personal password on first login |
| T-5-11 | Spoofing | Default password unchanged across deploys | mitigate | OPERATOR_INITIAL_PASSWORD is set once at provisioning; deploy hook is `db:seed --class=PierreSeeder --force` which updates the password column → if env var rotates, the next deploy updates Pierre's password (acceptable because the env var is Pierre's intended password). Risk: if env var leaks, attacker gets persistent access — mitigation = Pierre changes the value AND triggers a deploy after first login. |
| T-5-12 | Information Disclosure | /admin routes leak data structure to unauth visitors | mitigate | auth middleware on entire admin group; anonymous GET /admin returns 302 to /login with no body leakage; Test 12 verifies |
| T-5-SC | Tampering | laravel/fortify install | mitigate | Pre-cleared by Plan 01 slopcheck (Approved — official Laravel package); no new install in Plan 05 |
</threat_model>

<verification>
- All 18 Pest tests across AuthLoginTest + AdminShellTest + PierreSeederTest pass
- Pierre can log in with seeded credentials in CI (after `php artisan db:seed --class=PierreSeeder --env=testing`)
- Anonymous GET /admin redirects to /login
- Login throttle proven via Test 9 (6 attempts → throttle error)
- All Phase 1 tests from Plans 01-04 still pass (no regression — vitrine + blog + contact tests should be unaffected by Fortify install)
- Manual sanity (not gated): visit /login in browser, submit Pierre's seeded credentials, land on /admin with the styled shell + greyed Phase 2 nav items
</verification>

<success_criteria>
1. AUTH-01: Pierre logs in with email + password via Fortify; lands on /admin; can log out.
2. Throttle protects against brute-force per D-14-style policy (D-14 covers contact form, but same RateLimiter strategy applied to login per Pitfall 8).
3. Password reset flow works (GET /forgot-password renders, POST sends reset link via Mail::to driver — Mailgun in prod).
4. Fortify Phase 1 features are exactly: resetPasswords only. No registerUsers, no emailVerification, no 2FA.
5. Admin shell renders the dashboard stub per UI-SPEC §Admin shell + D-18/D-19: 'Bonjour Pierre,' + 4 stat cards with — + 4 greyed nav items with 'bientôt'.
6. PierreSeeder is idempotent and production-safe (runs from Laravel Cloud deploy hook without env gate).
</success_criteria>

<output>
Create `.planning/phases/01-vitrine-fondations/01-05-SUMMARY.md` documenting:
- The 3 Fortify feature flags actually enabled (only resetPasswords)
- Confirmation that PierreSeeder is registered in Laravel Cloud deploy command (re-check CLOUD-PROVISIONING.md from Plan 01)
- The exact French auth.php override applied
- Any flaky throttle test in CI and how isolation was secured
- Visual confirmation note for Plan 06 cutover: login + dashboard styled correctly on staging URL
</output>
